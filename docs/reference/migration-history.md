# Migration History (357 migrations)

Migrations are in `supabase/migrations/` ordered by timestamp prefix.

> ✅ **Doc-debt cleared 2026-05-17.** Entries #322–#345 backfilled in the main table below. See `docs/audits/2026-05-13-pr59-codex-review-summary.md` and `docs/CHANGELOG.md` for deeper context on each fix.

> 🩹 **Backfill 2026-05-25.** A doc-drift audit found 10 migration files on disk that had never been indexed here (the prior "no gaps #1–#345" claim was inaccurate). They are listed in the **Un-indexed migrations (backfilled)** section below rather than renumbered into the main descending table, because the `#` column is editorial (it already has duplicate-timestamp pairs) and renumbering 346 rows carries needless risk. Every `supabase/migrations/*.sql` file is now represented in this doc.

## Un-indexed migrations (backfilled 2026-05-25)

These 10 historical migrations apply by timestamp order like all others; they simply lacked a row here. Listed newest-first.

| Timestamp | File | Description |
|-----------|------|-------------|
| 20260528042000 | `preserve_quote_price_overrides` | **Recovered live-only migration (2026-05-28).** Was applied to live (version `20260528042000`, name label `20260528000001_preserve_quote_price_overrides`) but the `.sql` was never committed — found during the `/review-workflow` audit. Adds `quote_items.price_override NUMERIC NULL` and `CREATE OR REPLACE save_quote()` so the server recalc uses `COALESCE(qi.price_override, tier_price)` instead of always discarding a rep's custom price. SQL recovered verbatim from `schema_migrations.statements` and verified byte-faithful against the live function (single overload, SECURITY DEFINER + `search_path`, `anon` EXECUTE = false). `src/types/index.ts` `QuoteItem` gains `price_override: number \| null`. Filename uses the live apply-version so tooling treats it as already applied. |
| 20260516100000 | `create_prepay_check_splits_active_admin_check` | **Codex P1 (PR #59).** Added `AND is_active = true` to the admin role lookup in `create_prepay_check_splits` — a deactivated admin with a still-valid JWT could otherwise create `prepay_credits` and bump `customers.prepay_balance_cents`. Body otherwise verbatim. Applied live via MCP. |
| 20260516090000 | `entity_commission_recipients_service_profiles` | **Design call 2026-05-16 (Option 1).** Created non-loginable service profile rows for `CMCTW LLC` and `Crop Rx Solutions` (new `entity_recipient` role) so `create_commission_payment`'s group-by-`recipient_user_id` works without refactor. Unblocked 18 CMCTW commissions ($72,174.90) and backfilled their `recipient_user_id`. Deterministic UUIDs for idempotent replay. |
| 20260516080000 | `create_delivery_with_items_reject_duplicate_order_items` | **Codex P2 (PR #59).** Pre-scans `p_items` with `GROUP BY ... HAVING count > 1` and raises `ITEM_DUPLICATE` before any insert — two `{order_item_id:X}` entries against the same remaining qty would both pass the per-item check and silently over-fulfill. Body otherwise verbatim from 20260516050000. |
| 20260516070000 | `log_failed_notification_restore_defaults` | **Codex P2 (PR #59).** Migration 20260516020000 accidentally dropped `DEFAULT NULL` from `log_failed_notification`'s `p_entity_type`/`p_entity_id`/`p_payload`; the two-arg catch-block callers in `Dashboard.tsx` then failed PostgREST overload resolution and silently lost failure logs. Restored the defaults. Applied live via MCP. |
| 20260511095000 | `apply_prepay_drop_old_overload` | **Codex P1 (PR #59).** Drops the legacy 3-arg `apply_prepay_to_invoice(uuid,uuid,bigint)` so the next migration's 5-arg hardened version (adds `p_performed_by` + `p_idempotency_key`) installs cleanly — `CREATE OR REPLACE` won't replace across a changed identity signature, and `batch_apply_prepayments` would otherwise keep binding the un-hardened body. |
| 20260511020000 | `create_prepay_check_splits` | **Spawned task 2026-05-11.** Restored the never-applied `create_prepay_check_splits` RPC (PrepaymentManager.tsx called a function absent from `pg_proc` — the "split a check into buckets" flow was broken). Rebuilt with canonical idempotency helpers, valid `prepay_credit_created` audit op, dedicated `bucket_label` column, and strict-actor pattern. |
| 20260511010000 | `actor_spoof_cleanup` | **Post-audit PR-10.** `reassign_delivery` and `batch_cancel_deliveries` had inherited the spoofable `v_actor := COALESCE(p_performed_by, auth.uid())` pattern (privilege escalation in a SECURITY DEFINER RPC). Switched to strict `auth.uid()` + `AUTH_REQUIRED`/`ACTOR_MISMATCH`. No frontend changes needed. |
| 20260510999999 | `profiles_select_tighten` | **Audit PR-07 part 2.** Tightens the `profiles` SELECT policy from `USING (true)` (PII leak) to admins-and-self. Timestamped `999999` so it sits **dormant** in apply order until every non-PII profile join migrates to `profile_public_view` — applying prematurely renders "Unknown User" across ~34 UI joins. |
| 20260334900000 | `field_grouping_multi_polygon` | **Field Management V3.** Adds `fields.parent_field_id` (self-referencing, self-parent CHECK) for parent/child grouping and creates the **`field_polygons`** table (multi-polygon support per field, RLS-enabled). See `docs/plans/2026-03-31-field-management-v3-design.md`. |
| 20260331000000 | `fix_postgis_search_path` | **Bug fix.** Added `extensions` to `search_path` on all PostGIS-using SECURITY DEFINER functions (`get_fields_with_geojson`, `get_field_geojson`, …) — they had `public, pg_temp` only, but `ST_AsGeoJSON`/`ST_GeogFromGeoJSON` live in `extensions`, causing 404s on the Fields page. |


> ✅ **Migration 337 (20260516030000) applied live 2026-05-16.** Ultra-review P2 #5 prep — expanded `email_log.status` CHECK to allow `'pending'` (was `'sent', 'failed', 'bounced'`). Supports the durable write-ahead-log pattern in `send-email` Edge Function v11: insert pending row BEFORE Resend call, update to sent/failed after. Eliminates the "customer got email but no audit/idempotency record" failure mode when post-send INSERT fails. Existing code paths that filter by sent/failed/bounced unaffected.

> ✅ **Migration 336 (20260516020000) applied live 2026-05-16.** Ultra-review P1 #4 closure — added `p_idempotency_key text DEFAULT NULL` + canonical `check_idempotency`/`save_idempotency` wiring to `log_failed_notification` (returns uuid; cached as `{'id': uuid}` jsonb) and `notify_damaged_receiving` (returns void; cached as empty jsonb). Frontend was passing the key but SQL didn't accept it, so PostgREST function lookup was failing silently. DROP FUNCTION first because adding a defaulted param creates a new overload instead of replacing. Hook exempt: helper-pattern indirection.

> ✅ **Migration 335 (20260516010000) applied live 2026-05-16.** Ultra-review P1 #1 closure — wired canonical idempotency into `transfer_job_to_invoice`. The prior migration (#334) had used the `idempotency-body-check: exempt` marker to preserve the function's pre-existing gap (declared `p_idempotency_key` but never honored it). The ultra reviewer correctly flagged that after a successful invoice creation, a network-dropped response causes the retry to hit "Job already invoiced" instead of replaying the success — real customer-visible bug. Migration #335 adds `check_idempotency`/`save_idempotency` calls. All other behavior (safe_cents_qty wraps from #334, FOR UPDATE lock, advisory lock) preserved verbatim.

> ✅ **Migration 334 (20260516000000) applied live 2026-05-16.** Audit #7 closure — `transfer_job_to_invoice` had 2 unsafe `(cents * qty)::bigint` patterns in the v_share loop. Wrapped with `safe_cents_qty()` (price_override branch) + `ROUND()` (pct-split branch). Closes the last deferred safe-cents follow-up. Live `prosrc` verification confirmed both fixes present and old unsafe pattern absent.

> ⚠️ **Migrations 286–291 (2026-05-10) are committed but NOT YET APPLIED to live Supabase.** Mason will apply each via Supabase MCP `apply_migration` after review. See `docs/audits/2026-05-09-execution-summary.md`. Run `node scripts/regenerate-schema-registry.mjs` after applying any subset.

> ✅ **Migrations 307–308 (20260511090000, 20260511100000) applied live 2026-05-11.** Phase 1.4 (profile role-lock trigger) + Phase 1.D (apply_prepay_to_invoice hardening). Schema registry regenerated.

> ✅ **Migration 302 (20260511030000_vendor_bill_positive_total_guard) finalized live 2026-05-12.** The May-11 commit `5b9b05c` wrote the file but only `create_vendor_bill` had the guard live in pg_proc — `update_vendor_bill` was missing. Re-applied via Supabase MCP; both functions now have the `v_total > 0` recheck after `subtotal + adjustment`.

> ✅ **Migrations 309–312 (20260512000000–20260512030000) applied live 2026-05-12.** Phase 2 + Phase 3 audit closure: server-side pricing in `create_quick_delivery` + audit-log entries for order/delivery/quote-converted; immutability triggers on `inventory_transactions` (UPDATE+DELETE) and `prepay_applications` (UPDATE only); `payments.order_id` FK to `ON DELETE RESTRICT`; SELECT RLS tightened on `blend_ticket_fields` and `field_crop_history`. See `docs/audits/2026-05-12-execution-summary.md`.

> ✅ **Migrations 313–314 (20260512040000, 20260512050000) applied live 2026-05-12.** Decision-B #9a/#9b (`field_app_locations` + `field_app_location_shares` SELECT → admin/sales_rep/applicator) + Audit #5 (prepay_credits trigger-maintained cache via `_recompute_prepay_credit_balance` AFTER INSERT/DELETE on prepay_applications, baseline reconciled to drift=0).

| # | Timestamp | Description |
|---|-----------|-------------|
| 349 | 20260527020457 | **B10 follow-up to migration 348 — re-grant `authenticated` EXECUTE on 3 frontend-called SECDEF helpers + add internal role checks.** Codex's pre-push final audit (2026-05-26) caught that migration 348 was over-aggressive: it revoked `authenticated` EXECUTE on all 6 helpers, but the frontend actually calls 3 of them directly: `check_remainder_reminders` (Dashboard.tsx:348), `log_failed_notification` (Dashboard.tsx:353/365 + notificationTriggers.ts:37), `notify_damaged_receiving` (notificationTriggers.ts:278). The over-revoke broke those production paths immediately. This migration re-grants `authenticated` EXECUTE on the 3 frontend-called functions AND adds defense-in-depth body-level role checks to 2 of them (`check_remainder_reminders` is admin-only; `notify_damaged_receiving` is admin OR sales_rep). The role-check pattern `IF auth.uid() IS NOT NULL AND NOT <role-check> THEN RAISE 'INSUFFICIENT_ROLE'` deliberately allows `auth.uid() IS NULL` (pg_cron / service_role context) to bypass, so the existing `check-remainder-reminders` pg_cron job at 06:30 UTC daily keeps working. The 3 pure server-side helpers (`check_idempotency`, `check_rate_limit`, `cleanup_rate_limits`) remain `authenticated=false` — they have no frontend callers and are only invoked by other SECDEF wrappers running as `postgres` owner. `log_failed_notification` body is unchanged (pure logging helper, no role gate inside; GRANT-level only). Verification block asserts the final per-function policy (3 functions `authenticated=true`, 3 functions `authenticated=false`, all 6 `anon=false`) plus body-level guard presence in the 2 modified functions. **Disk-vs-live drift note:** disk filename was `20260526220000_*.sql` at write time; MCP `apply_migration` stamped live `20260527020457`; disk renamed to match. See `docs/audits/2026-05-26-claude-disposition-of-codex-execution.md §12`. |
| 348 | 20260526201319 | **B9 follow-up to migration 347 — revoke anon EXECUTE on SECDEF DML helpers.** Codex's post-apply audit of `fce0629` (2026-05-26) found that migration 347's regex sweep targeted *mutating-prefix* RPCs but missed 6 SECURITY DEFINER helper functions that perform DML and don't reference `auth.uid()`. Each was anon-callable via direct PostgREST RPC, giving anon clients abuse vectors of varying severity. This migration `REVOKE EXECUTE ... FROM PUBLIC, anon, authenticated` and explicit `GRANT EXECUTE ... TO service_role` on: `check_idempotency` (lookup helper — minor cleanup-loop DoS), `check_rate_limit` (forgeable `p_user_id` lets anon exhaust any user's rate limit — DoS legitimate logins), `check_remainder_reminders` (cron-style; anon can trigger all remainder notifications on demand), `cleanup_rate_limits` (cron-style; anon can clear rate-limit history evading limits), `log_failed_notification` (anon can spam fake notification-failure rows — audit-log poisoning), `notify_damaged_receiving` (anon can send fake "Damaged Items Received" alerts to all admins). Legitimate callers unaffected: SECDEF wrappers calling these helpers run as `postgres` owner (bypasses EXECUTE grants on owned functions); pg_cron jobs run as superuser. Verification `DO $$` block asserts post-state. **Disk-filename / live-version drift note:** the file was created on disk as `20260526170000_*.sql` but MCP `apply_migration` stamped it `20260526201319` (its `now()` timestamp); disk was renamed to match. See `docs/audits/2026-05-26-claude-disposition-of-codex-execution.md §11`. |
| 347 | 20260526151856 | **Execute 2026-05-25 full-codebase ultra review.** Revokes anon/PUBLIC execution on write-oriented SECURITY DEFINER RPCs, revokes anon table-level DML, hardens actor checks on `apply_write_off`, `issue_return_credit`, and `void_order`, restores server-side commission split validation and reconciles commission rounding in `_insert_commissions_for_order`, consolidates `next_invoice_number` to one overload, adds idempotency/locking to `duplicate_quote`, `create_followup_delivery`, and `generate_finance_charges`, allows voiding `unposted` commission payments, and adds a trigger guard requiring nonblank `signed_by` for completed deliveries. **Parallel-audit additions (2026-05-26, later same day, `docs/audits/2026-05-26-claude-disposition-of-codex-execution.md §10`):** (B6) `CREATE SEQUENCE IF NOT EXISTS public.cm_invoice_number_seq` — historical migration `20260316100002_return_credit_ar_integration.sql` lives on disk but was never applied to live, so `next_invoice_number('credit_memo')` would have crashed without this; (B4) explicit `REVOKE EXECUTE ON FUNCTION public.execute_sql_readonly(text) FROM anon, PUBLIC` (SECDEF + arbitrary-SELECT = anon RLS-bypass; regex prefix `execute_` missed); (B5) same on `unapply_credit_memo(uuid, text, uuid, text)` (RLS-1 actor-forgery anti-pattern; regex prefix `unapply_` missed); (C1) REVOKE regex extended with `auto|retry|revert` prefixes to sweep `auto_expire_quotes`, `retry_failed_notifications`, `revert_quote_status`. Verification `DO $$` block gained 3 assertions (sequence exists, B4/B5 anon revoked). |
| 346 | 20260518010000 | **`create_delivery_with_items` order-status + address ownership checks** (codex audit, PR #59 2026-05-18). Two P2 follow-ups on migration 345: (1) the locked order SELECT only read `customer_id` — a stale or direct caller could pass a `cancelled` / `fulfilled` / `voided` order_id; the delivery got created and `complete_delivery` then blindly updated `orders.status`, resurrecting cancelled orders and fulfilling drafts. Now selects `status` too and raises `ORDER_NOT_SCHEDULABLE` if not in `('confirmed', 'partially_fulfilled')`. (2) `p_delivery_address_id` was inserted verbatim with no ownership check — a caller could route customer A's delivery to customer B's address (PII leak + wrong route). Now looks up the address and raises `ADDRESS_NOT_FOUND` / `ADDRESS_CUSTOMER_MISMATCH` if it doesn't belong to `p_customer_id`. Single overload (no signature drift); carry-forward checks for `ITEM_OVER_REMAINING_INCL_ACTIVE` and `ITEM_DUPLICATE_IN_REQUEST` re-verified. 5 new error tokens registered in `RpcErrorCodes`. |
| 345 | 20260517020000 | **`safe_cents_qty` wraps on `create_invoice_from_blend_ticket` + `save_field_app_invoice`** (follow-up sprint, deferred from 2026-05-13 audit #7). Wrapped 10 remaining `(*_cents * qty)::bigint` instances (5 each) with the canonical `safe_cents_qty` helper. Adds NULL safety: previous `ROUND(NULL * v)::bigint` produced NULL `v_fee_cost`/`v_invoice_cost` and corrupted aggregation downstream. `safe_cents_qty` COALESCEs NULL→0. Math otherwise identical. |
| 344 | 20260517010000 | **`create_delivery_with_items` cross-delivery aggregation** (codex audit F2). Per-row `quantity_remaining` check (from #342) didn't subtract quantities scheduled on OTHER active deliveries. Two failure modes: (1) same order_item_id listed twice in p_items, (2) concurrent calls — each passes independently but sum exceeds remaining. Added `ITEM_DUPLICATE_IN_REQUEST` upfront check + `ITEM_OVER_REMAINING_INCL_ACTIVE` cross-delivery sum. Mirrors proven pattern from `edit_delivery_items_when_scheduled`. |
| 343 | 20260516060000 | **`edit/delete_prepay_credit` null-safe admin role check** (codex P1). `profile.role = 'admin'` returned NULL when profile row was missing (deleted/orphaned user), and `IF NULL THEN ... ELSE` falls through to ELSE — bypassing the admin gate. Wrapped with `COALESCE(role = 'admin', false)`. Defensive depth — `auth.uid()` check ahead of it already filters most. |
| 342 | 20260516050000 | **`create_delivery_with_items` validates quantity_remaining** (codex P2). Per-row check ensures the requested quantity does not exceed the locked order_item's `quantity_remaining`. Previously, `complete_delivery` would increment `quantity_delivered` past `quantity_ordered`, breaking inventory reconciliation. Added `ITEM_OVER_REMAINING` exception. |
| 341 | 20260516040000 | **`increment_customer_prepay` lock down** (codex P1). Function was missing the strict-actor and authorization checks added to all other prepay mutations during the audit. A non-admin sales_rep could call it directly via PostgREST. Added `is_admin()` gate + `auth.uid()` check + canonical idempotency. |
| 340 | 20260516030000 | **`email_log.status` += 'pending'** (ultra-review P2 #5). Expanded CHECK constraint from `('sent', 'failed', 'bounced')` to include `'pending'`. Supports the durable write-ahead-log pattern in `send-email` v11: insert pending row BEFORE Resend, update to sent/failed after. Eliminates 'customer got email but no audit row' failure mode. |
| 339 | 20260516020000 | **Notification RPCs canonical idempotency** (ultra-review P1 #4). Frontend `notificationTriggers.ts` was passing `p_idempotency_key: crypto.randomUUID()` to `log_failed_notification` (5-arg) and `notify_damaged_receiving` (3-arg). PostgREST lookup was failing silently — damaged-receiving alerts broken AND fallback failure-queue logging broken. Added defaulted `p_idempotency_key` + canonical helper wiring. DROP FUNCTION first (defaulted param = new overload). |
| 338 | 20260516020000 | **`create_delivery_with_items` validates customer match** (codex P2 follow-up). Companion to #336 — also rejects when `p_customer_id` does not match the order's customer. Caller could create a delivery for customer B against customer A's order, breaking invoice attribution. Added `CUSTOMER_ORDER_MISMATCH` exception. |
| 337 | 20260516010000 | **`transfer_job_to_invoice` canonical idempotency** (ultra-review P1 #1). Prior migration #334 used `idempotency-body-check: exempt` to preserve the pre-existing gap. Network-dropped responses on successful invoice creation caused retries to hit 'Job already invoiced' instead of replaying success. Wired `check_idempotency`/`save_idempotency`. |
| 336 | 20260516010000 | **`create_delivery_with_items` validates order match** (codex P2). Per-item lock fetched order_item but did not verify `order_item.order_id = p_order_id`. Caller could supply an order_item belonging to a different order, silently linking it to the wrong delivery. Added `ITEM_ORDER_MISMATCH` exception. |
| 335 | 20260516000000 | **`transfer_job_to_invoice` safe_cents_qty wrap** (audit #7 closure). Replaced 2 unsafe `(cents * qty)::bigint` patterns in v_share loop with `safe_cents_qty()` (price_override branch) + `ROUND()` (pct-split branch). Closes the last deferred safe-cents follow-up at the time (the v_share-specific path). |
| 334 | 20260515999999 | **Drop legacy 2-arg `transfer_job_to_invoice` overload** (codex P1). The safe_cents_qty rewrite in #335 was about to install a 3-arg version; without dropping the legacy overload first, both would coexist and PostgREST would 300-error on ambiguous function. Pre-cleanup migration. |
| 333 | 20260513160000 | **`rebate_claim_counters` explicit deny-all RLS policy** (codex P1). Migration #315 enabled RLS but added no policy — Postgres's implicit-deny was protecting it, but AGENTS.md requires explicit policies for auditable intent. Added `FOR ALL TO authenticated, anon USING (false) WITH CHECK (false)`. SECURITY DEFINER RPCs continue to work (owner-bypass). |
| 332 | 20260513150000 | **`edit/delete_prepay_credit` strict-actor hotfix** (codex P1 HOTFIX). After #330's signature restore, `p_performed_by` was being used as the audit actor without verifying it matched `auth.uid()`. Strict-actor pattern added: `IF p_performed_by IS DISTINCT FROM auth.uid() RAISE 'ACTOR_MISMATCH'`. |
| 331 | 20260513140000 | **`bulk_import_order` seeds `quantity_remaining` from `quantity_ordered`** (codex P2). Imported order_items had NULL `quantity_remaining`, breaking subsequent delivery scheduling (which checks remaining > 0). Seeded on INSERT to match the regular order creation path. |
| 330 | 20260513130000 | **`edit/delete_prepay_credit` correct 7-arg/4-arg signatures** (codex P1). Prior fix #329 dropped the `p_idempotency_key` and `p_performed_by` params; frontend was getting 404s. Restored full signatures + canonical idempotency + trigger-aware mutation. |
| 329 | 20260513120000 | **`edit/delete_prepay_credit` cooperate with prepay balance trigger** (codex P2). Direct UPDATE/DELETE was bypassing the trigger that recomputes `prepay_credits.balance_cents`. Bodies now call the trigger function directly after the mutation so the cache stays consistent. |
| 328 | 20260513110000 | **`allocate_payment` gates `check_period_open` on payment_date, not invoice_date** (codex P2). Allocating a new payment to an old invoice was throwing PERIOD_CLOSED when the invoice's period was closed, even though the payment itself belongs to an open period. Now checks payment_date. |
| 327 | 20260513100000 | **Commission recipient lookup uses MIN(id), not max(uuid)** (codex P1) — `max(uuid)` is not a valid Postgres aggregate. Use `MIN(p.id) FILTER (WHERE ...)` to deterministically pick one when multiple profiles share a full_name. Fixes silent recipient_user_id resolution failures. |
| 326 | 20260513090000 | **Populate `recipient_user_id` in `_insert_commissions_for_order`** (codex P2). Helper was leaving `recipient_user_id NULL` even when display name resolved to a profile. LEFT JOIN on profiles by `full_name`; sets `recipient_user_id` when match is unambiguous. |
| 325 | 20260513080000 | **`batch_reschedule_deliveries` 4-arg signature with canonical idempotency** (codex P2). Frontend was sending `p_idempotency_key` but signature didn't accept it — PostgREST lookup failing silently. Added `p_idempotency_key text DEFAULT NULL` + canonical `check_idempotency`/`save_idempotency` calls. |
| 324 | 20260513070000 | **Lock down `_insert_commissions_for_order` helper** (codex P1) — internal helper was callable by `authenticated` directly. REVOKEd from authenticated; only callable from inside SECURITY DEFINER wrappers (`convert_quote_to_order`, etc.) where caller chain is owner-bypassed. |
| 323 | 20260513060000 | **REVOKE anon EXECUTE on 9 new SECURITY DEFINER fns** — Audit #28 follow-up. Supabase grants EXECUTE to anon by default on new public functions; REVOKE FROM PUBLIC doesn't strip it. Revokes anon EXECUTE on 7 user-facing + 2 internal helpers. Drops anon SECURITY DEFINER warning count from ~223 to ~214. |
| 322 | 20260513060000 | **Audit log allow 'payment_allocated'** (codex P1) — `financial_audit_log.operation_type` CHECK was missing `'payment_allocated'` after `allocate_payment` started writing audit rows. Trigger was throwing instead of logging. Re-extended the CHECK; verified existing rows compatible. |
| 321 | 20260513060000 | **Audit #28 follow-up — REVOKE anon EXECUTE on 9 new SECURITY DEFINER functions** — Defense-in-depth cleanup. Supabase grants EXECUTE on new public-schema functions to BOTH anon and authenticated by default — `REVOKE ALL ... FROM PUBLIC` does NOT strip the role-specific anon grant. The function bodies all check `auth.uid() IS NULL → AUTH_REQUIRED` so anon callers can't actually do anything, but the security advisor `0028_anon_security_definer_function_executable` correctly flags it. Revokes anon EXECUTE on 7 user-facing/helper RPCs (keeps authenticated). Revokes anon + authenticated EXECUTE on 2 internal helpers (`_insert_commissions_for_order`, `_snapshot_order_item_cost`) which are only called from inside other SECURITY DEFINER bodies (where inner-call grants are irrelevant due to function-owner permissions). Special case: `_snapshot_order_item_cost()` also gets `REVOKE FROM PUBLIC` because migration 20260513050000 didn't include it (anon was inheriting via PUBLIC, hence the first migration attempt's verify failure). Drops the project-wide anon SECURITY DEFINER warning count from ~223 to ~214. |
| 320 | 20260513050000 | **Audit #32 — `order_items.cost_at_time_cents` snapshot** — order_items already had `cost_per_unit numeric` but it's caller-supplied (may be a stale quote cost or manual override). The audit's question — "did order X price the products at their cost-at-the-time?" — needed an authoritative snapshot. Added `cost_at_time_cents bigint` column + `_snapshot_order_item_cost()` BEFORE INSERT trigger that fills it from `products.current_cost * 100` (rounded). Trigger handles every insert path automatically — no RPC body changes needed. Backfill populated existing rows from `cost_per_unit * 100` (best historical signal — represents what was actually used for margin reporting). Verification: 0 rows left NULL after backfill (when `cost_per_unit IS NOT NULL`). New TS field added on `OrderItem` interface in `src/types/index.ts`. |
| 319 | 20260513040000 | **Audit #19 — `invoices_balance_non_negative` CHECK** — `invoices.balance_cents` is GENERATED ALWAYS as `total_amount_cents - paid_amount_cents - prepay_applied_cents`. The two base columns already have non-negative CHECKs but the derived balance had no guard, so any future code path that mis-allocated payments could persist phantom customer credit (`balance_cents < 0`). Live data confirmed 0 rows with negative balance (existing `allocate_payment` caps at outstanding), so safe to add VALIDATED in one step. Note: original audit said "credit memos" — codebase has no `credit_memos` table; "credit memos" are `invoices` rows created by `issue_return_credit`, so the constraint on `invoices` covers the credit-memo case too. |
| 318 | 20260513030000 | **Audit #7 — `safe_cents_qty` helper + create_quick_delivery fix** — PostgreSQL's `numeric::bigint` cast TRUNCATES, so `(price_cents * qty)::bigint` lost up to ~0.999 cents per line item. Live grep found 4 instances: 3 in `create_quick_delivery` (most-trafficked path) and 1 each in `transfer_job_to_invoice`, `create_invoice_from_blend_ticket`, `save_field_app_invoice`. New `safe_cents_qty(p_cents bigint, p_qty numeric) -> bigint` IMMUTABLE helper does `ROUND(p_cents * p_qty)::bigint` (to-nearest-cent). `create_quick_delivery` body rewritten to use the helper for all 3 instances. Other 3 instances deferred to follow-up sprint (each single-instance, smaller blast radius — fee_acres × cost_per_acre patterns). New schema-aware `sql-safety.mjs` hook rule blocks `(*_cents * <qty>)::bigint` patterns in future migrations (strips `--` comments first to avoid false-positives on doc text). Live verification: `safe_cents_qty(100, 2.5)=250`, `safe_cents_qty(333, 0.333)=111`, `safe_cents_qty(NULL, NULL)=0`. |
| 317 | 20260513020000 | **Audit #6 — Canonical commission math** — Three commission-creating paths (`convert_quote_to_order`, `create_direct_order`, `create_quick_delivery`) used three different formulas: no rounding/no clamp, clamp/no rounding, rounding/no clamp. Same order through three paths produced three different commission totals. Bonus discovery: `create_quick_delivery`'s commission insert referenced columns that don't exist (`recipient_id`, `notes`) — any QD with a default_commission_split would have crashed. Confirmed latent (zero rows in commissions with NULL recipient = code path never exercised). Fix: new `compute_commission_amount(numeric, numeric) -> numeric` IMMUTABLE helper embedding `GREATEST(ROUND(profit * pct / 100, 2), 0)` (rounded to 2 dp because commissions store dollars not cents; clamped to >= 0 because losing-trade orders shouldn't owe negative payables); new `_insert_commissions_for_order(order_id, customer_id, profit, split, date)` SECURITY DEFINER wrapper does the INSERT once, called via `PERFORM` from all 3 paths. Future commission-creating RPCs go through the helper too. Live verification: `compute_commission_amount(1000, 50)` = 500.00, `compute_commission_amount(-100, 50)` = 0, `compute_commission_amount(33.33, 33.33)` = 11.11. All 3 caller bodies confirmed via `prosrc` to invoke the helper and have zero remaining inline `INSERT INTO commissions` blocks. |
| 316 | 20260513010000 | **Audits #10, #31, #34 — Atomic multi-table write RPCs** — Three frontend code paths were doing parent+child inserts in separate `.insert()` calls with no transaction wrapper. If the child insert failed, the parent stayed orphaned (or in BlendRecipes' case, the child DELETE rolled forward without an INSERT to balance it). Migration introduces three SECURITY DEFINER RPCs that move the orchestration into Postgres so the function body's implicit transaction gives us atomicity for free: (a) `create_delivery_with_items(p_order_id, p_customer_id, p_scheduled_date, p_items jsonb, ...)` — replaces NewDelivery.tsx's `insert deliveries` + `insert delivery_items` flow, generates `delivery_number` via existing `next_delivery_number()`. (b) `bulk_import_order(p_order_number, p_customer_id, p_status, totals..., p_items jsonb, ...)` — replaces BulkOrderImport.tsx's per-order `insert orders` + `insert order_items` loop. Frontend keeps the per-row customer/product lookups (preserves friendly error messages) but ships the resolved IDs to the RPC. (c) `save_blend_recipe(p_recipe_id, p_name, p_recipe_type, p_items jsonb, ...)` — replaces BlendRecipes.tsx's create-or-update + DELETE-then-INSERT-items flow; for updates, the DELETE and INSERTs are atomic so a failed insert rolls back the DELETE too (no recipe ever ends up with zero items). All three accept canonical `p_idempotency_key` (helper pattern), require `auth.uid()`, role-gate to `is_admin() OR is_sales_rep()` (delivery + import) or `is_admin() OR creator` (recipe update), and raise machine-readable error tokens. |
| 315 | 20260513000000 | **Audit #33 — Rebate claim atomic RPCs** — Three problems closed in one migration. (1) `rebate_claims.claim_number` was generated frontend-side via `count(*) + 1` and had no UNIQUE constraint, so two concurrent inserts could produce duplicate numbers. (2) Status transitions used naked `.update()` with no row lock and no state-machine validation — two admins clicking "Approve" simultaneously could both succeed, and there was nothing stopping a pending claim from jumping straight to paid. (3) `paid_amount_cents` was never written; the "Rebates Received" stat fell back to `claim_amount_cents`. Migration adds: UNIQUE on `claim_number`; new `rebate_claim_counters(year, next_value)` table (RLS-on with no policies — system table accessed only via SECURITY DEFINER); `create_rebate_claim()` RPC that bumps the counter under `INSERT ... ON CONFLICT DO UPDATE` row-lock and inserts the claim atomically; `transition_rebate_claim()` RPC that takes `SELECT FOR UPDATE` on the claim, validates the state machine (pending→submitted\|rejected, submitted→approved\|rejected, approved→paid), and sets `paid_amount_cents` from `p_paid_amount_cents` or falls back to `claim_amount_cents`. Both use `check_idempotency`/`save_idempotency` helpers and require `is_admin()` (transition) or `is_admin() OR is_sales_rep()` (create). Markers: `rls-check: exempt` (system counter table), `idempotency-body-check: exempt` (helper-pattern). |
| 314 | 20260512050000 | **Audit #5 — `prepay_credits.balance_cents` trigger-maintained cache** — Original audit said "make it GENERATED" but Codex correctly noted that's not viable (cross-table SUM). Correct shape is a trigger that recomputes `balance_cents = original_amount_cents - SUM(applied_amount_cents)` on every INSERT/DELETE to `prepay_applications`. UPDATE not handled because `prepay_applications` is UPDATE-immutable (per migration 310). `_recompute_prepay_credit_balance()` is SECURITY DEFINER, `search_path = public, pg_temp`. Wired as `trg_recompute_prepay_credit_balance AFTER INSERT OR DELETE ON prepay_applications FOR EACH ROW`. Migration also runs a baseline UPDATE to re-set every existing row to the computed value, making the trigger's invariant true from this moment forward. Drift check on session start showed 0 mismatches in 0 rows — clean install. `apply_prepay_to_invoice` still hand-decrements balance_cents in its body; the trigger fires after and overwrites with the recomputed value (same end-state). Future cleanup can drop the hand-decrement after watching the trigger in prod. |
| 313 | 20260512040000 | **Decision-B #9a + #9b — Tighten `field_app_locations` + `field_app_location_shares` SELECT RLS** — Both tables had `USING ((SELECT auth.uid()) IS NOT NULL)` (any authenticated user can read billing splits). Mason picked Option C: `is_admin() OR is_sales_rep() OR is_applicator()`. Drivers excluded (no business need for billing-split data). Applicators retained because the field-app workflow (multi-customer jobs, split-billing on iPad) needs split context for rate decisions. Matches the pattern already applied to `field_crop_history_select` in migration 312. |
| 312 | 20260512030000 | **Phase 3 #9c + #9d — Tighten SELECT RLS on `blend_ticket_fields` + `field_crop_history`** — Both tables had `USING (true)` for SELECT (open read). #9c: SELECT now matches the existing INSERT/UPDATE EXISTS pattern (uploader OR admin OR sales_rep via parent `blend_tickets`). Drivers don't need access. #9d: dropped the `"Authenticated users can read crop history"` permissive policy, replaced with `field_crop_history_select` USING `is_admin() OR is_sales_rep() OR is_applicator()`. Applicators retained because the field-app workflow needs crop history for rate decisions. Both predicates use `(SELECT auth.uid())` per PR #61 initplan pattern. |
| 311 | 20260512020000 | **Phase 2 #23 — `payments.order_id` ON DELETE CASCADE → RESTRICT** — `payments_order_id_fkey` cascaded DELETEs from `orders`, which would destroy AR payment history. Verified safe to switch: zero RPCs or frontend code do raw `DELETE FROM orders` (orders go through cancel/void/restore state transitions). Now `ON DELETE RESTRICT` so any future code attempting to DELETE an order with payments is forced to void each payment first. Defense-in-depth backstop against accidental history loss. |
| 310 | 20260512010000 | **Phase 2 #24 — Immutability triggers on `inventory_transactions` + `prepay_applications`** — Both tables are conceptually append-only ledgers but had no enforcement triggers. `_guard_inventory_transactions_immutable` blocks UPDATE+DELETE (zero existing pg_proc callers do either — verified). `_guard_prepay_applications_immutable` blocks UPDATE only; `void_invoice` still DELETEs rows when reversing, and the comprehensive `financial_audit_log` write from void preserves evidence. Bypass via `SET LOCAL app.bypass_ledger_immutability = 'true'` (mirrors `app.admin_override` pattern) for rare DBA corrections. Both functions are SECURITY DEFINER with `search_path = public, pg_temp`. |
| 309 | 20260512000000 | **Phase 2 #8 + #15 — Quick Delivery server pricing + audit-log gap** — `create_quick_delivery` used `COALESCE(NULLIF((v_item->>'price_cents')::bigint, 0), <tier price>)` which let drivers/sales-reps spoof a $0.01 price by passing `price_cents: 1` in the JSONB. Now always uses tier price (the client field is silently ignored). QuickDeliveryModal already computed tier price client-side, so the UI displays the same total the server computes — only the manipulation case is blocked. #15 piece: both `create_quick_delivery` and `convert_quote_to_order` now write `financial_audit_log` entries (operation_type `order_created`/`delivery_created` for QD, `quote_converted` for the conversion). The `financial_audit_log_operation_type_check` constraint was extended to allow the three new tokens. |
| 308 | 20260511100000 | **Phase 1.D — Harden apply_prepay_to_invoice (closes audit Critical #4)** — The function was SECURITY DEFINER and wrote to `prepay_applications`, `prepay_credits`, `customers.prepay_balance_cents`, `invoices.prepay_applied_cents`, and `financial_audit_log` — but had NO actor check, NO role gate, and NO idempotency parameter. Direct PostgREST caller with any authenticated JWT could apply prepay to any invoice, or double-apply on network retry. Practical attack surface was small (only called from `batch_apply_prepayments` which already has canonical idempotency), but defense-in-depth: extends signature to `(p_prepay_credit_id, p_invoice_id, p_amount_cents, p_performed_by uuid DEFAULT NULL, p_idempotency_key text DEFAULT NULL)`. Existing 3-arg callers (`batch_apply_prepayments`) still work because defaults are NULL. Adds `v_actor := auth.uid()` + `AUTH_REQUIRED` check, strict actor pattern (`ACTOR_MISMATCH` if `p_performed_by IS DISTINCT FROM v_actor`), role gate (admin or sales_rep, `INSUFFICIENT_ROLE`), and canonical `check_idempotency`/`save_idempotency` pair. RETURN type stays `uuid` to avoid cascading changes through `batch_apply_prepayments`. DO-block verification asserts overload count = 1, new params present, and all four token-error codes plus both idempotency helpers are in the body. |
| 307 | 20260511090000 | **Phase 1.4 — Profile role-lock trigger (closes audit Critical #3)** — `profiles_update` RLS allowed self-update via `((auth.uid() = id) OR is_admin())` with no column-level restriction, so any authenticated user could PATCH their own row and set `role = 'admin'`, flip `is_active`, or clear `denied_pages`. Adds `_guard_profile_role_lock()` (SECURITY DEFINER, `search_path = public, pg_temp`) wired as `trg_guard_profile_role_lock BEFORE UPDATE ON profiles FOR EACH ROW`. Trigger rejects with `PROFILE_ROLE_LOCK` (errcode `42501`) if `OLD.role IS DISTINCT FROM NEW.role` OR `OLD.is_active IS DISTINCT FROM NEW.is_active` OR `OLD.denied_pages IS DISTINCT FROM NEW.denied_pages` AND `NOT is_admin()`. Non-privilege self-updates (full_name, phone, applicator_license_number, faa_certificate_number) remain allowed. The first half of the privilege-escalation chain (Critical #2 — `handle_new_user` trusting `raw_user_meta_data->>'role'`) stays mitigated by Mason disabling public signup; this trigger holds even if signup is re-enabled. SettingsPage already uses `admin_update_profile` RPC (admin-only, SECURITY DEFINER) so the admin UI is unaffected. DO-block verification asserts function created with correct attributes and trigger active. |
| 302 | 20260511030000 | **Codex review fix for PR #59 — restore positive-total guard on `create_vendor_bill` + add to `update_vendor_bill`** — Two P2 findings from codex review (2026-05-11). (1) `update_vendor_bill` (migration 20260510100000) validated `p_subtotal_cents > 0` but never re-checked the computed `v_new_total_cents`; a $100 bill edited with a -$200 adjustment yielded `total_cents = -10000`, `balance_cents = -10000` (GENERATED), `status = 'unpaid'` — broken aging/payment behavior. (2) `create_vendor_bill` rewrite in `ap_polish_completion` (20260510130000) silently dropped the `v_total <= 0` guard that codex audit F4 had added in PR-04 (20260510030000); negative adjustment could flip total negative. Both functions now `RAISE EXCEPTION 'INVALID_AMOUNT: bill total must be positive (got %)', v_total` after computing total = subtotal + adjustment. Bodies otherwise reproduced verbatim from the prior installed migrations (preserving PR-22b PO consistency check + drift soft-warn in `create_vendor_bill`; preserving update audit log + active-payments guard in `update_vendor_bill`). DO-block verification asserts both guards landed and that the PR-22b polish features (VENDOR_PO_MISMATCH, vendor_bill_drift) were not regressed. No frontend changes — existing handlers already display `INVALID_AMOUNT` errors raised for the subtotal check. |
| 1 | 20260206172436 | Full schema creation (all tables, indexes, RLS) |
| 2 | 20260206174345 | Security & performance fixes (consolidated RLS, auth.uid() optimization) |
| 3 | 20260206174743 | Profile auto-creation trigger |
| 4 | 20260206191309 | Automatic price calculation |
| 5 | 20260206191700 | Gross margin display fields |
| 6 | 20260206192224 | Fix margin terminology |
| 7 | 20260206195903 | EPA registration field |
| 8 | 20260206201328 | Team board collaboration features |
| 9 | 20260206203908 | Blend tickets system |
| 10 | 20260207000001 | Fix customer RLS for sales_rep |
| 11 | 20260207_gap_analysis_fixes | Payments table, reorder_point, commission paid fields |
| 12 | 20260207090000 | Additional gap fixes (gap_analysis_fixes_2) |
| 13 | 20260208194203 | Soft delete for team notes |
| 14 | 20260209040254 | Fix soft delete activity logging |
| 15 | 20260209040325 | Fix payment RLS policies |
| 16 | 20260209143537 | Inventory overhaul (holds, is_planned) |
| 17 | 20260209200000 | Tier 1 audit fixes — 6 atomic RPCs, CHECK constraints on numeric columns, order total verification trigger, is_active enforcement in RLS helpers |
| 18 | 20260210000000 | Tier 3 hardening — idempotency keys table + activity feed triggers on critical tables |
| 19 | 20260210_fix_rls_critical_issues | Sales rep INSERT permissions |
| 20 | 20260211000000 | Unit tracking & pricing overhaul — product_form, inventory_unit, container_unit/type, fl oz fix, cross-table validation trigger, best-effort backfill |
| 21 | 20260211100000 | Atomic inventory RPCs — `adjust_inventory()` and `create_direct_order()` replace client-side read-then-write patterns |
| 22 | 20260211110000 | Fix column mismatches — RPCs referencing non-existent columns (updated_at on order_items, notes/sort_order on order_items) |
| 23 | 20260211120000 | Blend ticket fields — job_number, invoice_number, ticket_time, vehicle_info, mixer_name, field_names, total_acres, application_rate, rate_per_acre on blend_ticket_products |
| 24 | 20260211130000 | Admin update profile RPC — admin-only function to update any user's role, name, phone, active status |
| 25 | 20260211200000 | Sprint 0 emergency fixes — inventory availability check, UNIQUE(product_id, location) on inventory, over-delivery detection in complete_delivery |
| 26 | 20260211210000 | Atomic quote save RPC — `save_quote()` replaces dangerous delete-all-then-reinsert pattern |
| 27 | 20260211220000 | Sprint 1 data integrity — atomic PO save/delete RPCs, tighten RLS + SET search_path, fix admin_update_profile is_active check |
| 28 | 20260211230000 | Atomic customer/blend/quote duplication — `save_customer()`, `save_blend_ticket()`, `duplicate_quote()` RPCs |
| 29 | 20260211240000 | Normalize net_margin to percentage — converts fraction-based values (0.20) to percentage (20.0) across quote_items |
| 30 | 20260211250000 | Sprint 2 validation fixes — restrict driver UPDATE on deliveries (drivers use complete_delivery RPC only) |
| 31 | 20260211260000 | Number generation sequences — `next_delivery_number()`, `next_po_number()` with advisory locks (replaces client-side count+1) |
| 32 | 20260211270000 | Dashboard summary RPC — consolidates 8+ separate dashboard queries into single `dashboard_summary()` function |
| 33 | 20260211280000 | Commission recipient FK — adds `recipient_user_id` UUID FK to commissions (replaces text name matching), backfill from profiles |
| 34 | 20260211290000 | Partial delivery support — `p_quantities` JSONB parameter on `complete_delivery`, `quantity_delivered` column on delivery_items |
| 35 | 20260213000000 | Phase 1: Fields foundation — PostGIS extension, fields + field_billing_defaults tables, parent_customer_id, season/salesman_id/deleted_at columns |
| 36 | 20260213100000 | Phase 2: Billing architecture — invoice system, allocation sets, billing splits, prepay credits, financial audit log, core RPCs |
| 37 | 20260213120000 | Phase 3: Blend ticket-order linkage — connects blend tickets to orders/invoices for billing workflows |
| 38 | 20260213140000 | Phase 4A: Blend recipes — saved blend recipe/program tables for one-click application during blend ticket creation |
| 39 | 20260213160000 | Phase 5: Inventory enhancements — warehouses table, cycle counting, improved receiving workflow |
| 40 | 20260213180000 | Phase 6: Returns/RMA system — returns + return_items tables, status lifecycle, credit generation |
| 41 | 20260213200000 | Phase 7: Reporting, compliance & rebates |
| 42 | 20260214000000 | Phase 4B: Mapbox integration (geo RPCs, PostGIS) |
| 43 | 20260214200000 | Vehicles table |
| 44 | 20260214210000 | Applicator role expansion |
| 45 | 20260214220000 | Application records table + RPCs |
| 46 | 20260215200000 | Job scheduling tables |
| 47 | 20260216200000 | Reporting RPCs (10 report functions) |
| 48 | 20260217200000 | Accounting periods (month-end close) |
| 49 | 20260217210000 | Commission payments |
| 50 | 20260218200000 | Financial workflows (write-offs, finance charges, prepayments) |
| 51 | 20260219200000 | Invoice/statement enrichment |
| 52 | 20260219210000 | Invoice/statement RPCs |
| 53 | 20260220200000 | Finance charge intelligence |
| 54 | 20260221200000 | Grower share pricing |
| 55 | 20260221200000 | Rate limiting — rate_limit_log table + index for critical RPC abuse prevention |
| 56 | 20260222200000 | Batch operations |
| 57 | 20260222200000 | Fix allocate_payment double-count — trigger + manual UPDATE both incrementing orders.total_paid; remove manual UPDATE, repair corrupted balances |
| 58 | 20260223200000 | Payment allocation |
| 59 | 20260223200000 | Pre-launch bug fixes — batch_void_invoices delegation, next_cycle_count_number() generator, receive_return silent restock fix |
| 60 | 20260223210000 | Add commissions denormalized columns — order_number + customer_name on commissions table, backfill from orders/customers |
| 61 | 20260223220000 | Fix cycle_counts FK to profiles — change FK from auth.users to public.profiles for PostgREST embedding |
| 62 | 20260224200000 | Year-end summary |
| 63 | 20260225200000 | Delivery system enhancements |
| 64 | 20260226200000 | Receiving system enhancements |
| 65 | 20260227200000 | Delivery integrity & quick delivery |
| 66 | 20260228200000 | Safety audit hardening — 21 vulnerabilities: dangerous RLS UPDATE policies, server-side recalculation in save_invoice/save_quote, price floor in create_quick_delivery, FOR UPDATE lock in post_invoice |
| 67 | 20260228200000 | Season calendar Oct-Sep convention |
| 68 | 20260228210000 | Failed notifications table (failed_at, retry_count) |
| 69 | 20260228220000 | Server-authoritative quote math (`calculate_quote_totals()` RPC with NUMERIC(15,4)) |
| 70 | 20260228230000 | Signature privacy — `create_signed_url()` RPC for time-limited delivery photo access |
| 71 | 20260228300000 | Critical prelaunch fixes — prepay batch wrong overload, tote regression, status transition enforcement |
| 72 | 20260228310000 | High priority fixes — period check in allocate_payment, audit log for allocate_payment, FOR UPDATE in receive_po_items, BEFORE DELETE guards, non-negative money CHECKs, idempotency in complete_delivery |
| 73 | 20260228320000 | Medium priority fixes — transaction review deprecated payments table, adjust_inventory negative qty guard, convert_quote_to_order FOR UPDATE, idempotency_keys RLS |
| 74 | 20260301000000 | Audit Phase 0 fixes: finance charge compounding exclusion, billing split `FOR UPDATE` locks |
| 75 | 20260301100000 | Tote tracking: `tote_number` and `is_non_returnable` columns on `delivery_items` |
| 76 | 20260301100001 | Complete delivery tote copy: threads `tote_number` through `complete_delivery` and `create_quick_delivery` RPCs |
| 77 | 20260301200000 | Prepay bucket system: `bucket_label` column on `prepay_credits`, 8 seeded bucket categories in `app_settings`, reference index |
| 78 | 20260301200000 | User page permissions — `denied_pages` text[] column on profiles, updated `admin_update_profile()` with denied_pages support + applicator role fix |
| 79 | 20260301200001 | Prepay application RPCs: `apply_prepay_to_invoice()` atomic allocation + `batch_apply_prepayments()` batch wrapper |
| 80 | 20260301300000 | New RPC `financial_dashboard_summary()` for Financial Dashboard — returns all financial KPIs (AR aging, revenue, payments, prepay balances, finance charges) |
| 81 | 20260301300001 | Slim `dashboard_summary()` to operational-only — removes financial KPIs (now served by `financial_dashboard_summary()`) |
| 82 | 20260302100000 | Quote items: add `calc_mode` (rate_acres/units_direct) and `price_unit` columns for bidirectional calc + price unit override |
| 83 | 20260302110000 | Orders: add `order_name` column, recreate `create_direct_order()` with auto-generated order numbers and order name support |
| 84 | 20260302120000 | Save quote bidirectional calc: `save_quote()` branches on `calc_mode`, persists `calc_mode` + `price_unit` on items |
| 85 | 20260302200000 | Business logic enhancements — draft invoice enforcement trigger, quick delivery commissions, order cancellation cascade, partial delivery invoice auto-adjustment, delivery remainder auto-reminders |
| 86 | 20260303200000 | Dashboard integrity alerts — 4 new alert counts (driver issues, customers over credit, expired holds, cancelled posted) + credit limit check helper RPC |
| 87 | 20260304200000 | Quick Receive — `match_quick_receive_items()` RPC + `receive_po_items` updated with `p_allow_over_receive` param |
| 88 | 20260304210000 | Add MG/g inventory units + Jar container type |
| 89 | 20260305200000 | Audit safety fixes — 16 findings: inventory transaction_type CHECK, balance_cents GENERATED column, void_invoice column/arithmetic bugs, atomic manual inventory add, blend ticket prebooking, finance charge idempotency, prepay reconciliation, receive_return audit/locks |
| 90 | 20260306200000 | Add idempotency key parameter to all mutating RPCs — `p_idempotency_key text DEFAULT NULL` injected via pg_get_functiondef() |
| 91 | 20260306200001 | Delivery & invoice inventory hardening — create_quick_delivery inventory prebooking, transfer_job_to_invoice FOR UPDATE on field_billing_defaults |
| 92 | 20260307100000 | **Accounts Payable + RUP Sales Reporting** — 4 new tables (`vendors`, `vendor_bills`, `vendor_payments`, `rup_sales_records`), 7 RPCs (`create_vendor_bill`, `record_vendor_payment`, `void_vendor_bill`, `get_ap_aging`, `get_ap_dashboard_summary`, `generate_rup_sales_records`, `get_rup_sales_register`), RLS policies, vendor backfill from existing PO/product data, `post_invoice()` enhanced to auto-generate RUP records |
| 93 | 20260307200000 | Pre-launch hardening — 13 findings: batch_cancel_deliveries inventory restoration, cancel_delivery prebooked release, idempotency enforcement in 7 RPCs, blend ticket prebooking double-deduct |
| 94 | 20260307200000 | **Sales & Chemical History Reporting** — 3 new RPCs (`get_sales_detail_report` with LATERAL JOIN to invoices, `get_sales_summary_report` with CTE-based GROUP BY dimension, `get_customer_farm_group` recursive CTE for parent/child farm grouping). Powers new `/sales-reports` page |
| 95 | 20260308100000 | **Email Infrastructure** — `email_type` enum (8 types), `email_log` table (audit trail with idempotency_key, resend_message_id, status tracking), `ar_reminder_tracking` table (dedup: max one reminder per customer per level per day), `get_ar_reminder_candidates()` RPC (customers with 30+ day overdue invoices). RLS: admin SELECT/INSERT on both tables |
| 96 | 20260308200000 | **Dashboard Margin Alerts** — extends `financial_dashboard_summary()` with 3 new CTEs: bottom 10 products by margin % this season, bottom 10 customers by margin % this season, monthly margin % trend (last 12 months). Uses `compute_season()` for current season filter. Total function now has 16 CTEs |
| 97 | 20260308200000 | Production fixes V2 — receive_po_items idempotency column fix, next_return_number() generator, storage buckets + RLS policies, check_duplicate_delivery() advisory helper |
| 98 | 20260309100000 | Order PO number & shares — `customer_po_number` column on orders + `order_shares` table for bill splitting between customers |
| 99 | 20260309200000 | Document OCR — `document-uploads` storage bucket + processing log table for OCR infrastructure |
| 100 | 20260309210000 | Fix complete_delivery prebooked inventory — pre-check now considers available + prebooked, deducts prebooked first; allow sales_rep role to complete deliveries |
| 101 | 20260310200000 | Go-live remediation — save_customer() missing fields (parent_customer_id, credit_limit_cents, finance_charge_rate, etc.), take_delivery role fix |
| 102 | 20260310210000 | Browser smoke test fixes — complete_delivery auto-updates delivery_remainders status, receive_po_items now()::text cast fix |
| 103 | 20260311000000 | Audit cancel state machine fixes — drop orphan cancel_order overload, unify cancel_order, fix cancel_delivery trigger bypass for completed deliveries + auth.uid() enforcement, GRANT EXECUTE for void_invoice |
| 104 | 20260311000001 | Audit financial fixes — allocate_payment season calc (month >= 10 not >= 7), allocate_payment period check, save_customer commission split validation restore, additional financial bug fixes |
| 105 | 20260311000002 | Audit inventory fixes — create_quick_delivery missing prebooking, convert_quote_to_order overload consolidation, complete_delivery duplicate invoice prevention, receive_return/complete_cycle_count FOR UPDATE locks, create_quick_delivery idempotency |
| 106 | 20260311000003 | Audit RLS fixes — financial_audit_log INSERT restriction, team_note_comments UPDATE/DELETE policies, ar_reminder_tracking INSERT policy, ocr_processing_queue cleanup, team_note_tags restriction to owner/admin |
| 107 | 20260311100000 | Fix reassign_delivery — allow admin and sales_rep users as delivery targets (not just drivers) |
| 108 | 20260311200000 | Invoice as AR single source of truth — DROP trg_payment_update_order, rewrite trg_recalc_order_totals, rewrite dashboard_summary AR from invoices, deprecate orders.total_paid/balance_due |
| 109 | 20260311200000 | Wave 2 audit fixes — role checks on SECURITY DEFINER RPCs, return cancel transitions, quote revert/cancelled transitions, void_vendor_bill guard, idempotency guards, search_path fixes, drop 11 stale overloads |
| 110 | 20260312100000 | Wave 3 audit fixes — 5 broken RPCs missing p_idempotency_key, invoice CHECK constraint missing paid/overdue, complete_delivery stale overloads, void_invoice lost admin check + needs commission reversal, allocate_payment stale overload |
| 111 | 20260312200000 | Business logic audit fixes: inventory hold auto-release trigger (declined/expired/accepted), `post_invoice()` period enforcement via `check_period_open()`, `save_customer()` commission split validation (must sum to 100%), `create_quick_delivery()` inventory pre-check with `FOR UPDATE` locks, `convert_quote_to_order()` explicit hold release |
| 112 | 20260312200000 | Deploy reverse_receiving_record — RPC to reverse inventory when deleting receiving entries + BEFORE DELETE safety trigger on receiving_records |
| 113 | 20260312300000 | Void order RPC — admin-only `void_order()` for fulfilled orders: restores inventory, voids draft invoices, cancels/flags commissions, financial audit log |
| 114 | 20260313004449 | **PO Receive: stop auto-updating product cost** — flips `p_skip_cost_update` default from `false` to `true` in `receive_po_items()`. Product master cost (pricing basis) is now admin-controlled only. Supplier cost still tracked on `purchase_order_items.unit_cost` and `receiving_records.unit_cost` |
| 115 | 20260313200000 | Add items to existing order — extends `update_order_items` to insert NEW items; also expands transaction_type CHECK to include prebooked/released |
| 116 | 20260313200001 | Add inventory_transactions FK constraints — purchase_order_id and delivery_id foreign keys for PostgREST joins |
| 117 | 20260313200002 | Add orders is_planned — display-only boolean flag for planned/tentative vs committed orders |
| 118 | 20260314100000 | Fix create_direct_order — remove references to deprecated total_paid/balance_due columns |
| 119 | 20260314100001 | Fix generate_rup_sales_records — column refs (use p.product_name from joined products, ii.unit_size) |
| 120 | 20260314100002 | Fix void_invoice — invoice_line_allocations uses amount_cents not allocated_cents |
| 121 | 20260314100003 | Fix save_quote — activity_feed uses event_type/related_entity_type/related_entity_id (not action/entity_type/entity_id), fix search_path |
| 122 | 20260315004110 | **Fix idempotency_key column refs in save_quote** — Corrects `save_quote` RPC to use `idempotency_key` column (not `key`) and `(idempotency_key, operation)` INSERT (not `key, entity_type, entity_id`). Fixes "column 'key' does not exist" error when idempotency keys are provided. 9 other admin RPCs with same bug deferred to future migration |
| 123 | 20260315100000 | **Fix ALL idempotency column refs in 11 RPCs** — Uses pg_get_functiondef() + replace() to patch save_quote, receive_po_items, reopen_accounting_period, reverse_write_off, void_commission_payment, revert_quote_status, restore_cancelled_order, restore_cancelled_delivery, unapply_credit_memo, reverse_blend_ticket_approval, void_delivery. Fixes `key` -> `idempotency_key`, `entity_type`/`entity_id` -> `operation`/`result`, `result_id` -> `result`. Includes verification block |
| 124 | 20260315200000 | Emergency RPC fixes: 9 broken functions with wrong column references (allocate_payment, cancel_delivery, auto_expire_quotes, cancel_order, check_customer_credit_limit, create_quick_delivery schema fixes) |
| 125 | 20260315200000 | **Team Board V2: Entity Linking, Attachments, Delivery RPCs** — (1) Adds `linked_entity_type text` + `linked_entity_id uuid` to `team_notes` with partial index. (2) Creates `team_note_attachments` table with RLS (authenticated read, own insert/delete, admin delete). (3) Creates `team-note-attachments` storage bucket with upload/view/delete policies. (4) New RPC `get_team_board_deliveries()` — role-aware today/tomorrow deliveries with priority sorting. (5) New RPC `get_yesterday_delivery_recap()` — completed/issues/cancelled summary. (6) New RPC `get_notes_for_entity()` — notes linked to a specific entity |
| 126 | 20260315200001 | Accounting integrity & hardening: expand audit log CHECK constraints, period enforcement in 10 financial RPCs, FOR UPDATE locks, void_invoice 4-bug fix, commission season convention, cycle count guards, return processing dedup |
| 127 | 20260316100000 | RPC state machine enforcement — save_quote status transition validation, complete_job require in_progress, receive_po_items PO header status gate + FOR UPDATE, close_accounting_period admin role check |
| 128 | 20260316100001 | Inventory hold restoration — release_holds_on_quote_status_change restores quantity_available, cancel_order hold release fix (holds linked to quotes not orders) |
| 129 | 20260316100002 | Return credit AR integration — real `issue_return_credit()` creates credit memo invoices (negative totals), credit memo number sequence, invoice_type CHECK expanded |
| 130 | 20260316100003 | Batch year-end summaries + storage policy auth fix — `get_batch_year_end_summaries()` RPC, document-uploads storage policies use (select auth.uid()) |
| 131 | 20260316200000 | Additional audit gap fixes: idempotency on `apply_write_off` + `batch_apply_prepayments`, admin role check on `generate_finance_charges` |
| 132 | 20260316200000 | Fix allocation_sets entity_type CHECK — add 'payment' to allowed values (was blocking all payment processing) |
| 133 | 20260316200000 | Fix receive_po_items ambiguity — drop 3-param overload, keep 4-param with DEFAULT false for p_allow_over_receive |
| 134 | 20260316200001 | Fix allocate_payment missing columns — invoice_line_allocations missing bill_to_customer_id/split_percentage, allocation_sets version always 1 causing unique violation |
| 135 | 20260317200000 | Security audit auth.uid() enforcement — 9 SECURITY DEFINER RPCs get auth.uid() checks, save_invoice posted immutability guard, ROUND fix in save_invoice, orphan overload cleanup |
| 136 | 20260317200001 | PO cancel soft delete — cancel metadata columns on purchase_orders, `cancel_purchase_order()` RPC with auth.uid() enforcement |
| 137 | 20260318100000 | Fix close_accounting_period — payments.amount_cents does not exist; replace with (sum(amount) * 100)::bigint |
| 138 | 20260318200000 | Fix record_invoice_payment — invoice_line_allocations.allocated_cents does not exist; correct column is amount_cents |
| 139 | 20260318300000 | Fix close_accounting_period — deliveries.delivery_date does not exist; correct column is scheduled_date |
| 140 | 20260318400000 | Fix record_invoice_payment — switch from allocation_sets to payments table (allocation_sets NOT NULL/UNIQUE constraints preventing multi-payment) |
| 141 | 20260319000000 | Fix trigger functions search_path for security |
| 142 | 20260320100000 | Workflow quote/order/invoice fixes — convert_quote_to_order warns instead of blocks on inventory shortfalls, new `create_invoice_from_delivery()`, complete_delivery auto-creates draft invoice |
| 143 | 20260320200000 | Manual inventory add unit cost — optional `p_unit_cost` parameter for setting/overriding product unit cost on manual add |
| 144 | 20260320210000 | Manual inventory no cost override — stop manual_inventory_add from overwriting products.current_cost; unit cost recorded in transaction notes only |
| 145 | 20260321100000 | Dashboard inventory position cards |
| 146 | 20260321200000 | Prepay edit/delete RPCs |
| 147 | 20260321300000 | Void payment RPC |
| 148 | 20260322100000 | Inventory warn-not-block with net position (available - prebooked + on_order) |
| 149 | 20260323100000 | New RPC `operational_dashboard_summary()` — comprehensive 25-CTE function powering the rebuilt Operational Dashboard (KPIs, team board, inventory, deliveries, alerts, monthly chart, season progress, activity feed) |
| 150 | 20260323200000 | Fix search_path security — SET search_path on 7 functions flagged by Supabase security advisor (_is_admin_override, compute_season, current_season, get_ar_aging, get_season_comparison, season_end_date, season_start_date) |
| 151 | 20260324100000 | Fix RPC overload mismatches — drop stale overloads created by dynamic p_idempotency_key injection, add missing increment_customer_prepay function |
| 152 | 20260325100000 | Sprint 1 fix critical overloads — complete_job, convert_quote_to_order, create_quick_delivery, cancel_order: drop all overloads + recreate with correct logic |
| 153 | 20260325200000 | Sprint 1 financial compliance — get_ap_aging/get_ap_dashboard_summary column name fixes, void_invoice zero paid_amount_cents, generate_rup_sales_records column fix, post_invoice auto-RUP, save_purchase_order status guard |
| 154 | 20260326100000 | Deep audit fixes — purchase_order_items missing product_name column + backfill, invalid status transition fix, stale overload drop |
| 155 | 20260327100000 | Wave 4 bug fixes — void_payment GENERATED ALWAYS column fix, RETURNING value bug, admin role checks on void_payment/edit_prepay_credit/delete_prepay_credit/batch_void_invoices, period check fixes |
| 156 | 20260327200000 | Wave 4 security & integrity — create_commission_payment/create_prepay_credit admin role checks, customers.prepay_balance_cents non-negative CHECK, order_items.quantity non-negative CHECK, create_prepay_check_splits atomic batch |
| 157 | 20260327210000 | Wave 4 critical fixes A — void_payment corrected: GENERATED ALWAYS column, RETURNING captures post-UPDATE value, add role check |
| 158 | 20260327220000 | Wave 4 medium fixes — receive_po_items weighted average cost, update_order_items delete removed items, next_invoice_number advisory lock |
| 159 | 20260328000000 | Wave 4 remaining fixes — complete_delivery pre-check (Net Free), create_application_record inventory deduction, record_invoice_payment CHECK status, receive_po_items over-receive validation, create_quick_delivery credit limit check |
| 160 | 20260329100000 | Fix inventory on-order & receive — receive_po_items restore quantity_on_order decrement, PO submission increments quantity_on_order |
| 161 | 20260330000000 | **Pre-Launch Critical Fixes** — 7 bug fixes: (1) `complete_delivery` inventory inflation (prebooked deliveries now deduct physical stock), (2) `receive_po_items` auth check restored + `p_skip_cost_update` default re-fixed to true, (3) `void_invoice` fixed wrong column name + deleted-row query + added admin auth + period check, (4) `create_direct_order` now creates commission records, (5) `calculate_prices_from_margin` SET search_path added, (6) `receive_return` LATERAL subquery location filter added, (7) `create_commission_payment` season calc fixed (>=10 not >=7) |
| 162 | 20260330100000 | **Pre-Launch State Machine & Security** — 21 fixes: (A) 4 state machine guards: `convert_quote_to_order` rejects non-sent/accepted quotes, `post_invoice` rejects cancelled-order invoices, `save_purchase_order` enforces forward-only status transitions, `_require_auth()` helper. (B) 11 `SET search_path` fixes via ALTER FUNCTION for trigger/billing/blend functions. (C) 6 financial RPC role guards: `get_ar_aging`, `get_sales_detail_report`, `get_sales_summary_report`, `get_commission_balance_report`, `get_inventory_cost_report`, `get_customer_statement` — all now require admin or sales_rep role |
| 163 | 20260330200000 | **Pre-Launch Final Fixes** — (1) `record_invoice_payment` now sets `status='paid'` when fully paid (matches `allocate_payment` behavior; stale H3 comment removed), also accepts `overdue` invoices for payment. (2) `_check_credit_limit()` helper function created — checks draft+posted+overdue invoices for AR balance (existing `create_quick_delivery` inline check only checked posted) |
| 164 | 20260331100000 | Admin corrections Phase 1 — `reopen_accounting_period()` allows admin to reopen closed period, `reverse_write_off()` marks write-off reversed and restores AR |
| 165 | 20260331110000 | Void delivery — `void_delivery()` reverses completed delivery: restores inventory, order_items, removes delivery_remainders. Expands deliveries status CHECK to include 'voided' |
| 166 | 20260331120000 | Void commission payment — `void_commission_payment()` reverses posted commission payment, resets commissions to 'pending'. Expands commission_payments status CHECK to include 'voided' |
| 167 | 20260331130000 | Admin corrections Phase 3 — `revert_quote_status()`, `restore_cancelled_order()`, `restore_cancelled_delivery()`, `unapply_credit_memo()`, `reverse_blend_ticket_approval()`. Also expands financial_audit_log operation_type CHECK |
| 168 | 20260331200000 | Fix complete_delivery pre-check — uses quantity_available (physical stock) instead of net_free; prebooked is a reservation tracker, not a physical constraint |
| 169 | 20260331200001 | Fix PO edit partially received — save_purchase_order UPDATE items in-place instead of delete/re-insert (FK violation when receiving_records exist) |
| 170 | 20260331200002 | Fix order edit product swap inventory — update_order_items handles product_id changes: release old prebooked qty, add new prebooked qty, update product_name/cost/unit_size |
| 171 | 20260331300000 | Fix product data for quote calculations — backfill inventory_unit from unit_size, set product_form, normalize rate_unit 'oz' -> 'fl oz', add 'oz' to unit_conversions (prevents 128x price inflation bug) |
| 172 | 20260331400000 | Consolidate receive_po_items overloads + fix tier price fallback in save_quote — drop unused 5-param overload, add security audit fix to 4-param version, COALESCE tier fallback |
| 173 | 20260331500000 | Drop unused overloads — convert_quote_to_order + record_invoice_payment: drop stale versions without p_idempotency_key, apply bug fixes from unused versions into active ones |
| 174 | 20260331600000 | **Consolidate All RPC Overloads** — Comprehensive fix for the systemic overload bug. Part 1: Explicit recreation of 4 missing functions (create_vendor_bill, record_vendor_payment, void_vendor_bill, receive_return). Part 2: Dynamic consolidation of 33 functions (find best overload, drop all, inject idempotency, recreate unified). Part 3: Verification that all 42 RPCs have exactly 1 overload |
| 175 | 20260331700000 | **Fix inventory_transactions transaction_type CHECK** — Restores 'prebooked' and 'released' values that were accidentally removed by 20260331110000 (void delivery). These values are used by update_order_items(), cancel_order(), create_quick_delivery() |
| 176 | 20260331800000 | **Restore commissions status CHECK** — Re-adds CHECK constraint with 'pending', 'paid', 'cancelled'. Was dropped in 20260302200000 to add 'cancelled' but never recreated |
| 177 | 20260331900000 | Fix cancel_delivery prebooked release — scheduled delivery cancel should NOT release prebooked (belongs to order); completed delivery cancel must re-increment prebooked |
| 178 | 20260332000000 | Fix void_delivery/batch_cancel_deliveries/cancel_order — void_delivery must restore prebooked, batch_cancel_deliveries same class of bugs, cancel_order logs negative quantity fix |
| 179 | 20260332100000 | Drop deprecated order columns — removes orders.total_paid and orders.balance_due (deprecated since 20260311200000; AR now on invoices.balance_cents) |
| 180 | 20260332200000 | **Fix idempotency column refs (Round 2)** — 10 RPCs had wrong idempotency_keys column names re-introduced by Mar 31 migrations (which overwrote the 20260315004110 fix). Fixes: `key`→`idempotency_key`, `result_id`→`result`, `entity_type`/`entity_id`→`operation`/`result` in reopen_accounting_period, reverse_write_off, void_delivery, void_commission_payment, revert_quote_status, restore_cancelled_order, restore_cancelled_delivery, unapply_credit_memo, reverse_blend_ticket_approval, save_quote |
| 181 | 20260316300000 | **Wire confirm_delivery idempotency logic** — The consolidation migration (20260331600000) added p_idempotency_key parameter but never wired up check_idempotency/save_idempotency calls inside the function body. Frontend was already passing the key (DeliveryDetail.tsx:550) but server was ignoring it. Drivers on mobile could create duplicate activity_feed + notification entries. |
| 182 | 20260316115721 | **Add mark_overdue_invoices()** — Batch function to auto-detect posted invoices past due_date and transition them to 'overdue' status. Logs each transition to financial_audit_log. Designed for cron/scheduled execution. |
| 183 | 20260316121800 | **Drop stale confirm_delivery overload + enable pg_cron** — Removes the broken `confirm_delivery(uuid, text)` overload from consolidation. Enables `pg_cron` extension and schedules `mark_overdue_invoices()` to run daily at 6 AM UTC. |
| 184 | 20260332300000 | **Fix void_delivery — 4 bugs** — (1) `quantity` → `total_units_needed` in order status check, (2) adds `app.admin_override` for reverse status transitions (fulfilled→confirmed), (3) includes `actor_user_id` in financial_audit_log INSERT, (4) fixes idempotency_keys column refs (`key`→`idempotency_key`, `result_id`→`result`) |
| 185 | 20260332400000 | **Fix audit_log actor + column bugs across 24 RPCs** — (1) cancel_delivery: move admin_override BEFORE order status updates, (2) mark_overdue_invoices: fix wrong column names + NULL actor, (3) link/unlink_blend_ticket: fix wrong column names, (4) BEFORE INSERT trigger on financial_audit_log to fill actor_user_id when NULL (safety net for 20 other functions) |
| 186 | 20260332500000 | **Fix receive_po_items + expand financial_audit_log CHECK constraints** — (1) remove `updated_at = now()` from purchase_order_items UPDATE (column doesn't exist), (2) add 5 missing operation_type values: `invoice_marked_overdue`, `prepay_reconciliation`, `batch_prepay_apply`, `blend_ticket_linked`, `blend_ticket_unlinked`, (3) add `blend_ticket` to entity_type CHECK |
| 187 | 20260332600000 | **Fix commission payment RPCs** — `create_commission_payment` and `void_commission_payment` both referenced non-existent `updated_at` column on `commissions` table, causing crashes when paying or voiding commission payments |
| 188 | 20260316100000 | **Add product internal_notes** — Adds `internal_notes` (text, nullable) column to `products` table. Copies existing `notes` content to `internal_notes` for all products. Existing `notes` column stays as-is (relabeled "Grower Description" in UI). |
| 189 | 20260316200000 | **Quote versioning V2** — Creates `create_quote_version` and `restore_quote_version` RPCs for snapshot/restore of full quote state (sections + items). SECURITY DEFINER, search_path = public, pg_temp |
| 190 | 20260316300000 | **Quote section header notes** — Adds `section_header_notes` column to `quote_sections` table. Displays above items table, below section name in PDF. |
| 191 | 20260316400000 | **Quote PDF templates** — Creates `quote_pdf_templates` table for saved column presets, with RLS policies. |
| 192 | 20260316500000 | **Planned programs** — Adds `needed_by_date` to `quote_sections` for planned programs. Creates RPCs for inventory hold management and expiring holds alerts. |
| 193 | 20260316600000 | **Quote templates** — Creates `quote_templates` and `quote_template_sections`/`quote_template_items` tables for reusable quote structures. RLS policies included. |
| 194 | 20260316700000 | **Notes pipeline flow** — Adds `notes` column to `order_items`, `program_notes` column to `orders`. Rewrites `convert_quote_to_order` to copy `qi.notes` from quote_items to order_items and aggregate section_header_notes into `orders.program_notes` |
| 195 | 20260316800000 | **Inventory forecasting** — Creates `get_inventory_forecast` RPC for planned demand aggregation by product/month. SECURITY DEFINER, search_path = public, pg_temp |
| 196 | 20260316900000 | **Seasonal rollover** — Creates `rollover_quote_to_season` RPC to duplicate quotes with updated pricing for a new season. SECURITY DEFINER, search_path = public, pg_temp |
| 197 | 20260316950000 | **Team Board Phase 2** — Adds `last_escalated_at` column to `team_notes`, creates `get_team_workload()` RPC (aggregates open tasks, overdue tasks, today's/week's deliveries per team member). SECURITY DEFINER, search_path = public, pg_temp |
| 198 | 20260317100000 | **Fix idempotency + search_path final** — Final comprehensive fix of idempotency column names and search_path on all affected functions |
| 199 | 20260319100000 | **Reverse cycle count RPC** — Creates `reverse_completed_cycle_count()` to undo inventory adjustments from completed cycle counts |
| 200 | 20260319200000 | **Cleanup fake cycle counts & rebates** — One-time cleanup: reverses all completed cycle counts and deletes fake test rebates |
| 201 | 20260319200000 | **Remove delivery inventory block** — Removes blocking inventory pre-check from `complete_delivery()` to allow negative inventory |
| 202 | 20260320100000 | **Add idempotency to remaining RPCs** — Adds `p_idempotency_key` to 5 mutation RPCs that were missing it |
| 203 | 20260320100000 | **Workflow quote/order/invoice fixes** — Fixes for quote-to-order conversion, order fulfillment, and invoice posting workflows |
| 204 | 20260332700000 | Fix idempotency column refs round 3 — surgical fix of 4 RPCs + safety-net scan of all public functions + self-testing verification |
| 205 | 20260332800000 | **pg_temp search_path fix** — ALTER FUNCTION on ALL SECURITY DEFINER functions to add `pg_temp` to search_path. Verification block raises EXCEPTION if any functions still missing. Prevents temp schema hijacking attacks |
| 206 | 20260332900000 | **Data validation & cleanup** — Fixes negative inventory quantities, recalculates prebooked from actual pending orders, verifies commission splits sum to 100%, checks invoice paid_amount_cents integrity, fixes invalid commission statuses |
| 207 | 20260333000000 | **Fix reverse_cycle_count** — Adds `pg_temp` to search_path, fixes idempotency column refs (`key`→`idempotency_key`, `entity_type`/`entity_id`→`operation`/`result`) |
| 208 | 20260333100000 | **Fix save_quote idempotency + activity_feed columns** — Corrects idempotency_keys refs (`key`→`idempotency_key`, `entity_type`/`entity_id`→`operation`/`result`), fixes `v_server_totals` field aliases (`.sum`→`.total_price`), fixes activity_feed column names (`action`→`event_type`, `entity_type`→`related_entity_type`, `entity_id`→`related_entity_id`), adds `pg_temp` to search_path |
| 209 | 20260333200000 | **Fix save_quote search_path + idempotency type** — Surgical fix for save_quote search_path and idempotency key column type mismatch |
| 210 | 20260333300000 | **Fix 5 RPCs missing p_idempotency_key** — Adds p_idempotency_key to reverse_receiving_record, void_payment, edit_prepay_credit, delete_prepay_credit. Recreates batch_post_invoices (was dropped in 20260311200000). All created after consolidation migration so never got the parameter. Restores set_config trigger awareness in reverse_receiving_record. |
| 211 | 20260333400000 | **Fix reverse_receiving and idempotency bugs** — Fixes reverse_receiving_record and other idempotency-related issues |
| 212 | 20260333500000 | **Allow PO reverse transitions** — Enables purchase order status rollback for admin corrections |
| 213 | 20260333600000 | **Quick delivery optional invoice** — Adds `p_skip_invoice boolean DEFAULT false` to `create_quick_delivery`. Fixes missing `save_idempotency()` call (was checking but never saving). Fixes `search_path` missing `pg_temp`. Wraps invoice creation in conditional. Frontend adds "Create draft invoice" checkbox (ON by default) + confirmation dialog |
| 214 | 20260333700000 | **Rate limit log RLS** — Deny-all policy on `rate_limit_log` table to prevent unauthorized access |
| 215 | 20260333800000 | **Drop inventory qty_available CHECK** — Removes `chk_inventory_qty_available` CHECK constraint that blocked the "allow negative inventory" design in `complete_delivery()` |
| 216 | 20260333900000 | **Mega audit Phase 1 & 2 fixes** — 12 RPC fixes: get_ar_aging (include overdue), get_monthly_summary (commission cents), financial_dashboard_summary (overdue AR + order filters), apply_prepay_to_invoice (update customer balance), cancel_delivery (save idempotency), generate_finance_charges (season >= 10), allocate_payment (audit log), convert_quote_to_order (release holds), create_invoice_from_order (filter duplicates), update_order_items (recalc cost/profit/margin), save_quote (preserve is_planned), void_invoice (cancel commissions). 6 frontend fixes: CSV cents conversion, deleted_at filters, soft delete, regex replace |
| 217 | 20260334000000 | **Fix order_item delete FK checks** — Adjusts FK constraints for order item deletion |
| 218 | 20260334100000 | **Fix save_customer_address FK** — Fixes FK validation in save_customer_address RPC |
| 219 | 20260334200000 | **Edit delivery items when scheduled** — Replaces `edit_delivery()` to honor `p_items` when status = 'scheduled'. Validates quantities against `order_items.quantity_remaining` minus other active deliveries. Blocks item editing on in_progress. Includes overload verification. |
| 220 | 20260334300000 | **App settings OCR thresholds** — Extends `app_settings` with `description` and `created_at` columns. Seeds `ocr_confidence_threshold` setting with `auto_approve=85, needs_review=50`. |
| 221 | 20260334400000 | **Blend ticket fields table** — Creates `blend_ticket_fields` for per-field application tracking. Supports multi-field loads, multi-customer billing (Q6-B), and planned vs actual acres. RLS enabled. |
| 222 | 20260334500000 | **Blend tickets Phase 1 columns** — Adds `applicator_id` (FK→profiles), `vehicle_id` (FK→vehicles), `source` ('ocr'\|'manual'\|'digital') to `blend_tickets`. Indexed. |
| 223 | 20260334600000 | **Batch approve blend tickets** — `batch_approve_blend_tickets(p_ticket_ids, p_approved_by, p_idempotency_key)` RPC. Bulk approves completed+unreviewed tickets. |
| 224 | 20260334700000 | **Save blend ticket fields** — `save_blend_ticket_fields(p_blend_ticket_id, p_fields, p_performed_by, p_idempotency_key)` RPC. Replaces all field assignments for a blend ticket atomically. |
| 225 | 20260334800000 | **Duplicate blend ticket detection** — `check_duplicate_blend_ticket(p_ticket_number, p_ticket_date)` RPC. Returns matching tickets for duplicate warning. |
| 226 | 20260330032232 | **get_field_dashboard RPC** — Returns comprehensive JSONB for field dashboard: field data with customer/billing defaults/geometry, season summary stats, application records with applicator/vehicle/weather, and recent activity feed. |
| 227 | 20260335000000 | **Workflow gaps Phase 1: broken connections** — Adds `unit_cost_cents`/`unit_price_cents` to `blend_ticket_products`, updates `create_application_record_from_blend_ticket` to return `uuid[]` (one per field), adds `blend_tickets.job_id` FK column |
| 228 | 20260335100000 | **Workflow gaps Phase 2: blend ticket invoice** — `create_invoice_from_blend_ticket` RPC + `sync_blend_ticket_payment_status()` trigger for auto-syncing payment_status when invoice voided |
| 229 | 20260335200000 | **Workflow gaps Phase 3: field billing splits** — Adds `quote_sections.field_id`, `invoices.invoice_group_id`, field billing split helper RPCs + `create_split_invoices_from_order` RPC |
| 230 | 20260335300000 | **Workflow gaps Phase 4: dispatch columns** — Adds `jobs.priority` and `jobs.estimated_hours` columns |
| 231 | 20260335400000 | **Workflow gaps Phase 5: crop history** — Creates `field_crop_history` table with auto-snapshot trigger (`snapshot_field_crop_history`) for tracking multi-year crop rotation |
| 232 | 20260335500000 | **Invoice audit fixes** — (1) Adds `trg_guard_audit_log_immutable` trigger to block UPDATE/DELETE on `financial_audit_log`. (2) Fixes `apply_write_off` to auto-set status='paid' when write-off brings balance to 0. (3) Fixes `get_detailed_statement_data` to include 'overdue' invoices and corrects aging bucket overlap (over_90 → over_120). |
| 233 | 20260404040100 | **Add global_search() RPC** — `global_search(p_query, p_limit)` for Command Palette entity search across customers, orders, invoices, deliveries, products with ILIKE |
| 234 | 20260404040200 | **Add get_customer_summary() RPC** — `get_customer_summary(p_customer_id)` for Customer 360 KPI bar |
| 235 | 20260404040300 | **Add get_dashboard_action_items() RPC** — Returns specific actionable items per category for Dashboard Action Queue |
| 236 | 20260404080000 | **Fix global_search ILIKE escape** — Escape `%`, `_`, `\` metacharacters in user input to prevent wildcard injection |
| 237 | 20260404174600 | **Add batch_reject_blend_tickets() RPC** — Batch reject blend tickets, mirrors batch_approve pattern |
| 238 | 20260405000000 | **Application services setup + bug fix** |
| 239 | 20260405100000 | **Quote-to-job connection** |
| 240 | 20260405200000 | **Smart pricing flow** |
| 241 | 20260405300000 | **Program completion RPC** |
| 242 | 20260405400000 | **Code review fixes** -- SELECT RLS policy, index optimization |
| 243 | 20260406000000 | **Code review RPC hardening** -- quote status validation, duplicate job guard, bigint casts |
| 244 | 20260406100000 | **Field App Workflow V2** -- field_app_locations + field_app_location_shares tables, derive_customer_shares_from_fields + save_field_app_invoice RPCs, jobs.customer_id DROP NOT NULL |
| 245 | 20260416100000 | **Fix search_path on 4 trigger functions** — ALTER FUNCTION to add `pg_temp` to `guard_audit_log_immutable`, `_fill_audit_actor`, `_enforce_quote_status_transition`, `_enforce_return_status_transition` |
| 246 | 20260429140635 | **Field App Workflow Phase 1** — grouped split invoices for multi-customer field applications. Schema: `field_app_locations.invoice_group_id`, `invoices.application_service_id`. Rewrites: `derive_customer_shares_from_fields` (per-field detail + fallback), `save_field_app_invoice` (Mode A grower-share / Mode B line-item branching, group lock, location keying), `create_invoice_from_blend_ticket` (return type uuid→jsonb, acres fallback chain). New: `post_invoice_group` (atomic), `preview_field_app_invoice_split` (read-only). |
| 247 | 20260430150000 | **Field App Workflow Phase 2 — Job Lifecycle Repair** — fixes codex audit #4, #5, #6. Schema: `application_record_fields` join table (multi-field records), `application_records.field_id` → nullable (legacy), `jobs.customer_id` → NOT NULL (revert of v2 mistake). New RPC: `start_job(uuid, uuid, text)` — scheduled→in_progress with `actual_start_time` stamp + idempotent replay. Rewrite: `complete_job` now writes one application_records row + N application_record_fields rows from `job_fields`, fixes silent multi-field data loss. |
| 248 | 20260430160000 | **Field App Workflow Phase 3 — Inventory Completion Behavior** — fixes codex audit #7. Schema: `inventory_transactions.requires_review boolean DEFAULT false`, `inventory_transactions.job_id uuid REFERENCES jobs(id)`. Rewrite: `complete_job` no longer raises on insufficient stock — applications happen in the field and the DB must record reality; short-stock transactions tagged `requires_review = true` and surface in activity feed. Linked-prebook decrement: `quantity_prebooked` only drops when the job's `quote_section_id` matches an `inventory_holds.source_id`, fixing the leak where Customer B's job silently halved Customer A's prebook. |
| 249 | 20260430170000 | **Field App Workflow Phase 4 — Application Service Fees** — fixes codex audit #8. Schema: `jobs.application_service_id uuid REFERENCES application_services(id)` (parity with `blend_tickets.application_service_id` and `invoices.application_service_id`). New helper RPC: `compute_application_service_fee(p_service_id uuid, p_customer_id uuid, p_acres numeric, p_season int)` — single source of truth for fee math; lookup priority is `customer_application_rates` override → `application_services.default_rate_per_acre_cents` → 0. Returns jsonb `{ rate_per_acre_cents, total_fee_cents, cost_per_acre_cents, total_cost_cents, source, service_name }`. Existing inline fee blocks in `save_field_app_invoice` and `create_invoice_from_blend_ticket` continue to work; future cleanup can refactor them onto the helper. |
| 250 | 20260430180000 | **Field App Workflow Phase 5 — RLS Hardening** — fixes codex audit #10. Tightens 3 policies to close a privilege escalation vector: `jobs_update` is now admin/sales only (was: any applicator with auth.uid = applicator_id, which let applicators silently mutate price/customer/applicator_id via direct PostgREST); `job_applied_info_insert` and `job_applied_info_update` now require ownership (the auth user must be the applicator assigned to the linked job). `start_job` and `complete_job` are SECURITY DEFINER so applicator workflows continue to work through the RPCs. |
| 251 | 20260430190000 | **Field App Workflow Phase 7 — Codex re-review #1+#2+#3 hot fixes** — three bugs surfaced by the codex re-review of Phases 1–6. **#1 (P1)** added internal auth gates to `start_job` + `complete_job`: validate `auth.uid() = p_performed_by`, then check role (`is_admin()` OR `is_sales_rep()` OR (`is_applicator()` AND `v_job.applicator_id = auth.uid()`)). Closes the privilege-escalation hole that Phase 5's RLS could not (SECURITY DEFINER bypasses RLS). **#2 (P1)** changed `complete_job`'s linked-prebook lookup from `inventory_holds.source_id = jobs.quote_section_id` to `... = jobs.quote_id` — planned-program holds are keyed by quote_id (verified against migration `20260317100000:384,397`); the previous lookup never matched. **#3 (P2)** replaced the single-row hold UPDATE with an oldest-first loop that takes up to each row's `quantity`, preventing CHECK violations when multiple holds exist for the same quote+product. |
| 252 | 20260430200000 | **Field App Workflow Phase 8 — Codex re-review #4 hot fix** — `save_field_app_invoice` edit path now cancels and detaches orphan group child invoices when an admin edits a draft grouped invoice and the new derived customer list drops a previously-billed customer. Per Mason's call (Option B), orphans are marked `status = cancelled`, `invoice_group_id = NULL`, totals zeroed (consistent with the items/shares already wiped), and an `invoice_orphan_cancelled` activity_feed row records the audit trail. Posted/voided members remain protected by the existing edit lock. |
| 253 | 20260430210000 | **Field App Workflow Phase 9 — Sprint A1 auth gates on 3 field-app billing RPCs** — addresses P1 findings from money-inventory and security-permissions audits (2026-04-30). Adds `auth.uid()` gate + actor-mismatch reject + admin/sales role check to `save_field_app_invoice`, `create_invoice_from_blend_ticket`, and `post_invoice_group`. Pattern mirrors `save_quote` and Phase 7 `start_job`/`complete_job`. SECURITY DEFINER bypasses RLS, so internal auth checks are the only protection — previously these RPCs could be called by any authenticated user with a spoofed `p_performed_by`/`p_created_by`. |
| 254 | 20260430220000 | **Field App Workflow Phase 10 — Sprint A2 + Sprint B (folded)** — auth gates + invoice integrity rules on `save_invoice` and `create_invoice_from_order`. Both RPCs use `auth.uid()` directly inline (no `p_performed_by`), so the fix is admin/sales role check + business-rule validation. **`save_invoice`**: now reads `order_id`/`blend_ticket_id` from the `p_invoice` payload and **rejects** standalone-create attempts that link to neither (CLAUDE.md hard rule). **`create_invoice_from_order`**: now **rejects** duplicate active invoices for the same order (any non-cancelled/non-voided invoice counts), preventing the click-twice overbilling bug. Frontend: removed the orphan "New Invoice" buttons on `Invoices.tsx` (top action bar + empty-state CTA) since they pointed to a path that violates the hard rule; users now create invoices via Order Detail or Blend Ticket Detail. |
| 255 | 20260430230000 | **Field App Workflow Phase 11 — Sprint C: field-app RLS lockdown** — closes 2 P1 findings from `2026-04-30-security-permissions-audit-findings.md`. `field_app_locations` and `field_app_location_shares` previously had `USING (true) / WITH CHECK (true)` on every operation, allowing any authenticated user to read AND mutate these tables directly via PostgREST and bypass `save_field_app_invoice` entirely. Tightened: SELECT remains broad (parent invoice/job RLS still applies), INSERT/UPDATE/DELETE restricted to admin/sales. `application_records.app_records_select` now scopes applicators to `applicator_id = auth.uid()` so they can only see their own assigned application history. |
| 256 | 20260430240000 | **Field App Workflow Phase 12 — Sprint A3 + part of D** — auth gates on `confirm_delivery`, `complete_delivery`, `create_quick_delivery`. Replaced `v_actor := COALESCE(p_performed_by, auth.uid())` with strict pattern: reject if not authenticated, reject if `p_performed_by IS NOT NULL AND IS DISTINCT FROM auth.uid()`. Each function's existing role check preserved. **Folded fix from Sprint D**: `complete_delivery` now requires `status='in_progress'` (was: only rejected `completed`/`cancelled`, allowed completing directly from `scheduled` and skipping the start step). 3 of 5 remaining P1 actor-spoofing vectors closed. |
| 257 | 20260430250000 | **Field App Workflow Phase 13 — Sprint A4 ops RPC auth gates** — closes 3 of last 4 actor-spoofing P1 vectors. **`save_purchase_order`**: strict actor check + admin-only role check (was: role check used `p_performed_by` directly without `auth.uid()` comparison). **`receive_po_items`**: strict actor check, admin/sales role preserved (was: COALESCE pattern). **`void_commission_payment`**: strict actor check, admin-only role check (was: COALESCE pattern). Also reordered statements within each function so `purchase_order_items` and `commissions` UPDATEs (tables without `updated_at`) come AFTER any `updated_at`-bearing UPDATE on the same write — this is purely for sql-safety hook compatibility, behavior unchanged. Only `allocate_payment` remains on the actor-spoofing P1 list. |
| 258 | 20260430260000 | **Field App Workflow Phase 14 — allocate_payment auth gate (LAST P1 actor-spoofing vector)** — closes the final P1 from the actor-spoofing class flagged in the codex audits. `allocate_payment` previously used `v_actor := COALESCE(p_performed_by, auth.uid())` then authorized on `v_actor`, allowing any logged-in admin/sales user to spoof another admin/sales user's UUID. Now: strict actor check (auth.uid() not null + p_performed_by mismatch reject) before role check. Existing admin/sales role check preserved. **All 12 of 12 P1 actor-spoofing RPCs are now closed.** |
| 259 | 20260501100000 | **Field App Workflow Phase 15 — Sprint D-policy: drivers can complete + auto-invoice restoration** — closes the two business-decision items Mason answered A1+B1. **A1**: `complete_delivery`'s role check now mirrors `confirm_delivery` — admin/sales OR (driver AND v_actor = v_delivery.assigned_driver). UI was already showing the completion section to drivers; this makes the RPC accept what the UI offered. **B1**: restored server-side auto-create of a draft invoice on completion. Skips when delivery has no order_id, or when any non-voided/non-cancelled invoice already exists for the order (covers `create_invoice_from_order`, `create_quick_delivery`, manual saves). Uses `delivery_items.quantity_delivered` so partial deliveries bill correctly. Result jsonb gains `auto_invoice: { invoice_id, invoice_number, total_cents }` field — frontend `DeliveryDetail.tsx:812-825` already reads this. Closes the revenue-leakage risk where the UI promised an auto-invoice that the rewrite had silently dropped. |
| 260 | 20260501110000 | **Field App Workflow Phase 16 — Sprint E #1: `retire_inventory_item` RPC** — replaces the split frontend flow in `InventoryPage.tsx:644-710` (3 separate validation queries → ledger insert → separate delete) with one atomic `SECURITY DEFINER` RPC. Closes the race-condition window where another user could create a hold/order/pending-delivery between the frontend's validation and its delete. Re-validates atomically with `FOR UPDATE` on the inventory row, inserts the audit ledger row, and deletes — all in one transaction. Admin-only. Frontend `InventoryPage.tsx` updated to call the RPC instead of the multi-step flow. |
| 265 | 20260501160000 | **Field App Workflow Phase 22 — Cleanup Sprint G3 + G4: cleanup tooling RPCs** — two new admin-only SECURITY DEFINER RPCs that resolve production-data issues found by the audit's live SQL checks. **`reconcile_negative_inventory(p_inventory_id, p_new_quantity, p_reason, p_performed_by, p_idempotency_key)`** — admin sets the correct on-hand value for a row in a negative state; the delta is recorded as a paired `inventory_transactions` audit entry with the reason captured in notes. Used to fix the 17 production rows currently in negative buckets. **`create_invoice_for_unbilled_delivery(p_delivery_id, p_performed_by, p_idempotency_key)`** — same auto-invoice logic Phase 15 added inside `complete_delivery`, factored into a manual-trigger RPC. Used to backfill the 60 historical completed deliveries that pre-date Phase 15. Both auth-gated admin-only with the strict actor pattern. New admin page `/integrity-cleanup` surfaces all three issue classes (negative inventory, over-received POs, unbilled deliveries) with per-row action buttons. |
| 264 | 20260501150000 | **Field App Workflow Phase 20 — Cleanup Sprint G1: commission lifecycle fix** — closes the audit's "commissions marked paid before payment posts" finding (six-phase deep audit Phase 1 P1, Phase 2 P1). `create_commission_payment` no longer flips commissions to `paid` status — that transition now happens in `post_commission_payment` when the payment is actually committed. Replaced the old `WHERE c.status != 'paid'` double-pay guard with a new check against `commission_payment_items` membership in non-voided payments (the canonical signal that a commission is already committed). Both RPCs gain the strict auth-gate pattern from Phase 13. `post_commission_payment` was missing `p_idempotency_key` despite the frontend passing it — now properly wired. Live data check: 0 commissions currently in the bad state, but the path was producing it on every create. |
| 263 | 20260501140000 | **Field App Workflow Phase 19 — Sprint F #3: pg_cron for Dashboard-triggered jobs** — closes the audit's concern that `check_remainder_reminders()` and `release_expired_quote_holds()` only ran when someone opened the Dashboard. Both are batch operations safe to run on a schedule. Adds two pg_cron schedules: `release-expired-quote-holds` at 6:15 AM UTC daily, `check-remainder-reminders` at 6:30 AM UTC daily (mark_overdue_invoices already runs at 6:00 AM). Frontend Dashboard.tsx still triggers both RPCs on load — intentional belt-and-suspenders since the RPCs are idempotent (running twice the same day is a no-op). 3 cron jobs now registered: mark-overdue-invoices, release-expired-quote-holds, check-remainder-reminders. |
| 262 | 20260501130000 | **Field App Workflow Phase 18 — Sprint E #3: cycle count item edit gating** — closes the last cycle count integrity gap. `cycle_count_items` rows were directly editable from React via PostgREST `.update()` without any check on parent `cycle_counts.status` — meaning an admin could mutate counted_qty/variance after the parent was already completed, breaking the evidence chain that the inventory_transactions row referenced. Two new RPCs: **`update_cycle_count_item(p_item_id, p_counted_qty, p_notes, p_performed_by, p_idempotency_key)`** locks the parent row, validates `status='in_progress'`, computes variance/variance_pct server-side. **`cancel_cycle_count(p_cycle_count_id, p_performed_by, p_idempotency_key)`** replaces the direct UPDATE in CycleCounts.tsx, validates `status='in_progress'` before flipping to `cancelled`. Both auth-gated admin-only with strict actor pattern. **RLS WITH CHECK guards added** on `cycle_count_items` INSERT/UPDATE/DELETE so even direct PostgREST writes are blocked when parent is not `in_progress` — defense in depth. Frontend `CycleCounts.tsx` updated to call the new RPCs. |
| 261 | 20260501120000 | **Field App Workflow Phase 17 — Sprint E #2: cycle count clamp/ledger drift fix (E2a — block)** — closes audit finding P1-4 from `2026-04-30-data-integrity-workflow-locks-audit-findings.md`. `complete_cycle_count` and `reverse_completed_cycle_count` previously used `GREATEST(0, quantity_available + variance)` to clamp inventory at zero but recorded the FULL variance in `inventory_transactions`, causing permanent ledger ↔ on-hand drift. Mason chose option E2a: `RAISE EXCEPTION` instead of clamping when the math would drive on-hand below zero, with an actionable error pointing at the upstream issue (missing delivery, unlogged return, prior reconciliation gap). Cycle count stays `in_progress` until reconciled; transaction rolls back atomically. Both RPCs gain the strict auth-gate pattern (`auth.uid()` not null + `p_*_by` mismatch reject + admin-only role check, matching Phase 16 `retire_inventory_item`). Bonus fix: `complete_cycle_count` now actually accepts `p_idempotency_key` (frontend was passing it but the previous signature silently dropped it, allowing double-application on double-click). |
| 266 | 20260504100000 | **Open Items #1 — lock order_shares once an invoice is posted** — closes the deferred P2-2 finding from the 2026-04-30 data-integrity audit. New `BEFORE INSERT/UPDATE/DELETE` trigger `trg_order_shares_lock_when_posted` on `order_shares` raises `check_violation` when any non-soft-deleted invoice on the same order has status in (posted, paid, overdue). Drafts/unposted/voided/cancelled invoices stay editable. Trigger function `prevent_order_shares_edit_after_post()` is `SECURITY DEFINER` with `search_path = public, pg_temp`. Frontend `OrderDetail.tsx` derives `sharesLocked` from `invoices[]` and hides the "Add Split" button + per-row trash icons + shows an amber banner naming the locking invoice number. Defense-in-depth: trigger catches admin-script writes, UI lock catches user mistakes early. |
| 268 | 20260506160000 | **Add `invoices.delivery_id` — fixes Phase 15 complete_delivery production failure** — Phase 15 (deployed 2026-05-01) introduced an auto-invoice path in `complete_delivery` that INSERTs into `invoices.delivery_id`, but the column was never added to the table. PL/pgSQL deferred the column resolution to execution time, so the function compiled and lived in `pg_proc` until the auto-invoice block fired (first delivery completion for any order with no existing invoice). Caught in production via DEL-00074 / ORD-2026-0186 on 2026-05-06: `column "delivery_id" of relation "invoices" does not exist` was sanitized by `errorSanitizer.ts` schema-leak catch-all to "An internal error occurred. Please try again." This migration adds the nullable `delivery_id uuid REFERENCES deliveries(id)` column and a partial index `idx_invoices_delivery_id` (only indexed where NOT NULL — most invoices come from orders/blend tickets, not deliveries). Existing invoices keep `delivery_id = NULL` (correct). `Invoice` interface in `src/types/index.ts` updated. |
| 298 | 20260510130000 | **Audit fix sprint PR-22b — deferred AP polish completion** (NOT YET APPLIED, depends on PR-04 + PR-10). Closes the 3 deferred items from PR-22 by re-creating 3 RPCs with the new checks: (#1) `cancel_purchase_order` + `delete_purchase_order` refuse if active vendor_bills are linked (raises `PO_HAS_ACTIVE_BILLS` / `PO_HAS_LINKED_BILLS`). (#2) `create_vendor_bill` PO-to-bill amount soft warn — when `p_purchase_order_id IS NOT NULL` and `ABS(bill_total - po_total) / po_total > 0.05`, inserts notification rows for active admins (never blocks). (#3) `create_vendor_bill` PO-to-bill vendor consistency — verifies `vendors.name` matches `purchase_orders.vendor` (legacy TEXT) on case-insensitive trim, raises `VENDOR_PO_MISMATCH` on mismatch. PO total computed from `SUM(quantity_ordered * unit_cost * 100)::bigint`. Bodies otherwise verbatim from PR-04 (create_vendor_bill) and PR-10 (delete_purchase_order); cancel_purchase_order body verbatim from current pg_proc state. |
| 297 | 20260510120000 | **Audit fix sprint PR-25 — vendor master-data RPCs** (NOT YET APPLIED). Adds two admin-only RPCs: `save_vendor(p_vendor_id, p_payload, p_idempotency_key)` (INSERT-or-UPDATE; pass NULL p_vendor_id to create) and `delete_vendor(p_vendor_id, p_idempotency_key)` (soft-delete via `deleted_at`; refused if any unpaid bills exist). Vendor name is required; payload follows the partial-update pattern from save_customer (`p_payload ?` keys = update, missing = preserve). PO-link check intentionally omitted — purchase_orders uses a legacy `vendor` text column, not `vendor_id`; the FK migration is documented as out of scope in CLAUDE.md "What's NOT in this plan." |
| 296 | 20260510110000 | **Audit fix sprint PR-22 (partial) — AP polish bundle** (NOT YET APPLIED). Three of six plan items applied: (#4) `vendor_bills_total_check` CHECK constraint asserting `total_cents = subtotal_cents + COALESCE(adjustment_cents, 0)`; (#4) partial UNIQUE index `idx_vendor_payments_unique_active_ref` on `(vendor_bill_id, payment_method, reference_number) WHERE reference_number IS NOT NULL AND voided_at IS NULL` to prevent duplicate-payment races; (#5) drop pointless `p_idempotency_key` from `get_ap_aging` (read-only RPC; param was unused); (#6) `trg_vendor_bills_positive_subtotal` BEFORE INSERT/UPDATE trigger enforcing INVALID_AMOUNT on subtotal <= 0. Items #1 (PO cancel/delete linked-bill check), #2 (PO-to-bill amount soft warn >5%), #3 (PO-to-bill vendor consistency) deferred to PR-22b — they require modifying create_vendor_bill/cancel_purchase_order/delete_purchase_order bodies which were just rewired by PR-04/PR-10. |
| 295 | 20260510100000 | **Audit fix sprint PR-14 — update_vendor_bill RPC** (NOT YET APPLIED, depends on PR-04). New admin-only RPC `update_vendor_bill(p_bill_id, p_subtotal_cents, p_adjustment_cents, p_bill_date, p_due_date, p_notes, p_idempotency_key)` returning jsonb. Guards: status='unpaid', no active payments, subtotal>0, due_date>=bill_date, period_open at p_bill_date (re-checked since bill_date may change). total_cents recomputed from inputs; balance_cents (GENERATED post-PR-04) and paid_cents untouched. bill_number NOT editable (uniqueness invariant). Audit log entry with operation_type='vendor_bill_updated'. |
| 294 | 20260510090000 | **Audit fix sprint PR-13 — void_vendor_payment RPC** (NOT YET APPLIED, depends on PR-04). New admin-only RPC `void_vendor_payment(p_payment_id, p_reason, p_idempotency_key)` returning jsonb. Locks payment + bill rows (FOR UPDATE), validates payment isn't already voided, decrements bill.paid_cents (balance_cents is GENERATED in PR-04 and recomputes), recalculates bill status (paid/partially_paid/unpaid), populates payment.voided_at/voided_by/void_reason, writes financial_audit_log with operation_type='vendor_payment_voided'. REASON_REQUIRED token raised on blank reason. |
| 293 | 20260510080000 | **Audit fix sprint PR-10 — bulk idempotency wiring on 12 mutating RPCs** (NOT YET APPLIED). All 12 declared `p_idempotency_key` but bodies never used it — silent re-execution on retry. Functions wired: post_invoice, void_invoice, save_invoice, save_customer, increment_customer_prepay, convert_quote_to_order, reassign_delivery, batch_cancel_deliveries, delete_purchase_order, link_blend_ticket_to_order, unlink_blend_ticket_from_order, close_accounting_period. Canonical helper-function pattern from CLAUDE.md (`check_idempotency` / `save_idempotency`); search_path on all 12 already `public, pg_temp`. Return-shape variations handled: void → RETURN; save success-jsonb; uuid → unwrap via `(v_existing->>'key')::uuid`; integer → unwrap `(v_existing->>'count')::integer`; jsonb → return v_existing; json → cast to/from jsonb. File-level `-- idempotency-body-check: exempt` marker. |
| 292 | 20260510070000 | **Audit fix sprint PR-07 — customer + profile RLS tightening** (NOT YET APPLIED). customers_select gains driver time-window (deliveries within `scheduled_date >= CURRENT_DATE - INTERVAL '1 day'`) and applicator scoping (jobs within `job_date >= CURRENT_DATE - INTERVAL '7 days'`); admin and assigned-sales_rep branches preserved. profiles_select tightened from `USING (true)` to admin-or-self. New `profile_public_view` (id, full_name, role, is_active) with `security_invoker = off` so non-admin reads of safe columns work for assignment dropdowns. Live DB inspection corrected the plan: jobs uses `job_date` (not `scheduled_date`), sales_reps were already scoped, drivers were already scoped (just lacked the time window). |
| 291 | 20260510060000 | **Audit fix sprint PR-17 — tighten `team_note_tags` SELECT RLS** (NOT YET APPLIED). Replaces over-permissive `USING (true)` SELECT policy with EXISTS check on the parent `team_notes` row. Mirrors the join pattern of the existing INSERT/DELETE policies and removes the unconditional `true` red flag. Compromise vs the original plan's admin/sales_rep gating since `team_notes` itself is `USING (true)` — full tightening would break the team-board UI for non-admin roles; the parent table's policy should change first if stricter scoping is desired. |
| 290 | 20260510050000 | **Audit fix sprint PR-12 — add `pg_temp` to SECURITY DEFINER violators** (NOT YET APPLIED). Adds `SET search_path = public, pg_temp` to `auto_expire_quotes` and `release_holds_on_quote_status_change` (both had empty `''` search_path). Plan called out 4 functions; live inspection showed the other 2 (`record_invoice_payment`, `close_accounting_period`) already had it. Both fixed bodies use schema-qualified references throughout, so the change is purely additive. Verification block asserts the new search_path setting on both. |
| 289 | 20260510040000 | **Audit fix sprint PR-06 — quick-delivery credit limit soft warn** (NOT YET APPLIED, must apply AFTER PR-02). Replaces the hard-blocking `RAISE EXCEPTION` in `create_quick_delivery`'s credit-limit check with admin notification + activity_feed entry per Mason's Q4 (Option C). Three behavior changes: (1) AR scope now `status IN ('draft','posted','overdue')` (was `'posted'` only); (2) projected exposure = current AR + new delivery total (post-delivery exposure, not current state); (3) on overage, INSERT activity_feed `event_type='credit_limit_warning'` + INSERT notifications row per active admin — delivery proceeds normally. Return jsonb gains `credit_warning: boolean`. Supersedes PR-02's `create_quick_delivery` body — apply order matters. |
| 288 | 20260510030000 | **Audit fix sprint PR-04 — AP structural fixes (HIGH RISK, NOT YET APPLIED).** Six-block migration: (1) Schema — adds `voided_at`/`voided_by`/`void_reason` columns to `vendor_bills` + `vendor_payments`; converts `vendor_bills.balance_cents` to GENERATED ALWAYS AS `(total_cents - paid_cents)` STORED via DROP+ADD (atomic per migration runner); adds UNIQUE partial index on `(vendor_id, bill_number) WHERE deleted_at IS NULL AND status <> 'voided'`; expands `financial_audit_log.entity_type` CHECK to include `vendor_bill`/`vendor_payment`/`purchase_order` and `operation_type` to include the matching create/void/record events. (2) RLS — `vendors_select` tightened from `USING (deleted_at IS NULL)` to admin/sales_rep only. (3-5) Three RPC rewrites — `create_vendor_bill` (canonical idempotency, `check_period_open(bill_date)`, vendor active check, audit log INSERT), `record_vendor_payment` (canonical idempotency, no longer writes balance_cents — it's GENERATED, audit log INSERT, no period gate per Q8), `void_vendor_bill` (canonical idempotency, populates void columns instead of stuffing into notes, paid-bill hard guard per Q11, audit log INSERT). (6) Verification block asserts overload counts + schema landed. Machine-readable error tokens: `AUTH_REQUIRED`, `NOT_AUTHORIZED`, `BILL_NOT_FOUND`, `BILL_VOIDED`, `INVALID_AMOUNT`, `OVER_PAYMENT`, `BILL_ALREADY_VOIDED`, `BILL_HAS_ACTIVE_PAYMENTS`, `VENDOR_NOT_FOUND`, `REASON_REQUIRED`. PR-13/14/22/25 depend on this — test on a Supabase preview branch before applying to prod. |
| 287 | 20260510020000 | **Audit fix sprint PR-02 — fix idempotency replay in 3 RPCs** (NOT YET APPLIED). `record_invoice_payment`, `create_quick_delivery`, `update_order_items` were using a broken replay check `(v_existing->>'status') = 'completed'` (or `= 'created'`) that never matched — network retries silently re-executed the mutation. Migration replaces each with the canonical `IF v_existing IS NOT NULL THEN RETURN ...` pattern. `record_invoice_payment` returns uuid, so cache hit unpacks via `(v_existing->>'payment_id')::uuid` matching the saved `jsonb_build_object('payment_id', v_pay_id)` shape. The other two return jsonb, so cache hit returns `v_existing` directly. Plan listed 5 RPCs; live inspection narrowed to 3 — `receive_po_items` already had the canonical pattern, `create_prepay_check_splits` does not exist in the production database. Function bodies are otherwise verbatim from their most recent definitive migrations; only the broken idempotency block changed (plus search_path normalization on `record_invoice_payment` from `public` → `public, pg_temp`). |
| 286 | 20260510010000 | **Audit fix sprint PR-01 — fix `delivery_date` column refs in `complete_delivery` + `void_delivery`** (NOT YET APPLIED). Both functions referenced `v_delivery.delivery_date` 3× each but the `deliveries` table column is `scheduled_date` — any closed-period warning path was crashing with PostgreSQL 42703 ("column delivery_date does not exist"). The latest definitive bodies (`20260507220000` for complete, `20260507160000` for void) were preserved verbatim with column substitutions; one user-facing message in `void_delivery` was also updated from "voided for delivery_date" → "voided for scheduled date" so the message matches the actual column. |
| 285 | 20260507220000 | **Final-wave-review F8 — restore tote_number copy step in `complete_delivery`** — Phase 15's `20260501100000` rewrite (and the wave 4 corrective `20260507190000` that faithfully copied it) silently dropped the post-loop `UPDATE invoice_items SET tote_number = di.tote_number FROM delivery_items ...` block originally added in `20260301100001`. Net effect since 2026-05-01: any non-quick delivery completed leaves `invoice_items.tote_number = NULL`, breaking load sheet PDFs, delivery PDFs, and 6 other frontend files that read the column. This migration restores the copy step verbatim and also threads `tote_number` into the Phase 15 #B1 auto-create-draft-invoice INSERT. Function signature unchanged. |
| 284 | 20260507210000 | **Final-wave-review F7 — stop phantom-inventory accrual on declined/expired/cancelled holds** — `release_holds_on_quote_status_change`, `auto_expire_quotes`, and `cancel_order` all UPDATE-added `v_hold.quantity` to `inventory.quantity_available` when deactivating planned-quote holds, but `create_planned_holds` and `create_inventory_hold` never DEBIT `quantity_available` on hold creation (CLAUDE.md's "Net Free = available − planned holds − prebooked" canonical formula treats holds as soft reservations). Net effect since 2026-03-16: every declined/expired planned quote inflates `quantity_available` by the hold's qty with no `inventory_transactions` audit row. This migration removes the restoration UPDATE from all three functions; deactivation + activity logging preserved. **Stops the bleeding.** A historical-cleanup pass was scoped at [docs/plans/2026-05-08-holds-historical-inflation-cleanup.md](../plans/2026-05-08-holds-historical-inflation-cleanup.md) but **resolved 2026-05-08 with no migration needed** — diagnostic queries (Q1/Q2/Q3) returned zero per-product inflation; the bug existed in code for ~7 weeks but the conditions to fire it (a planned quote reaching `declined`/`expired`, or a cancelled order whose source quote had active planned holds) never occurred in production. `tests/e2e/holds-cleanup-paths.spec.ts` Path B passes on production code with only this migration applied. |
| 283 | 20260507200000 | **Final-wave-review F1 (BLOCKER) — fix NULL-concat crash in `create_inventory_hold`** — The wave 4 P4-3 migration (`20260507170000`) put the FORCE_REQUIRES_REASON / FORCE_REQUIRES_ADMIN guards INSIDE the `IF v_todays_free - p_quantity < 0` branch. A caller passing `p_force=true AND p_force_reason=NULL` with sufficient inventory entirely bypassed validation; execution fell through to `'WARNING: Hold created with admin override (' \|\| p_force_reason \|\| ')'` which becomes NULL when concatenated with NULL. `activity_feed.description` is `NOT NULL DEFAULT ''` (Postgres only applies DEFAULT on column omission, not on explicit NULL), so the function raised a constraint violation and rolled back the entire txn — including the inventory_holds INSERT. Wave 4 self-review labeled this "unreachable from UI" but a direct API caller could reach it. This migration hoists the force-contract checks above the inventory threshold so they fire unconditionally when `p_force=true`. Function signature unchanged; same error tokens (just fire earlier now). |
| 282 | 20260507190000 | **Wave 4 self-review — restore Phase 15 features in `complete_delivery`** — Wave 3's migration `20260507160000` copied `complete_delivery`'s body verbatim from `20260319200000` (March 19) instead of `20260501100000` (Phase 15, May 1, the latest definitive body). Applying wave 3 to live would silently regress: (1) inventory pre-check from `20260331200000`, (2) `in_progress` status guard from Phase 12, (3) driver-can-complete authorization from Phase 15 #A1, (4) Phase 15 #B1 auto-invoice creation, (5) strict actor pattern (Phase 12), and (6) re-introduce the auto-create-row branch (P4-7 phantom rows). This corrective migration runs AFTER wave 3 (timestamp ordering) and `CREATE OR REPLACE`s `complete_delivery` with the Phase 15 body PLUS the P4-10 backdated warn block. `void_delivery` is not touched — wave 3 correctly used the latest body for that one. Idempotent: applying both wave 3 and wave 4 in order yields the correct final state. |
| 281 | 20260507180000 | **Phase 4 P4-7 — `inventory.manufactured_at_delivery` flag + `mark_inventory_row_verified` RPC** — Defensive infrastructure for phantom-row tracking. Adds a `boolean NOT NULL DEFAULT false` column to `inventory`. The current Phase 15 `complete_delivery` body (`20260501100000`) raises `'Insufficient inventory'` on the NOT FOUND branch rather than auto-creating, so the flag stays at false in normal operation — but if a regression or a future driver-app path ever manufactures a row, the column is in place to track it. Admin-only RPC `mark_inventory_row_verified(p_inventory_id, p_performed_by, p_idempotency_key)` clears the flag after physical-stock confirmation; logs to `activity_feed` (`event_type='inventory_row_verified'`). Idempotent: keyed on `(idempotency_key, operation)`, 24-hour TTL. SECURITY DEFINER, search_path = public, pg_temp. Frontend: `Inventory` interface gets the `manufactured_at_delivery: boolean` field; `InventoryPositionRow` does NOT (the `get_inventory_position` RPC does not return it — only IntegrityCleanup queries the column directly). New "Phantom inventory rows" section on `/integrity-cleanup` with a per-row "Mark Verified" button gated by a `ConfirmModal`. |
| 280 | 20260507170000 | **Phase 4 P4-3 — server-side `create_inventory_hold` RPC** — Closes the manual-hold concurrency gap. Two admins clicking "Create Hold" simultaneously could each pass the client-side warning and both succeed, leaving total holds in excess of available inventory. New RPC takes a `FOR UPDATE` lock on the Main Warehouse inventory row, recomputes today's free (= `available − prebooked − active holds where expires_at IS NULL OR ≥ CURRENT_DATE`) inside the transaction, and `RAISE EXCEPTION 'INSUFFICIENT_HOLD_INVENTORY'` if the requested qty would push it negative. Admin can override by passing `p_force=true` with a non-blank `p_force_reason` (mirrors PO over-receive override pattern from Phase 21 G2). Sales reps can create non-force holds; force-create is admin-only. Idempotent: keyed on `(idempotency_key, operation)`, 24-hour TTL, `ON CONFLICT DO NOTHING`. Activity feed entry on every successful create with `WARNING:` prefix when forced. Frontend: `InventoryPage.tsx` `handleCreateHold` rewired to call the RPC; new `<ReasonModal>` mounted to capture force-reason when the server rejects with `INSUFFICIENT_HOLD_INVENTORY` and the user is admin. Bare `supabase.from('inventory_holds').insert(...)` removed. |
| 279 | 20260507160000 | **Phase 4 P4-10 — WARN-only backdated check on `complete_delivery` + `void_delivery`** — Mason's Q3 policy decision (warn, don't block, no admin override). Both function bodies copied verbatim from their most recent definitive migrations (20260319200000 for complete_delivery, 20260332300000 for void_delivery) with one new block inserted after status validation but before any mutation: a non-blocking lookup against `accounting_periods` for a closed period covering `v_delivery.delivery_date`. If found, inserts an `activity_feed` row (`event_type='backdated_delivery_in_closed_period'`) and a per-admin `notifications` row (`notification_type='period_warning'`); then the function continues normally and the lifecycle transition + inventory side effects proceed. We deliberately do NOT call `check_period_open()` because that helper RAISES (it's the hard gate for `post_invoice` / `record_payment`); for delivery completion the policy is soft warning only. Function signatures unchanged → no TypeScript callers needed updating. |
| 278 | 20260507150000 | **Wave B.3 (B.3.a) — `get_inventory_position()` read-only RPC (audit findings P4-1 + P4-2)** — replaces the 4 separate fetches + JS reduce in `InventoryPage.tsx:174-224` with a single round-trip. Returns one row per (product, location) for active products: `quantity_available`, `quantity_prebooked`, `quantity_on_order`, `holds_qty`, `planned_qty`, `delivered_ytd` (season-to-date), `net_position` (= available − prebooked + on_order, Option B canonical per Mason's pick), plus product metadata (name, unit, container, vendor, current_cost, reorder_point, min_stock_level, is_low_stock). Holds and planned-quote demand are RETURNED separately, not subtracted into net_position — the existing "Planned" column on InventoryPage will keep showing them. Read-only → no idempotency key, follows the `get_inventory_forecast` precedent. CTEs aggregate each input source once, then LEFT JOIN by product_id (avoids N+1 sub-selects on a 1000-row catalog). SECURITY DEFINER, search_path = public, pg_temp. `InventoryPositionRow` interface added to `src/types/index.ts`. UI consumers (main grid, hold warning, Forecast tab) wired in subsequent B.3 commits. |
| 277 | 20260507140000 | **Wave B audit B-10 — `returns_cancelled_has_reason_check` CHECK constraint** — Wave B.2 left `cancellation_reason` nullable; the RPC enforces it at write time but nothing at the DB level prevented a future bare `.update({status:'cancelled'})` from leaving the column blank. Defensive backfill (no-op against live; zero cancelled rows existed) followed by `CHECK (status <> 'cancelled' OR cancellation_reason IS NOT NULL)`. cancel_return RPC always passes; any future bypass fails loudly. |
| 276 | 20260507130000 | **Wave B audit B-2 + B-11 — `cancel_return` surfaces skipped_count + defensive ordering** — when the inventory row was deleted between receive and cancel (e.g. product retired), the LEFT JOIN LATERAL returned NULL and the IF block was silently skipped, leaving items with `restocked=true` despite no inventory reversal. Now mirrors `receive_return`'s `RAISE WARNING` + `v_skipped_count` pattern, surfaces `skipped_count` in the result jsonb and activity_feed, still flips restocked=false on skipped items. Returns.tsx warns the admin to reconcile manually when skipped_count > 0. Both RPCs also reorder the bulk `UPDATE return_items` to run BEFORE `save_idempotency` (defensive — PG function bodies are atomic so the cache and state can't actually diverge today, but the new ordering removes any need to think about it). |
| 275 | 20260507120000 | **Wave B audit B-3 — rename `invoices.temperature` → `temperature_text`** — Wave B.1 added the column as free-form text, conflicting with five sibling temperature fields elsewhere in the codebase that are all numeric (BlendTicket, FieldAppLocation, JobAppliedInfo, plus reportPdf.ts and LogbookReport.tsx that assume numeric). The example "72F" would render as "72F°F" in JobDetail.tsx. Per Mason's Q9 the value is intentionally free-form; column rename only (no data touched), Invoice TypeScript interface and FieldApplicationInvoice.tsx fetch+save updated. |
| 274 | 20260507110000 | **Wave B.2 — Rebuild returns-lifecycle RPCs (audit findings P4-4, P4-5)** — `approve_return`, `receive_return`, `issue_return_credit` accepted `p_idempotency_key` but the bodies never called `check_idempotency` / `save_idempotency` (the dynamic-injection consolidation in 20260331600000 added the param but baked in stale bodies). The frontend's idempotency hook was operating in the dark. `cancel_return` did not exist — the frontend's `handleCancel` was a bare `.update({ status: 'cancelled' })` with no RPC, no FOR UPDATE, no audit trail, and no inventory reverse when cancelling a 'received' return. Per Mason's Q3: cancelling a 'received' return MUST reverse the restock. New `cancel_return(p_return_id, p_reason, p_performed_by, p_idempotency_key)` RPC reverses inventory by inserting a 'returned' inventory_transactions row with NEGATIVE quantity (no new transaction_type, existing CHECK stays untouched), decrements `inventory.quantity_available`, and flips `return_items.restocked` back to false. Adds `cancelled_at`, `cancelled_by`, `cancellation_reason` columns to `returns` for symmetry with the existing approved/received/credited triplets. All four RPCs now return jsonb with detailed result shapes. Live `issue_return_credit` had been silently regressed to `RETURNS void` by the consolidation — the frontend reads only `error` so the regression was invisible; this migration restores `RETURNS jsonb` with explicit DROP+CREATE. |
| 273 | 20260507100000 | **Wave B.1 — Persist field-app Applied Info (audit finding P2-1)** — `FieldApplicationInvoice.tsx` rendered three editable inputs under the "Applied Info" tab (windDirection, temperature, applicator) that looked saved (same focus ring, flipped the dirty flag) but were React state only. windDirection was read as a per-location fallback; temperature and applicator were never read anywhere except their own onChange handlers. After save+reload, all three were blank — silently discarding state-compliance, dispute-defence, and applicator-history data. Per Mason's Q9 answer (business-internal, NOT legally required) the columns are nullable: `ADD COLUMN IF NOT EXISTS wind_direction text, temperature text` on `invoices`. `applicator_name` already existed but was never wired. Frontend now loads from `invoices.*` in `fetchInvoice` and persists via a follow-up `.update()` after `save_field_app_invoice` succeeds (every invoice in a multi-customer group gets the same Applied Info). |
| 272 | 20260506200000 | **Wave A.1 follow-up — `reverse_write_off` re-derives 'overdue' too** — caught during Wave A self-review by an independent SQL reviewer. The Wave A.1 rewrite at 20260506170000 only flipped `'paid'` -> `'posted'` after a reversal lifted balance > 0. An invoice that was originally `'overdue'` (past due_date with balance > 0), then got fully written off (balance to 0, status to `'paid'`), and now has the write-off reversed, would land in `'posted'` instead of `'overdue'` — `mark_overdue_invoices()` would self-heal it on the next batch run, but until then AR aging was wrong. This migration adds a due_date check after the lift: `'paid' && balance > 0 && due_date < CURRENT_DATE` -> `'overdue'`, otherwise `'posted'`. Same predicate as `mark_overdue_invoices()` (20260316115721). Result jsonb gains `new_status` field. |
| 271 | 20260506190000 | **Wave A.6 — Replace dynamic-clone idempotency fixes with explicit rewrites (audit finding P3-2)** — Migration `20260332200000_fix_idempotency_column_refs_round2.sql` used the runtime-introspection / regex-replace pattern to surgically patch 9 RPCs plus `save_quote`. That pattern is fragile (output depends on whatever happens to be in `pg_proc` at apply-time, not a known source-of-truth body) and project-banned. This migration replaces 6 of those RPC definitions explicitly: `reopen_accounting_period`, `revert_quote_status`, `restore_cancelled_order`, `restore_cancelled_delivery`, `unapply_credit_memo`, `reverse_blend_ticket_approval`. Each gets the correct `idempotency_keys` column shape, idempotency-replay now returns the cached jsonb (so callers see the SAME response on retry instead of the prior `{status:'already_processed'}` placeholder), and `SET search_path = public, pg_temp`. `reverse_write_off` was already explicitly rewritten in Wave A.1 / migration 20260506170000; `void_delivery`, `void_commission_payment`, and `save_quote` were re-authored statically in their own post-Mar-31 migrations and stay untouched here. Side-fix: `unapply_credit_memo` audit-log new_values now records the canonical `'voided'` status string. |
| 270 | 20260506180000 | **Wave A.4 — Guard prepay-application paths with per-invoice period check (audit finding P3-4)** — `apply_remaining_prepayments` and `batch_apply_prepayments` previously called `check_period_open(CURRENT_DATE)` — verifying TODAY's period was open, but never the period of the invoice being touched. So an admin running on May 4 could apply a prepay against an April 28 posted invoice (April closed) without going through `reopen_accounting_period`, bypassing the period-close lock that month-end depends on. New explicit `CREATE OR REPLACE` for both functions runs `PERFORM check_period_open(v_invoice.invoice_date)` for every invoice mutated; any invoice in a closed period raises and rolls back the entire batch. `apply_remaining_prepayments` now has its 3-arg signature `(uuid, uuid, text)` declared statically (the previous 3-arg version was authored via the dynamic-clone migration that audit P3-2 banned). Both functions get `search_path = public, pg_temp`. `apply_prepay_to_invoice` (server-side helper called only from batch) keeps its inner `check_period_open(CURRENT_DATE)` as a belt-and-suspenders today-check; the per-invoice check happens upstream in batch_apply_prepayments. |
| 288 | 20260516140000 | **Restore `profile_public_view` — partial revert of 20260511120000** — at audit time grep across `claude/app-review-audit-yseuL` showed zero callsites for the view, so it was dropped to clear the `security_definer_view` ERROR advisor. In parallel, `fix/audit-2026-05-09` was migrating 30+ callsites TO the view (PR-07 follow-up) so non-admin callers could fan out admin notifications and populate name dropdowns under the admin-or-self profiles RLS. The conflict surfaced as a PR #60 review comment on 2026-05-16. View restored with identical shape (`id, full_name, role, is_active FROM profiles`) and owner-defined semantics. The `security_definer_view` advisor warning will reappear; documented as accepted-by-design on the view comment. Applied to prod via `apply_migration` immediately when the conflict was reported; this file documents that state for fresh DB replays. |
| 287 | 20260511120100 | **Security audit follow-up — drop bucket-specific SELECT policies on 3 public buckets** — clears the remaining `public_bucket_allows_listing` warnings on `delivery-photos`, `receiving-photos`, `team-note-attachments`. The dropped policies (`delivery_photos_select`, `recv_photos_storage_read`, `Anyone can view team note attachments`) were broad bucket-scoped SELECT policies that let any authenticated user list every file. Public-bucket file rendering (`<img src={publicUrl}>`) is served from CDN and bypasses RLS, so dropping these does not affect normal use. Verified by grep on 2026-05-11 that no frontend code calls `.list()`, `.download()`, or `.createSignedUrl()` against these buckets. Upload, delete, and getPublicUrl paths use INSERT/DELETE policies (not SELECT) and stay functional. |
| 286 | 20260511120000 | **Security audit fixes from 2026-05-11 `/audit` run** — five Supabase advisor findings in one consolidated migration. (1) `function_search_path_mutable` — pins `SET search_path = public, pg_temp` on `guard_audit_log_immutable`, `_fill_audit_actor`, `_enforce_quote_status_transition`, `_enforce_return_status_transition` (closes 4 WARN advisories). (2) `security_definer_view` — drops unused `profile_public_view` (zero callsites in src/, supabase/, or `pg_proc.prosrc`). (3) `rls_policy_always_true` on `blend_ticket_fields` — replaces 3 always-true INSERT/UPDATE/DELETE policies with parent-scoped predicates that mirror `blend_tickets` (uploader OR admin OR sales_rep can mutate; admin-only delete). SELECT (currently `true`, not flagged) left untouched. (4) `rls_policy_always_true` on `field_crop_history` — replaces 3 always-true INSERT/UPDATE/DELETE policies with role-gated predicates that mirror `fields` (admin OR sales_rep can mutate; admin-only delete). (5) `public_bucket_allows_listing` — drops 4 global `qual=true` policies on `storage.objects` (`Authenticated users can upload 1evsna5_*`) that leaked listing access across every bucket. Each of the 6 buckets retains its own bucket-specific INSERT/SELECT/DELETE policies (verified by `pg_policies` inspection 2026-05-11). Net advisor change: 1 ERROR + 13 WARN → 0 ERROR + 0 WARN for fixable findings. Auth `leaked_password_protection` is a Dashboard toggle, deferred per Mason. The 424 routine `anon/authenticated_security_definer_function_executable` WARNs are by-design (frontend RPC calls). |
| 269 | 20260506170000 | **Wave A.1 — Fix `reverse_write_off()` (audit finding P3-1)** — three bugs fixed in one explicit `CREATE OR REPLACE`: (1) the function wrote `balance_cents = balance_cents + amount_cents` but `invoices.balance_cents` has been GENERATED ALWAYS since 20260305200000, so every call has been raising `column "balance_cents" can only be updated to DEFAULT` — the bug was masked because no admin had clicked "Reverse Write-Off" in production yet. Fix decrements `write_off_cents` (one of the GENERATED inputs) with `GREATEST(..., 0)` floor for drift. (2) Status was not re-derived: a write-off that drove balance to 0 flipped status to `'paid'`, but reversing it left status as `'paid'` even with balance > 0 — silent AR-aging misclassification. Fix re-reads `balance_cents` after the decrement and flips `'paid'` -> `'posted'` when balance > 0. (3) Idempotency block referenced wrong column names; replaced explicitly with the correct `idempotency_key`/`operation`/`result` (jsonb) shape and now stores the full result jsonb so replays return identically. Adds `pg_temp` to `search_path`. Result jsonb gains `new_balance_cents` and `status_changed` keys. New contract tests in `src/lib/rpcContracts.test.ts`. |
| 286 | 20260511050000 | **Perf sweep #1 — rewrite RLS policies to wrap `auth.uid()` in `(SELECT auth.uid())`** (Supabase advisor `auth_rls_initplan`, 63 WARN). 55 policies across 35 tables had `auth.uid()` evaluated per row; Postgres re-runs the function each row when it's a bare call but caches it once per query when wrapped in a scalar subquery. Each policy was dropped and recreated with the same action, roles, permissive flag, and predicate — only the auth.uid() calls changed. The 8 remaining flagged policies overlapped with `multiple_permissive_policies` and were handled in migration 287 (which consolidates AND wraps in one go). Hot tables affected: `inventory_holds`, `invoice_shares`, `order_shares`, `blend_tickets`, `vendor_bills`, etc. Verification block asserts no flagged policy still contains an unwrapped `auth.uid()`. Net: 65 `auth.uid()` calls wrapped, 0 remaining. After migration: auth_rls_initplan dropped from 63 → 8 (residual handled in #287). |
| 287 | 20260511060000 | **Perf sweep #2 — consolidate overlapping permissive RLS policies** (Supabase advisor `multiple_permissive_policies`, 33 WARN → 23 unique overlap groups across 16 tables). Multiple permissive policies covering the same (role, action) cause Postgres to evaluate each predicate per row and OR them together. Consolidation merges each overlap group into one permissive policy whose predicate is the OR of the originals — same union of access, one evaluation per row instead of N. Patterns handled: (A) `_admin_all FOR ALL` + role-specific INSERT/UPDATE/DELETE/SELECT (`deliveries`, `delivery_items`, `delivery_photos`, `delivery_remainders`, `receiving_photos`, `receiving_records`) — split the FOR ALL into action-specific policies merged with the role policies; (B) `_admin_all FOR ALL` + open `_select` qual=true (`application_services`, `customer_application_rates`, `quote_pdf_templates`, `quote_templates`) — drop the admin_all SELECT side (the open SELECT covers it), keep admin INSERT/UPDATE/DELETE narrow; (C) admin SELECT + sales_rep SELECT (`purchase_orders`, `purchase_order_items`) — merge with `is_admin() OR is_sales_rep()`; (D) admin SELECT + user-own SELECT (`document_processing_log`, `commissions`) — merge with admin OR ownership predicate; (E) redundant admin policies (`failed_notifications` had a SELECT policy redundant with a FOR ALL one — drop the SELECT). **Sensitive-table change:** `rate_limit_log` had a permissive `qual=false` "Deny all direct access" policy that was a no-op (permissive false OR'd with permissive admin = admin; access semantics unchanged). Replaced with a **RESTRICTIVE** `FOR ALL admin_only` policy as defense-in-depth — if a future migration ever adds a permissive policy by mistake, the RESTRICTIVE still blocks non-admins. All rewritten policies also use `(SELECT auth.uid())` wrapping so this migration closes the 8 residual auth_rls_initplan findings. Verification asserts old policies are gone, 39 new policies exist, and the RESTRICTIVE policy is the correct permissive flag. |
| 288 | 20260511070000 | **Perf sweep #3 — `CREATE INDEX CONCURRENTLY` on 72 unindexed foreign keys** (Supabase advisor `unindexed_foreign_keys`, 72 INFO across 46 tables). FK columns lacking covering indexes cause sequential scans on JOINs and cascading deletes. Each `idx_<table>_<column>` was created with `CREATE INDEX CONCURRENTLY IF NOT EXISTS` (non-blocking; existing reads/writes weren't paused during the build). Marker comment `-- supabase-no-transaction` in the file; applied via per-statement `execute_sql` (CONCURRENTLY can't run inside a transaction block). Hot indexes: `invoices.posted_by`, `invoices.salesman_id`, `invoices.voided_by`, `invoices.blend_ticket_id`, `returns.approved_by/cancelled_by/received_by/credit_invoice_id`, `delivery_remainders.{followup,order_item,product}`, `rebate_claims.*`, `write_offs.*`, `vendor_bills/payments.{created,voided}_by`, etc. After migration: unindexed_foreign_keys 72 → 0. New indexes will show as `unused_index` until real queries hit them; this is expected and self-resolves. **Note (2026-05-12 audit):** the migration file's own header comment estimates "71 indexes" (predicting one would be a no-op due to an existing composite-leading-column index). DB ground truth confirmed via `SELECT count(*) FROM pg_indexes WHERE indexname IN (...72 names...)` returned **72** — all 72 statements created new indexes; the estimate was off by one. Migration file left as-is per immutability policy; this entry and CLAUDE.md / CHANGELOG.md (which all say 72) are the authoritative count. |
| 289 | 20260511080000 | **Perf sweep #4 — drop duplicate index `idx_payments_order`** (Supabase advisor `duplicate_index`, 1 WARN). `payments` had two identical btree indexes on `(order_id)`: `idx_payments_order` and `idx_payments_order_id`. The `_order_id` variant matches the `idx_<table>_<column>` convention used elsewhere; the unsuffixed was the historical duplicate. Drop the unsuffixed; the surviving index still covers the `payments.order_id` FK. Verification asserts the dropped index is gone and the surviving one is still present. After migration: duplicate_index 1 → 0. |
