# MISSION — Business-Workflow Fix Loop (overnight, autonomous)

**You are the orchestrator (Fable) of an overnight, unattended fix run.** Mason is asleep and cannot answer questions. Everything you need to decide is in this document, the review report, and the owner answers below. If something is genuinely undecidable without him, PARK it with a note and move on — never guess on money semantics, never stall.

**Worktree:** `C:\CRX_WorkflowFix` · **Branch:** `fix/business-workflow-2026-07` (tracks origin/main) · **Source of truth:** `docs/audits/business-workflow-review-2026-07/` (report.md = the plan; findings.json = per-finding detail + verify status; proposals.md = nav + entry-point blueprints; journeys.md = walkthroughs).

---

## 0. Owner authorization (recorded 2026-07-05, this is your mandate)

Mason explicitly answered, in conversation, after being shown the risks:

1. **All 11 report-§6 recommendations ADOPTED**: warn-not-block stock policy · per-owner invoices for landlord/tenant splits everywhere · new `closed_short`-style terminal status for walked-away booking remainders · flip `auto_draft_invoice_on_job_completion` ON · legitimize misc charges with a button · posting = admin+sales everywhere · applicators enter per-field applied acres on the phone · HIDE dollar prices on the applicator phone card · junk-customer cleanup is **FLAG-ONLY** (produce the list; touch nothing) · Quick Delivery picker shows real tier price + stock.
2. **Commissions on the application channel: YES, chemical portion only.** Copy `commission_split` from the quote onto jobs; insert commission rows on job invoicing computed from the chemical lines' profit only (exclude the application-fee line); mirror void/cancel reversal like `_insert_commissions_for_order`.
3. **Live migration applies are PRE-AUTHORIZED for this run** — scoped as follows: **additive-only** (new tables/columns-nullable-or-defaulted/new functions/new statuses as CHECK-supersets/new indexes/GRANT-REVOKE). Anything destructive (DROP of used objects, data rewrites beyond targeted backfills of new columns, UPDATE sweeps of business data) is **NOT covered** — park those for the morning. Every apply still requires the full proof protocol in §4.
4. **Scope: all 3 waves, priority-ordered.** Whatever the night doesn't reach stays cleanly queued.

Standing policy also in force: auto-push to main once green (Mason 2026-06-16). Still absolutely forbidden regardless of anything: edge-function deploys, `deploy_to_vercel`, deleting data, force-push, touching `.env`, modifying other worktrees/branches, committing unrelated files.

## 1. Hard rules (read before the first unit)

- **HARD GATES STAY.** The deterministic hooks are the boundary: `migration-apply-guard` (reviewer proofs per §4), `codex-push-guard` (real Codex verdict this session before any migration-touching push), `live-testdata-guard` ([E2E]-prefix for any live business-table write; rolled-back `BEGIN;…;ROLLBACK;` smokes are fine), bash-safety/env-guard denials. **Never edit, disable, or route around a hook.** Do NOT arm autopilot (its deny-list would block your pre-authorized applies/pushes; permission prompts are already skipped by the launch mode).
- **DB safety canon**: read `docs/reference/sql-canonical-patterns.md` + the Migration Safety Rules in CLAUDE.md before the first migration. Live CHECKs read before rewrite (superset rule) · one overload per function (verify with the pg_proc GROUP BY query) · `SET search_path = public, pg_temp` on every SECURITY DEFINER · `p_idempotency_key` used properly (`idempotency_key`/`operation`/`result` columns; operation-scoped lookup) · money = bigint cents · **REVOKE anon on every new SECURITY DEFINER function** (Supabase grants anon by default) · never set `updated_at` on the tables that lack it (list in CLAUDE.md) · fresh migration timestamps ABOVE the live high-water mark (check `list_migrations` first; `parked_001–010` names are taken).
- **Additive-only** per §0.3. New enum values = superset CHECK rewrite. New columns nullable or defaulted. No drops of anything referenced.
- **Parallel sessions exist.** Before EVERY push: `git fetch origin main` and rebase if main moved; re-run typecheck/build after rebase. Never claim "already fixed" without checking `git log origin/main` + `list_migrations`. Never touch other worktrees.
- **Prove, don't assume**: every unit ends with a real proof (rolled-back SQL smoke, a SELECT of the changed row, a preview-page check, or an [E2E] entity exercised then cleaned). "Tests pass" alone is not done. Post a `PROOF — Ran: … · Saw: …` line in the ledger per unit.
- **Findings marked UNVERIFIED in findings.json must be re-verified against the cited code before building on them.** If one turns out wrong, record `REFUTED-AT-IMPLEMENTATION` in the ledger and skip its work.
- **`npm run typecheck` is the real typecheck** (not tsc via build alone). Build + lint + targeted tests before every commit; the pre-commit hook enforces the rest.
- **Doc duty**: after schema/RPC changes run `node scripts/regenerate-schema-registry.mjs` guidance — the registry refresh needs live introspection via Supabase MCP (the script alone only stamps); update `docs/reference/*` + CHANGELOG once at the end (one entry for the whole run), and regen AGENTS.md counts via `node scripts/regenerate-agents-md.mjs`.

## 2. Model routing (Mason's explicit design)

- **Orchestrator = you (Fable, the session model)**: sequencing, judgment, final review of every unit, all apply/push actions, the ledger.
- **Opus subagents** (`model: 'opus'`): design + implementation of DB/money units (migrations, RPC rewrites) — U2, U4, U7, U8, U9, U11 especially; also the adversarial second-read of any migration before its proof file is written.
- **Sonnet subagents** (`model: 'sonnet'`): UI implementation, test writing, verification legwork, doc updates, page walkthroughs.
- Keep the main loop lean: delegate heavy file reading; implement trivial S-units directly yourself rather than paying agent overhead.

## 3. The work queue (execute in order; each unit = plan → build → prove → review → gate → apply → push → ledger)

Unit refs are report sections + findings.json indices. Effort S/M/L. Builder = who implements.

**WAVE 1 — money correctness (DB-heavy, opus builders)**
- **U1 [S]** Overdue payable: PaymentAllocation query + InvoiceDetail payment/write-off gates include `overdue` (#41); `allocate_payment` over-allocation guard, one-line RAISE (#108).
- **U2 [M]** Partial-delivery billing leak: `complete_delivery` auto-invoice check becomes per-delivery (#34); remainder rows flip `fulfilled` when their follow-up completes (#39); IntegrityCleanup unbilled finder matches per-delivery (#34e).
- **U3 [M]** Application fee billable: ApplicationServicePicker on JobDetail; `save_job` persists `application_service_id`; optional per-customer default; verify fee branch fires end-to-end with an [E2E] job (#52).
- **U4 [M]** `customer_supplied` flag on job_chemicals: column + checkbox; `complete_job` skips deduction but keeps the application record (marked "customer supplied"); `transfer_job_to_invoice` emits $0 informational line; `_sync/reserve` skip holds for flagged rows; FieldAppChemicalEntry gains "Add custom line" (manual no-product lines — server already supports) (#53/#54/#102/#56).
- **U5 [M]** Booking closure + guards: new terminal status `closed_short` (CHECK superset + transition enforcer + hold release + allocator awareness — mirror `closed_by_application`'s wiring exactly, incl. `create_job_from_quote_section` rejection) + "Close booking — release remainder" button; rollover offers it; Schedule-Job on `accepted`/`draft` bookings gets block-with-explanation (accepted) / warn (draft) (#1/#103).
- **U6 [S]** Void safety: `void_invoice` reverts a field_application invoice's job to `completed` + clears `invoice_id` (mirror blend-ticket pattern) (#65); blend↔job billing cross-guard both directions (#91).
- **U7 [L]** Splits unification (the big one — opus + extra review): `transfer_job_to_invoice` emits a per-owner invoice group via the existing split engine when field_billing_defaults exist (single-invoice-with-shares remains only as explicit per-job choice); `complete_delivery` auto-draft becomes allocation-aware (per-owner drafts) or routes allocated orders to a "needs split billing" queue — pick the smaller safe variant if the full one resists; engine editor gains "seed from job" (#42/#43/#100/#50). If this unit fights back after 2 Codex rounds, PARK it whole — do not half-land split behavior.
- **U8 [M]** Commissions on application channel per §0.2 (#99).
- **U9 [M]** Stock policy unification: `create_quick_delivery` + `complete_delivery` warn-not-block (negative allowed + `requires_review` + admin notification; net math includes on-order in both) (#9/#101/#37); Quick Delivery picker shows the customer's real tier price + On Floor/Net (#14 ADJUSTED).
- **U10 [M]** Record integrity + toggles: `application_records.application_date` from actual start time (fallback job_date) + applicator name/license-number snapshot columns (#106); job-born invoices stamp season from the job (#109); flip `auto_draft_invoice_on_job_completion` → true (live app_settings update — reversible; log it) + `auto_draft_skipped` activity row for applicator completions (#107/#117).
- **U11 [M]** The KNOWN `complete_job` unit-conversion bug: deduct in inventory units via the shipped units module (`src/lib/units.ts` + canonical units, live since 2026-07-04). This is a P1 money bug; opus builder + both reviewers + Codex, and prove with an [E2E] pint-rate/gallon-stock job showing correct deduction.

**WAVE 2 — daily-flow speed (mostly UI, sonnet builders)**
- **U12 [L]** Applicator "My Day": `get_dispatched_list` returns `job_date` (+ hide terminal-status jobs by default); card shows date, sorts Today-first, Done section; **Start/Complete buttons on the card** calling existing RPCs (assigned-applicator path); offline `queueAction` for complete_job (copy FieldStop pattern); role-based landing (applicator→/field, driver→/my-route); honest fetch-error state; hide Cancel/Transfer trap buttons from applicators; per-field applied-acres entry at completion (AppliedRecordsManager canEdit for assigned applicator); REI/PHI line when data exists; **remove dollar charges from the card** (§0.1); notification on dispatch/reschedule/undispatch (#22/#24-33/#64/#79/#114).
- **U13 [L]** Assignment unification: JobDetail applicator save auto-syncs whole-job `job_location_dispatches`; Jobs list Applicators column reads dispatches (fallback applicator_id); "Needs dispatch" cockpit tile + Jobs badge/filter; dispatch rows auto-close on job completed/cancelled (small migration); wizard opens job-scoped from the per-job button; "already → name" chip + reassign confirm; block/warn field-less Schedule Job + per-section job badges in QuoteBuilder; `unassigned_jobs` Action-Queue category (#15-21/#111).
- **U14 [M]** Phone-order fast path: "Sell & Deliver Now" (Quick Delivery) on Dashboard quick actions + CustomerDetail + command palette; open-booking banner on NewOrder + QuickDelivery ("draw from booking instead?" → draw_down path); NewOrder/NewDelivery read `customer_id` param; searchable customer picker on NewOrder; duplicate-warning only on product-overlap ≤2 days (inline, non-blocking); drop QuickDelivery double-confirm; requested-delivery-date: auto-open NewDelivery pre-filled after order save (UI-only variant) (#8/#10-13).
- **U15 [M]** Deliveries both ways: office one-shot "Mark Delivered" (chains confirm+complete, one dialog, after-the-fact copy, on list rows for scheduled too); optional `p_completed_at` backdate (validated not-future) flowing to invoice_date; phone flow: remove redundant confirm, fold photo prompt, "No one present — left at site" signer option; deliveries default sort Upcoming/today-first; Cancelled/Voided filter options on Deliveries + Orders (#35/#36/#38/#66/#72/#76).
- **U16 [L]** Billing home: cockpit tiles "Chemical drafts to post" + "Delivered, not invoiced" (reuse existing RPC) + payments shortcut; Unbilled Applications rows get one-click Create Invoice + batch loop + "bill next (N left)" chaining; posting rights admin+sales on all five surfaces + `batch_post_invoices` per-invoice results (or client loop) + confirm modal on Invoices batch; Unposted-tray postAll group-atomic via post_invoice_group; Unpost button for chemical invoices; draft+unposted presented as one "Ready to Post"; grower-share mode banner on editor + JobDetail; voided commission-batches tab; "Book as Order" on draft quotes; convert-on-revised in builder; re-bill button on completed delivery without invoice (#44-51/#57-60/#69-71/#73-75/#78/#49).
- **U17 [L]** Booking hygiene: hold-status line per planned section (held qty, expiry, red lapsed); cockpit "Planned bookings needing attention" via the existing `get_expiring_planned_holds` + past-expiry query; red past-due expiry styling on Quotes; units on every Booked/Drawn/Remaining number + input suffix; skip stale-price banner on draw-down; Email-to-Grower becomes primary + honest tooltips + "Emailed <date> ✓" chip from email_log + unsaved no-op toast; field-acres default cascade on section field pick; product picker keep-open mode (#0/#2-7/#89/#110).
- **U18 [M]** Safety nets: negative stock counts as low stock (position fn + Action Queue + notification filter); expiry warning sets aligned (sent+revised, skip planned) + sweep writes real notifications (+ fix/drop its dead activity insert); port the 2 Dashboard-load checks into a 06:20 pg_cron fn; prepay-credit column on AR aging + reminder candidates; "due today — not started" deliveries category; unbilled-delivery count chip (#90/#105/#112/#113/#115/#116/#118/#119).

**WAVE 3 — navigation & polish (sonnet)**
- **U19 [L]** Implement the nav blueprint exactly per `proposals.md` mapping table (sidebar regroup incl. Finance split, role menus, role landings, "+ New" top-bar menu, palette regenerated from PAGE_PERMISSIONS, PAGE_PERMISSIONS categories synced, label/icon fixes, Getting-Started demoted). All 82 routes must keep a home — verify against App.tsx after.
- **U20 [M]** Entry-point demotions per proposals Part 3 (Create Order ▾ merge, manual-editor demotion + rename, misc-charge button with type locked, dashboard quick-action reorder, blend-ticket order auto-number); Quotes filter gains "Fulfilled (Applied)"; icon-only buttons get labels/aria; flat >50-option selects → searchable pickers sweep (#58-63/#88 + live-anchor items).
- **U21 [wrap]** Junk-customer FLAG-ONLY list (query live, write the list into the morning report — NO writes); full E2E-relevant test pass (`npm run test`; run the affected e2e specs if the suite is stable headless — otherwise note); docs sync (CHANGELOG single entry, reference docs, schema registry via live introspection, AGENTS.md counts); **final whole-branch Codex review**; morning report.

## 4. Per-unit pipeline (the law — every unit, no exceptions)

1. **Ground**: re-read the report section + findings + the cited code. UNVERIFIED findings get re-verified now.
2. **Build**: migrations first (additive-only), then RPC changes, then UI. Grep other pending work for overlap before rewriting any function (pending-migration clobber rule). One unit = one coherent diff.
3. **Prove locally**: `npm run typecheck` + `npm run build` + `npm run lint` + targeted `npm run test -- <scope>`; per migration: `BEGIN; <migration SQL>; ROLLBACK;` against live via execute_sql (single statements where diagnosing) + `plpgsql_check` where applicable; overload check query returns zero rows.
4. **Review fan-out** (parallel Agent calls): `rls-security-reviewer` + `migration-drift-reviewer` on every migration (+ `typescript-types-drift-reviewer` when types/columns changed; `compliance-reviewer` at least once per wave). Fix findings; re-run until clean.
5. **Write proof files** for each migration at `.claude/session-state/migration-review-<name>.json` — **with Node, not PowerShell** (BOM breaks the hook) — including the `queryHash` = sha256 of the EXACT SQL string you will pass to apply_migration (the guard binds content; re-hash after any edit).
6. **Codex gate**: run the repo's headless Codex review (per `.claude/commands/codex-review.md`) scoped to the unit's diff. Fix real findings (cap 3 rounds); record the verdict. A unit without a clean/acked Codex verdict does not apply and does not push.
7. **Apply** each migration via Supabase MCP `apply_migration` (guard checks your proofs). Then **verify live**: SELECT the new column/constraint/function, exercise the changed RPC with [E2E]-prefixed data (clean up after), confirm expected behavior. If an apply fails: do NOT improvise DDL to patch live — roll the unit back to file-only, park it, note it.
8. **Push**: fetch/rebase on origin/main if moved → pre-push hook (typecheck/build) → push to main (codex-push-guard will check the session's Codex verdict). Vercel auto-deploys; spot-check the deployment status once per wave, not per push.
9. **Ledger** (`docs/loops/business-workflow-fix-ledger.md`, committed with each unit): unit id · status (SHIPPED/PARKED/SKIPPED-refuted) · migrations applied (live version stamps) · commit SHA · Codex verdict · PROOF line · notes for the morning.

## 5. Failure & stop rules

- A unit failing its Codex gate twice → PARK (revert working tree to last good commit for that unit's files), ledger note, next unit.
- 3 consecutive PARKs → stop the run: write the morning report, final commit, end cleanly.
- **Usage-limit / credits / API errors**: finish or park the current unit file-only, commit everything, ledger note "stalled on limits at <unit>", write the morning report early. Never leave uncommitted work.
- Anything that would require a NON-additive migration, an edge-function change, or a data deletion → PARK with a written plan for Mason.
- **07:00 hard wrap**: whatever the state, run U21's wrap steps (docs, final Codex branch review, morning report) and stop.

## 6. Morning report (the deliverable Mason wakes up to)

`docs/loops/business-workflow-fix-morning-report.md` + a plain-English chat summary, containing: shipped units (live migration versions + commits + one-line proof each) · what's live on croprxsolutions.app right now · parked units with why + exact next step · decisions needed (should be ~zero — decisions were pre-answered) · the junk-customer flag list (his approval per record) · test/build status · token/limit events. Lead with status → ONE next step.
