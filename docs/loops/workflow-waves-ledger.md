# Workflow Waves Loop — Ledger

Mission: `docs/loops/workflow-waves-loop-2026-07.md` · Worktree: `C:\CRX_WorkflowLoop` · Branch: `feat/workflow-waves-2026-07`
Driver: Codex 5.6 builds (sol/terra/luna) · Claude Fable orchestrates + reviews (Opus money/DB, Sonnet light)

**Loop start (UTC): 2026-07-09T20:12:33Z** → hard wrap target ~2026-07-10T05:12Z (~9h)

## Step 0 — setup

- Collision check: `git worktree list` clean — this session owns `C:\CRX_WorkflowLoop` @ `f467d6c5`, no foreign WIP. `origin/main` NOT moved past base (branch ahead 2 = the harness commits).
- codex-cli **0.144.0** (≥ 0.144 ✓). `npm install` → up to date.
- Wrapper self-test: Codex `gpt-5.6-terra` wrote `scripts/.codex-build-selftest.txt` with token `WAVES-SELFTEST-OK-20260709` — verified content, deleted. **Builder mechanic proven.**
- Schema registry: disk registry (generated 2026-07-07) verified FRESH against live — migrations high-water `20260707181920` identical, 135 CHECK constraints = 135 live, `quotes.status` includes `closed_short`, 5 generated cols, 7 sequences, 117 tables. No rebuild needed.
- Live migration high-water: **20260707181920** — all new migrations must timestamp above this.
- Baseline green: `npm run typecheck && npm run build && npm run lint && npm run test` → exit 0. **195 test files, 3155 passed / 125 skipped, 0 failures.**

PROOF — Ran: wrapper self-test (terra), live introspection Q1–Q5 + count cross-check, full baseline pipeline · Saw: self-test token file exact match; registry↔live counts identical; tests 3155/0 fail · Not verified: n/a.

## Units

<!-- Per-unit entries appended below: id · status · tier used · build rounds · review verdict · migration live-version · commit SHA · PROOF line · notes -->

### A1 — Finished job stays visible on My Day → SHIPPED
- **Tier:** builder `gpt-5.6-sol` (2 rounds — round 1 stalled asking for confirmation, no diff; round 2 built clean). Reviewers: Opus `rls-security-reviewer` (0 BLOCKER / 0 HIGH / 1 MED — gate comment claimed exact RLS mirror; comment corrected in-file to document the deliberate completed-tail relaxation), Opus `migration-drift-reviewer` (0 BLOCKER — re-emit verified against latest U12 base line-by-line, columns vs registry), Sonnet `compliance-reviewer` (1 MED — legacy tail anchors on `jobs.updated_at`, accepted + documented in FieldView comment).
- **Root cause (grounded live):** trigger `_close_job_location_dispatch_on_job_terminal` flips dispatch rows to 'completed' at job-terminal; `get_dispatched_list` filtered `='dispatched'` → card vanished. FieldView's legacy whole-job merge also filtered to scheduled/in_progress. (The audit finding blamed only the RPC; the trigger interaction + legacy path were found at grounding.)
- **Change:** migration `20260709190000_a1_dispatched_list_recent_completed.sql` (APPLIED LIVE **v20260709203120**) — RPC re-emit + 7-day completed tail ('cancelled' excluded so undispatched applicators don't keep seeing jobs); FieldView legacy merge widened (completed/invoiced, 7-day window); DispatchBoard filters the tail back out; stale dispatchDisplay comment rewritten; +1 unit test.
- **Independent review note:** Codex built; Claude (Fable + Opus/Sonnet subagents) reviewed — author≠reviewer preserved.
- PROOF — Ran: rolled-back live smoke (BEGIN;migration+post-checks+execution probe;ROLLBACK) · live post-apply SELECT (has_a1_tail=true, anon_can_exec=false, overloads=1, stamped v20260709203120) · overload+search_path invariant sweeps (0 rows) · live PostgREST parse-check of the nested `.or()` filter (HTTP 200) · typecheck/lint/build/targeted vitest green · Saw: all green, fn live with the tail · Not verified: a real phone completion moving a card into Done (0 live dispatch rows to exercise; behavior covered by the unit test + RPC execution probe).

### A1b — `_is_dispatched_to_me` completed tail (A1 follow-up) → SHIPPED
- **Origin:** the §4.7 push-gate Codex verdict (sol, read-only) returned P2: the RLS helper still required `dispatch_status='dispatched'`, so a split assignee's Done card showed customer "Unknown" + empty expanded detail. Verified live (helper source + the 4 dependent `*_select_location_dispatchee` SELECT-only policies) — CONFIRMED, fixed rather than argued. (Its P1 — the wrapper's `danger-full-access` — is the mission doc §1 documented, owner-accepted tradeoff; acknowledged-by-design, no action.)
- **Tier:** builder `gpt-5.6-sol` (1 round, exact-to-spec). Reviewers: Opus RLS (0 B/H/M, 1 LOW → owner policy question: should a member added to a crew AFTER completion read that crew's finished job for the remaining 7-day window? bounded/read-only) + Opus drift (0 BLOCKER, byte-diff vs base).
- **Change:** migration `20260709210000_a1b_dispatched_to_me_completed_tail.sql` APPLIED LIVE **v20260709204814** — helper re-emit with the identical 7-day completed tail; cancelled excluded; ACL untouched.
- PROOF — Ran: rolled-back live smoke (migration + 3 post-checks + probe asserting false for a random uuid) · pre-verified anon lacks EXECUTE live · post-apply SELECT (has_tail=true, anon_exec=false, stamped v20260709204814) · Saw: all green · Not verified: an actual split-assignee session rendering the Done card detail (no live dispatch rows; gated logic exercised via the probe + policy inspection).

### A2 — Un-dispatch notice for displaced split assignees → SHIPPED (frontend-only)
- **Tier:** builder `gpt-5.6-terra` (2 rounds — round 2 fixed the Sonnet review's MED). Reviewer: Sonnet UI-diff review (1 MED: false-positive notify when the new assignee is admin/sales_rep — the trigger early-returns without displacing; FIXED by mirroring the trigger's role='applicator' guard against the active-only picker list · 1 LOW shadowing — FIXED `aid`). My own line-by-line read + live grounding of the trigger.
- **Root cause (grounded live):** `_sync_job_location_dispatch_on_applicator_change` overwrites ALL per-location dispatch rows on a whole-job reassign (`ON CONFLICT DO UPDATE`), but JobDetail only notified the new + old whole-job applicators — split assignees displaced by the overwrite got nothing.
- **Change:** `src/pages/JobDetail.tsx` only — pre-save best-effort snapshot of active dispatched applicator_ids (taken before `save_job`, which is also before the license-override `assignWithOverride` path — reviewer verified both timings), post-save `notifyApplicatorUndispatched` per displaced id (dedup; skips new/old/self). Crew-only rows out of scope (no per-member identity), noted in-code.
- PROOF — Ran: typecheck + lint green · Sonnet reviewer independently verified condition/timing/signature vs `notificationTriggers.ts` + confirmed the null-clear path correctly does NOT fire · verified the picker list live-shape (`profile_public_view`, active-only, carries role) · Saw: all green · Not verified: a real browser reassign firing the notification row (needs an authenticated office session + [E2E] job with split dispatch rows — the notify helper itself is U12-shipped code already proven in prod).

### A3 — Crew-applicator Start/Complete on JobDetail → SHIPPED (frontend-only)
- **Tier:** builder `gpt-5.6-terra` (1 round, exact-to-spec). Review: my line-by-line read grounded against the LIVE `job_location_dispatches_select` policy, `start_job` auth block, and `complete_job` crew check.
- **Root cause (grounded live):** the `hasActiveDispatch` probe filtered `.eq('applicator_id', profile.id)` — a crew dispatch row carries `crew_id` with `applicator_id` NULL, so it never matched; yet `start_job` (via `_is_dispatched_to_me`'s crew branch) and `complete_job` (direct `ground_crew_members` check) both authorize crew members. My Day showed the buttons; JobDetail didn't.
- **Change:** `src/pages/JobDetail.tsx` only — dropped the applicator_id filter from the probe; RLS scopes the read to own rows + active-crew rows (policy branches verified live: both require dispatch_status='dispatched' + active profile/crew). Comment updated. `canStart`/`canComplete` logic untouched.
- PROOF — Ran: typecheck + lint green · live SELECT of the `job_location_dispatches_select` policy qual confirming the crew branch grants exactly what the probe now relies on · live confirmation `start_job`/`complete_job` authorize crew members · Saw: all green, policy/auth text matches the new probe semantics · Not verified: a real crew-member browser session on JobDetail (no live crew dispatch rows exist to exercise).

### U14a — "Sell & Deliver Now" entry points → SHIPPED (frontend-only)
- **Tier:** builder `gpt-5.6-terra` (2 rounds). Reviewer: Sonnet UI-diff (1 HIGH: the one-shot ref made the deep link work only ONCE per page mount — palette re-invocation silently no-op'd; FIXED by dropping the ref, the param strip is now the re-entry guard · 2 test MEDs FIXED: palette test asserts the navigation target; modal override-guard regression test added · Deliveries page-level test deliberately skipped — page too heavy, logic simplified instead).
- **Change:** QuickDeliveryModal gains `initialCustomerId` (open-transition fetch, never overrides a user pick); Deliveries consumes `?quickDeliver=1&customer_id=` per-navigation (role-aware, driver-safe, URL cleaned with replace); Dashboard quick-action tile (grid 6→7, first position); CustomerDetail header button; CommandPalette entry. +3 tests.
- PROOF — Ran: typecheck + lint + targeted vitest (29 pass) · reviewer verified role-timing (waits for auth before consuming), driver no-leak, all four modal close paths reset state · Saw: all green · Not verified: a real click-through in a browser session (deep-link semantics covered by review + tests; will ride the Sprint-B Vercel spot-check).

### U14b — NewOrder fast path + open-booking banners → SHIPPED (frontend-only; completes U14)
- **Tier:** builder `gpt-5.6-terra` (2 rounds). Reviewer: Sonnet UI-diff (1 HIGH: the new SearchableSelect fired `onChange('')` on the first diverging keystroke → NewOrder wiped every line's price override at tier-1; FIXED — onChange now fires only on a committed selection or a committed blur-with-empty-text clear, plus a defensive no-recalc guard in `handleCustomerChange('')` · LOWs accepted as-is: redundant openBookings clears (harmless), un-memoized options (tiny lists), minor test gaps, latent order+customer_id param combo that no caller produces).
- **Change:** NEW `src/components/ui/SearchableSelect.tsx` (reusable combobox — U20's >50-option sweep will reuse it) + NEW `src/lib/openBookings.ts` (sent/revised = open booking helper) + tests. NewOrder: reads `?customer_id` once post-load; searchable customer picker; blocking 7-day any-order ConfirmModal → debounced non-blocking 2-day product-overlap inline warning; optional requested-delivery-date (UI-only) → auto-opens NewDelivery pre-filled; dismissible open-booking banner linking each quote. NewDelivery: consumes `?date` (validated) + soft `?customer_id` order filter — this also fixed the previously-dead calendar `onDateClick` date link. QuickDeliveryModal: compact open-booking line.
- **Mission-scope refutation:** "drop QuickDelivery double-confirm" — REFUTED-AT-IMPLEMENTATION: the modal has exactly ONE ConfirmModal before commit (verified in code); nothing to drop.
- PROOF — Ran: typecheck + lint + targeted vitest (24 pass across 3 files) · reviewer ran tsc + the new tests independently and traced the requested-date param end-to-end into NewDelivery's validated initializer · Saw: all green · Not verified: real browser click-through of the new order flow (covered by review + tests; rides the Sprint-B Vercel spot-check).

### U15 — Deliveries both ways → SHIPPED (migration + frontend)
- **Tier:** migration `gpt-5.6-sol` (1 round, machine-diff-verified: base = U7 emit, only the 3 intended changes) · UI `gpt-5.6-terra` (2 rounds — round 2 fixed the Sonnet MED: SignatureCanvas now uses its own `disabled` prop; + 2 cosmetic nits: today() hoisted, modal title/button say "Mark Delivered" for scheduled rows). Reviewers: Opus RLS (0 B/H/M — backdate posting-safe via draft+check_period_open, verified; DROP+CREATE dependency-clean; 1 non-blocking note: closed-period WARN anchors on scheduled_date not the backdate — owner call, in morning report) + Opus drift (0 BLOCKER — named-param callers incl. offline replay safe) + Sonnet UI (idempotency chain, comparator/DataTable interplay, FieldStop step removal, filter syntax precedent all verified clean).
- **Change:** migration `20260709220000_u15_complete_delivery_backdate.sql` APPLIED LIVE **v20260709220435** — `complete_delivery` 7→8 args (DROP+CREATE, ACL re-established, self-post-checked), optional not-future `p_completed_at` → `completed_at` + draft invoice_date. Frontend: Deliveries "Mark Delivered" one-shot on scheduled rows (chains confirm→complete; confirm-ok/complete-fail leaves in_progress with retry path), "Delivered on" date input (≠today → midday-local ISO param), today-first default sort, cancelled/voided filter options (base fetch now also excludes voided from All); Orders voided filter; FieldStop: final redundant ConfirmModal removed (review screen is the confirmation, copy moved inline), photo step folded into review, "No one present — left at site" signer checkbox.
- PROOF — Ran: apply via hash-bound proof · post-apply plpgsql_check (0 errors) · impersonated rolled-back probes (admin JWT claims: future timestamp → COMPLETED_AT_IN_FUTURE raised; valid backdate → reaches delivery lookup) · signature/overload/ACL live SELECT (8-arg, 1 overload, anon=false) · typecheck/lint/FieldStop-idempotency tests green · Saw: all green, stamped v20260709220435 · Not verified: full [E2E] delivery completion with backdate writing a real draft invoice (body writes financial_audit_log — rolled-back full smoke blocked by design; probes + review cover the changed lines).
- **Push-gate verdicts (sol, read-only):** R1 → 1 P2 (confirm-ok/complete-fail discarded the retained idempotency key → committed-but-lost-response completion could never replay its notification; FIXED: modal stays open, local target flips to in_progress, same key reused). R2 → 1 P3 (generated `types/supabase.ts` stale for the new param; FIXED by hand-patching the one Args entry). Pushed to main @`0476cb41` (proof recorded `blockers-fixed`). **Sprint-B Vercel spot-check: production READY on `0476cb41`, aliased croprxsolutions.app — U14a/U14b/U15 all live.**

### U16a — Billing home: see the work, bill it one-click → SHIPPED (frontend-only)
- **Tier:** builder `gpt-5.6-sol` (2 rounds; round 1 was killed by a tool timeout AFTER completing its writes — verified complete via green typecheck/lint before proceeding). Reviewer: **Opus adversarial second read** (money diff): 0 BLOCKER, 4 MED — ALL FIXED in round 2: (1) DeliveryDetail Create-Invoice gate now admin-only (the RPC hard-gates admin; sales saw a button that always errored), (2) button coverage now per-delivery (`delivery_id = this OR NULL`) matching the server + cockpit instead of any-sibling-invoice-hides-it, (3) cockpit coverage candidates exclude orphan (null-order_id) deliveries, (4) silent 50-row caps now carry honesty notes on both tiles. Opus verified clean: per-row idempotency child pattern (no stale/reused key path), U7 split-group result handling, RPC params vs JobDetail canon, money display, conventions.
- **Change:** OfficeCockpit + "Chemical drafts to post" tile (read-only, deliberately outside the hardened field-app post-all path) + "Delivered, not invoiced" tile (client mirror of `create_invoice_for_unbilled_delivery`'s coverage precondition) + Record-payments shortcut. UnbilledApplications: per-job-row one-click Create Invoice via `transfer_job_to_invoice` (confirm modal, per-row keys, split-group-aware success copy) + "Bill next (N left)" chaining. DeliveryDetail: admin Create Invoice (re-bill) via `create_invoice_for_unbilled_delivery` when completed + uncovered.
- PROOF — Ran: typecheck + lint green ×2 rounds · Opus adversarial reviewer independently verified RPC result shapes against migration emits (U7 split path, unbilled-delivery preconditions) · Saw: all green, 4 MEDs closed · Not verified: a real one-click billing round-trip (0 completed unbilled jobs live; RPC paths are shipped prod code already exercised from JobDetail/IntegrityCleanup).

### U16b — Posting rights, Ready-to-Post, Unpost, banners, booking actions → SHIPPED (frontend-only; completes U16)
- **Tier:** builder `gpt-5.6-sol` (2 rounds; round 2 = two LOW polish items). Reviewer: **Opus adversarial second read: CLEAN — 0 BLOCKER/HIGH/MED** (verified against the LIVE DB: per-invoice key minting immune to the Codex-P2 stale-key class; group partition mutually-exclusive; Book-as-Order's mark-sent step is the `create_quote_version` RPC — same state as the manual path, with a draft→sent revert if convert fails mid-chain; unpost routes by group-id not type; `field_billing_defaults` readable by sales under live RLS). 4 LOWs: #1+#2 FIXED (runCriticalAction wrapper restored + PRICING_INCOMPLETE copy); #3 noted for Mason (opening the batch bar to sales_rep also exposes batch PRINT — benign, flagged in morning report); #4 pre-existing.
- **Change:** InvoiceDetail Post admin→admin+sales (DB already allows) + Unpost extended to chemical_sale; Invoices batch-post → client per-invoice `post_invoice` loop (per-invoice results, per-invoice stable keys, ConfirmModal, sales allowed — replaces the admin-only all-or-nothing `batch_post_invoices` call) + "Ready to Post (draft+unposted)" filter + clickable Unposted card; FieldInvoicesUnposted postAll group-atomic (`post_invoice_group` once per split group, whole-group-even-if-filtered copy); FieldApplicationInvoice canPost role check + grower-share banner (Customers tab); JobDetail grower-share line before Transfer; CommissionPayments voided tab; QuoteBuilder convert-on-revised + draft "Book as Order" (create_quote_version → convert chain).
- PROOF — Ran: typecheck + lint + QuoteBuilder/InvoiceDetail/FieldApplicationInvoice tests (36 pass) ×2 rounds · Opus verified live RPC gates (post/unpost/group = admin+sales; batch_post = admin-only → bypassed) + live RLS on field_billing_defaults · Saw: all green, CLEAN verdict · Not verified: a real posting round-trip as sales_rep (≈0 posted invoices live; DB gates were live-verified instead).
- **Push-gate verdicts (sol, read-only, 3 rounds):** R1 → P1 (convert-failure catch could resurrect a COMMITTED conversion as 'sent' on a lost response — double-bill exposure; FIXED: verify live status first, navigate to the created order when accepted) + P2 (lost mark-sent response bricked the Book-as-Order retry; FIXED: resume the chain from live DB status). R2 → P1 (verification-failure corner still fell through to the blind revert; FIXED: no write at all without a successful read proving not-accepted — the revert write is only ever a no-op or harmful). R3 → **CLEAN**. Pushed to main @`0b149263` (proof `blockers-fixed`). U16 COMPLETE.

### U17 — Booking hygiene → SHIPPED (frontend + 1 tiny RLS migration)
- **Tier:** builder `gpt-5.6-terra` (2 rounds) + `gpt-5.6-sol` for the policy migration. Reviewer: Sonnet UI (2 HIGH: email_log RLS silently blanked the "Emailed ✓" chip for sales_reps → FIXED by additive own-rows SELECT policy `20260709233000` APPLIED LIVE **v20260710001736** (combined Opus RLS+drift CLEAN — edge fn stamps created_by=caller.id, NULL rows stay admin-only) · hold lines used `.find()` but holds are one row PER QUOTE ITEM → FIXED: sum per product, earliest expiry, mixed-null handling · 1 MED: today-expiry fell between the RPC's `>` and the lapsed `<` → FIXED `.lte` · 1 LOW comment fixed).
- **Change:** QuoteBuilder — per-section planned-hold lines (summed qty + earliest expiry, red lapsed, no-expiry callout); units on Booking Settlement + partial-draw modal; stale-price guard removed from draw-down only (locked prices; still on convert); Email-to-Grower promoted primary w/ honest tooltips; "Emailed <date> ✓" chip from email_log (fields grounded against the edge fn); unsaved-quote toast; field-acres cascade (override→measured→total, fills only empty acres, same recalc path); product-picker keep-open mode. Quotes — red past-due expiry on sent/revised. OfficeCockpit — "Planned bookings needing attention" card (`get_expiring_planned_holds` first frontend caller + lapsed-holds query).
- PROOF — Ran: typecheck/lint/QuoteBuilder tests green ×2 rounds · reviewer verified acres-cascade recalc parity, keep-open state resets, RPC shape vs migration source, email-field grounding vs send-email edge fn · policy migration rolled-back smoke + post-apply SELECT (policy live) · Saw: all green · Not verified: a real sales_rep sending a quote email and seeing the chip (needs a real send; the RLS path was live-verified structurally).
- **Push-gate verdicts:** R1 partially lost to my own `tail -4` truncation (wrapper is ephemeral — lesson: never pipe a verdict through tail; the two visible findings P2 date-TZ + P3 stray keep-open item were fixed). R2 fresh full run → 3 findings (hold-line lapse rule `<= today`, parseLocalDate on hold expiry, unique attention-row keys) — ALL FIXED. R3 → **CLEAN**. Pushed to main @`74dc3418`. (Also hit the §4.7 gotcha once: proof + push in ONE bash call = guard denies before the proof exists; split into two calls.)

### U18 — Safety nets → SHIPPED (migration + frontend)
- **Tier:** migration `gpt-5.6-sol` (1 round, 790 lines) · UI `gpt-5.6-terra` (1 round). Reviewers: Opus RLS (0 B/H/M, 3 INFO — get_ar_aging dependents pre-verified 0 live; window-boundary divergence harmless; service_role grant omission matches original) + Opus drift (0 BLOCKER — all 5 re-emits machine-diffed vs newest bases, only intended changes; post-check markers verified).
- **Change:** migration `20260709230000_u18_safety_nets.sql` APPLIED LIVE **v20260710005441** — negative stock counts as low stock everywhere (position fn + Action Queue + the ported check); expiring-quote sets aligned (sent+revised, skip planned) across dashboard + morning check; auto-expire sweep's dead zero-uuid activity INSERT fixed (real admin actor) + real `quote_expired` notifications to creators; NEW 06:20 cron `run_morning_notification_checks` (ports the 2 Dashboard-load checks w/ 24h dedup — they now run even when nobody opens the Dashboard); AR aging + reminder candidates expose `prepay_balance_cents`. Frontend: ActionQueue renders `due_today_not_started` + `unbilled_deliveries`; ARaging "Prepay on file" column; Dashboard client-check pair retired (cron replaces).
- PROOF — Ran: apply via hash-bound proof (rolled-back full smoke substituted per protocol — fn bodies textually contain business-table INSERTs; the migration self-post-checks 8 assertion groups in-transaction) · post-apply plpgsql_check on all 5 plpgsql fns (0 errors) · impersonated-admin probes exercised get_dashboard_action_items (9 categories incl. both new), get_inventory_position, get_ar_aging, get_ar_reminder_candidates · cron job live @ '20 6 * * *' · typecheck/lint/ActionQueue+businessLogic tests (47 pass) · Saw: all green, stamped v20260710005441 · Not verified: the cron actually firing at 06:20 (first run is tomorrow morning — flagged in morning report to check notifications after 06:20).
### U20 — Entry-point demotions + sweeps → SHIPPED (frontend-only; U20a terra + U20b luna)
- **Tier:** U20a `gpt-5.6-terra` (2 rounds — round 2 locked the Misc Charge type via `?type=misc_charge`, which round 1 had dropped) · U20b `gpt-5.6-luna` (2 rounds — round 2 rewrote the ManualTicketCreate regression-guard test to drive the new combobox). Reviewer: Sonnet ran the FULL suite (3,296 tests) + build + tsc across BOTH halves: 1 HIGH (that broken money-bug guard test — FIXED, assertions equivalent) · all 10 SearchableSelect conversions verified semantics-preserving (QuoteBuilder customer clear = byte-identical to the old empty option; every consumer handles '' safely) · every U20a item cross-checked against proposals.md Part 3.
- **Change (U20a):** QuoteBuilder "Create Order ▾" chooser (Convert whole booking / Draw part…, per-choice gating) + header ⋯ overflow (Save-as-Template/Roll Over/Customer View); "New Field Application" REMOVED from /invoices and demoted behind ⋯ as "Manual Invoice (no job)" on both field-invoice pages; DispatchBoard "Add New Job" promoted to header; Dashboard quick actions rebuilt (row 1: Sell & Deliver Now · New Quote · New Job · New Order · To-Ship; row 2 secondary: Schedule Delivery · New PO · Inventory · Receiving); blend-ticket create-order prefills via `generate_order_number` (editable); "Misc Charge" button → /invoices/new?type=misc_charge with the type dropdown LOCKED (bare /invoices/new unchanged — owner-call default: visible button, reversible).
- **Change (U20b):** 23 icon-only buttons gained aria-labels (dynamic expand/collapse labels where stateful); 10 flat >50-option selects converted to SearchableSelect (QuoteBuilder/JobDetail×3/BlendTicketDetail×3/ManualTicketCreate×2/CropPrograms). Filter dropdowns deliberately left for a later pass.
- **Mission-scope refutation:** "Quotes filter gains Fulfilled (Applied)" — REFUTED-AT-IMPLEMENTATION: already present (incl. Closed — Short); nothing to build.
- PROOF — Ran: typecheck/lint/targeted tests per round + the reviewer's independent FULL suite (3,170 pass / 1 fail → fixed → targeted re-run green) · Saw: all green · Not verified: real browser click-through of the new chooser/menus (rides the Sprint-C Vercel spot-check).

### Post-ship Codex gauntlet 2026-07-10 (Mason: "did all changes go to Codex?") → gap closed
- **Honest gap surfaced:** the DB/money units all had Codex verdicts, but the frontend-only units (A2, A3, U14a/b, U19, U20a/b) had only Claude (Opus/Sonnet) reviews — per the mission's own rule, but Mason asked, so two adversarial sol read-only passes ran over those seven shipped commits.
- **Findings:** 1 real P1 (SearchableSelect's 150ms blur delay — found INDEPENDENTLY by both passes — let a visibly-cleared picker submit the old hidden customer id: wrong-customer/wrong-tier order risk; FIXED: reconciliation now synchronous on blur, option clicks can't blur thanks to mousedown-preventDefault; regression test added) · P2 JobDetail early-returns could drop the displaced-assignee notices after a committed displacement (FIXED: displacementCommitted flag + notify fires at each commit point) · P2 blend order-number prefill could clobber typed input (FIXED: functional setter yields) · P3 stale hasActiveDispatch across job navigation (FIXED: reset-on-change) · P3 palette + "+ New" ignored role/deny-list (FIXED) · confirm-round P3: the office gate over-swept the all-role Team Board/Notifications palette entries (FIXED: per-entry roles; Settings admin-only). **REFUTED:** non-awaited duplicate warning (designed warn-don't-block) · bare `/invoices/new` editable type (pre-existing; the proposal's "route closed" alternative remains an owner call).
- Final confirm verdict **CLEAN**. Pushed @`c60fa765`→ rebased over a sibling session's report commit → main @`0a4d6a2a`. Gates: typecheck/lint + 35 targeted tests green, re-verified post-rebase.
- PROOF — Ran: 2 adversarial sol passes + fix build + confirm verdict ×2 · typecheck/lint/vitest per round + post-rebase · Saw: CLEAN final verdict, pushed · Not verified: browser click-through of the blur-fix (covered by the new synchronous-commit regression test).

### Sprint D — RE-OPENED 2026-07-10 on Mason's "run sprint D" → D1+D2 SHIPPED, D3 PARKED
- **D2 (reserve-side unit normalization) → SHIPPED.** Migration `20260710120000` APPLIED LIVE **v20260710111734** — all 8 `field_app_priced_quantity` call sites across the 4 hold/planning fns now normalize the unit first, mirroring complete_job. Builder `gpt-5.6-sol` (1 round; orchestrator machine-diff = exactly the 8 wraps + comments); combined Opus RLS+drift CLEAN (normalize_rate_unit IMMUTABLE, NULL-preserving — no behavior change for NULL/blank units; bases md5-verified byte-identical to live).
  PROOF — Ran: live blast-radius count pre-apply (0 active job holds / 0 conversion-changing rows) · apply via hash-bound proof · plpgsql_check ×4 (0 errors) · identity probe (raw 'pt/ac' → NULL; normalized → 0.25 gal from 2 pt) · **rolled-back [E2E] end-to-end**: [E2E] customer+product(Gal)+inventory+scheduled job+2 pt/ac chemical → `_sync_job_holds` → hold = exactly 0.25 Gal (raised E2E_PASS_ROLLBACK to unwind) · overload+searchpath sweeps 0 · Saw: all green, stamped v20260710111734 · Not verified: n/a — this one got the full end-to-end. Gotcha: `validate_product_units` trigger requires `unit_conversions` frozen keys ('Gal' not 'gal') on product inserts.
- **D1 (logbook snapshot preference) → SHIPPED.** Migration `20260710130000` APPLIED LIVE **v20260710112102** — six read RPCs prefer the N2-7 snapshots (COALESCE to profiles). Builder `gpt-5.6-sol` (1 round); combined Opus RLS+drift CLEAN. **Drift catch:** the logbook fns' only disk emit had drifted from live (~56 bytes/fn) — bases switched to VERIFIED LIVE definitions; the other two bases md5-matched live.
  PROOF — Ran: apply via hash-bound proof · impersonated live exercise of all six fns · plpgsql_check ×2 (0 errors) · sweeps 0 · Saw: all green, stamped v20260710112102 · Not verified: a record whose profile subsequently changed showing the frozen snapshot (no live application_records rows with snapshots yet — the COALESCE order is the verified mechanism).
- **D3 (U8 leftovers) → PARKED, both halves, with grounded plans:**
  (a) **Blend-ticket commission mint** — `create_invoice_from_blend_ticket` mints zero commissions (verified: no 'commission' reference in its newest emit) and `commissions` has no blend lineage (`chk_commission_source` = order_id OR job_id). ADDITIVE in shape (nullable `commissions.blend_ticket_id` or relaxed CHECK + a `_insert_commissions_for_blend_ticket` helper + reversal wiring in void/cancel/delete paths) but money-critical and DORMANT by owner decision ("blend OCR dormant year-one; wire before blend billing is used"). Parked deliberately — build it in a focused session before blend billing goes live.
  (b) **`jobs.commission_split` visibility** — grounding overturned the framing: the "tweak" isn't additive at all. The jobs_select policy already exposes the split JSONB (names+percentages) to assigned applicators; Codex's accepted-and-flagged proper fix is an ADMIN-ONLY SIDE TABLE (moving the column) or RPC-gating — a design decision for Mason, not a mechanical fix. → owner decision, in the report below.

### Sprint D — original park note (2026-07-10 early AM; superseded above)
- The ~9h window was consumed by Sprints A-C (every unit needed 2-4 review/fix rounds — the gates found real bugs each time, which is the system working, but it costs wall-clock). Per §6 ("PARK the rest with a note"), the three Sprint-D items are parked with next steps:
  1. **Logbook/field-dashboard snapshot preference** (re-emit `get_logbook_*` + FieldDashboard/LotTrace read RPCs to prefer the U8 applicator name+license snapshots, profiles fallback) — additive-safe re-emit; next step: ground the exact fn list via `grep get_logbook supabase/migrations`, machine-diff re-emit per the U18 pattern, Opus×2, apply.
  2. **Hold-engine reserve-side unit normalization** (apply `normalize_rate_unit()` before conversion on the reserve path, mirroring the completion-side fix) — money-adjacent; needs the full §4 pipeline + an [E2E] rolled-back exercise; next step: locate the reserve-path fn (`_sync_quote_job_reservations`/`reserve_job_inventory`), diff its unit handling vs the completion side.
  3. **U8 parked follow-ups** (blend-ticket-path commission mint; `jobs.commission_split` RLS visibility) — both documented in the U8 ledger; the mint is a lifecycle change (NOT additive-trivial) → needs its own focused session.
- PROOF — Ran: n/a (no code) · Not verified: n/a. Honest state: unstarted, deliberately.

### Sprint E — dispatch backfill → PARKED as mandated (drafted + validated)
- `scripts/.staging-migrations/workflow-waves-parked/PARKED-dispatch-backfill.sql` — plain-English header, live row-count check (**currently 0 jobs / 0 rows — a NO-OP today**; the count query is embedded to re-run before applying), trigger-mirroring guards (active applicator role, ON CONFLICT DO NOTHING, attribution via updated_by/created_by).
- PROOF — Ran: live count (0/0) · rolled-back `BEGIN;INSERT…;ROLLBACK` executed clean against live · Saw: parses + executes, 0 rows · Not verified: a real apply (Delivery-gate: NEVER without Mason's OK — and pointless today at 0 rows).

### U19 — Nav blueprint → SHIPPED (frontend-only)
- **Tier:** builder `gpt-5.6-terra` (2 rounds). Reviewer: Sonnet UI-diff (1 BLOCKER: the rewrite dropped the U12 deny-list guard in RoleLanding — a denied landing page = infinite redirect loop, reachable via an ordinary Settings checkbox; FIXED with hasPageAccess guards + an all-role loop-proof /team-board fallback · 1 MED: /dashboard missing from usePageMeta — FIXED · plus MY catch confirmed: the new `/dashboard` PAGE_PERMISSIONS entry had been opened to ALL roles — tightened to admin+sales). Reviewer cross-checked EVERY sidebar group/item/label/role against proposals.md lines 125-218/237-325: exact match incl. order; PR-11 fail-closed route-coverage test intact and strengthened.
- **Change:** office sidebar rebuilt to the proposal's 9-group tree + Today/To-Ship standalones; separate applicator (My Day landing) and driver (My Route landing) menus; "+ New" top-bar menu (7 create entries, office-only, a11y'd); admin/sales land on /office-cockpit ("Today"); KPI dashboard moved to additive `/dashboard` (Insights → Overview); PAGE_PERMISSIONS categories renamed to the new group names with ALL keys unchanged + new `path` field; CommandPalette ALL_PAGES now DERIVED from PAGE_PERMISSIONS (+ EXTRA_PAGES deep links); Getting-Started demoted under Setup & Admin.
- PROOF — Ran: typecheck/lint + Sidebar/pagePermissions/CommandPalette/AppLayout tests (62 pass R1; 43 pass targeted R2) · reviewer independently ran the suites + tsc and machine-compared the tree to the proposal · Saw: all green, exact-match verdict · Not verified: a real login-per-role click-through of the new nav (rides the Sprint-C Vercel spot-check; the redirect logic is unit-logic + was the BLOCKER focus).

- **U18b follow-up (push-gate R1 P2/P3):** `20260710003000_u18b_morning_checks_fixes.sql` APPLIED LIVE — same-day-inclusive date expiry window (`expires_at::date BETWEEN CURRENT_DATE AND CURRENT_DATE + 7`) + per-recipient 24h dedup on both notification classes. Reviewed by orchestrator machine-diff vs the double-Opus-reviewed U18 base (delta = exactly the two fixes; ACL re-asserted; cron untouched; fn is cron-only) — noted honestly: no fresh Opus round on this 3-line delta; push-gate R2 was the independent read → **CLEAN**. Pushed to main @`bf7c7e32`. U18 COMPLETE.
