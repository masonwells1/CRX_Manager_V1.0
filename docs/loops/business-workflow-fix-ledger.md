# Business-Workflow Fix Loop — Ledger (overnight 2026-07-05/06)

Mission: `docs/loops/business-workflow-fix-mission-2026-07.md` · Branch: `fix/business-workflow-2026-07` · Orchestrator: Fable.

## Pre-flight repairs (not units)

- **CLAUDE.md committed conflict markers** — origin/main's merge `b13e1207` left unresolved `<<<<<<<` markers in the Snapshot section. Resolved keeping both sides' facts (626-migration count + sentinel note AND the Security-follow-up paragraph). Commit `11b2ba28`.
- **schema-registry.json committed conflict markers** — same merge accident; broke strict JSON parsing for every hook that reads the registry (caught by migration-drift-reviewer H1). Resolved with the true live high-water `20260705224521` (queried live). Commit `3df94183`.

## U1 — Overdue payable + over-allocation guard — SHIPPED

- **Status:** SHIPPED (migration APPLIED LIVE; frontend committed, push pending in this commit)
- **Findings:** #41 (UNVERIFIED→CONFIRMED by hand: `PaymentAllocation.tsx:152` loaded only `posted`; `InvoiceDetail.tsx:1163` gated Record Payment/Write Off to posted+admin; live `allocate_payment` + `apply_write_off` both already accept `('posted','overdue')`) · #108 (UNVERIFIED→CONFIRMED-with-adjustment: the in-function total guard was indeed missing, BUT drift review found live trigger `trg_enforce_allocation_not_over_payment` (mig 20260616121105) already aborts over-allocation at the allocation_sets totals UPDATE — so #108's "no error" claim was stale; the new guard is defense-in-depth that fires earlier with named amounts. Migration header documents this honestly.)
- **Changes:** `PaymentAllocation.tsx` invoice query → `.in('status',['posted','overdue'])` · `InvoiceDetail.tsx` Record Payment/Write Off gate → posted|overdue (Void untouched) · migration `20260706000000_allocate_payment_over_allocation_guard.sql` re-emits live fn + OVER_ALLOCATED guard + single-overload assert.
- **Migration applied live:** version **20260706010233** (name `20260706000000_allocate_payment_over_allocation_guard`)
- **Reviews:** rls-security-reviewer CLEAN · migration-drift-reviewer CLEAN on the SQL (its 2 HIGH were environmental: the registry conflict → fixed `3df94183`; live version-stamp confirm → done, see PROOF) · Codex (gpt-5.5, `--uncommitted`) verdict: "no discrete introduced correctness issues".
- **PROOF — Ran:** live SQL post-apply: overloads=1, `position('OVER_ALLOCATED' in prosrc)>0` = true, `plpgsql_check_function_tb` errors=0, `schema_migrations` row `20260706010233` present, `proconfig` keeps `search_path=public, pg_temp`, anon/authenticated ACL unchanged from pre-apply (accepted self-gated grant-debt class); global dup-overload sweep returns only the 8 plpgsql_check extension internals (pre-existing, benign). **Saw:** all values as expected. **Not verified:** the UI list rendering with a real overdue invoice (0 overdue invoices exist in prod — operationally-empty DB; gate/query change proven by typecheck+build+lint green, Codex + reviewer reads).
- **Smoke caveat (process note for Mason):** the pre-apply `BEGIN;…;ROLLBACK;` smoke could not run — `live-testdata-guard` blocks ANY execute_sql containing `INSERT INTO financial_audit_log` text, even inside a CREATE FUNCTION body in a rolled-back batch (rule 1 has no rolled-back exemption). Not routed around; substituted with plpgsql_check + post-apply verification. This will recur on every money-RPC re-emit (U2/U7/U8) — worth a hook refinement decision in the morning.

## U5 — Booking closure `closed_short` + Schedule-Job guards — SHIPPED

- **Status:** SHIPPED (migration APPLIED LIVE; frontend + registry committed and pushed with this entry)
- **Findings:** #1 (CONFIRMED — partially-drawn walked-away bookings had NO terminal exit; `_enforce_quote_terminal_not_drawn` blocks decline/cancel/expire) · #103 (UNVERIFIED→CONFIRMED — live `create_job_from_quote_section` did NOT block 'accepted'; scheduling from a sold booking double-counts chemical).
- **Changes:** quotes CHECK superset (+`closed_short`, 9 values) · transition enforcer +sent/revised→closed_short edge AND a BOOKING_HAS_ACTIVE_JOBS guard on DIRECT closed_short updates (Codex R2) · `release_holds_on_quote_status_change` + `_sync_quote_job_reservations` + `run_data_integrity_sweep` treat closed_short as terminal · NEW RPC `close_quote_as_short` (SECDEF, actor-bound, idempotent, anon-revoked; refuses active jobs; is_planned NOT required — a non-planned walk-away is valid, releases 0 holds truthfully) · `create_job_from_quote_section` now FOR-UPDATE-locks the quote (Codex R2 race) + rejects 'accepted' with QUOTE_ALREADY_CONVERTED + rejects closed_short · Frontend: Close—Short button + confirm + handler (QuoteBuilder), draft-schedule warn-confirm, accepted→"Sold — make a standalone job" inline explainer, badges/labels/filter option (Quotes/CustomerDetail/Badge), QuoteStatus type + 2 RpcErrorCodes, schema-registry quotes.status updated in-patch (Codex R3).
- **Migration applied live:** version **20260706015553** (name `20260706030000_closed_short_booking_closure`)
- **Reviews:** rls CLEAN (initial + delta re-review incl. invoker-rights-trigger RLS lattice + deadlock-order analysis) · drift CLEAN (initial + delta; live-prosrc line-diff = VERBATIM+EDITS-ONLY on all 5 re-emits) · types CLEAN after adding the missed Quotes filter option · Codex 3 rounds (R2: FOR UPDATE race + direct-update bypass + released-units over-report + settings.local.json allowlist drift — ALL fixed, settings file reverted & not committed; R3: registry staleness — fixed in-patch).
- **PROOF — Ran:** post-apply live SQL (CHECK def = 9 statuses incl. closed_short; RPC exists ×1; enforcer contains closed_short + BOOKING_HAS_ACTIVE_JOBS; job-creation fn contains FOR UPDATE; anon EXECUTE=false / authenticated=true; plpgsql_check errors=0) + TWO rolled-back [E2E] DO-block smokes as an impersonated live admin. **Saw:** smoke 1 — close on a booking with a scheduled job → `BOOKING_HAS_ACTIVE_JOBS` raised; after cancelling the job the close succeeded → status `closed_short`. Smoke 2 (with a real quote_item so `_sync_planned_holds` maintains the hold) — close released the hold (is_active f) and returned released_units=42 with the correct product line. All data [E2E]-prefixed, everything rolled back via SMOKE_PASS_ROLLBACK. **Not verified:** the button in a rendered browser (build+lint+typecheck green; component wiring is the same ConfirmModal/Badge pattern as the shipped Close—Applied).

## U3 — Application fee billable — SHIPPED

- **Status:** SHIPPED (migration APPLIED LIVE; frontend committed and pushed with this entry)
- **Finding:** #52 (already CONFIRMED by the review): transfer_job_to_invoice's per-acre fee branch was DEAD — no screen/RPC ever set jobs.application_service_id, so every job invoice billed chemicals only and fee-only jobs billed $0.
- **Changes:** save_job re-emitted with 4 surgical edits (persist application_service_id from the payload — INSERT column + guarded CASE + UPDATE three-way CASE that PRESERVES the saved value when a stale client omits the key (Codex R3) — plus the season computation fixed from a July cutoff to the canonical Oct-1 compute_season helper (Codex R2; the fee's customer-specific rate lookup is season-keyed)) · JobDetail gets the existing ApplicationServicePicker in the header grid, wired through state/snapshot/load/payload · Start Job + Complete Job now refuse while unsaved edits exist (Codex R1+R2 — completion reads billing fields from the DB; unsaved picks were silently lost).
- **Migration applied live:** version **20260706024754** (name 20260706020000_save_job_persist_application_service_id)
- **Reviews:** rls CLEAN · drift CLEAN · live-diff VERBATIM+EDITS-ONLY · Codex 4 rounds (R1/R2/R3 findings all fixed; final round's only note was Mason's own untracked relaunch-overnight.cmd — left untouched, see morning report).
- **PROOF — Ran:** hash-verified apply + static checks (overloads=1, body persists service + uses compute_season, plpgsql_check=0) + a fully rolled-back [E2E] END-TO-END fee proof on live: save_job with a service → jobs.application_service_id persisted (pre-fix: NULL) · season=2026 for an Aug-2026 job (Oct-1 rule) · complete → transfer_job_to_invoice → invoice has EXACTLY ONE is_application_fee line, extended_cents=80000 (100 ac × /ac). **Saw:** all assertions pass; zero leftover rows post-rollback. **Not verified:** the picker rendered in a browser (build/lint/typecheck green; same component the field-app editor ships).
- **Deferred (optional):** per-customer default service (drafted as 20260706021000 in scratchpad; needs save_customer + customer-form surface — queue for a follow-up session).

## U2 — Partial-delivery billing leak — SHIPPED

- **Status:** SHIPPED (migration APPLIED LIVE; frontend committed and pushed with this entry)
- **Findings:** #34 (UNVERIFIED→CONFIRMED: live complete_delivery auto-invoice guard was per-ORDER — a follow-up delivery of shorted product could never be billed) · #39 (UNVERIFIED→CONFIRMED: no path ever flipped delivery_remainders scheduled→fulfilled; the Remainders Fulfilled filter was permanently empty).
- **Changes (migration 20260706070000, renamed above high-water per Codex R3):** complete_delivery — auto-invoice guard now per-delivery-aware (an invoice tied to a DIFFERENT delivery no longer blocks; this delivery's own invoice or an order-level invoice still blocks = no double-bill; the auto-invoice bills only THIS delivery's items) + remainder rows with followup_delivery_id = this delivery flip scheduled→fulfilled + tote-copy scoped to this delivery/order-level invoices (Codex R1) · create_invoice_for_unbilled_delivery — same delivery-aware guard so the backfill button works on newly-surfaced rows (Codex R1) · cancel_delivery + void_delivery — invoice-cleanup blocks scoped so reversing ONE delivery can no longer cancel/flag a SIBLING delivery's invoice (Codex R2 P1); order-status rollups and commission logic deliberately stay order-wide · IntegrityCleanup.tsx unbilled finder matches per-delivery.
- **Migration applied live:** version **20260706033059** (name 20260706070000_complete_delivery_per_delivery_billing)
- **Reviews:** rls CLEAN · drift CLEAN ×3 rounds (initial + 2 deltas) · live-diff VERBATIM+EDITS-ONLY on all FOUR re-emits · Codex 3 rounds (R1 tote+backfill, R2 reversal-path P1 — all fixed; R3 = filename ordering, fixed by rename, + the Mason-owned launcher note).
- **PROOF — Ran:** hash-verified apply + static checks (4 fns × 1 overload, all four delivery-aware predicates present in live prosrc, remainder-flip present, plpgsql_check=0, live row v20260706033059) + a fully rolled-back [E2E] E2E on live: order with delivery-A invoice ($500) → complete_delivery(B) auto-created a SECOND invoice CS-2026-0062 for $500 tied to B (old code: blocked), flipped the remainder row scheduled→fulfilled, left invoice A byte-untouched, order rolled up to fulfilled. **Saw:** all assertions verbatim in the SMOKE_PASS_ROLLBACK payload; zero [E2E] residue post-rollback. **Not verified:** IntegrityCleanup page rendered in a browser (query-shape change; lint/typecheck/build green).

## U4 — Customer-supplied chemicals — SHIPPED

- **Status:** SHIPPED (migration APPLIED LIVE; frontend committed and pushed with this entry)
- **Findings:** #53/#54/#102 (CONFIRMED — no customer-supplied concept; honest recording corrupted inventory + billed the grower for their own product; leaving it off gutted the legal record) · #56 (CONFIRMED — server manual lines unreachable from the UI).
- **Changes (migration 20260706080000):** job_chemicals.customer_supplied boolean NOT NULL DEFAULT false + 7 fn re-emits — save_job persists the flag (base=U3-applied text, season+preserve fixes intact) · complete_job skips deduction for flagged rows, keeps them in application_records product_data with customer_supplied:true · transfer_job_to_invoice emits the flagged line at $0 "(customer supplied)" with price_source='manual' (Codex R5 P1 — pins the $0 against tier re-pricing on invoice edit) · _sync_job_holds + _sync_quote_job_reservations (base=U5-applied) + get_job_inventory_shortfalls + get_dispatch_stock_status exclude flagged rows from shed demand. Frontend: JobDetail per-row "Customer supplied" checkbox + totals exclusion; DispatchBoard stock loop skips flagged rows (Codex R2); FieldView phone card charges $0 for flagged rows (Codex R3); FieldAppChemicalEntry "Add custom line" (#56) with persisted-state mode detection (Codex R1) + manual_override rehydration from price_source on invoice reload (Codex R4 — also fixes a pre-existing manual-line reload zeroing).
- **Migration applied live:** version **20260706053722** (name 20260706080000_customer_supplied_chemicals)
- **Reviews:** rls CLEAN · drift CLEAN incl. explicit base-text checks (no clobber of U3's season/preserve fixes or U5's closed_short denylist) · live-diff VERBATIM+EDITS-ONLY on all 7 fns · Codex 6 rounds (R1-R5 findings all fixed incl. one P1; R6 clean except the Mason-owned untracked relaunch-overnight.cmd note — file left untracked, never committed).
- **PROOF — Ran:** hash-verified apply + static checks (column exists default false, 7 fns × 1 overload, supplied logic present in complete_job/transfer, plpgsql_check=0, live row v20260706053722) + a fully rolled-back [E2E] two-row E2E on live: job with a control chem + a supplied chem (both stocked 100, priced $50/gal, qty 10) → complete_job → transfer_job_to_invoice. **Saw:** control inventory 100→90 + $500.00 line; supplied inventory stayed 100 + application_record entry customer_supplied=true + invoice line 0 cents "(customer supplied)" price_source=manual; control record flag false. Zero residue post-rollback. **Not verified:** the checkbox rendered in a browser (lint/typecheck/build green; standard input pattern).

## U6 — Void safety + blend↔job cross-guards — SHIPPED

- **Status:** SHIPPED (migration APPLIED LIVE; frontend committed and pushed with this entry)
- **Findings:** #65 (CONFIRMED — voiding a posted/cancelled field-app invoice permanently stranded the job at 'invoiced'; blend tickets had this safety net, jobs didn't) · #91 (CONFIRMED — no cross-guard between the two billing paths for one application).
- **Changes (migration 20260706110000, rebased onto the U4-applied transfer text):** void_invoice — BOTH void paths (draft/unposted→cancelled and posted/overdue→voided) release a field_application invoice's job (invoiced→completed via the sanctioned SET LOCAL app.admin_override bracket, mirror of transfer_invoice_to_job; only when the job still owns THIS invoice) + detach application_records.invoice_id + NULL-safe admin gate (IS DISTINCT FROM, rls-review M1) · create_invoice_from_blend_ticket — JOB_ALREADY_INVOICED guard testing for a LIVE invoice via invoices.job_id (immune to pre-U6 stranded jobs, Codex R2) with a jobs-row FOR UPDATE serializing against transfer (Codex R1 P1 TOCTOU) · transfer_job_to_invoice — BLEND_TICKET_ALREADY_BILLED guard testing live blend-ticket invoices via the invoices join (not the syncable flag, Codex R3) excluding soft-deleted tickets (Codex R2). Frontend: 2 RpcErrorCodes + friendly toasts (JobDetail, BlendTicketDetail) + 5 plain-English HelpTips on BlendTicketDetail's outbound actions.
- **Migration applied live:** version **20260706064207** (name 20260706110000_void_safety_and_blend_job_crossguard)
- **Reviews:** rls CLEAN (admin_override bracket mechanics verified against precedent; M1 hardening taken) · drift CLEAN (U4 base carried forward — verified by grep+diff; additive marked edits only) · Codex 4 rounds (R1 P1 lock serialization, R2 ×2, R3 symmetry — all fixed; R4 clean except the Mason-owned untracked launcher note).
- **PROOF — Ran:** hash-verified apply + static checks (3 fns ×1 overload, bracket+both guards+U4 text present in live prosrc, NULL-safe gate live, plpgsql_check=0, live row v20260706064207) + a fully rolled-back [E2E] E2E: complete job → transfer → VOID → job status=completed + invoice_id=null; blend ticket with a live invoice → transfer refused BLEND_TICKET_ALREADY_BILLED; job re-invoiced → create_invoice_from_blend_ticket refused JOB_ALREADY_INVOICED. **Saw:** all three assertions verbatim in the SMOKE_PASS_ROLLBACK payload; zero residue. **Not verified:** HelpTips rendered in a browser (lint/typecheck green; same HelpTip component used across the app).

## U11 — complete_job unit conversion — SHIPPED (headline claim REFUTED-AT-IMPLEMENTATION; adjacent real defect fixed)

- **Status:** SHIPPED (migration APPLIED LIVE; committed and pushed with this entry)
- **Finding re-verification:** the KNOWN "deducts 128 pints as 128 gallons" P1 was **already fixed live** by A6 (mig 20260702134000, 2026-07-02) — the wave plan's slotting was stale. Recorded as REFUTED-AT-IMPLEMENTATION per mission §1. What WAS real: (a) A6's hard RAISE (JOB_INV_UNIT_UNCONVERTIBLE) strands a field job carrying ANY unconvertible unit at in_progress — while the hold engine falls back to raw, breaking reserved==deducted; (b) legacy per-acre-suffixed units ('pt/ac') NULL out of the converter (Codex R1 P1) — under (a)'s fix alone they would have raw-deducted 16 "pt/ac" as 16 gal.
- **Changes (migration 20260706120000):** complete_job re-emitted (base = U4-applied live text) with normalize_rate_unit() applied before conversion + warn-and-raw-fallback replacing the hard RAISE (requires_review=true on the inventory_transactions row, note names the original qty/unit, activity_feed warn count; converted rows note "(converted from N unit)"). customer_supplied exclusion carried. Follow-up queued: the hold engines share the normalization gap (reserve side) — smaller severity since completion now corrects the physical deduction.
- **Migration applied live:** version **20260706071302** (name 20260706120000_complete_job_unit_conversion)
- **Reviews:** rls CLEAN · drift CLEAN (base diff = 4 marked hunks only) · Codex 2 rounds (R1 P1 normalize-before-convert — fixed and live-proven pre-apply: normalize_rate_unit('pt/ac')='pt', helper 16→2.0; R2 clean except launcher note).
- **PROOF — Ran:** hash-verified apply + static checks (1 overload, normalize+fallback present, old RAISE gone from executable code — the string remains only in an explanatory comment, verified by re-reading the applied SQL; plpgsql_check=0; live row v20260706071302) + a fully rolled-back [E2E] two-case E2E: 16 'pt/ac' on a Gal-stocked liquid → inventory 100→98 (2 gal deducted, txn qty=2, no flag); 5 'each' (unconvertible) → job COMPLETED, 5 deducted raw, requires_review=true, note contains "UNIT NOT CONVERTIBLE". **Saw:** all assertions verbatim; zero residue.

## U9 — Stock policy warn-not-block + Quick Delivery picker — SHIPPED

- **Status:** SHIPPED (migration APPLIED LIVE; frontend + doc committed and pushed with this entry)
- **Findings:** #9/#101 (CONFIRMED — create_quick_delivery hard-blocked on net stock) · #37 (CONFIRMED — complete_delivery hard-blocked on on-hand; with 17 products already negative, real paper tickets would be un-enterable) · #14 ADJUSTED-CONFIRMED (picker showed tier-1 price to every customer; tier helper sat unused in the same file). **Finding ADJUSTMENT:** the report's "net math includes on-order in both" did NOT match live source — neither RPC counts on-order; we kept each guard's existing stock basis and changed only the consequence (block→warn), per the surgical rule. On-order shows in the picker DISPLAY (NewOrder parity), not the guard.
- **Changes (migration 20260706130000):** both RPCs re-emitted (complete_delivery base = U2-applied text, guards carried) — hard RAISE → collect-warnings-and-proceed: negative stock allowed, requires_review=true on the ledger rows, activity_feed row + one notification per active admin, result gains stock_warning/short_stock_count/warnings. Missing Main-Warehouse rows are seeded at zero with manufactured_at_delivery=true (Codex R2 — the P4-7 phantom-row flag, surfaces on /integrity-cleanup) so the shortage stays visible. Frontend: QuickDeliveryModal picker shows the customer's REAL tier price (gated by showPricing) + On Floor/Net per row (Main-Warehouse-scoped, Codex R1; honest hide-on-lookup-failure, Codex R4/R5) + non-blocking warn toast; DeliveryDetail completion warn toast. docs/workflows/INVENTORY_RULES.md updated in-patch (Codex R3) — warn-not-block is now UNIVERSAL policy (owner §6.1).
- **Migration applied live:** version **20260706080738** (name 20260706130000_stock_policy_warn_not_block)
- **Reviews:** rls CLEAN · live-diff VERBATIM+EDITS-ONLY on both fns (U2 base carried byte-for-byte) · Codex 5 rounds (R1-R5 all fixed; severity decayed to P3 by R5).
- **PROOF — Ran:** hash-verified apply + static checks (2 fns, warn keys + U2 guard + seed flag present in live prosrc, plpgsql_check=0, live row v20260706080738) + a fully rolled-back [E2E] E2E: quick delivery needing 5 with only 1 net available. **Saw:** the call SUCCEEDED (old code RAISEd) with stock_warning=true/short_stock_count=1; prebooked 0→5 (net −4); ledger row requires_review=t; 3 admin stock_warning notifications. Zero residue post-rollback. **Not verified:** the picker rendered in a browser (build/lint/typecheck green).

## U10 — Record integrity + auto-draft toggle — PARTIAL (toggle SHIPPED, rest PARKED)

- **SHIPPED:** `auto_draft_invoice_on_job_completion` flipped **ON** live (owner decision §6.5; app_settings UPDATE 2026-07-06 08:21 UTC, RETURNING confirmed 'true'). Draft-only by design — never auto-posts; reversible one-click in Settings. Logged here per mission.
- **PARKED (needs a migration, queued for the next session):** application_records.application_date from actual start time · applicator name/license snapshot columns (#106) · job-born invoices stamp season from the job (#109 — NOTE: U3's compute_season fix in save_job already corrected the job.season basis, so the remaining gap is only the invoice-side stamping) · auto_draft_skipped activity row for applicator completions (#117).

## PARKED UNITS (drafted or queued — nothing half-landed)

- **U7 splits unification [L]** — NOT drafted (mission's park-whole-if-resistant unit). All prerequisites shipped (U4/U6 transfer_job_to_invoice chain is stable). Next step: opus builder per mission §3, base transfer_job_to_invoice = live post-U6.
- **U8 commissions on application channel [M]** — NOT drafted; owner rule recorded (§0.2: chemical portion only). Depends on transfer_job_to_invoice (now stable post-U6).
- **U12 Applicator My Day [L]** — **FULL DRAFT READY** in scratchpad/u12 (migration 20260706060000_field_view_my_day.sql + FieldView replacement + patches). Builder verified: latent dispatch-auth gap in start/complete_job (real), notifications RLS blocks sales_rep-triggered inserts (real pre-existing bug), FieldView has zero write calls (confirmed). Needs: integration + reviewer/Codex gates. Its complete_job re-emit must be REBASED on the U11-applied live text (post-20260706120000) before apply.
- **U13 assignment unification [L]** — **FULL DRAFT READY** in scratchpad/u13 (migration 20260706100000_assignment_unification.sql: 3 triggers + get_dashboard_action_items re-emit + 7 frontend patches). Builder found an UNLISTED real bug: every JobDetail save cascade-wipes job_location_dispatches (job_fields delete+recreate + ON DELETE CASCADE) — the trigger design fixes it. Needs integration + gates; QuoteBuilder/JobDetail anchors were drafted against tonight's tree.
- **U14-U18 (Wave 2), U19-U20 (Wave 3)** — not started; report sections stand.
- **U3 optional follow-up** — per-customer default application service (drafted as 20260706021000 in scratchpad/u3).
- **U11 follow-up** — hold engines share the normalization gap (normalize_rate_unit before field_app_priced_quantity in _sync_job_holds/_sync_quote_job_reservations); deduction side is fixed, reserve side still NULLs-to-raw on 'pt/ac' units.

## Wrap checks (U21)

- **DB invariant sweeps:** 13/15 predicates PASS. 4 un-allowlisted rows, ALL pre-existing standing drift unrelated to tonight's migrations: 3 inert anon-exec trigger grants (enforce_blend_ticket_fields_billed_lock, fields_acre_authority_guard, recalc_product_price_per_acre — trigger fns, not directly callable) + 1 genuine follow-up: generate_rup_sales_records(uuid,text) is auth-bound but role-ungated (from A8 era). Queue the REVOKE/role-gate fix.
- **Junk-customer FLAG-ONLY list (owner approves each; NOTHING touched):**
  | farm_name | created | activity |
  |---|---|---|
  | Crop Rx Solutions (own company) | 2026-02-10 | none |
  | Purchase Orders for WELLS AG SUPPLY CHEMICAL | 2026-02-10 | none |
  | Test Farm Alpha | 2026-02-22 | 1 order |
  | A1 TEST FARM | 2026-03-16 | 1 quote, 1 order |
  | [E2E] Farm Alpha (test fixture) | 2026-03-16 | 3 orders, 1 invoice |
  | [E2E] Farm Beta (test fixture) | 2026-03-16 | none |
  Note: the two [E2E] rows are the standing Playwright fixtures — deleting them breaks the E2E suite's shared fixtures; the other four are Mason's call.

## NIGHT 2 (2026-07-06, daytime start — same mandate, mission-night2.md)

**Run start: 2026-07-06T11:17Z** · hard wrap ~20:17Z (9h) · live high-water at start: 20260706080738 (matches mission expectation).

- **STEP 0 [housekeeping]** — overnight-intent flag removed per mission §1 (autopilot must NOT be armed; launch mode handles permissions). Schema registry refreshed via REAL live introspection (Q1–Q5 → `--from-introspection`): only drift was `migrations_high_water` 20260706053722→20260706080738 (Night-1 wrap had already captured schema content; U6/U9/U11 were function-only). `npm run generate-map` re-run (117 nodes, 218 edges, 0 problems). PROOF — Ran: regenerate-schema-registry.mjs --from-introspection + git diff · Saw: registry v2 rebuilt (27 enums / 5 gen-cols / 66 no-updated_at tables), diff = high-water line only.
- **U12 Applicator "My Day" [L] — SHIPPED.** Migrations APPLIED LIVE: `20260707010000_field_view_my_day` (live v20260706124057) + follow-up `20260707011000_start_complete_job_null_actor_guard`. The parked draft's complete_job base was PRE-U4/PRE-U11 — rebased onto live text before anything else (would have reverted customer-supplied skip + unit conversion). Frontend: FieldView "My Day" rebuild (date on card, Today-first/Done, Start/Complete on card, per-field applied-acres entry, REI/PHI line, NO dollar amounts §0.1, offline complete, legacy whole-job cards merged in), RoleLanding (driver→/my-route, applicator→/field, denied-pages safe), dispatch/reschedule/undispatch notifications (+ dispatch-path reschedule fan-out), notifications RLS INSERT widened to sales_rep, JobDetail Start/Complete gates recognize location dispatchees. Reviews: rls+drift 2 rounds each (0 BLOCKER/0 HIGH; MEDs folded in: null-safe guard, gc.is_active, field_id role-gate). Codex: 5 rounds, all P1s fixed (whole-job completion narrowed to owns-ALL-dispatches; field_id threading; applicator attribution; total_acres sync; offline stale-snapshot; form leak), final round P2s fixed (dispatch reschedule notify, split-job friendly error). **BONUS SECURITY FIX:** live verification's negative test exposed a PRE-EXISTING hole — `v_job.applicator_id = v_actor` NULL-propagates, so ANY applicator could start/complete NULL-applicator jobs; `20260707011000` closes it (proven: exploit SUCCEEDED pre-fix, 'Not authorized' post-fix, both rolled back). PROOF — Ran: rolled-back [E2E] E2E on LIVE (applicator dispatched to 2 fields, start→complete with field_acres override) · Saw: SMOKE_PASS_ROLLBACK, record attributed to completer, total_acres 95.5 (=55.5 override + 40 fallback), zero residue; post-fix exploit re-test SMOKE_PASS_ROLLBACK 'live exploit now blocked'. Tests 3141 passed; typecheck/lint/build clean. Known P2 follow-ups (ledger only): split-job Complete button shows then friendly-fails (RLS hides other parties' rows client-side); legacy whole-job cards drop off the list at completion (no Done entry).
- **U13 Assignment unification [L] — SHIPPED.** Migration APPLIED LIVE: `20260707020000_assignment_unification` (live v20260706160147). Fixes the CONFIRMED bug: every JobDetail save cascade-wiped job_location_dispatches (save_job deletes+recreates job_fields; dispatches CASCADE on job_field_id). Design: 1 new internal table `job_dispatch_preservation` (RLS on, clients revoked) + 4 SECDEF trigger fns/triggers — BEFORE DELETE on job_fields stashes active dispatches by stable (job_id,field_id); AFTER INSERT restores the stash FIRST (wizard splits survive saves) then falls back to the whole-job applicator; applicator-change re-syncs (clear-to-NULL cancels the old assignee's rows); terminal status auto-closes dispatch rows. get_dashboard_action_items gains `unassigned_jobs` (scheduled + no active dispatch + no legacy applicator — NO data backfill: outside the additive mandate; legacy jobs visible via U12's FieldView merge). Frontend: Jobs list dispatch-aware Applicators column (search/sort/export/print unified) + Needs-Dispatch badge/quick-filter; OfficeCockpit Needs-Dispatch tile (server-side anti-join); JobDetail job-scoped Dispatch Locations wizard button (dispatchable statuses only) with current-assignee chips + steal-confirm (job-level fallback); QuoteBuilder blocks field-less Schedule Job + per-section job badge; ActionQueue unassigned_jobs category. Reviews: rls final CLEAN (0 findings, all 10 checks), drift 0 B/0 H; compliance 0 B/0 H (1 MED: error-code token follow-up). Codex: 4 rounds — R1 backfill P2 resolved by definition fix, R2 P1 clear-applicator stale dispatches fixed, R3 P1 split-steal fixed via the preservation stash, R4 only 2 UI P2s (fixed). PROOF — Ran: rolled-back live E2E post-apply (create job+field → trigger auto-dispatches → simulate save_job DELETE+INSERT churn) · Saw: SMOKE_PASS_ROLLBACK dispatch_status=dispatched, applicator preserved, points_to_new_job_fields=t, stash_count=0; pre-apply split test: crew split survived the churn (CREW_RESTORED_OK). Tests 3146 passed; typecheck/lint/build clean. PARKED for Mason: optional one-time backfill of dispatch rows for legacy-assigned jobs (business-data write — needs his OK; without it, rows materialize on each job's next edit).
