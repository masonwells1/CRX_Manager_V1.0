{
  "_doc": "Machine-readable progress for the ChemMan Gap-Closeout loop. LEDGER.md is regenerated from this. status per section: PENDING | IN-PROGRESS | BUILT | PARTIAL | BLOCKED. codex: done | pending | partial | n/a.",
  "loop": "chemman-gap-closeout",
  "state": "Stage 1 (weather auto-fill) BUILT + committed (36382c95) + ORCHESTRATOR-VERIFIED (read the migration; DB-confirmed single overload / 13 nullable cols / no new CHECK on invoices / anon revoked / updated_at present; independent Codex CLI = clean, 0 findings). Next: build Stage 2 (diluent per acre).",
  "branch": "feat/chemman-gap-closeout",
  "based_on": "feat/fieldapp-beyond-parity",
  "precondition_verified": "2026-06-30: solo check passed — on feat/chemman-gap-closeout, tree clean (package-lock churn from npm install restored/ignored). HEAD 3d9a51e1 (beyond-parity ledger tip). node_modules installed. Live schema grounded: field-app invoice = invoices table (invoice_type='field_application'), weather today = invoices.temperature_text+wind_direction (manual text), acres = invoices.total_acres; weatherCapture.ts already has fetchWeatherForDateTime; diluent missing on billing side (exists job-side as jobs.carrier_rate_gpa). Newest migration on disk 20260630170000.",
  "owner_decisions": {
    "scope": "EXACTLY two sections (weather auto-fill, diluent per acre) on the field-application invoice. Everything else from the ChemMan comparison is already built — do NOT rebuild.",
    "models": "Orchestrator = Opus 4.8. BOTH build stages touch the invoices money table -> build subagents on Opus 4.8. Read-only grounding/search subagents may use Sonnet 5. Fable unavailable."
  },
  "buildOrder": [1, 2],
  "sections": {
    "1": { "name": "Weather auto-fill on the field-application invoice", "model": "opus", "status": "BUILT", "codex": "DONE — subagent ran ~12 Codex CLI rounds + migration-drift-reviewer + compliance-reviewer (all fixed: re-added dropped EXECUTE grant, RPC-layer weather validation [no DB CHECK], stale-centroid race, p_update_weather sentinel so a stale 6-arg caller can't erase weather, stale-auto invalidation on field/date change, in-flight stale-fetch drop, fallback-time stamp); ORCHESTRATOR independent re-verify: read the migration + DB-confirmed (1 overload of update_field_app_applied_info=20-arg superset, SECDEF+search_path, 13 nullable weather cols, invoices CHECK count still 6, anon revoked/authenticated granted, invoices has updated_at) + independent Codex CLI on commit 36382c95 = CLEAN (0 findings).", "needs_migration": true, "migration": "20260630180000_field_app_invoice_weather_capture.sql", "risk": "medium", "commit": "36382c95", "verified": "subagent booted dev vs LOCAL, opened a field_application invoice, clicked Get Weather start+end (real Open-Meteo fill + Auto badge), hand-edited a value (badge->Manual + override flag), saved (all 13 cols persisted, weather_manual_override=t), reloaded (auto weather survived), forced offline (error toast + manual entry still works), disclaimer renders. Orchestrator independently DB-verified the schema + ran independent Codex (clean). 23 new unit tests + 90 pass across affected suites.", "parkedLow": ["(a) DEPLOY-ORDER (gate note, not a code change): the new frontend sends the 20-arg update_field_app_applied_info call, which only exists after the migration applies — so at the PROD gate the migration MUST apply before/with the code deploy (timestamp order already enforces this).", "(b) provenance-after-reload: a save->reload of a partially hand-corrected auto set loses per-value auto/manual provenance (DB stores one source flag per set, not per value) — accepted trade-off (a pessimistic wipe would destroy saved manual data; the modeled-not-measured disclaimer covers verify-on-site)."], "openQuestions": [] },
    "2": { "name": "Diluent / carrier-water per acre on the field-application invoice", "model": "opus", "status": "PENDING", "codex": "n/a", "needs_migration": true, "risk": "medium", "commit": null, "verified": null, "parkedLow": [], "openQuestions": [] }
  },
  "openQuestions": [
    "SETUP NOTE: confirm the LOCAL throwaway Supabase (C:/Users/mason/fieldapp-local-db) carries the beyond-parity migrations through 20260630170000 before applying the new migrations; if its ledger is behind, apply new migrations via direct psql against the parity schema (the parity/beyond-parity loops' established MO), not a full db reset.",
    "Stage 2: confirm save_field_app_invoice reliably writes invoices.total_acres before choosing a GENERATED diluent_total_gallons column (else store a plain total or compute for display).",
    "PRODUCTION GATE: beyond-parity (feat/fieldapp-beyond-parity) must be on main first or together — these two gaps sit on top of it. Flag the exact state at the gate."
  ]
}
