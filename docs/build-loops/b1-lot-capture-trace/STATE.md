# STATE — B1 Lot Capture & Trace build loop

> The loop reads this at the start of every turn and updates it after every phase. Status values: `PENDING` · `IN-PROGRESS` · `DONE` · `BLOCKED` · `AWAITING-OWNER-APPROVAL`.
> **The loop runs ONLY the first `PENDING` phase each turn, and STOPS (does not run) on `BLOCKED`, `IN-PROGRESS`, or `AWAITING-OWNER-APPROVAL`.**
> Keep the log append-only; never delete history (record what happened so a resume/owner can trust it).

**Overall status:** IN-PROGRESS (Phase 0–3 DONE; Phase 4 next)
**Branch:** feat/application-lot-capture (created off origin/main @ Phase 0; tracks origin/feat/application-lot-capture)
**Supabase dev branch ID (migration proof target — `apply_migration` ONLY here; NEVER prod `rhyzpcqhnizqbxphqdkr`):** none — proof method = rolled-back BEGIN..ROLLBACK smoke against live (zero footprint; dev-branch billing avoided)
**Started:** 2026-06-22T18:37Z
**Last updated:** 2026-06-22T20:05Z

## Phase checklist
| Phase | Status | Commit SHA | Codex verdict | Notes |
|---|---|---|---|---|
| 0 — Setup & grounding | DONE | (env gate, no feature code) | n/a (helper) | worktree OK · codex 0.140.0 logged-in · registry refreshed → high-water 20260622165336 · SOW re-grounded vs live · codex design-critique folded in |
| 1 — DB migration (table + 3 RPCs + blend propagation) | DONE | bfe811e (+09df391 fixes) | SHIP (Codex) | `20260622170000_application_record_lots.sql` — table (RPC-only writes + unique idx + qty CHECK), 3 RPCs, blend lot propagation. Proven via rolled-back smoke (zero footprint). NOT applied live (owner gate). Subagents 0 BLOCKER; Codex 7 rounds (all P2, no BLOCKER/HIGH) → final "no actionable issues". |
| 2 — Types (`src/types/index.ts`) | DONE | 1626811 | SHIP (Codex) | 5 interfaces (ApplicationRecordLot, ApplicationRecordLotInput, SetApplicationRecordLotsResult, RecentLotForProduct, LotApplicationTraceRow). typecheck clean; types-drift reviewer 0/0/0; Codex "no actionable issues". |
| 3 — UI: lots-applied editor | DONE | 6f5f9ca | SHIP-WITH-FOLLOWUPS (Codex) | LotsEditorModal (multi-lot/product, suggestion dropdown + free-type, qty/unit) on Application Records, admin/sales-gated; saves via set_application_record_lots. Typed-client shim `src/lib/lotRpc.ts` bridges the parked table/RPCs. Proof: mocked-shim component test (6 cases) + pages-render smoke + build + typecheck. compliance-reviewer CLEAN. Codex 3 rounds: fixed load-failure-disable-save, dedupe-products, stale-load-race; round-3 P2 (button exposed pre-apply) ACCEPTED-BY-DESIGN (lands-together gate + graceful degradation). |
| 4 — UI: lot-trace lookup page | PENDING | — | — | LotTrace.tsx + route + nav |
| 5 — Wire-up, tests, docs | PENDING | — | — | blend auto-propagation; doc-drift = 0 |
| 6 — Final gate + handoff | PENDING | — | — | apply-guard proof + HANDOFF.md; then STOP |

## Run log (append-only)
- (loop appends one line per phase: timestamp · phase · what happened · proof method · review outcome)
- 2026-06-22T20:05Z · Phase 3 · Built `src/components/LotsEditorModal.tsx` (lots-applied editor: per-product lot rows, received-lot suggestion datalist + free-type, optional qty/unit, replace-all save) wired into `ApplicationRecords.tsx` via an admin/sales-gated "Lots" button; registered 7 B1 RPC error tokens in db.ts. **Typed-client constraint:** the parked migration's table+RPCs aren't in generated `src/types/supabase.ts`, so a shim `src/lib/lotRpc.ts` (one `as unknown as` for the table read + `as never` RPC name/arg casts, assertRpcResult preserved — NO any/@ts-ignore, generated file untouched) backs the calls. **Proof** (no reachable DB — migration parked): mocked-shim component test (6 cases: load+replace-all payload, suggestion→receiving_record_id, dup guard, load-failure-disables-save, dedupe-products, stale-load-race) + pages-render smoke + full build + npm run typecheck + assertRpcCoverage; live UI walkthrough deferred to post-apply. compliance-reviewer CLEAN. Codex 3 rounds: R1 (2 P2: load-failure data-loss, duplicate-product sections) fixed; R2 (1 P2: stale async load race) fixed with a load-token guard; R3 (1 P2: button exposed pre-apply) ACCEPTED-BY-DESIGN — the SOW "lands-together" gate means the branch is never deployed before the migration, and the modal degrades gracefully (loadError → friendly message + Save disabled) if reached pre-apply. Committed 6f5f9ca; pushed. **Post-apply follow-ups (→ handoff):** (a) regenerate `src/types/supabase.ts` then the lotRpc shim can revert to direct typed calls; (b) optional feature-flag hardening for the Lots button (deferred — lands-together gate covers it).
- 2026-06-22T19:45Z · Phase 2 · Added 5 B1 interfaces to src/types/index.ts (ApplicationRecordLot + input + SetApplicationRecordLotsResult + RecentLotForProduct + LotApplicationTraceRow), matching the parked migration's table columns + 3 RPC return shapes. `npx tsc --noEmit` exit 0. typescript-types-drift-reviewer: 0/0/0 (every column/type/nullability verified vs the migration, incl. LEFT-JOIN-nullable trace columns). Codex SHIP ("no actionable issues"). Committed 1626811; pushed. Note: registry has no application_record_lots entry yet — expected pre-apply, not drift (regen after the owner applies).
- 2026-06-22T19:10Z · Phase 1 · Built `20260622170000_application_record_lots.sql`: new table `application_record_lots` (RLS mirrors application_records, applicator-sees-own; no updated_at; source_receiving_record_id ON DELETE SET NULL; UNIQUE (record,product,lower(btrim(lot)))) + 3 SECDEF RPCs (`set_application_record_lots` strict-actor/in-body-gate/parent-FOR-UPDATE-then-idempotency/product-membership+source-receipt+DUPLICATE_LOT validation; `get_recent_lots_for_product` returns receiving_record_id; `get_lot_application_trace` case-insensitive, field fallback to legacy field_id) + CREATE OR REPLACE `create_application_record_from_blend_ticket` verbatim + lot-propagation INSERT (case-insensitive dedup, skip null product/blank lot). **Proof = rolled-back BEGIN..ROLLBACK smoke** (confirmed execute_sql runs it as one txn; zero prod footprint verified: table null, 0 new fns, 0 smoke rows). Smoke covered all RPCs + every validation branch + blend dedup + trace + role gate across 3 runs. Subagents CLEAN (rls-security 0/0/0, compliance 0/0/0, migration-drift 0 BLOCKER — blend body byte-identical except added INSERT; 1 HIGH=restamp + 1 MED=doc both resolved). Codex 7 rounds → SHIP (all P2, no BLOCKER/HIGH; every finding addressed: idempotency-under-lock race, source-receipt validation, restamp past high-water, loop-state, trace field fallback, suggestions return receiving_record_id, unique-row invariant + case-insensitive dedup, trace index matches predicate, non-negative quantity CHECK, idempotent DROP-then-CREATE policies, RPC-only writes [dropped client write policies — direct write now RLS-denied; verified via role-switched smoke that the SECDEF RPC still writes as owner]; final round = "no actionable issues"). Committed bfe811e + 09df391 (fixes). Pushed to origin/feat/application-lot-capture. NOT applied live (owner gate).
- 2026-06-22T18:37Z · Phase 0 · Worktree confirmed (`.claude/worktrees/youthful-roentgen-b5d48a`, NOT main checkout), clean, branch `feat/application-lot-capture` off origin/main, `npm ci` done. Codex CLI 0.140.0 logged-in (gate available). Schema registry refreshed via live MCP introspection (Q1–Q5) + `regenerate-schema-registry.mjs --from-introspection` → high-water 20260620240000→20260622165336, only real delta `blend_recipe_items.price_per_unit_cents` (no status/gen-col/table drift). Re-grounded vs live: `application_records` (source_id NOT NULL, product_data jsonb), `application_record_fields` (no updated_at), `receiving_records.lot_number` (text null, no updated_at), `blend_ticket_products.lot_number` (text null, **product_id nullable**) — all match SOW. Captured verbatim live body of `create_application_record_from_blend_ticket` (single overload, oid 77976, idempotency op `create_app_record_from_bt`, returns uuid[]) + `application_records` RLS policies (insert admin/sales, update/delete admin, select admin/sales/applicator-own). Codex helper design-critique PASS with refinements folded into Phase 1 (idempotency-before-DELETE, parent FOR UPDATE, source_receiving_record_id ON DELETE SET NULL, filter null product_id/blank lot in blend insert, btrim+case-insensitive, trace from application_record_lots not receiving, dedupe blend dup lines). Proof method: env gate (no feature code).

## Open issues for the owner (only if a phase is BLOCKED)
- (none yet)

## Hard gates NOT crossed by the loop (handled at handoff, owner approval required)
- [ ] apply migration to live DB
- [ ] regen schema registry (post-apply)
- [ ] post-apply smoke-chain + db-sweeps
- [ ] merge feat/application-lot-capture → main
- [ ] deploy (Vercel) + live UI proof
- [ ] owner in-app smoke
