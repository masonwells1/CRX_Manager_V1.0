{
  "_doc": "Machine-readable progress for the field-app parity loop. LEDGER.md is regenerated from this + STEP0-AUDIT.json. status per section n: PENDING | IN-PROGRESS | BUILT | BLOCKED. codex: done | pending | partial.",
  "codexNote": "Codex CLI hit its usage limit during section #2 (resets 2026-06-25 ~07:02). Interim gate = builder self-review + orchestrator spot-check + (for DB/money/RLS sections) a fresh adversarial reviewer agent. Run a real Codex pass over all codex:pending sections when limits reset, BEFORE the production gate. ALSO batch real Codex over 1f1b564 (#12 billed-job immutability DB guard) — the hardening agent's own Codex pass was LOST when it stalled mid-run; substituted by orchestrator live 6-case proof + 3-lens multi-lens CLEAN, but add cross-model coverage at the gate.",
  "sections": {
    "1": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "faf90ad7",
      "criteria": "11/11",
      "codex": "partial",
      "codexDetail": "3 Codex rounds completed, all High/Med fixed; 4th confirmation pass skipped (limit). Re-confirm at reset.",
      "migration": "20260624120000_job_parity_scheduling_agronomy_shares.sql",
      "verified": "Independently confirmed: job_field_shares table + RLS, 10 new jobs cols (remaining_acres GENERATED), job_chemicals/job_fields extras all on local; typecheck+build+2226 tests green; agent ran full flow in-app vs local DB.",
      "parkedLow": [
        "[#1 P3] Job-list footer totals don't follow the DataTable's internal in-table search box (the named status/date/customer filters DO update totals). Fixing needs a shared DataTable API change — out of section scope."
      ],
      "openQuestions": []
    },
    "2": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "7210f583",
      "criteria": "5/5",
      "codex": "pending",
      "codexDetail": "No Codex pass (limit). Builder self-review vs 5 failure classes + fixed a fieldless-job rate edge case. Orchestrator spot-check: HEAD commit, typecheck green, destructive load_recipe_into_job call removed. Non-DB, client-side + localStorage — low risk. Codex at reset: src/pages/JobDetail.tsx, src/lib/recipeHelpers.ts.",
      "migration": null,
      "verified": "Spot-checked: commit on branch, typecheck PASS, no active frontend load_recipe_into_job call. Agent verified save-as/last-used/load (merge+replace, price-preserving) in-app vs local DB.",
      "parkedLow": [],
      "openQuestions": []
    },
    "3": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "6357b94",
      "criteria": "5/5",
      "codex": "pending",
      "codexDetail": "Codex unavailable. INTERIM multi-lens review Workflow (correctness+security+data) = CLEAN, no High/Med. Live-verified: additive migration (job_batches RLS 4 policies, jobs.batch_ref FK ON DELETE SET NULL, partial index), search_path on helpers, anon/applicator writes denied, checkMutationResult+23505 handling. Re-run Codex at reset: migration + JobBatches components + Jobs.tsx.",
      "migration": "20260624150000_job_batches.sql",
      "verified": "Spot-check: HEAD 6357b94, tree clean, typecheck PASS, job_batches RLS=true + jobs.batch_ref present. Multi-lens panel CLEAN. Agent ran create/assign/filter/remove/reload in-app vs local.",
      "parkedLow": [
        "[#3 Low] Batch member-count hints (JobBatchesManager 'N jobs', assign-modal coverage, delete-confirm copy) derive from the capped/filtered in-memory jobs list so can undercount with >500 jobs or an active filter (display-only; ON DELETE SET NULL still un-batches correctly). Same root as #6 row-cap. Fix: dedicated aggregate count query, or soften the copy.",
        "[#3 Low / RECURRING across #3/#4/#6 new tables] INSERT policies gate on is_admin()/is_sales_rep() but don't bind created_by to auth.uid() (privileged-only audit-attribution nit; frontend always passes real profile.id). Consider a one-shot hardening migration before the production gate."
      ],
      "openQuestions": []
    },
    "4": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "ac1bf74",
      "criteria": "8/8",
      "codex": "pending",
      "codexDetail": "Codex unavailable (limit). INTERIM independent adversarial reviewer = CLEAN: verified RLS policies live (catalog public-read by design; assignments inherit job visibility w/ applicator scoping; writes admin/sales_rep), additive migration, hex CHECK + name UNIQUE + (job_id,tag_id) PK + ON DELETE CASCADE both FKs, both FK indexes present, no N+1, checkMutationResult on all mutations, no injection. Re-run real Codex at reset: supabase/migrations/20260624130000_job_tags.sql + src/components/jobs/*.",
      "migration": "20260624130000_job_tags.sql",
      "verified": "Orchestrator spot-check: HEAD ac1bf74, tree clean, typecheck PASS, both tables RLS=true (4+3 policies) on local. Independent adversarial reviewer CLEAN (live RLS/constraint/index probing). Agent ran tag create/assign/chip/filter/bulk in Chrome vs local.",
      "parkedLow": [
        "[#4 test-gap] No automated test asserts an applicator CANNOT write job_tag_assignments (RLS verified live by reviewer; a defense-in-depth test would be nice). Non-blocking."
      ],
      "openQuestions": []
    },
    "5": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "6de0509",
      "criteria": "5/5",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Pure UI reorder on existing job_fields.sort_order; native HTML5 DnD + keyboard fallback (NO new dependency). Builder self-review clean; orchestrator spot-check: HEAD, tree clean, no package.json change, typecheck PASS. Low risk. Codex at reset: src/lib/routeOrder.ts + src/pages/JobDetail.tsx.",
      "migration": null,
      "verified": "Spot-check + agent ran in-app vs local: dragged + keyboard-reordered 3 fields, saved, DB sort_order confirmed (0=East 40,1=North 80,2=South Creek), persisted across reload; per-field shares moved with rows.",
      "parkedLow": [],
      "openQuestions": []
    },
    "6": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "c326e05e",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. INTERIM independent adversarial reviewer = CLEAN (no High/Med). Verified live: server-vs-client filter split consistent (server filters before .limit(500); client narrows fetched rows; share-customer filter server-side so not truncated), both new crew tables RLS correct, FKs (ground_crew_id SET NULL, members CASCADE) + indexes + uniqueness correct, selection/totals derive from visibleJobs. Re-run Codex at reset: 9 files esp. migration + Jobs.tsx filter pipeline + JobDetail crew direct-update.",
      "migration": "20260624140000_job_ground_crews_and_filter_indexes.sql",
      "verified": "Spot-check: HEAD c326e05e, tree clean, typecheck PASS, crew tables RLS=true, ground_crew_id + filter indexes present on local. Reviewer CLEAN (live RLS/FK/index probing). Agent ran multi-filter AND-narrowing in-app (7→3 jobs, totals 645→325 ac) + CLEAR ALL reset.",
      "parkedLow": [
        "[#6 Low] Deactivating a crew already on a job makes the JobDetail crew dropdown render blank (id retained in state; only lost if user actively changes the dropdown). Optional: include/append the inactive crew in the picker.",
        "[#6 Low] Date-preset click spreads the current draft into applied, committing any pending non-date draft edits along with the date. Optional: base preset on applied.",
        "[#6 Low] Direct crew .update() fires on every save even when unchanged (idempotent no-op write; harmless).",
        "[#6 Low/known-limitation, PRE-EXISTING from #4] A job matching only a CLIENT-side filter (tags/crop/county/state/chemical/fieldName) that sorts past row 500 (job_date DESC) is truncated before the client filter sees it. Affects only >500 server-matched jobs. Long-term: move all filters server-side or paginate."
      ],
      "openQuestions": []
    },
    "7": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "5e827066",
      "criteria": "7/7",
      "codex": "pending",
      "codexDetail": "Codex unavailable. INTERIM focused correctness reviewer = CLEAN. Confirmed live: client status pre-flight EXACTLY matches _enforce_job_status_transition; buildFieldPatch never emits status so NO forward transition reachable via raw update (start_job/complete_job/transfer side-effects can't be bypassed); only 'cancelled' is bulk-safe; bulk-cancel drops no side-effects (no cancel triggers; single-cancel identical; holds consumed at complete_job). Selection = visibleJobs.filter(selected), no stale ids. Loader-worksheet bulk DEFERRED to #10. Re-run Codex at reset: JobMassEditModal/jobMassEdit/Jobs.tsx.",
      "migration": null,
      "verified": "Spot-check: HEAD 5e827066, tree clean, typecheck PASS, no cancel_job RPC to bypass. Reviewer CLEAN (status-legality + cancel side-effects + selection). Agent ran 3-job mass-edit (date/status/applicator/batch) in-app vs local; unselected + unfilled-field jobs proven untouched.",
      "parkedLow": [
        "[#7 Low] Non-status field bulk update (date/applicator/batch) is one atomic .update().in() — if one job fails a trigger (e.g. enforce_applicator_license LICENSE_EXPIRED), the whole field batch rolls back all-or-nothing (surfaced honestly via toast, not swallowed). Could note atomicity in the modal copy.",
        "[#7 Low] Mass-edit writes updated_by; single-job cancel (JobDetail) does not — cosmetic consistency nit (column nullable)."
      ],
      "openQuestions": []
    },
    "8": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "77ad2ef1",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. INTERIM multi-lens review Workflow (correctness+security+data) = CLEAN, no High/Med. Live-verified: additive user_list_settings table, 4 owner-scoped RLS policies (auth.uid()=user_id, cross-user read/insert/update/delete DENIED, anon SELECT=0 rows), UNIQUE(user_id,list_key) matches upsert onConflict, FK->profiles CASCADE, length CHECK, updated_at via shared INVOKER trigger. mergeJobListSettings whitelists column ids vs hardcoded registry (no jsonb injection). Re-run Codex at reset: migration + jobListColumns + Jobs.tsx + FieldInvoices.tsx.",
      "migration": "20260624160000_user_list_settings.sql",
      "verified": "Spot-check: HEAD 77ad2ef1, tree clean, typecheck PASS, user_list_settings RLS=true 4 policies all auth.uid()-bound. Multi-lens CLEAN. Agent ran toggle+persist+reload+per-user-isolation in-app/DB; 2326 tests pass.",
      "parkedLow": [
        "[#8 Low] List Settings toggles fire-and-forget full-snapshot upserts; rapid out-of-order completions could land a stale snapshot last (self-heals on next load/change; no money/cross-user). Optional: debounce ~300ms or serialize.",
        "[#8 Low] anon retains an inherited table-level SELECT grant on user_list_settings, neutralized by RLS + no anon policy (anon SELECT=0 rows; matches project convention). Optional project-wide REVOKE."
      ],
      "openQuestions": []
    },
    "9": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "c0bdbe17 + fix 09183ba8",
      "criteria": "9/9",
      "codex": "done",
      "codexDetail": "Codex CLI came back online during #9's fix (09183ba8) and its Codex pass ran CLEAN. The build (c0bdbe17) landed during the rate-limit window with an interim multi-lens review (migration/RLS CLEAN; it caught the gal/lb Med — Total Applied used rate_unit instead of c.unit — fixed in 09183ba8).",
      "migration": "20260625160000_applicator_sheet_print_audit.sql",
      "verified": "Re-verified: HEAD 09183ba8, tree clean, typecheck PASS, jobs.last_printed_by + inert applicator_sheet_custom config on local. Fix proven (gal/lb now matches the in-page preview on divergent units — uses c.unit not rate_unit; legacy layout preserved). 17 tests. Build agent proved 3 formats (Original/Custom/Enhanced) via PDF content-parse (route order, tank mix, REI/PHI, EPA reg, print stamp).",
      "parkedLow": [
        "[#9 Low] Field-sheet Total-Applied PRECEDENCE prefers saved total_quantity; server prefers rate*acres (normally agree; diverge only on a stale reloaded quantity). + deeper server-vs-UI unit inconsistency (server convert_to_gl_lb uses rate_unit; UI/sheet/loader use unit) — a future app-wide decision, same Low family."
      ],
      "openQuestions": []
    },
    "10": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "ee5be988",
      "criteria": "8/8",
      "codex": "done",
      "codexDetail": "Real Codex CLI gate ran 9 rounds → final verdict CLEAN ('No actionable correctness issues'). Fixed every High/Med Codex raised: duplicate-product index-aligning, bulk print-stamp, pre-save bad-number validation, MAX_LOADS cap, inactive-vehicle capacity fallback, override-validity, default-field-share customers in bulk, unrounded carrier rate for print, gallon-unit-only vehicle capacity, deactivated-customer name resolution, per-load rounding over-apply (cap each load at remaining → sum exactly == total).",
      "migration": "20260625170000_job_loader_worksheet_fields.sql",
      "verified": "Orchestrator spot-check: HEAD ee5be988, only LEDGER/PROGRESS dirty (excluded), 12 files all mine, typecheck PASS, both loader cols (carrier_rate_gpa nullable ≥0, loader_tank_capacity nullable >0) + CHECKs live on local, no new fn overloads. Independent multi-lens (correctness/security/data) = CLEAN (0 High/0 Med); security live-probed RLS blocks applicator writes to loader cols; migration purely additive; load math brute-forced ~1M combos zero anomalies. Build agent proved per-load split via parsed PDF (100ac×16gpa, 500gal tank → 4 loads 500/500/500/100; product 160gal → 50/50/50/10 = exactly 160), capacity override persisted+recomputed, invalid-capacity guard, per-row + bulk PDFs, print stamp. 50 new tests + 89 across touched areas pass.",
      "parkedLow": [
        "[#10 Low] Bulk/row Loader PDF (loaderWorksheetFetch.ts:173) includes a job_chemicals row when (product_name OR quantity!=null) labeled 'Product', while JobDetail's Loader button + in-page preview require c.product_id → the two entry points can print different product lines for the same job on a degenerate quantity-only/product-cleared row. Display-only, no data/money/security. Fix: one shared product filter (require resolved product_id/name + positive qty) across all three paths.",
        "[#10 Low] loaderWorksheet.ts Math.max(0,remaining) clamp would break the per-load-sums-to-total invariant for a NEGATIVE product total (unreachable — job_chemicals.quantity never negative; JobDetail coerces parseFloat||null). Optional: clamp product total >=0 up front + test.",
        "[#10 Low] MAX_LOADS=200 ceiling treats a 20–200-load job as 'valid' and renders per-load columns squeezed to ~3.5pt (legible→illegible; never crashes/miscomputes). Cosmetic. Optional: lower printable ceiling (~30) or compact rows above a threshold."
      ],
      "openQuestions": []
    },
    "11": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "855c11f + fix 10af1bb",
      "criteria": "6/6",
      "codex": "done",
      "codexDetail": "Build agent ran real codex-review 5 rounds (all Med fixed). Independent multi-lens review found 1 Med (applicator split-bill compliance omission) + 4 Low → fix agent. Fix agent ran codex TWICE: found+fixed a name-collision dedup Med (two distinct customers sharing a name collapsed → dedup by customer_id) + an orphan-share join Low (join shares to the job's actual fields on field_id), re-ran codex → CLEAN. No Lows parked.",
      "migration": "20260625180000_get_job_billed_customers.sql",
      "verified": "Build: orchestrator spot-check HEAD 855c11f, 6 files all mine, typecheck PASS; agent proved per-job report via parsed PDF (rate/total/gal-lb/REI/PHI/EPA present, EPA blank on no-reg product, no double rate suffix) on JobDetail button + Jobs-row action, print-audit stamp + activity log, blank-job guard. Fix (10af1bb): orchestrator re-verified live — get_job_billed_customers is SECURITY DEFINER + STABLE + search_path=public,pg_temp, 1 overload, anon EXECUTE revoked (only authenticated/postgres), gate = is_admin() OR is_sales_rep() OR (is_applicator() AND applicator_id=auth.uid()) and RAISEs before returning data if caller can't view the job; typecheck PASS. Fix agent proved live as the applicator: OLD embed dropped co-billed Sunrise (null) → NEW RPC returns all 4 billed customers (primary+default+2 shares); a DIFFERENT applicator is rejected; guard ABORTS on incomplete resolution with printed_at confirmed still NULL (no stamp on incomplete compliance doc); admin path + #10 loader unchanged (same 4 customers). 18 new tests; lib suite 1623 pass.",
      "parkedLow": [
        "[#11-fix Low / local-state, not a defect] get_job_billed_customers exists on local but isn't in supabase_migrations.schema_migrations (applied ad hoc via psql during testing) — the known local caveat (direct psql doesn't update the migration ledger). Migration 20260625180000 is correct + idempotent (CREATE OR REPLACE); it runs through the normal runner at the production gate. No action beyond the gate apply.",
        "[#11-fix Low / pre-existing, watch in #12] Bulk fetch paths (chemicalApplicationReportFetch/loaderWorksheetFetch) do one sequential get_job_billed_customers RPC per selected job (N+1) and stamp printed_at for all selected ids even if the resolved fetch returns a smaller subset. Not introduced by 10af1bb; bounded by selection size. If revisited: batch the resolver + stamp only resolved ids. RELEVANT TO #12 (bulk cross-job) — #12 should never silently undercount a selected job."
      ],
      "openQuestions": []
    },
    "12": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "8fd04e2d + fix 24a8fd2 + hardening 1f1b564",
      "criteria": "5/5",
      "codex": "partial",
      "codexDetail": "Build (8fd04e2d): real codex-review → SHIP. Fix (24a8fd2, form-conflict + REPORTABLE selection): real codex-review found 1 Med (invoiced jobs reachable by Mass Edit/Batch/Tags) → fixed (EDITABLE subset) → re-ran CLEAN. Hardening (1f1b564, billed-job immutability DB guard): the hardening agent STALLED waiting on its own background Codex task and never returned that result — so the orchestrator substituted (a) a live 6-case psql proof on local (non-admin soft-delete + total_price_cents rewrite on an invoiced job both RAISE; live-job edit, applied_acres recompute, printed_at stamp, admin-override hatch all PASS) AND (b) a 3-lens multi-lens review = CLEAN (0 High/0 Med; full 44-col allow-list, SECURITY INVOKER+search_path, closed role matrix, additive). ALSO orchestrator caught + fixed a timestamp collision/replay-order bug the agent missed (renamed 20260625140000→20260625190000, after the #10 loader-cols migration it references). NEEDS the end-of-run REAL Codex batch over 1f1b564 to add cross-model coverage on the guard.",
      "migration": "20260625190000_guard_billed_job_immutability.sql",
      "verified": "Build (orchestrator spot-check HEAD 8fd04e2d, 6 files all mine, NUL-byte=0, typecheck PASS): agent hand-verified cross-job sums vs live local (Roundup 350pt→43.75gal re-derived from the SUM; different-unit Atrazine separate line; single-job + excluded-job paths), Codex SHIP, 2669-test suite green. Fix (24a8fd2): form-conflict guard now emits '—' on any divergent product_form (incl null-vs-known), invoiced jobs REPORTABLE (in summary totals) while Delete re-filters to DELETABLE + mutating bulk actions gated to EDITABLE; proven live + Codex CLEAN. Hardening (1f1b564): see codexDetail — guard proven on local (6 cases) + multi-lens CLEAN; Jobs.tsx delete query also .in(status,DELETABLE) with honest skipped count; typecheck PASS, pre-commit lint/build/test/map green.",
      "parkedLow": [
        "[#12-hardening Low / TOCTOU cosmetic] Jobs.tsx handleDelete: if EVERY selected deletable job is billed between fetch and delete, .in('status',DELETABLE) matches 0 rows → result.data===[] → checkMutationResult throws the generic 'no rows affected, you may not have permission' BEFORE the friendly 'all N skipped' toast. Delete is still correctly blocked (DB guard holds) — no data/security/money impact. Fix: call checkMutationResult only when result.error is set; derive deletedCount/totalSkipped from result.data?.length ?? 0 so the empty-data case flows into the 'all N skipped' info toast.",
        "[#12-hardening Low / HARD-GUARD gap — DO AT END-OF-RUN HARDENING PASS] No automated regression test backs the security-critical, column-position-fragile allow-list guard _enforce_billed_job_immutability (verified manually only: 6 live cases all correct). A future migration adding a billed-relevant column to the exempt set would silently weaken protection with nothing failing CI. Add a pgTAP/SQL-assertion (or scripts/db-invariant-sweeps) test mirroring the proven scenarios: non-admin soft-delete + total_price_cents/customer_id rewrite on an invoiced job RAISE check_violation; exempt paths (printed_at/last_printed_by stamp, applied_acres recompute, admin, app.admin_override hatch) PASS. Converts the manual proof into a HARD guard (owner principle: prefer hard guards over rules)."
      ],
      "openQuestions": []
    },
    "13": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "c36dedf + fix a90efe3",
      "criteria": "6/6",
      "codex": "done",
      "codexDetail": "Build (c36dedf): real codex-review found 1 Med (partial CSV had no excluded-job marker → under-order risk) → fixed → re-ran SHIP. Multi-lens review then found 1 HIGH (NULL jobs.total_acres jobs silently dropped from the forecast via the GENERATED remaining_acres reading 0-not-NULL — a 'never undercount' failure) + 1 Med (factor denominator jobs.total_acres diverges from the gatherer field-acre basis) + 2 Low → fix a90efe3. Fix agent ran real codex-review and WAITED: round 1 found a P2 (jobs.total_acres fallback basis not threaded into the aggregator → silent ZERO projection) → fixed by threading acre_basis fetch→aggregator→PDF → round 2 CLEAN/SHIP. Final multi-lens on the fix = CLEAN (0 High/0 Med, all 3 lenses).",
      "migration": null,
      "verified": "Build (orchestrator spot-check HEAD c36dedf, 5 files all mine, typecheck PASS): agent proved per-product projected totals vs hand-math incl. in_progress using REMAINING acres (Roundup 470pt = 200+150+120[60 remaining of PUSE-IP, not 200], invoiced job excluded), gal/lb re-derived from projected sum, basis subtitle + date range, no-selection guard; 14 tests. Fix (a90efe3): orchestrator spot-check HEAD a90efe3, 4 files all mine, typecheck PASS. Fix agent proved live before/after on REAL seed jobs: JOB-PARITY-11 + SPLITBILL-1 (NULL jobs.total_acres) were DROPPED by old gate, NOW INCLUDED with full field-acre projection; PUSE-DIVERGE (total_acres=80, field=100, applied=40) now projects correct 120pt (was 100/150); fully-applied excluded 'already applied', no-basis excluded honest 'set acres'. 21 tests; src/lib suite 1658 pass. Final multi-lens CLEAN (same-unit subtraction, ÷0/negative guarded, acre_basis threaded, RLS unchanged).",
      "parkedLow": [
        "[#13 Low / theoretical, cannot occur in live data today] projectedUseReportFetch.ts fallback edge: a rate-only product with NO saved quantity AND a job with NO field acres would project 0. All 3 lenses confirmed it cannot occur today (0/21 job_chemicals rows have NULL quantity). Optional: add a guard/log if a future product can carry a rate with no saved qty and no field basis."
      ],
      "openQuestions": [
        "PROJECTED-ACRES BASIS: the Projected Use Report now uses the sum of a job's FIELD acres (Σ job_fields.acres_to_treat, the gatherer's authoritative basis) as the projection basis, and projects the NOT-YET-APPLIED portion (field-acre total − applied_acres) for scheduled/in_progress jobs. Confirm this matches how you want 'projected use' computed — vs using the job header's total_acres field, and vs projecting the FULL scheduled acres (not just remaining) for in_progress jobs. Built + proven on the remaining/field-acre basis; non-blocking."
      ]
    },
    "14": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "73ef8cd",
      "criteria": "6/6",
      "codex": "done",
      "codexDetail": "Real codex-review (gpt-5.5) ran 6 rounds to convergence. Fixed: P1 recipe cross-contamination (recipe signature added to the load-group key so different recipes never merge into one tank), P2 swallowed capacity-read error (honest loads_unreadable reason), P2 quantity-only/stale-rate collapse (effective per-acre rate from total/acres), P2 product+unit key space-collision (NUL-delimited key at all 3 sites), P3 no-load heading mislabel. Final verdict CLEAN. Then multi-lens review (correctness/security/data) = CLEAN (0 High/0 Med/0 Low) — actively probed + refuted every suspected bug (per-load split math sound; new jobs+vehicle read RLS-scoped; get_job_billed_customers fail-closed; schema additive).",
      "migration": null,
      "verified": "Orchestrator spot-check: HEAD 73ef8cd, 5 files all mine, NUL-byte=0 (NUL keys are \\u0000 escapes, not literal bytes), typecheck PASS. Build agent exercised end-to-end in the running app vs LOCAL DB as admin via the real modules (live Supabase, billed-customer RPC, #11 gatherer): CSUM-A+B combined Roundup 350pt (=200+150 hand-sum) → 43.75gal re-derived from the 350 sum; AMS 1000lb; Atrazine 50qt→12.5gal; same-capacity+recipe loads combine (1000gal@10, 150ac→1500gal→2 loads one block); DIFFERENT capacities GROUPED not averaged (1000 vs 500 → two blocks, no fake 750); no-capacity job noted, mix still counted; ConflictMix liquid-vs-null form → gal/lb '—' while raw qty sums; valid application/pdf. 20 new tests; report-family suite 68/81 green.",
      "parkedLow": [
        "[#14 Low / cross-section hardening — DO AT END-OF-RUN] #14 uses NUL ('\\u0000') delimiters in productUnitKey/recipeSignature to prevent key collisions (Codex P2 fix: 'Roundup fl'+'oz' vs 'Roundup'+'fl oz' would collide under a space delimiter). #12 (chemicalSummaryReportData) and #13 (projectedUseReportData) STILL use a space/'|' delimiter for their product+unit aggregation keys — a latent collision for pathological product names with trailing spaces matching a unit prefix. Pre-existing, narrow (requires crafted product names). Harden #12/#13 to NUL delimiters in the end-of-run cleanup pass."
      ],
      "openQuestions": []
    },
    "15": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "8dfa557",
      "criteria": "5/5",
      "codex": "done",
      "codexDetail": "Real codex-review (gpt-5.5) ran + WAITED: 2 Med (P2) — (1) print used raw visibleJobs ignoring the DataTable's in-table search/sort → fixed by lifting search+sort to the page (controlled DataTable, prints displayedJobs); (2) totals foot repeated on every page → showFoot:'lastPage'. Re-ran Codex → clean. Then multi-lens (correctness/security/data) = CLEAN (0 High/0 Med/0 Low): 'paper==screen' holds via the identical applyTableSearchSort over displayedJobs, no pagination so totals can't omit off-screen rows, all column renders match jobPrintCellValue; pure client-side, zero backend surface; the shared DataTable controlled-props change has an internal fallback so every existing caller is unaffected.",
      "migration": null,
      "verified": "Orchestrator spot-check: ledger intact (32), HEAD 8dfa557, 8 files all mine, typecheck PASS. Build agent proved live (preview, local admin): filter Job#=PUSE → on-screen 4 PUSE jobs, bottom totals 230/150; toggled List Settings non-default (Price ON, Crops OFF) + ZERO checkbox selection → Print List PDF (17.8KB) headers followed settings (Price present, Crops absent, registry order), body = exactly the 4 PUSE jobs, footer totals 230/150 == screen; MediaBox 792×612 landscape, 13-col wide set rendered with 7pt shrink + linebreak (no truncation); in-table-search fix proven (search 'DIVERGE' → PDF only PUSE-DIVERGE, totals 80/40). 28+9+ tests; full suite 2746 pass; lint/build clean.",
      "parkedLow": [],
      "openQuestions": []
    },
    "16": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "b2e636c + fix b4d05299",
      "criteria": "6/6",
      "codex": "done",
      "codexDetail": "Finish agent ran codex-review 5 rounds (fixed: customer-wide field over-fetch → NEW job-scoped RPC get_job_fields_with_geojson; bucket missing file_size_limit/allowed_mime_types; map not lazy-loaded; boundary multi-part geom drop; accept-string vs bucket MIME; measured/override acres dropped; async download loses popup activation; applicator sees dead delete button; noopener window handle; marker no-op) → CLEAN. Multi-lens review then found 1 HIGH (storage INSERT policy gated on owner=auth.uid() — the ONLY owner-check in the codebase; if prod storage-api leaves owner NULL every upload 403s = feature DOA, invisible to mocked tests) + 1 Med (uploaded_by FK ON DELETE RESTRICT breaks user deletion) + 3 Low → fix b4d05299. Fix agent ran codex-review 2 rounds (storage DELETE looser than catalog DELETE → kept owner_id term + at-least-as-strict; empty remove-array false-failure) → CLEAN, AND stood up a REAL storage-api container (v1.60.15) to PROVE the byte round-trip. Final re-review CLEAN (0 High/0 Med; 2 fail-safe Low parked).",
      "migration": "20260625200000_job_attachments.sql + 20260625203000_job_attachments_fixups.sql",
      "verified": "Orchestrator spot-check: HEAD b4d05299, tree clean, typecheck PASS. job_attachments table RLS (select/insert/delete) + PRIVATE bucket (public=false, file_size_limit 25MiB, allowed_mime_types) + storage.objects policies. Fix realigned storage policies to the PROVEN codebase bucket pattern: INSERT/SELECT gate on bucket_id + job-visibility (j.id::text = foldername[1], NO owner term), DELETE adds owner_id=auth.uid()::text (not deprecated owner), text-compare (no ::uuid throw). FK uploaded_by now ON DELETE SET NULL (confdeltype=n, proven live: deleting an uploader succeeds, row.uploaded_by→NULL). SECURITY proven 6 ways via psql role-sim (rolled back) on BOTH job_attachments AND storage.objects: admin/sales see all visible-job objects, applicator confined to own-job path, anon 0, cross-job/forged INSERT denied. REAL storage-api container round-trip PROVEN: applicator upload to own job 200, signed-URL download returns bytes, cross-job upload 400, anon 400, disallowed MIME 400. Map/Logs tab renders live (fields + 'no boundary on file' empty state); job-scoping proven live (job A's files not on job B). New RPC get_job_fields_with_geojson SECDEF + search_path + 1 overload + anon revoked, job-scoped (no customer over-fetch). 31+ tests.",
      "parkedLow": [
        "[#16-fix Low / fail-safe, PROD-GATE confirm] The storage DELETE policy's applicator-ownership term owner_id=auth.uid()::text relies on the storage-api populating owner_id at insert. Proven for local storage-api v1.60.15 (populates owner+owner_id), but the PROD storage-api version is unconfirmed. If prod leaves owner_id NULL, an applicator couldn't delete their OWN file (admin/sales still can) — fail-safe (never lets an applicator destroy a teammate's file; no security/data issue). PROD-GATE: confirm owner_id is populated by a real upload; if it can be NULL, broaden the applicator delete term to `OR owner = (select auth.uid())` (read-only on DELETE → no insert DOA risk) + soften the migration comment's 'verified' wording.",
        "[#16-fix Low / storage-cost only, same root] In the same NULL-owner_id scenario, an applicator's storage-DELETE is silently denied (storage-js returns empty array, no error) while the catalog row deletes → orphaned BYTES (cost leak, no data loss, no broken UI). Resolved by the owner_id confirmation above; no separate fix."
      ],
      "openQuestions": [
        "PROD-GATE storage smoke (#16): after the job_attachments bucket migration applies to prod, do one real authenticated upload + signed-URL download + an oversized/disallowed-type reject to confirm against the PROD storage-api version (owner_id population + size/MIME enforcement). Map polygon visual render needs VITE_MAPBOX_TOKEN (not set in worktree .env) — confirm on a token'd env. (Both were proven locally via an ad-hoc storage-api container + the geometry RPC/UI; this is the prod confirmation.)"
      ]
    },
    "17": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "38fb120 + fix 1b96aee",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. INTERIM multi-lens review found 2 Med (both FIXED in 1b96aee, verified live as sales_rep): (1) applicator name read via RLS-blocked profiles embed → '(removed applicator)' for non-admins → now resolved from profile_public_view-backed prop Map; (2) vehicle name from active-only list → inactive vehicle showed '(removed)' + edit silently nulled it → now joined-row value + inject current vehicle as extra <option>. Migration + RLS were CLEAN (additive job_applied_records, 4 job-scoped policies live-proven admin/sales_rep OR applicator-owns-job). Re-run Codex at reset: migration + AppliedRecordsManager + appliedRecords + JobDetail.",
      "migration": "20260624170000_job_applied_records.sql",
      "verified": "Spot-check: HEAD 1b96aee, tree clean, typecheck PASS. Fix verified live as a real sales_rep user (old embed null, new shows real names; inactive vehicle kept on edit). Build agent ran 2-record add/edit/delete in-app vs local. Multi-lens: migration/RLS CLEAN, 2 Med fixed.",
      "parkedLow": [
        "[#17 Low / policy] canEdit is office-only (admin/sales_rep); applicators can't self-record from this UI even though RLS grants them write on their own job's records. Intentional default left as-is; revisit if applicators should record their own passes (would gate canEdit on applicator-owns-job + allow while completed).",
        "[#17 Low / RECURRING] created_by client-supplied, not bound to auth.uid() (audit-spoofing only). One-shot hardening migration before prod gate."
      ],
      "openQuestions": []
    },
    "18": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "7c64fb9 + fix 5f25a4c",
      "criteria": "7/7",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Multi-lens review found HIGH + 3 MED + 1 LOW — ALL FIXED in 5f25a4c (new migration 20260624181000) and re-verified live by orchestrator: (HIGH) field_id FK was ON DELETE CASCADE (would erase legal as-applied detail on field delete) -> now ON DELETE RESTRICT (confirmed confdeltype). (MED) recompute_job_applied_acres SECURITY DEFINER was anon-callable -> EXECUTE REVOKED from PUBLIC+anon (confirmed false; triggers run as definer). (MED) stale per-entry on last-child-delete -> settle_applied_record_acres zeroes the record. (MED) #17-era manual records uncounted -> jobs.applied_acres now SUMs applied_acres across ALL records; frontend effectiveRecordAcres mirrors. (LOW) float over-application false positive -> 1e-6 epsilon. Re-run Codex at reset: both migrations + appliedRecords + AppliedRecordsManager.",
      "migration": "20260624180000_job_applied_record_fields.sql + 20260624181000_fix_applied_acres_integrity.sql",
      "verified": "Build agent proved applied/remaining math + over-clamp in-app. Multi-lens caught HIGH+3MED. Fix 5f25a4c: 5 live smoke results (FK RESTRICT blocks field delete; anon EXECUTE revoked; last-child zeroes; manual record counted; remaining_acres consistent) + mixed-job UI agreement (228.5=198.5+30) + 42 tests. Orchestrator re-verified live: FK RESTRICT, anon EXECUTE false on both fns, old migration untouched, typecheck PASS.",
      "parkedLow": [],
      "openQuestions": []
    },
    "19": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "440f70c",
      "criteria": "9/9",
      "codex": "pending",
      "codexDetail": "Codex unavailable. INTERIM multi-lens review = CLEAN (all 3 lenses). Live-verified: additive idempotent migration (12 nullable weather cols, ADD COLUMN IF NOT EXISTS), 7 CHECK constraints reject bad values (humidity 150 / wind -3 / source 'api' all rejected), #18 acres rollup + triggers untouched, RLS cross-tenant isolation (non-owner SELECT/UPDATE = 0 rows, anon denied), Open-Meteo fetch from numeric lat/lng.toFixed(4) + regex-gated dateISO on CSP-whitelisted host (no SSRF/XSS), kept FREE provider. Re-run Codex at reset: migration + weatherCapture + appliedRecords + AppliedRecordsManager.",
      "migration": "20260624190000_job_applied_record_weather.sql",
      "verified": "Spot-check: HEAD 440f70c, tree clean, typecheck PASS, 12 weather cols + CHECK constraints, #18 triggers intact, no paid provider. Multi-lens CLEAN. Agent ran real Open-Meteo auto-pull (start 75F, end at 10:30 73F — time-keyed), manual override flips source, Total Time 2h30m, persist+reload, in-app vs local.",
      "parkedLow": [
        "[#19 Low] Auto-pull with a blank Time defaults to a noon reading but doesn't stamp the assumed time (editable, non-gating). Optional: default time to selected hour or note 'midday assumed'.",
        "[#19 Low] computeTotalMinutes wraps any end<start to +24h (intentional overnight support) so a same-day typo (10:00→08:00) shows '22h 0m' (editable). Optional: flag >12h windows or an explicit overnight flag."
      ],
      "openQuestions": []
    },
    "20": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "43d09143",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Low-risk additive-columns section on already-RLS-proven job_applied_records (no new RLS/RPC/money) → thorough orchestrator spot-check in lieu of full panel: net_tach GENERATED ALWAYS (can't drift), CHECK live-REJECTS end<beginning + negatives, #18 recompute triggers intact, RLS inherited, typecheck PASS, 83 tests. Re-run Codex at reset: migration + appliedRecords + AppliedRecordsManager.",
      "migration": "20260624200000_job_applied_record_tach.sql",
      "verified": "Spot-check (thorough): HEAD 43d09143, tree clean, typecheck PASS, net_tach GENERATED, CHECK live-rejected an end<begin insert, #18 triggers intact. Agent proved net 6.5 from 1200.5/1207.0 in-app + persist/reload + end<begin warning.",
      "parkedLow": [],
      "openQuestions": []
    },
    "21": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "cf11739 + fix 0c860bd",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Multi-lens review found 1 HIGH (+ Lows) — FIXED in 0c860bd (frontend-only) and re-verified: editing an as-applied entry ran an UNCONDITIONAL crew delete+reinsert that WIPED the SET-NULL'd legal snapshot rows -> now scoped .delete().not('member_id','is',null) so legal rows survive (proven before/after in a rolled-back txn: old=0 rows, fixed=1 survives). + Low guard: load-failure toast + skip crew mutation when catalog empty while members selected. Security lens CLEAN (RLS job-scoped link->record->job; anon denied; applicator-cross-job write blocked). member_id ON DELETE SET NULL + member_name_snapshot preserves legal record. Re-run Codex at reset: migration + AppliedRecordsManager + appliedRecords.",
      "migration": "20260624210000_job_applied_record_crew.sql",
      "verified": "Spot-check: HEAD 0c860bd, tree clean, typecheck PASS, fix .not('member_id','is',null) at AppliedRecordsManager:326. Fix agent reproduced before/after (NULL legal row destroyed by old code, survives with fix), 101 tests. Build agent verified attach/persist/member-filter + member-deleted-record-survives + applicator RLS block.",
      "parkedLow": [
        "[#21 Low / doc-only, can't fix without editing committed migration] migration comment says 'NULLS NOT DISTINCT' but DDL is plain UNIQUE = NULLS DISTINCT — behavior is CORRECT (multiple removed-member NULL rows coexist by design); only the comment misleads. Left unedited (red line).",
        "[#21 Low / accepted pattern] client-supplied member/crew name snapshots aren't server-validated (forgeable by applicator on own job; RLS-contained, display-only, React-escaped, matches #17/#18). Optional future: derive snapshots server-side at insert."
      ],
      "openQuestions": []
    },
    "22": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "b8d7b8c + fix 2f1f93a3",
      "criteria": "8/8",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Money-lens review: security+money lenses CLEAN (post_invoice SECURITY DEFINER pinned search_path; RLS proven non-owner/anon=0 rows; idempotency minted-once no double-post; integer cents no float). Found 1 HIGH + Lows — FIXED in 2f1f93a3 (frontend) + re-verified live: HIGH split/grouped invoices showed blank locations/crops/0 acres (list embedded field_app_locations by invoice_id but grouped key by invoice_group_id) -> now resolve locations by group (mirrors FieldApplicationInvoice branch); proven split invoice shows East 40/West Pivot, Corn/Soybeans, 125 ac + PDF header + reconciled Subtotal-Payments=BalanceDue. Also fixed: toPdfRow now carries paid/prepay/write_off/total_cost cents (money foot-gun before #23 reuse), plain-English PRICING_INCOMPLETE, fetch windows on invoice_date not created_at. Re-run Codex at reset: FieldInvoicesUnposted + fieldInvoiceList + invoicePdf.",
      "migration": null,
      "verified": "Spot-check: HEAD 2f1f93a3, tree clean, typecheck PASS. Fix proven live (split-invoice locations/acres/PDF populate; in-range invoice_date now appears). 18 list tests + 2448 suite. Build agent proved Post All flips status via real post_invoice.",
      "parkedLow": [
        "[#22 Low / pre-existing, not a regression] shared email-layer buildEmailHtml doesn't HTML-escape interpolated body (DB-sourced invoice number/date; same path as InvoiceDetail). Optional: escapeHtml() across the shared email layer."
      ],
      "openQuestions": []
    },
    "23": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "7e7a2161",
      "criteria": "7/7",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Money-lens review = CLEAN (no High/Med). Security+money lenses CLEAN: read-only page, no new RLS surface (live RLS blocks unauthorized reads), Unpost All genuinely inert (no unpost_invoice RPC, no mutation — verified), integer cents, committed-status filtering (posted/overdue/paid) correct, Balance Due reconciles with payments/prepay (FA-POST-J1 1000-300-100=600). Reuses #22 hardened helpers + split-location fix. Re-run Codex at reset: FieldInvoicesPosted + fieldInvoiceList scope helpers.",
      "migration": null,
      "verified": "Spot-check: HEAD 7e7a2161, tree clean, typecheck PASS, Unpost All inert (no mutation). Multi-lens CLEAN. Agent proved split posted invoice locations/acres, Balance Due reconcile, month-batch warning toggling by scope, posted/unposted disjoint, in-app vs local.",
      "parkedLow": [
        "[#23 Low] Orphaned batch-scope desync: pick a month batch then a filter excluding that month -> blank select + 'no invoices match' though matches exist (recoverable via Clear; no data/money impact). Fix: demote scope to season or render synthetic 0-count option.",
        "[#23 Low] Cross-batch caution copy implies a destructive bulk action but Unpost All is inert (#28 placeholder) and Print doesn't touch totals. Soften copy or gate banner behind #28.",
        "[#23 Low / SCALE-HEADROOM, flag at prod gate] Monthly-batch footer totals computed client-side over the loaded slice capped at QUERY_LIMIT=2000 rows (invoice_date DESC); a season >2000 posted invoices could truncate old batches and understate a batch total while the banner asserts committed numbers (mitigated by cap-hit toast; ~0 live invoices today). Fix before real volume: fetch the selected batch month server-side."
      ],
      "openQuestions": []
    },
    "24": {
      "status": "BUILT",
      "date": "2026-06-24",
      "commit": "cb642c0 + fix db061bc5",
      "criteria": "7/7",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Money-lens review: security+lifecycle CLEAN (posted-lock, strict-actor, idempotency, RLS on 8 read tables). Found 1 MED + 2 Low — FIXED in db061bc5 + re-verified: MED save_field_app_invoice UPDATE coerced non-admin v_salesman_id:=v_actor + COALESCE -> non-admin save silently overwrote admin-chosen Consultant (commission) -> new migration CREATE OR REPLACE gates salesman_id = CASE WHEN is_admin() THEN COALESCE(v_salesman_id,salesman_id) ELSE salesman_id END (diff-proven single line, 1 overload, search_path/SECDEF preserved; smoke w/ control: non-admin preserves Alice, admin changes to Sam, OLD body overwrites). Low: placeholder applicator names could print on legal PDF -> unfiltered name map + realApplicatorName() null-coercion (#17 lesson); Notifications-tab copy honest + error check. Re-run Codex at reset: new migration + FieldApplicationInvoice + invoiceAppliedCarryover.",
      "migration": "20260624211000_save_field_app_invoice_preserve_consultant.sql",
      "verified": "Re-verified live: HEAD db061bc5, tree clean, old migration untouched, typecheck PASS, save_field_app_invoice has 1 overload + the CASE-gate present in the live fn. Attribution smoke proven w/ control. Build agent verified 5 tabs + carry-over (weather/tach/crew names resolved) + posted-lock on TST-24A/B/C.",
      "parkedLow": [],
      "openQuestions": []
    },
    "25": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "b9e09c12 + fix ef668faa",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Money+security lenses CLEAN: pricing/cents path byte-IDENTICAL after the RPC recreation (migration 20260624212000 added invoice_items.warehouse/vendor + recreated save_field_app_invoice), single overload, #24's salesman_id CASE-gate PRESERVED (orchestrator-verified, no regression), idempotency/posted-lock/actor-bind/RLS intact. 1 MED (informational, NOT money) FIXED in ef668faa: client gal/lb preview inferred liquid/dry from unit STRING but server convert_to_gl_lb uses product_form -> liquid 'oz' showed 5 lb on screen vs 0.625 GL saved/printed -> client now branches on product_form to match server (proven liquid 320oz screen 2.5gal=server 2.5GL=DB 2.5000; tests assert client==server). Re-run Codex at reset: FieldAppChemicalEntry + chemCalculator + migration.",
      "migration": "20260624212000_field_app_invoice_line_warehouse_vendor_glb.sql",
      "verified": "Re-verified: HEAD ef668faa, tree clean, typecheck PASS. Money path byte-identical + #24 gate preserved (live). gal/lb screen==server==DB proven. Build agent: tier auto-fill $28, $25.50x120=306000c, posted-lock on chemical lines.",
      "parkedLow": [],
      "openQuestions": []
    },
    "26": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "3098ac4 + fix a0fbe33a",
      "criteria": "7/7",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Money review: PENNY-EXACT confirmed CLEAN BY CONSTRUCTION (basis toggle provably can't change dollars; integer cents throughout; reconcile cent-check non-tautological; engine unchanged, proven odd-total 1501c -> 500+1001 idempotent). 1 MED (correctness, ZERO money) FIXED in a0fbe33a: reconcileSplit ACRE half compared per_customer acres (excludes soft-deleted member) to GROSS total_applied_acres (includes them) -> false red 'Mismatch' on a valid split -> now compares acres over the SAME present customer set (money/cents byte-unchanged; proven vs real preview RPC: soft-deleted c3 OLD=120vs240 red, FIXED=120=120 green). Low split-ref -001 ordering (display-only, parked). Re-run Codex at reset: customerSplit + CustomerSharesTable.",
      "migration": null,
      "verified": "Re-verified: HEAD a0fbe33a, tree clean, typecheck PASS. Fix proven vs real preview_field_app_invoice_split RPC (false-Mismatch gone, genuine drift still red). 28 tests. Build agent proved penny-exact split (odd 1501c) + presentation-only basis toggle + Customers summary in-app.",
      "parkedLow": [
        "[#26 Low] Split-Ref -001/-002 suffix is a positional index, not guaranteed primary-first (display-only; invoice_number untouched; Primary badge shown separately). Optional: order primary-first or drop the implication."
      ],
      "openQuestions": []
    },
    "27": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "7f86101",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. THE SPINE. Full money-lens review = CLEAN (no High/Med, all 3 lenses). Forward transfer already wired; agent ADDED reverse leg (new RPC transfer_invoice_to_job, migration 20260625120000). Verified: SECDEF + locked search_path + anon EXECUTE revoked+rejected + admin/sales_rep gate + non-forgeable ACTOR_MISMATCH; exact -total_amount_cents audit row, bigint cents, draft/unposted carry no payment so zeroing orphans nothing; whitelisted draft|unposted->cancelled (no override) + job invoiced->completed in TRANSACTION-SCOPED admin_override; two-sided race guard blocks double-reverse + engine-built; operation-scoped idempotency; append-only audit; smoke PASS (round-trip/idempotent-replay/forged-actor/posted-guard/no-job-guard). Re-run Codex at reset: migration + InvoiceDetail + FieldApplicationInvoice.",
      "migration": "20260625120000_transfer_invoice_to_job.sql",
      "verified": "Spot-check: HEAD 7f86101, tree clean, typecheck PASS, reverse RPC 1 overload + anon revoked + search_path pinned. Money-lens CLEAN. Agent proved end-to-end: completed 2-cust (60/40) job -> ONE unposted invoice $1200 (shares $720/$480 penny-exact), job=invoiced; reverse -> cancelled + job=completed + audit both ways; idempotency 2nd call=no dup.",
      "parkedLow": [
        "[#27 Low] Dead/unreachable Transfer-to-Scheduling button+handler+modal in FieldApplicationInvoice.tsx (forward transfer creates 0 field_app_locations so the discriminator always routes job-built invoices to InvoiceDetail before this editor renders; the working button lives in InvoiceDetail). Misleading comment ('transferred invoice has both job_id AND locations' — untrue). Optional cleanup: remove dead code + fix comment.",
        "[#27 Low] Inverted lock order (forward jobs-then-invoice, reverse invoice-then-job) — deadlock window ~nil (mutually-exclusive states, retry-safe via idempotency). Optional: lock source job FOR UPDATE first in transfer_invoice_to_job."
      ],
      "openQuestions": []
    },
    "28": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "9ac9b2f",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Full money-lens review = CLEAN (all 3 lenses, proven live). New unpost_invoice RPC (migration 20260625130000): role gate admin+sales_rep, strict-actor ACTOR_MISMATCH, MONEY GUARD refuses paid/allocation/prepay/write-off, closed-period guard, status guards, operation-scoped idempotency (no double mutation/audit), status CHECK widened to SUPERSET (all 7 statuses verified present), transaction-local app.admin_override auto-clears, append-only audit (blocked even the agent's own cleanup DELETE). 5 smoke cases PASS (post->unpost money-intact, idempotent replay, paid REFUSED, never-posted REFUSED, closed-period REFUSED) + forged-actor + overdue->unposted. 1 overload, anon revoked, search_path. Wired Unpost on per-acre editor + InvoiceDetail + Posted-list bulk. Re-run Codex at reset: migration + 3 pages.",
      "migration": "20260625130000_unpost_invoice.sql",
      "verified": "Spot-check: HEAD 9ac9b2f, tree clean, typecheck PASS, unpost_invoice 1 overload + anon revoked, status CHECK = all 7 statuses (superset). Money-lens CLEAN. Agent proved post->unpost round-trip + paid-blocked + idempotency + append-only audit in-app/DB.",
      "parkedLow": [
        "[#28 Low / PRE-EXISTING shared infra] idempotency_keys UNIQUE on idempotency_key alone + save_idempotency ON CONFLICT(idempotency_key) DO NOTHING -> a key reused under a different operation would silently fail to save (post/void identical; READ side operation-scoped; clients mint per-(actor:invoice) UUIDs so collision ~impossible). Optional shared-layer hardening: make constraint + ON CONFLICT target (idempotency_key, operation).",
        "[#28 Low / cosmetic] Unpost button gated isAdmin on InvoiceDetail but status-only on FieldApplicationInvoice (RPC role gate admin+sales_rep is the real boundary, enforced identically). Optional: pick one UI policy.",
        "[#28 Low] Overdue invoice can be unposted after generate_finance_charges levied interest (accounting oddity, not money-stranding; finance-charge invoice references its own id; void_invoice shares this). Optional: confirm-copy heads-up for overdue targets."
      ],
      "openQuestions": []
    },
    "29": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "96bf0cd2",
      "criteria": "5/5",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Focused money/correctness review = CLEAN (no defects, all 6 probes pass). Frontend wiring of existing void_invoice RPC (signature (uuid,text,text); actor bound via auth.uid(); self-gates admin-only before any work). Client does ZERO money recompute (RPC owns reversal: deletes allocations, restores prepay+customer balance, cancels pending commissions, zeroes totals, append-only audit). Group-aware void: per-invoice voidKeysRef + RPC check_idempotency = no double-void/dup-audit; partial failure honest. voidTargets excludes terminal; after void fully locked + RPC refuses re-void. Required reason enforced UI+handler+RPC. Re-run Codex at reset: FieldApplicationInvoice.",
      "migration": null,
      "verified": "Spot-check: HEAD 96bf0cd2, tree clean, typecheck PASS, no migration. Focused review CLEAN. Agent proved void draft->cancelled + void posted+$1000-payment->voided (total/paid/balance->0, allocation reversed money-unstranded, audit total_impact -320000/allocations_reversed 100000), double-void guarded, idempotent, terminal-locked, in-app/DB.",
      "parkedLow": [],
      "openQuestions": []
    },
    "30": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "bbd2d81 + fix d221aa2",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Print review = everything CLEAN (field-mapping correct incl. agent's applicator->salesman self-fix, splits sum penny-exact, bulk-print over filtered set w/ Promise.allSettled + empty guard, format routing safe default-current no circular import, data completeness good) EXCEPT 1 MED money-display — write_off_cents dropped from BOTH layouts (balance_cents=total-paid-prepay-write_off) so write_off>0 made printed Subtotal-Payments-Prepay overstate Balance Due by the hidden write-off. FIXED in d221aa2 + PROVEN by parsing actual PDF Tj text operators: both formats now print '- Write-off' line, $185-50-15-30=90=Balance Due. No money math changed. Re-run Codex at reset: invoicePdf + 3 lists + FieldApplicationInvoice.",
      "migration": null,
      "verified": "Re-verified: HEAD d221aa2, tree clean, typecheck PASS. Fix proven via PDF content-stream parse (both formats reconcile with write-off). 58 tests. Build agent: both formats valid PDF, splits sum, legacy distinct from current.",
      "parkedLow": [
        "[#30 Low/resolved-by-fix] reconciliation test gap (no write_off case) — closed by the fix's new write_off>0 tests in both formats."
      ],
      "openQuestions": []
    },
    "31": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "a56c4604",
      "criteria": "5/5",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Focused email safety review = CLEAN (no defects). Send-safety verified: recipient ALWAYS the invoice's own customer at 3 client layers (editor pdfSnapshot.customer_id, list emailRow re-fetch by row.customer_id no stale closure, InvoiceDetail invoice.customer_id) + SERVER backstop (send-email re-fetches customers.email by customer_id, 403 on mismatch); NEVER sends on missing/blank email (guard throws before send, null/empty/whitespace; server also rejects); no double-send (idempotency key + Edge dedup + disable); gate loosening SCOPED to invoice_type='field_application' non-terminal only, all routes admin/sales_rep, Edge allows invoice type for those roles; money-correct PDF (write-off carried). Verified with NO real send (intercepted payload; local send-email 503). Re-run Codex at reset: emailService + FieldApplicationInvoice + FieldInvoices + InvoiceDetail.",
      "migration": null,
      "verified": "Spot-check: HEAD a56c4604, tree clean, typecheck PASS, no migration. Email review CLEAN. Agent proved recipient resolves to right customer email + correct PDF attached + honest no-email error (zero sends) + NO real send, in-app vs local.",
      "parkedLow": [
        "[#31 Low / pre-existing, same as #22] buildEmailHtml doesn't HTML-escape interpolated invoice number/date (DB-sourced; same path as existing InvoiceDetail/list handlers). Optional: escapeHtml() across the shared email layer."
      ],
      "openQuestions": []
    },
    "32": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "58b2682 + fix db592af",
      "criteria": "6/6",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Fuel surcharge built OFF by default (app_settings.fuel_surcharge={enabled:false,basis:'',rate:''} = INERT, NO formula invented — confirmed). Money-lens review: default-OFF inertness/money math/RLS/grants/strict-actor/idempotency/per_acre+flat all CORRECT; found 3 MED + 1 Low — ALL FIXED in db592af (migration 20260625141000) + re-verified: MED1 preview-vs-save percent divergence (preview's whole-record `v_app_service IS NOT NULL` = FALSE on null vehicle/created_by dropped the app-fee -> understated percent base) -> preview now uses presence + is_active guard matching save (PROVEN preview total===save total=193200c); MED2 unguarded setting_value::jsonb cast aborted entire save/preview on malformed config -> wrapped EXCEPTION->RETURN 0; MED3 PDF printed flat surcharge as '$200/AC x 1.00 AC' -> isFuelSurchargeLine() render branch prints plain amount (both layouts); Low IMMUTABLE doc comment -> STABLE. ORCHESTRATOR RE-VERIFIED: save+preview 1 overload each, #24 CASE-gate + #25 warehouse PRESERVED after the recreation, no old migration touched. Re-run Codex at reset: both fuel migrations + invoicePdf + fuelSurcharge + SettingsPage.",
      "migration": "20260625140000_fuel_surcharge_setting.sql + 20260625141000_fuel_surcharge_fixes.sql",
      "verified": "Re-verified: HEAD db592af, tree clean, typecheck PASS, save+preview 1 overload each + #24/#25 PRESERVED, default OFF/blank, compute anon revoked. Fix smoke 3/3 (preview==save percent, malformed-config->0+save-ok, default-off inert) + original P1-P4. 58 tests.",
      "parkedLow": [],
      "openQuestions": [
        "FUEL-SURCHARGE FORMULA (money rule — Mason must provide): the surcharge mechanism is built and OFF by default with a BLANK rate/basis (no rule invented). To ever apply a fuel surcharge, Mason enters the rate + basis (per-acre / percent / flat) in Settings -> Fuel Surcharge. Bring this to him at the production gate."
      ]
    },
    "33": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "5a41af6 + fix dd53598",
      "criteria": "5/5",
      "codex": "pending",
      "codexDetail": "First build agent's connection dropped after the DB part; completion agent finished frontend + committed 5a41af6 (migration 20260625150000 + 7 frontend files). Discount Earned is OWNER-DRIVEN + DISPLAY-ONLY (NOT in GENERATED balance_cents — proven $50 discount left $1500 balance; no formula invented). save_field_app_invoice UNTOUCHED (separate RPC update_field_app_invoice_billing, admin/sales gated, actor-bound, editable-only, idempotent, anon-revoked). Money-lens review = security CLEAN; 2 MED (silent, no money corruption): (1) per-customer discount editor DEAD on a NEW invoice (customerToInvoiceId={} -> handleDiscountChange early-returns -> keystrokes dropped in create->Preview->discount->Save flow); (2) discount+payment_terms don't print/email from the 3 LIST pages (toPdfRow/fieldInvoiceList omit the cols). Frontend-only fix in flight (key discount draft by customer_id + translate at save; carry cols into list PDF path). 3 Low parked. RE-VERIFY new-invoice-discount-persists + list-print before BUILT.",
      "migration": "20260625150000_invoice_header_footer_discount.sql",
      "verified": "Re-verified: HEAD dd535989, tree clean, typecheck PASS, save_field_app_invoice untouched (separate billing RPC anon-revoked). Build proven in-app (fields persist+print both formats, internal memo absent, posted read-only, discount display-only). 2 Med FIXED in dd53598 + proven live: new split invoice discount persists ($25->Green Acres invoice 2500, Sunrise 0, balances unchanged); list-print re-fetches discount/terms by id (2500 + 'Net 15...'). 1657 tests.",
      "parkedLow": [
        "[#33 Low] billing idempotency key cached under id||'__new__'; on a new invoice whose billing RPC fails after row creation, retry uses a fresh key bypassing dedup (benign — RPC naturally idempotent, UPDATE to fixed values). Optional: migrate '__new__' cache to new id.",
        "[#33 Low / pre-existing policy] update_field_app_invoice_billing gates (admin OR sales_rep) with no per-row ownership -> horizontal write within sales-rep cohort on non-PII draft billing metadata (matches shipped companion update_field_app_applied_info). Policy call; if isolation wanted add AND(is_admin() OR created_by=actor OR salesman_id=actor) to BOTH.",
        "[#33 Low / systemic] idempotency_keys UNIQUE(idempotency_key) alone (not (key,operation)) — unreachable (op-prefixed UUID keys never collide). Cross-cutting, out of scope."
      ],
      "openQuestions": []
    },
    "34": {
      "status": "BUILT",
      "date": "2026-06-25",
      "commit": "0bdaf5b",
      "criteria": "5/5",
      "codex": "pending",
      "codexDetail": "Codex unavailable. Read-only combined statement (unposted chem-sales + field-app), built client-side (no migration/RPC/mutation). Money review RECONCILE CONFIRMED: combinedBalance === exact integer sum of per-type subtotal balances (no float; addSummaryTotals field-by-field integer add); each row balance = live GENERATED balance_cents (total-paid-prepay-write_off); discount_earned_cents correctly EXCLUDED (display-only); PDF==screen (same buildCustomerInvoiceSummary object, no recompute); type partitioning (chem vs field_application) + unposted-scope (.in draft/unposted, .is deleted_at null) correct; split = 1 invoice row per customer (no double-count). Reviewer's 'MED phantom column' was a FALSE ALARM (it mis-read stale schema_migrations; orchestrator confirmed discount_earned_cents EXISTS on local + query runs + build agent rendered report in-app). Re-run Codex at reset: customerInvoiceSummary + invoiceSummaryPdf + CustomerInvoiceSummary.",
      "migration": null,
      "verified": "Spot-check: HEAD 0bdaf5b, tree clean, typecheck PASS, no migration. Orchestrator confirmed local query runs (all cols incl discount_earned_cents selectable). Money review reconcile CLEAN. Agent proved combined $1430=$430+$1000, posted excluded, discount display-only, PDF content-stream parse reconciles, in-app.",
      "parkedLow": [
        "[#34 Low / pre-existing app-wide] non-admin sales_rep sees only invoices they created/are salesman for (invoices_select RLS) -> a rep viewing a customer with other reps' invoices gets an understated combined balance (NOT a leak — RLS only restricts; identical to existing FieldInvoices.tsx list). Treat the summary as admin-facing or route through a SECURITY DEFINER RPC if reps need complete statements."
      ],
      "openQuestions": []
    },
    "35": {
      "status": "BUILT",
      "date": "2026-06-26",
      "commit": "390245a",
      "criteria": "7/7",
      "codex": "done",
      "codexDetail": "Real codex-review ran 3 rounds: R1 P1 (500-row cap ordered ASC hid recent jobs) + P2 (Add New Job wrong route) + P2 (map inits while hidden, no resize); R2 P2 (filters ran AFTER the 500-cap → false-empty; fixed by pushing status/applicator/date server-side before the cap) + P3 (soft-deleted recipes listed → filtered deleted_at IS NULL); R3 P1 (the redesign broke the old DispatchBoard.test.tsx → rewrote the suite). Final 'no discrete actionable bugs'. Multi-lens review CLEAN (0 High/0 Med; security: the one new read blend_recipes is RLS-scoped admin/sales/applicator + anon denied; the assign RPC self-gates; no injection/XSS; data: no schema change, numeric serialization verified so formatAppliedOfTotal never silently zeros).",
      "migration": null,
      "verified": "Orchestrator spot-check: ledger intact (34), HEAD 390245a, typecheck PASS. Build agent proved live vs LOCAL as admin: dark field mode renders; left rail shows Jobs (Map/List) + Dispatch (Dispatch Jobs/Dispatched List) groups; X-of-Y acres match seeds exactly (FJOB-002 '55 of 40 ac', PUSE-IP '40 of 100 ac', FJOB-004 '0 of 80 ac'); status badges scheduled→PENDING/in_progress→ACTIVE/completed→DONE/invoiced→BILLED/cancelled→CANCELLED across 20 jobs; 'Assigned To: Bob Applicator' on FJOB-002 (real name via profile_public_view, not '(removed)'), unassigned show none; OPTIONS menu all 5 entries; server-side status filter → 3 ACTIVE (filters pushed before the 500-cap); map/list toggle keeps filter (search PUSE/CSUM persists across toggle). 31 tests.",
      "parkedLow": [
        "[#35 Low / observability] DispatchBoard.tsx:151 only checks jobsRes.error; applicatorsRes/recipesRes errors are silently swallowed → a failed applicators read empties applicatorNameMap so assigned jobs silently lose their 'Assigned To: <name>' label (a failed recipes read empties the recipe picker), no toast. Graceful degradation but a dispatcher could misread assigned jobs as unassigned. Fix: capture applicatorsRes.error/recipesRes.error to Sentry after Promise.all (mirror the best-effort fields-fetch pattern at 181-186).",
        "[#35 Low / unlikely race] DispatchBoard.tsx:227-231 map-resize nudge effect deps only [activeView], early-returns when mapRef.current null with no retry; if Mapbox onMapLoad (493, only sets mapRef.current, no resize) fires AFTER the first switch to map view, the first open can render mis-sized until a window resize. Unlikely (Mapbox usually loads while hidden so the ref is set first). Fix: onMapLoad also resize when activeView==='map', or add a mapLoaded state to the effect deps."
      ],
      "openQuestions": []
    },
    "36": {
      "status": "BUILT",
      "date": "2026-06-26",
      "commit": "6c74e5d + fix 5188a69",
      "criteria": "7/7",
      "codex": "done",
      "codexDetail": "Build (6c74e5d): real codex-review ran 12 ROUNDS to convergence (the heavily-guarded RPC + RLS); final CLEAN; R12 PARKED a cross-cutting P1 (should per-location/crew assignees be allowed to start_job/complete_job? — out of #36 scope, EXISTING-RPC change; spawned follow-up task_ca5cb456 → see openQuestions). Multi-lens review: Security CLEAN + Data CLEAN (RLS leaks nothing across per-location applicator/crew/unrelated/whole-job/admin; cancelled revokes visibility; writes RPC-only; additive migration correct FKs/XOR/UNIQUE), found 1 Med (DispatchBoard applicator filter built an UNBOUNDED id.not.in.(every dispatched job) → URL length time-bomb hard-fails the board over seasons) + 2 Low → fix 5188a69. Fix: NEW SECDEF RPC get_dispatch_board_jobs (resolves applicator match IN SQL, RETURNS shaped RLS-gated rows, no id-list in URL ever); Codex 4 rounds (caught field-user dispatch-row visibility, unordered/uncapped, whole-job-applicator visibility omitted, and a SECDEF customer/field NAME read bypassing customers_select/fields_select → fixed with RLS-equivalent CASE gates) → CLEAN; both Low fixed (dead job-level assign code deleted, silent filter-degradation now toasts + skips partial). Fix re-review CLEAN (1 Low crew-is_active embed drift parked).",
      "migration": "20260626120000_job_location_dispatches.sql + 20260626130000_get_dispatch_board_jobs.sql",
      "verified": "Orchestrator spot-check: ledger 35, HEAD 5188a69, typecheck PASS; job_location_dispatches RLS (SELECT-only, writes RPC-only), dispatch_job_locations + _is_dispatched_to_me + get_dispatch_board_jobs all SECDEF+search_path+1 overload+anon-revoked; 3 additive dispatched SELECT policies on jobs/job_fields/customers via _is_dispatched_to_me. Build agent proved E2E live (admin): wizard 3 steps → 2 SPLITBILL-1 locations → loc A→Bob, loc B→Carl → Finish → 2 DB rows, job row aggregated 'Bob Applicator, Carl Applicator', re-dispatch upserts (no dup); 20-step rolled-back security smoke PASS (XOR/role/actor/lifecycle/license/soft-delete/duplicate gates, RPC-only writes, crew+applicator RLS reads, cancelled/deactivated visibility revoke, FK cascade, audit). Fix proven: split job under Bob's filter not Carl's stale; boundedness shown (flooded dispatches 1→13, Bob result stayed 1); new RPC does NOT over-expose (Sam sales_rep → farm_name NULL until assigned a customer). 2833-test suite + 56 fix tests pass; final multi-lens CLEAN (RPC faithfully mirrors every live RLS predicate, deactivated caller 0 rows).",
      "parkedLow": [
        "[#36-fix Low / defense-in-depth, unreachable today] get_dispatch_board_jobs migration (~174-184, _dispatches crew branch) omits the standalone caller is_active re-check that the live job_location_dispatches_select policy has. NOT exploitable — the outer visibility gate (lines 97-102) already enforces is_active (verified live: deactivated user gets 0 rows). Optional hardening: add `AND EXISTS (SELECT 1 FROM profiles p WHERE p.id=auth.uid() AND p.is_active)` to the crew branch to keep the embed a faithful policy mirror."
      ],
      "openQuestions": [
        "DISPATCH LIFECYCLE-AUTH (gates #38 mobile; Codex-flagged in #36 build R12, follow-up task_ca5cb456): today ONLY the whole-job applicator (jobs.applicator_id) can start_job/complete_job. With per-location dispatch, an applicator assigned only a LOCATION (or a crew member) cannot start/complete the job they were dispatched to. SHOULD a dispatched location/crew assignee be authorized to start/complete the job (or its locations)? This is a deliberate lifecycle-authorization policy decision (would change the EXISTING start_job/complete_job RPCs — out of #36's added-surface scope). #38 (mobile applicator field view) needs an answer: a dispatched applicator on mobile must be able to DO the work. Recommend deciding before/at #38."
      ]
    }
  },
  "carryForward": [
    "#22 → PHASE-3 INVOICING MAP: Field-app invoice pages = src/pages/FieldInvoices.tsx (combined list) + FieldInvoicesUnposted.tsx (new unposted tray). TWO editors: FieldApplicationInvoice.tsx (engine-built per-acre, /invoices/field-app/:id) + generic InvoiceDetail (/field-invoices/:id). /field-invoices/* auto-maps to 'field-invoices' permission. #23 POSTED list: REUSE src/lib/fieldInvoiceList.ts wholesale (status-agnostic mapFieldInvoiceRow/applyFieldInvoiceFilters/computeFieldInvoiceTotals/FieldInvoiceListFilters); query .in('status',['posted','overdue','paid']); the LIST_SELECT PostgREST join is the canonical field-invoice list query (customer + job:jobs!invoices_job_id_fkey(job_number) + field_app_locations + invoice_items). RPCs that EXIST: post_invoice(p_invoice_id,p_idempotency_key) returns VOID -> call with .throwOnError() NOT '=' capture (assertRpcCoverage gate); post_invoice_group(uuid,uuid,text); save_field_app_invoice; preview_field_app_invoice_split; transfer_job_to_invoice (snapshots field_names/crop_type/total_acres/applicator_name at invoice level + writes invoice_items). **NO unpost_invoice RPC EXISTS -> #28's unpost half needs a NEW migration.** Shared print: buildInvoicePdfDataFromRow(row,options) in src/lib/invoicePdf.ts -> reuse for #30. LOCAL SEED gotcha: invoices has enforce_invoice_draft_on_insert (forces status=draft) + enforce_invoice_status_transition; to seed non-draft statuses locally wrap in SET session_replication_role='replica' (throwaway DB only). Money = bigint cents; /100 ONLY for display (toLocaleString), never for math.",
    "#31 → #40/#41 (customer pre/post notifications): send-email Edge Function gates email_type BY ROLE (drivers only delivery_completed; admin/sales other types). #40/#41 need a NEW email_type (pre_application_notice/post_application_notice) added to the EmailType union in src/lib/emailService.ts AND allowed in the Edge Function — else the send is rejected. NOTE: deploying the edge function NEEDS MASON APPROVAL (edge-function deploy is gated) — #40 can PREPARE the edge change but it can't be deployed during the run; handle at the production gate. RECIPIENT resolution for NOTIFICATIONS DIFFERS from invoice email: invoice email = single billing customer (invoices.customer_id); a JOB spans MULTIPLE customers via per-field shares -> use job_field_shares(job_id,field_id,customer_id,split_pct) (#1)/field_billing_defaults to resolve EACH customer then customers.email per recipient. The Notifications tab currently shows only VIEWER-facing notices (RLS user_id=auth.uid()), NOT customer-facing -> #41's 'addressed to the customer' post-notification log does NOT exist yet (needs its own table/log). REUSABLE: buildInvoiceEmailPayload (recipient missing-email guard throws BEFORE send + payload shape) in emailService.ts; runCriticalAction-wrapped send; idempotency key <op>-<entityId>-<Date.now()>; logActivity({event:'invoice_emailed'}) send record.",
    "#30 → #31/#33/#34: PDF print = src/lib/invoicePdf.ts; generateInvoicePdf(data) honors data.options.format ('current'|'legacy', default current); generateLegacyInvoicePdf is the ChemMan 'Old Print' courier/monochrome layout. #31 email: thread format:'legacy' through InvoicePdfData.options (emailRow + InvoiceDetail handleEmailInvoice already use generateInvoicePdf); money reconciles identically in the attachment. #33: the legacy layout ALREADY prints purchase_order_ref/due_date/payment_terms/header_notes/footer_notes from InvoicePdfData (meta + notes blocks) -> when #33 adds inputs populating those, BOTH formats pick them up automatically; the legacy share block has a pricing_note slot but no Discount-Earned column yet (#33's $0.00 placeholder). #34 customer summary: reuse buildInvoicePdfDataFromRow + the format flag; legacy style is a ready template. Money-correct buildInvoicePdfDataFromRow carries paid/prepay/write_off so Total-Payments-Prepay=Balance in both formats; splits sum via deriveSplitRef labels (<base>-<NNN>). Legacy generator dynamic-imports customerSplit.deriveSplitRef (no circular import).",
    "#28 → #29/#30: #29 Void — void_invoice routes draft/unposted->cancelled, posted->voided (Void on InvoiceDetail, posted-only). On the per-acre FieldApplicationInvoice editor Void is NOT yet exposed -> add gated on posted (admin), mirroring how #28 added Unpost (state + ConfirmModal + group-aware loop). A posted invoice can now be Unposted (->editable) OR Voided (terminal) — both buttons coexist. #30 Print — per-acre editor handlePrint reuses buildInvoicePdfDataFromRow (wired); InvoiceDetail uses showPrintDialog; LEGACY format -> add to src/lib/invoicePdf.ts. REUSABLE: the per-invoice idempotency-key REF-CACHE pattern (unpostKeysRef/postKeysRef) is the ESLint-clean way to do BULK RPC loops (the useIdempotencyKey single-key hook trips local-rules/idempotency-key-from-hook inside a loop). unpost_invoice (migration 20260625130000) is a clean model for any future guarded status-reversal RPC (money-guard refuses allocation/prepay/write-off, period-open respected, transaction-scoped app.admin_override, append-only audit, idempotent, anon revoked).",
    "PRODUCTION GATE ORDERING (from #34 review): at promotion, APPLY ALL fieldapp-parity migrations (in timestamp order) to PROD *before* deploying the branch code. Several frontend queries SELECT columns added by these migrations (e.g. #33 FieldApplicationInvoice.tsx:534 + #34 CustomerInvoiceSummary select discount_earned_cents/payment_terms; job sections select the new job/job_fields/job_chemicals cols; as-applied selects job_applied_records cols) — deploying code ahead of its migration would 400. Migration filename timestamps already encode the correct order. (On LOCAL these were applied via direct psql so supabase_migrations.schema_migrations shows a stale 'latest' version — the COLUMNS/objects are all present; don't trust schema_migrations for what's applied locally.)",
    "DOC-SYNC AT GATE (loop-wide): migration-history.md + CLAUDE.md counts are behind by ~15+ migrations (ALL of #1-#28's new migrations are unindexed) — build agents correctly DON'T touch these out-of-section docs (would conflict). Reconcile ALL counts (migrations/pages/RPCs/tables) via /update-docs + regenerate-* scripts at the END before the production gate.",
    "#26 → #30/#33: per-customer split UI = src/components/field-app/CustomerSharesTable.tsx + pure helpers in customerSplit.ts (deriveSplitRef(base,idx,isSplit) renders ChemMan <base>-<NNN> display label [jobNumber if transferred else group primary invoice number]; invoice_number itself UNTOUCHED; reconcileSplit/customerShareAcres/customerSharePct pure+tested). The penny-exact engine (save_field_app_invoice per-customer loop + derive_customer_shares_from_fields + invoice_shares) is PRE-EXISTING/unchanged — proven penny-exact on ODD totals (1501c 1/3-2/3 -> 500+1001). 'Customer Shares By' basis toggle (Locations vs Share%) is PRESENTATION-ONLY (dollars unchanged). #30 Print: reuse deriveSplitRef for the printed <jobnumber>-<NNN> label. Discount Earned column is an honest $0.00 PLACEHOLDER pending #33 (no money rule invented; doesn't feed totals). smoke-specs.json now has a save_field_app_invoice spec (covers preview_field_app_invoice_split) — run at the Codex/prod gate.",
    "#27 → #26/#28/#29/#30/#31 (SPINE ROUTING — corrects an earlier #24 note): a TRANSFERRED field invoice has job_id but NO field_app_locations rows (transfer_job_to_invoice writes field_names[] + invoice_items, NOT field_app_locations). Per the #24 discriminator it therefore opens in the GENERIC InvoiceDetail (/field-invoices/:id, routeArea='field'), NOT the per-acre FieldApplicationInvoice. So ANY toolbar action #28 (post/unpost), #29 (void), #30 (print), #31 (email) wants on TRANSFERRED invoices must live in InvoiceDetail.tsx (Post/Void/Print/Email/RecordPayment already there), gated on invoice_type==='field_application'. (Engine-built per-acre invoices WITH field_app_locations still open in FieldApplicationInvoice.) Multi-customer transfer = grouped invoice with per-customer invoice_shares (penny-exact, share-sum==header); separate-invoices-per-customer is #26. #28 unpost: NO unpost_invoice RPC exists -> needs a NEW migration; the new reverse RPC transfer_invoice_to_job(p_invoice_id,p_performed_by,p_idempotency_key) is a clean model (guarded, admin_override SET LOCAL hatch for invoiced->completed, audited both ways, idempotent FOR UPDATE, refuses posted/paid). #29 void: void_invoice already routes draft/unposted->cancelled, posted->voided (Void button on InvoiceDetail, posted-only).",
    "#25 → #26/#27/#32: Field-app invoice chemical LINE model — lines persist to invoice_items with quantity (=rate*acres per customer), unit_price_cents, extended_cents (bigint via safe_cents_qty) + warehouse/vendor/total_applied/total_applied_unit/total_applied_gl_lb/gl_lb_unit/epa_registration/product_form. save_field_app_invoice has TWO write branches: grower-share/OVERRIDE (qty_a, revenue $0) and PRICED (qty_b) — EXTEND BOTH if adding a line attribute. Tier per-acre price auto-fills from product price book at customer tier (override allowed). #26 auto-split: the per-customer chemical INSERT ALREADY loops v_customers + prices each customer's share independently (cents engine per customer done) — remaining work is split numbering/'Customer Shares By' basis UI/RPC. #27 transfer: transfer_job_to_invoice already writes total_applied/gl_lb/epa/product_form via convert_to_gl_lb but NOT warehouse/vendor — source from job_chemicals.warehouse/.vendor in its FOR v_chem loop if wanted. #32 fuel surcharge: add as a new informational/priced line OR invoice-level field, OFF by default + blank formula; the is_application_fee + sort_order=9999 app-service-fee pattern is the model for a non-product surcharge line. readOnly/canEdit posted-lock prop on FieldAppChemicalEntry is reusable.",
    "#24 → #25/#27/#29/#33/#16: Field-app invoice EDITOR = src/pages/FieldApplicationInvoice.tsx, now 5 tabs (Locations, Chemical/Charges, Customers, Applied Info, Notifications). ROUTING DISCRIMINATOR (load-bearing): the per-acre editor keeps any field invoice that HAS field_app_locations (count keyed on invoice_id); only bounces a field invoice with NO locations + job_id/blend_ticket_id to generic InvoiceDetail. A TRANSFERRED invoice (job_id + locations) stays in THIS editor. If #27 produces SPLIT transfers keyed by invoice_group_id (not invoice_id), EXTEND the discriminator count query to also check the group. Editor exposes canEdit/isLocked; posted=LOCKED (agent fixed location-acres + Change-Locations holes). #25: FieldAppChemicalEntry has NO disabled/readOnly prop -> posted-lock hole on chemical lines (Save hidden + RPC status-guard block the write, but UI stays interactive) -> #25 must add readOnly/disabled driven by canEdit. Consultant = invoices.salesman_id (NO separate consultant_id), admin-editable only (RPC rejects non-admin attributing to another; non-admin always sends own salesman_id). #27 'Transfer to Scheduling' button = honest info-toast placeholder (no mutation, gated to job_id+editable) -> replace with the real reverse-transfer RPC. #33: pdfSnapshot already carries due_date/purchase_order_ref/footer_notes; header currently exposes only header_notes -> #33 adds PO ref/footer notes/due-date/terms inputs to the header card + save payload + pdfSnapshot. Reusable pure/tested: src/components/field-app/invoiceAppliedCarryover.ts (carryoverRowsFromRecords/applicatorNameFor/formatNetTach); APPLIED_RECORD_SELECT mirrors AppliedRecordsManager read shape.",
    "#23 → #28/#24/#30: #28 post/unpost — NO unpost_invoice RPC exists (needs a NEW migration in #28). The Posted page's 'Unpost All' button is wired to an INERT placeholder (unpostAll()->info toast, no mutation, verified no DB change); when #28 lands the RPC, replace the handler with real bulk unpost (per-invoice generateIdempotencyKey('unpost_invoice',...), .throwOnError()/assertRpcResult, ConfirmModal, then fetchInvoices). #24 editor — posted list opens rows at /field-invoices/:id (generic InvoiceDetail = read-only/locked path); #24 ensures that editor enforces the posted lifecycle + surfaces the posted-records warning. #30 print — posted list reuses buildInvoicePdfDataFromRow(toPdfRow(row)) (money-correct, paid/prepay carried). ChemMan 'Old Print All' LEGACY format is NOT built — that belongs to #30 (add legacy format to invoicePdf.ts). Reusable pure+tested batch-scope helpers in fieldInvoiceList.ts: deriveMonthBatches/applyPostedScope/spansMultipleBatches/batchMonthOf/currentBatchMonth (batch key = calendar month of invoice_date, same key close_accounting_period uses).",
    "LESSON (all sections rendering a person's name): NEVER resolve applicator/user/created-by names via a direct PostgREST `profiles` embed — the profiles SELECT RLS is `is_admin() OR id=auth.uid()`, so the embed returns NULL for sales_reps/applicators and the name renders '(removed)'. Use profile_public_view (or a prop/label-map sourced from it) for names. Caught as a Med in #17. Similarly, when an edit-modal <select> is populated from an active-only list (vehicles status='active', active applicators), INJECT the record's current value as an extra <option> if it's not in the active list, or editing silently nulls it.",
    "#1 → #26 auto-split: per-field per-customer shares persist in job_field_shares(job_id, field_id, customer_id, split_pct, is_primary), defaulted from field_billing_defaults, validated to 100% per field. save_job rejects shares for fields not on the job. Canonical split engine to reuse: derive_customer_shares_from_fields(uuid[], jsonb).",
    "#1 → #10/#18 remaining-acres: jobs.applied_acres (default 0) + GENERATED jobs.remaining_acres = GREATEST(total_acres - applied_acres, 0). As-applied work UPDATEs applied_acres; NEVER write remaining_acres directly.",
    "#1 new columns: jobs gained call_date,date_proposed,time_proposed,schedule_date,date_expires,consultant_id,loader_comment,additional_info,internal_memo(NOT printed - keep off customer PDFs),updated_by,printed_at(stamped by WPS-notice action). job_fields gained planted_acres,crop,strip,pests. job_chemicals gained diluent_rate,rei_hours,phi_days,warehouse(free text - products has no warehouse col),vendor.",
    "#1 save_job signature unchanged (6 args, ONE overload). New data rides in p_job_payload (field_shares[], scheduling, memos) + p_fields/p_chemicals arrays. Don't add an overload.",
    "#1 reusable: tabbed-editor pattern + snapshot-based dirty tracking in JobDetail.tsx; toGallonOrLbEquivalent + converter in src/lib/chemCalculator.ts.",
    "ENV (local only, not code): the throwaway local DB needed the standard GRANT SELECT/INSERT/UPDATE/DELETE TO authenticated/anon on public tables (applied to local; live already has them). If a later section's local app can't load any page, that's the fix.",
    "#4 → #6 filter set: job-tag filter state = useState<string[]>([]) of job_tags.id; pure predicate jobMatchesTagFilter(jobTagIds, selectedTagIds) in src/components/jobs/jobTagFilter.ts (empty=passes all, OR within tags). It's applied CLIENT-SIDE in a visibleJobs=useMemo() that drives DataTable+totals+useRowSelection. Status/customer/date are SERVER-side query filters; tags ride the joined query. #6 must FOLD into this same visibleJobs pipeline, not rebuild it. assignmentsByJob: Map<jobId,Set<tagId>> already built on the page.",
    "#5 → #9/#16: job_fields.sort_order is the SINGLE authoritative applicator route order (0..n, contiguous, renumbered on every save). Read fields ORDER BY sort_order (PostgREST doesn't guarantee embedded-row order). Map pin # = sort_order+1 = 'Stop N'.",
    "#6 → #3/#7/#8: Jobs filter is now ONE JobFilters object (draft edited, applied drives query+memo) feeding the SAME visibleJobs memo that drives DataTable + bottom totals + useRowSelection. Extend there; don't add a parallel filter. selectedRows (useRowSelection) operates over visibleJobs, so #3/#7 bulk actions get the filtered+selected set for free. REUSE src/components/jobs/MultiSelectDropdown.tsx (generic {value,label,color?}). Reference lists already on the page: customers/applicators/vehicles/appServices/crews/allTags.",
    "#6 → #7: status multi-select uses canonical list scheduled/in_progress/completed/invoiced/cancelled — keep mass-edit status a SUPERSET of the live CHECK.",
    "#6 → #13/#21: ground crews now EXIST (ground_crews + ground_crew_members tables w/ RLS, jobs.ground_crew_id). #6 owns the crew CATALOG + job-level crew (persisted via a direct .update() in JobDetail, NOT save_job — the 6-arg save_job contract is frozen; if a future section puts crew in the save_job payload, extend the RPC body too). #13/#21 should attach crew/members to the APPLIED-INFO record and reuse GroundCrewsManager — do NOT recreate the tables.",
    "#3 → #7/#8: job batches = job_batches table (RLS) + jobs.batch_ref FK (ON DELETE SET NULL, one-batch-per-job, distinct from many-to-many tags). Batch filter = JobFilters.batchIds (SERVER-side on batch_ref); batchOptions/batchByJob Map/batchMemberCounts already on the Jobs page. #7 mass-edit can 'Set batch' via the same direct .update() (admin/sales gated) — reuse JobBatchAssignModal's single-choice pattern. #8 columns: a sortable 'Batch' column (key 'batch_name') is in dataColumns — include it in the per-column on/off toggle set.",
    "#7 → #10: wire the loader-worksheet bulk option at the '#10 INSERTION POINT' comment in src/components/jobs/JobMassEditModal.tsx; add the opt-in flag(s) to MassEditDraft in jobMassEdit.ts and include the column(s) in buildFieldPatch() (clean patch object — drops in without touching date/status/applicator/batch).",
    "#7 → any bulk-status section: reuse partitionByStatusLegality() + allowedSourceStatuses() in src/components/jobs/jobMassEdit.ts (they mirror _enforce_job_status_transition). FORWARD steps (in_progress/completed/invoiced) MUST go through start_job/complete_job/transfer_job_to_invoice (they create application records/inventory moves/invoices) — never a raw status update. Bulk mass-edit only applies 'cancelled'.",
    "#8 → all later list work: the Jobs list now renders via a per-user VISIBLE set (orderedVisibleColumnIds, registry order). Any new job-list column must be added to JOB_COLUMN_REGISTRY in src/components/jobs/jobListColumns.ts (id,label,shared,defaultVisible) AND to dataColumns in Jobs.tsx, or it won't appear. user_list_settings is a GENERIC per-user table keyed (user_id, list_key) with RLS auth.uid()=user_id — REUSE it for the Phase-3 invoice lists (add a list_key or extend the shared map; isInvoiceColumnVisible already shares the 'jobs' list_key for 3 shared columns). showCompleted=false hides completed jobs UNLESS the status filter explicitly includes 'completed'.",
    "#21 → PHASE-3 + #27/#16 (AS-APPLIED RECORD NOW COMPLETE): job_applied_records carries per entry — applicator_id(->profiles), vehicle_id(->vehicles), application_date, per-location acres (child job_applied_record_fields = authoritative source of jobs.applied_acres/remaining_acres via #18 trigger), start+end weather (12 cols), tach (beginning_tach/end_tach/GENERATED net_tach), ground crew (child job_applied_record_crew, member_id FK SET NULL + member_name_snapshot/crew_name_snapshot to preserve the legal record). READ crew via job_applied_record_crew + resolve name with crewMemberDisplayName() (live name, snapshot fallback) — do NOT join profiles (ground_crew_members SELECT RLS USING(true) is all-role readable). recordHasCrewMember(rec,memberId) is the pure crew-member filter predicate (idx_jarc_member backs server-side). Do NOT put crew/weather/tach in save_job (frozen 6-arg); all are children/columns of the applied record managed by AppliedRecordsManager (delete-then-insert like #18). jobs.ground_crew_id (#6) is a separate coarser job-level grain.",
    "#19 → #20/#21/#27/#16: start+end WEATHER lives on job_applied_records.start_*/end_* columns (12 cols; source flag 'auto'|'manual'; 3 CHECK constraints). #27 applicator sheet + #16 invoice editor read those for the weather block. Reusable: summarizeWeatherSet(), computeTotalMinutes/formatTotalTime in src/components/jobs/appliedRecords.ts (pure); fetchWeatherForDateTime(lat,lng,dateISO,timeHHMM) in src/lib/weatherCapture.ts (date-keyed Open-Meteo pull, free provider only). #20 tach: add beginning_tach/end_tach/net_tach columns to job_applied_records the same additive way (extend AppliedRecordDraft + buildAppliedRecordPatch + JobAppliedRecord type + modal slot + list col; does NOT affect #18 rollup). Legacy job_applied_info 'Completion Weather Snapshot' stays separate — don't conflate.",
    "#18 → #19/#20/#21: jobs.applied_acres is now OWNED by trigger recompute_job_applied_acres(job_id) — fires AFTER INS/UPD/DEL on job_applied_record_fields + AFTER DELETE on job_applied_records; sums child rows -> jobs.applied_acres -> GENERATED jobs.remaining_acres. NEVER write jobs.applied_acres or remaining_acres directly. Adding weather/tach (#19/#20) COLUMNS to job_applied_records does NOT affect the rollup (it reads only job_applied_record_fields). #21 crew goes on a NEW link table — leave the field-acres child untouched. AppliedRecordsManager save uses delete-then-insert for child rows; the cleanup-delete is ERROR-ONLY (not checkMutationResult — 0 rows is legitimate on a fresh record). #19/#20 fields ride the parent patch via buildAppliedRecordPatch + AppliedRecordDraft (extend both; modal has slots above Notes). Per-location helpers (computeRemainingAcres, sumDraftFieldAcres) live in appliedRecords.ts — pure/tested.",
    "#17 → #18/#19/#20/#21 (PHASE-2 ANCHOR): table job_applied_records (PK id uuid), one job→MANY records (no UNIQUE on job_id, ON DELETE CASCADE from jobs). Shape: id, job_id(FK jobs CASCADE), applicator_id(FK profiles SET NULL), vehicle_id(FK vehicles SET NULL), application_date(date NOT NULL), applied_acres(numeric>=0 optional), notes, created_by, created_at, updated_at. RLS = job-scoped (admin/sales_rep OR applicator-on-own-job) all 4 verbs (live-proven). UI lives in src/components/jobs/AppliedRecordsManager.tsx (add/edit modal) + helpers in appliedRecords.ts (AppliedRecordDraft / buildAppliedRecordPatch — extend these). NEXT: #18 add child job_applied_record_fields(application_record_id->CASCADE, field_id, applied_acres) + sum records into jobs.applied_acres (feeds GENERATED jobs.remaining_acres — NEVER write remaining_acres directly). #19 add start_*/end_* weather cols on job_applied_records (Open-Meteo weatherCapture.ts). #20 add beginning_tach/end_tach/net_tach on job_applied_records. #21 add link job_applied_record_crew(application_record_id, member_id->ground_crew_members.id) — REUSE #6 ground_crews/ground_crew_members catalog. LEAVE the legacy 1:1 job_applied_info (complete_job weather snapshot, now a separate 'Completion Weather Snapshot' card) UNTOUCHED — don't conflate.",
    "#13 → #14 + ANY acre-scaling/projection: use the gatherer's FIELD-ACRE total (ApplicatorSheetData.total_acres = Σ job_fields.acres_to_treat) as the AUTHORITATIVE acre basis — NEVER jobs.total_acres or the GENERATED jobs.remaining_acres. (remaining_acres = GREATEST(COALESCE(total_acres,0)−COALESCE(applied_acres,0),0) reads 0, NEVER NULL, when total_acres is NULL → silently drops real jobs from a forecast. jobs.total_acres also diverges from the field-acre total on legacy/imported/quote/blend jobs, so scaling a gatherer per-product total by remaining/jobs.total_acres is numerically wrong.) Compute 'remaining' = max(fieldTotal − jobs.applied_acres, 0), same-unit (both acres). If you split basis derivation across modules, THREAD the resolved basis through (don't re-derive downstream — re-deriving caused a P2 silent-ZERO-projection regression). Honest exclusion reasons: 'already applied' ONLY when applied>=fieldTotal; 'no acres basis — set acres' when fieldTotal=0 and applied=0; never label an untouched job 'already applied'.",
    "#9 → #10/#11/#12/#13/#14 (PHASE-4 PRINTOUT REUSE): src/lib/applicatorSheetData.ts buildApplicatorSheetData() gathers job + customers + fields(ROUTE ORDER by sort_order) + chemicals(rate/total/gal-lb via toGallonOrLbEquivalent WITH product_form per #25 fix) + REI/PHI/EPA — REUSE it for the chemical reports (ApplicatorSheetProduct[] carries epa_registration/rei/phi/total_gl_lb + product_form). Shared PDF helpers in src/lib/applicatorSheetPdf.ts: drawFooter/ensureRoom/fmtDate + the dynamic-import jsPDF/autotable pattern (copy the structure). PRINT-AUDIT stamp pattern for any job printout: best-effort .update({printed_at, last_printed_by: profile.id}) AFTER generate; the Jobs list 'Last Printed By' column resolves the name via profile_public_view (NOT a profiles embed). Custom layout config lives in app_settings.applicator_sheet_custom (admin-gated, inert default {blank header/footer/logo, all columns true}) — the model for any layout-only print setting, NO new table needed. ApplicatorSheetData.total_acres = Σ job_fields.acres_to_treat is the AUTHORITATIVE acre basis (see #13 carry-forward).",
    "#10 → #14/#7 (PHASE-4 PRINTOUT REUSE): pure split engine computeLoaderWorksheet/loaderProductTotals in src/lib/loaderWorksheet.ts + per-job fetch+aggregate in src/lib/loaderWorksheetFetch.ts (billed customers resolved job_field_shares→field_billing_defaults→primary, names via by-id lookup, gallon-unit guard isGallonCapacityUnit). PDF batch pattern generateLoaderWorksheetBatchPdf→shared drawLoaderWorksheet per page = the model for #14 Master Mix multi-job summary: for a COMBINED mix sum each product's total_quantity across the batch's jobs keyed by product name+unit. jobs.carrier_rate_gpa now available per job for spray-volume math. #7 mass-edit loader-worksheet bulk option = DONE here (carrier rate / tank capacity / loader comment wired through MassEditDraft + buildFieldPatch [coerces invalid→NULL per CHECK] + loaderInputsValid [blocks Apply on bad numbers] + describeChanges at the '#10 INSERTION POINT' in JobMassEditModal.tsx) — no further #7 work needed.",
    "#11 → #12/#9/#10 (SAFE BILLED-CUSTOMER RESOLUTION): the NEW RPC get_job_billed_customers(p_job_id) (migration 20260625180000, SECURITY DEFINER, gate mirrors jobs_select applicator predicate, dedup by customer_id, shares joined on field_id to the job's real fields) + the shared frontend wrapper resolveBilledCustomers in src/lib/billedCustomersResolver.ts are now the CANONICAL safe way to resolve a job's billed customers without the customers_select RLS dropping co-billed parties for applicators. resolveBilledCustomers returns complete=false if any billed customer fails to resolve → all report paths (chemicalApplicationReportFetch, loaderWorksheetFetch, JobDetail buildSheetDataFromState) ABORT (no PDF, no printed_at stamp) — a compliance doc is complete-or-refused, never silently partial. #12 cross-job summary MUST call resolveBilledCustomers per job (don't embed customers). REPORT ROW-FILTER CONVENTION (keep #12 identical to #9/#11 so documents never disagree): the product set is EXACTLY the shared buildApplicatorSheetData gatherer's products — NO quantity filter; a real 0 quantity is preserved consistently (JobDetail uses c.quantity.trim()===''? null : parseFloat). buildSheetDataFromState(strict) — strict=true aborts on unresolved customer (#11), strict=false labels null-farm_name as 'Customer' + falls back to primary (#9 field sheet).",
    "#12 → ALL later sections that UPDATE jobs (NEW DB GUARD): BEFORE UPDATE trigger enforce_billed_job_immutability on jobs (migration 20260625190000) makes invoiced/cancelled jobs IMMUTABLE for non-admins — blocks soft-delete (deleted_at NULL→non-NULL) + any billed-relevant column rewrite via an ALLOW-LIST (new columns protected BY DEFAULT). EXEMPT (still writable on a billed job by a non-admin): status (governed by _enforce_job_status_transition), applied_acres (recompute_job_applied_acres), printed_at/last_printed_by (print-audit stamp), updated_at/updated_by, invoice_id. Admins AND SECURITY DEFINER flows that SET LOCAL app.admin_override='true' (the _is_admin_override() hatch — e.g. transfer_invoice_to_job) bypass it. IMPLICATION: any NEW code/RPC that updates a possibly-invoiced/cancelled job as a non-admin must touch ONLY exempt columns, or run as a definer flow that sets the override hatch, or it will RAISE check_violation. If a later section adds a new jobs column that SHOULD remain non-admin-editable on a billed job, add it to the exempt allow-list in that migration. UI side: Jobs.tsx three-tier selection REPORTABLE(+invoiced, read-only reports/exports) / DELETABLE / EDITABLE(=DELETABLE, mutating bulk actions via openMutatingAction+editableSelectedIds); the soft-delete query is also .in(status,DELETABLE).",
    "#14 → #15/#16 (cross-job report family + batched side-query): the read-only report family now has 4 members (chem summary #12, projected use #13, master mix #14, plus per-job applicator/chem #9/#11). Canonical place to add a NEW read-only job-list report = the bulkActions array in Jobs.tsx (~lines 1063-1068): use selectedRows (REPORTABLE), a distinct Lucide icon, a no-selection toast, and NO printed_at stamp for planning/batch docs (#13/#14 precedent). masterMixSummaryFetch shows the pattern for #11-gatherer-per-job + ONE batched side-query (.in('id',ids)) for extra job columns the list query doesn't carry (capacity/rate/vehicle) — reuse for #16 (per-job map/logs needs extra job columns too). Aggregation keying: use a NUL ('\\u0000') delimiter for product+unit/recipe composite keys (not space/pipe) to avoid collisions. NOTE #15 (Print job list) is DIFFERENT: it prints the FILTERED/displayed list (visibleJobs), not selectedRows, with the per-user VISIBLE columns (#8 list-settings JOB_COLUMN_REGISTRY/orderedVisibleColumnIds) + the on-screen bottom totals row — a toolbar PRINT action, not a selection bulk action.",
    "#15 → #16 + any 'print exactly what's displayed' need: reportPdf.ts now supports an optional totalsRow (bold autotable foot, showFoot:'lastPage' so it isn't repeated per page) — additive, existing callers unchanged. tableSearchSort.ts (applyTableSearchSort, pure) + DataTable's NEW optional controlled search/sort props (searchValue/onSearchChange/sortState/onSortChange, with internal fallback so every existing caller is unaffected) let a page lift the in-table search+sort so print==displayed. jobListPrint.ts (jobPrintCellValue / computeJobListTotals / buildJobListPrintData) is the screen→print mapping over the per-user VISIBLE columns (#8 List Settings). #15 PRINT is a top-level toolbar action over displayedJobs (NOT a selection bulk action, NO printed_at stamp).",
    "#16 → Phase 5 (#35-41): NEW reusable RPC get_job_fields_with_geojson(p_job_id) returns a job's selected fields (geom + measured/override/total acres + boundary_geom) gated by jobs_select — REUSE for any per-job map/dispatch view instead of get_fields_with_geojson(customer_id) (which over-fetches a whole customer). PROVEN job-scoped PRIVATE-bucket policy shape (for dispatch photos/attachments if needed): bucket public=false + file_size_limit + allowed_mime_types on the bucket row; storage.objects policies gate ONLY on bucket_id + a job-visibility EXISTS predicate keyed on (storage.foldername(name))[1] compared AS TEXT to <table>.id::text (NEVER ::uuid, NEVER the deprecated `owner`); path convention '<job_id>/<uuid>_<filename>'; downloads via short-lived createSignedUrl; for a per-owner DELETE rule use owner_id::text=auth.uid()::text and keep the object-DELETE at least as strict as the catalog-row DELETE. Local real-upload proof recipe (if needed): run public.ecr.aws/supabase/storage-api on the stack's docker network with DATABASE_URL=supabase_storage_admin@<db> + the stack's JWT secret; tear down after.",
    "#35 → #36/#37/#38/#39 (DISPATCH VIEW STRUCTURE built): /dispatch is now the dark field/tablet 'Dispatch & Applicator View' (DispatchBoard.tsx rewritten) — Jobs (Map/List) + Dispatch (Dispatch Jobs / Dispatched List) nav groups, an OPTIONS menu (5 entries), a map/list toggle over ONE shared filter object, row format job#+customer / 'X of Y ac' / PENDING-ACTIVE-DONE-BILLED-CANCELLED badge / 'Assigned To: <name>' (jobs.applicator_id via profile_public_view). Pure helpers in src/lib/dispatchDisplay.ts: formatAppliedOfTotal, jobStatusToDispatchBadge, selectDispatchView (shared-filter selector, map==list equivalence, filters recipe_id client-side), hasActiveDispatchFilter. STUBS to fill: #36 = the per-location 3-step wizard (the 'Dispatch Jobs' section currently reuses the EXISTING job-level assign_job_applicator inline control); #37 = replace DispatchedListPlaceholder in DispatchBoard.tsx (section='dispatched' branch + 'View Dispatched List' nav already wired); #39 = richer recipe-filter UX (OPTIONS 'Filter Jobs By Recipe' entry + a simple recipe picker that sets filters.recipeId already exist; recipes fetched non-deleted). #38 mobile: the view is responsive (left rail collapses, 44px tap targets) but NOT yet exercised at phone width. IMPORTANT: after #36 lands, the job-row 'Assigned To' must AGGREGATE per-location assignees (multi-applicator split), not just jobs.applicator_id. LOCAL CAVEAT: local fields table lacks boundary_geojson/centroid + no VITE_MAPBOX_TOKEN → map shows no tiles/boundaries locally (wrapped in ErrorBoundary; list view unaffected); prod has both.",
    "#36 → #37/#38 (PER-LOCATION DISPATCH built): table job_location_dispatches (writes RPC-ONLY: only a SELECT policy; INSERT/UPDATE/DELETE via the SECDEF RPC), RPC dispatch_job_locations(p_assignments jsonb,...) upserts ON CONFLICT(job_field_id) (re-dispatch=reassign; XOR applicator/crew; scoped to scheduled/in_progress; license+actor+role+idempotency+soft-delete gates; activity audit). Helper _is_dispatched_to_me(job_id) = recursion-safe, is_active-aware visibility primitive; additive SELECT policies on jobs/job_fields/customers let a dispatched applicator/crew see THEIR assigned job/locations/customers (proven non-leaky). NEW server-side board RPC get_dispatch_board_jobs(p_applicator_id,p_status,p_start_date,p_end_date) returns shaped, RLS-gated job rows incl a per-caller `_dispatches` array + customer.farm_name/field_name gated by RLS-equivalent CASE (NULL when the caller couldn't see them) — REUSE for #37's dispatched list (don't build client-side id-lists; PostgREST URL caps ~19KB and a 500-uuid id.in list ALREADY fails the gateway). aggregateAssignedTo (dispatchDisplay.ts) renders all per-location assignees on the job row. #37 UNDISPATCH/reassign: reassign = call dispatch_job_locations again (upsert); UNDISPATCH (remove/cancel) needs a NEW small SECDEF RPC (writes are RPC-only) — set dispatch_status='cancelled' (the CHECK allows it) or delete, gated admin/sales_rep + idempotency. NOTE the still-present UNFILTERED board aggregation read uses job_id=in.(<=500 ids) (bounded, shipped #36); if #37 grows the unfiltered page, give it the same rows-returning RPC treatment."
  ],
  "pausedNote": null
}
