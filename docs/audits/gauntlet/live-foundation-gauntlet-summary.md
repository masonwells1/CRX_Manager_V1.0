# CRX Live Foundation Gauntlet Summary

Last updated: 2026-08-23

The 2026-08-23 Section 9 refresh found **0 BLOCKER / 3 HIGH / 0 MED / 0 LOW**. AP Aging buckets use bill date rather than due date; the dashboard's `Due This Month` number is a rolling 30-day window; and AP/receiving receipts are operation-only, so an edited retry after a lost response can replay the first money or inventory result without validating the new payload. Remote `main` `780e88aa` and fresh live function definitions agree. The former vendor-bill period-close race is resolved live, and RLS, RPC ACLs/search paths, PO serialization, row locks, and status/recompute triggers remain present. The deterministic section runner could not settle because the active checkout is dirty and 50 commits behind, but the finding-bearing files are identical between the checkout and the verified remote-main objects.

The 2026-08-12 Section 8 refresh found **0 BLOCKER / 2 HIGH / 1 MED / 1 LOW**. Return actions retain operation-wide keys, so a committed-but-unanswered action on Return A can replay as false success for Return B before B is validated. Invoice Detail resets the Apply Credit key every time its modal opens, so a committed partial application with a lost response can be applied again after close/reopen. Reversal/unapply restores a fully credited overdue invoice to `posted` until the later overdue sweep, and the Returns schema reference is stale. RLS, RPC-owned return writes, server-derived credit source, credit-application locking/immutability, exact-key apply binding, whole-memo unapply binding, generated balances, and cutoff-correct statement reconstruction remain verified. Source was pinned to remote `main` `2837263d` (confirmed read-only with `git ls-remote`) plus live catalog; the active checkout stayed seven commits behind, so the deterministic runner was not claimed as settled. **All four findings are remediated** — the three runtime findings applied live via migration `20260812130145_bind_return_receipts_to_intent_and_restore_overdue` (ledger `20260812212323`, applied 2026-08-12) plus follow-up `20260813070000_pin_return_idempotency_helper_contract` (ledger `20260813011751`, applied 2026-08-13), and the stale schema reference corrected in repository documentation; all landed in PR #388 (merged 2026-08-13). They are carried below as RESOLVED, not as open work.

The 2026-08-09 Section 7 refresh found **0 BLOCKER / 2 HIGH / 0 MED / 0 LOW**. Commission Balance accepts an historical cutoff but classifies paid and outstanding amounts from current commission status, so later payment or void activity rewrites prior-period liability. Commission payout create, post, and void idempotency receipts are operation-only; because the browser retains a key after an uncertain response, changing the selected commissions, payment, or reason can replay the first result as success for a different intended action. The prior custom-recipient dead end remains closed live, and current RLS, RPC-only payout writes, selection locks, posting reconciliation, cancellation/void guards, and cent reconciliation passed review.

The 2026-08-06 Section 6 refresh found **0 BLOCKER / 1 HIGH / 3 MED / 3 LOW**. The HIGH is an inventory-finalization race: Cycle Count saves each keystroke asynchronously but allows Complete while item writes remain pending, so completion can consume stale persisted quantities or an older request can land last. The MED findings are unstable Bulk Field Import keys, an unkeyed and unguarded Blend Recipe Duplicate path, and retry-ambiguous negative-inventory reconciliation audit events. Duplicate damaged-receipt alerts, caller-blind prevention, and seven naturally replay-safe but hard-rule-violating keyless mutators are LOW. The keyed server foundation remains strong: 144 of 151 direct-DML application signatures were keyed in the dated packet and no partial live intent binding was found, but the seven exemptions do not satisfy the canonical no-exception contract. A 2026-08-07 publication refresh at `b34b5ddb` passed 22 idempotency and 101 RPC-contract tests; the newly landed `log_customer_fact` RPC requires and correctly enforces its key.

The 2026-08-06 Section 5 refresh found **0 BLOCKER / 0 HIGH / 0 MED / 1 LOW**. Disk migrations, the schema registry, and the read-only live packet aligned through `20260806023048`; all 458 live `SECURITY DEFINER` functions used an accepted search path, no generated-column write was found, and no live overload collision was present. The LOW is a prevention gap: the overload predicate filters extension-owned members before grouping and could therefore hide a mixed extension/application collision. The report keeps its August 6 live high-water explicit; two August 7 migrations landed before publication and are not misrepresented as covered by the earlier packet.

The 2026-08-05 clean-current-main Section 2 refresh found two HIGH historical-report correctness defects: AR Aging accepted a prior As-of Date while reading current mutable balances and missing invoice/credit cutoffs; Customer Balance Listing excluded `overdue`, included `unposted`, and rewrote a prior End Date from current mutable state. Both are fixed live by migration `20260805151605_fix_historical_ar_report_cutoffs`. Exact-commit review found and follow-up `20260805171334_fix_customer_balance_paid_invoice_totals` closed a paid-invoice aggregate regression before publication. Exact SQL validation, refreshed content-bound reviewers, pre/post-apply rollback smoke including paid-plus-open totals, authenticated role execution, catalog/grant checks, and a live-introspection schema-registry refresh are complete.

The 2026-08-05 clean-current-main Section 4 refresh found one HIGH order-lifecycle side door: `bulk_import_order` accepted partial/terminal statuses while seeding all quantities undelivered and omitted inventory booking, commission creation, and atomic activity. Migration `20260805211951_harden_bulk_order_import_lifecycle` is live; imports are confirmed-only and perform the canonical side effects. Exact/publication reviews caught eight remediation gaps before merge: non-finite numeric values, missing Product cost, caller-controlled commission cost, fractional-line canonical-profit drift, unbound replay, sub-cent line profit, split concurrent cost snapshots, and silent inventory shortages. Forward-only follow-ups through `20260806023048_surface_bulk_import_inventory_warnings` close all eight; the browser also computes compatibility totals in integer cents. Sol/high migration reviews, full tests/build, all 21 live invariant sweeps, pre/post-apply rollback smoke, catalog/grant checks, zero-residue checks, and the live schema-registry refresh passed.

The July 14 all-section Codex-only run has a built, rollback-proven, Codex-adversarially clean remediation branch. Direct corrected-diff Claude verification is complete with no BLOCKER/HIGH finding. Mason approved the live cutover on July 15: all three migrations are live, `process-blend-ticket` is v25 ACTIVE/JWT-enabled, live-derived artifacts are refreshed, the rollback smoke passes, and all 17 invariant sweeps are clean. The July 19 Section 1 refresh found two MED security hardening items. The July 20 Section 2 refresh found one HIGH and one MED statement correctness gap; current live rows do not yet exhibit either shape. The July 22 Section 3 refresh found one HIGH inventory-location ledger bypass, one MED Net Position preview drift, and one LOW grant-hardening item. The July 22 Section 4 refresh found one HIGH bulk-quote side door and fixed it in branch by routing imports through `save_quote`, enforcing draft-only creation, preserving dosage/price override payloads, and rejecting accepted/terminal imported statuses. The July 22 sections 5–8 T3 close-out (fresh live evidence + gpt-5.6-sol adversarial verification from origin/main) closed sections 5 and 6 (five mutable-search_path functions hardened live via `20260722111620`; the 07-08 idempotency HIGH/MED verified fixed), verified the section-2 statement HIGH/MED are applied live (`20260720185135`), fixed one section-8 MED live (`unapply_credit_memo` replay binding), and parked one section-7 HIGH for Mason (custom "Other" commission recipients can never enter a payout batch — zero live rows affected). Historical cleanup remains separate. The 2026-07-26 PR #230/#231 reconciliation pass re-verified the Section 3 findings against current `origin/main` and the live migration ledger: the inventory-location HIGH and the matcher-grant LOW were already fixed live (PR #208 app-side allowlist plus migration `20260722122651`, confirmed applied), so ranks 6 and 11 are now marked resolved. The 2026-07-29 closeout confirmed the remaining picker MED was also fixed by PR #208 (`e3ecc9b2`): NewOrder, OrderDetail, and QuickDeliveryModal use `get_inventory_position()` through the shared tested lookup, and the focused Section 3 regression guard passes, so rank 10 is resolved. The same pass confirmed the section-7 commission-recipient HIGH parked in the T3 close-out was subsequently closed on 2026-07-22: Mason chose reject-at-creation, and live migration `20260722134252_reject_unresolvable_commission_recipients` (validator + backstop trigger + `list_commission_recipients()` dropdown; free-text "Other" removed) is in the live ledger, so rank 15 is also resolved.

## Ranked Fix Queue

| Rank | Severity | Area | Item | Current evidence | Recommended next action |
|---:|---|---|---|---|---|
| 3.1 | RESOLVED | AR historical reporting | AR Aging labels a selectable historical cutoff while reading current invoice, prepay, and credit state | Fixed live by ledger `20260805151605`; posted-as-of/cutoff body checks, admin execution, one-overload/grant checks, and applied rollback smoke passed. | None — closed. |
| 3.2 | RESOLVED | Customer balance reporting | Customer Balance Listing omits `overdue`, includes `unposted`, and rewrites past End Dates from current mutable state | Fixed live by `20260805151605` plus paid-invoice aggregate follow-up `20260805171334`; current sales-rep execution works, historical sales-rep execution fails admin-only, and overdue/unposted/cutoff/paid-plus-open smoke passed. | None — closed. |
| 3.3 | RESOLVED | Order lifecycle import | Bulk Order Import could forge partial/terminal fulfillment and omit inventory/commission/audit side effects | Fixed live by `20260805211951` and six forward-only review follow-ups through `20260806023048`; confirmed-only status, finite values, locked Product-authoritative cents, whole-cent line profit, trigger-canonical commission profit, actor/payload-bound replay, inventory booking, visible Net Position warnings, ledger, activity, grants, and zero-residue rollback smoke are verified. | None — closed. |
| 3.4 | HIGH | Commission historical reporting | Make Commission Balance reproduce paid and outstanding state at the requested cutoff | Current live/disk function filters earnings by `order_date <= p_as_of_date` but uses current `status` for paid/outstanding; no payout-post/void cutoff is consulted. | Reconstruct from durable dated payout events or fail closed for historical dates; add a post-after-cutoff/void-later rollback smoke. |
| 3.5 | HIGH | Commission payout idempotency | Bind create/post/void receipts to actor and exact payout intent | Current live/disk payout RPCs call operation-only `check_idempotency` before validating the selected commissions/payment; retained browser keys survive uncertain errors. | Fingerprint sorted selections/payment/reason plus actor, reject mismatches, and add identical-versus-different-intent replay smokes. |
| 3.6 | RESOLVED | Return lifecycle idempotency | Bind return create/approve/receive/reject/cancel/credit receipts to actor and exact target/payload | Fixed live 2026-08-12 by `20260812130145_bind_return_receipts_to_intent_and_restore_overdue` (ledger `20260812212323`) with contract follow-up `20260813070000_pin_return_idempotency_helper_contract` (ledger `20260813011751`, applied 2026-08-13); both in PR #388. Every return key is required and bound to the authenticated actor plus the exact return id or exact create payload; cancel also binds its normalized reason. | None — closed. |
| 3.7 | RESOLVED | Credit application retry safety | Preserve one unresolved key across Apply Credit close/reopen | Fixed live 2026-08-12 in the same rollout (PR #388): the Apply Credit key is bound to memo/target/amount and definitive database refusals release the retry lock while uncertain transport failures retain it. | None — closed. |
| 3.8 | HIGH | AP aging | Age vendor bills from due date, not bill date | Current remote-main and live `get_ap_aging(date)` subtract `vb.bill_date`; the AP page labels the buckets Current / 31-60 / 61-90 / 90+. | Define due-date bucket boundaries, change the RPC, and add exact 30/31/60/61/90/91-day rollback smoke coverage. |
| 3.9 | HIGH | AP dashboard | Make `Due This Month` match the calendar month | Current remote-main and live `get_ap_dashboard_summary(text)` use `due_date BETWEEN CURRENT_DATE AND CURRENT_DATE + 30`; the UI labels it `Due This Month`. | Use Chicago business date through calendar-month end, or rename the card `Due in Next 30 Days`; add a month-end boundary test. |
| 3.10 | HIGH | AP/receiving idempotency | Bind receipts to actor and exact money/inventory intent | Current remote-main and live vendor-bill/payment and receiving functions check operation-only receipts before target/payload validation; browser keys survive uncertain responses while editable values can change. | Use `check_idempotency_intent` with canonical fingerprints and add exact-replay/different-intent/different-actor lost-response smokes. |
| 1 | RESOLVED | Deployment order | Three reviewed migrations applied before frontend release | Applied live in order after zero-row preflights; live registry/types/RPC snapshot regenerated and queued exceptions removed. | Complete exact-commit review and PR/check/merge. |
| 2 | RESOLVED | Edge deployment | Hardened `process-blend-ticket` deployed | v23 → v25, ACTIVE, JWT enabled, shared CORS/auth/Sentry and local guards present; v25 is the formatting-normalized refresh of functional v24; recent Edge logs clean. | Verify the signed-in production flow after frontend merge. |
| 3 | RESOLVED | Customer statements | Carry opening balance into bounded-date statements | Fixed by `20260720173059_fix_statement_opening_balance`, applied live (ledger v`20260720185135`); T3 close-out (2026-07-22) adversarially re-verified: opening row emitted, credit memos appear once, voided excluded, applications net zero AR. | None — closed. |
| 4 | HIGH | Commission historical data | Resolve eight empty unposted `SEED` batches ($1,500 header total) | New RPC/UI guards make them unpostable, but the rows remain. | Mason decides whether to void/quarantine them; do not mutate automatically. |
| 5 | RESOLVED | Customer statements | Make same-day running balance deterministic | Same migration as rank 3 (`20260720173059`, live v`20260720185135`); T3 close-out adversarial pass verified deterministic running balances in the live statement logic. | None — closed. |
| 6 | RESOLVED | Inventory location audit trail | Inline edit can change `inventory.location` without transfer ledger | Fixed at both layers: PR #208 (`e3ecc9b2`) restricts InventoryPage bulk/inline edits to a `BULK_EDITABLE_INVENTORY_FIELDS` allowlist (`reorder_point`, `min_stock_level` only), and migration `20260722122651_guard_inventory_location_and_matcher_grants` adds BEFORE UPDATE trigger `guard_inventory_location_change` raising `INVENTORY_LOCATION_TRANSFER_REQUIRED` on direct location changes to nonzero-stock rows. Verified applied in the live migration ledger 2026-07-26. | None — closed. |
| 7 | RESOLVED | Quote lifecycle import | BulkQuoteImport bypassed `save_quote` and allowed accepted/terminal imported statuses | Current `origin/main` calls `save_quote`, creates only draft quotes, preserves dosage/price override payloads, rejects partial imports with unknown products, and retains the direct-insert regression guard. Re-verified in the 2026-08-05 Section 4 refresh. | None — closed. |
| 8 | MED | SECURITY DEFINER grants | Revoke anon/Public access to six number-generator RPCs | Section 1 refresh: live routine grants expose `next_*_number()` helpers to `PUBLIC` and `anon` with no active-role gate. | New migration: revoke from `PUBLIC`/`anon`, add/centralize active-role gating, and add a live grant sweep. |
| 9 | MED | Field activity attribution | Bind `save_field` actor to `auth.uid()` | Section 1 refresh: `save_field` writes caller-supplied `p_performed_by` into `activity_feed.performed_by` without mismatch rejection. | Add strict actor guard and a sweep for audit-column writes. |
| 10 | RESOLVED | Inventory preview consistency | Product pickers rebuild Net Position without server over-receive clamp | Fixed by PR #208 (`e3ecc9b2`): NewOrder, OrderDetail, and QuickDeliveryModal call `get_inventory_position()` and parse its canonical rows through `inventoryPositionByProduct`; `gauntletSection3InventoryGuards.test.ts` prevents raw PO arithmetic from returning. Re-verified on current `origin/main` 2026-07-29. | None — closed. |
| 11 | RESOLVED | Receiving helper grants | Revoke anon EXECUTE from `match_quick_receive_items` | Migration `20260722122651_guard_inventory_location_and_matcher_grants` revokes `PUBLIC`/`anon` EXECUTE on `match_quick_receive_items(jsonb, text)`. Verified applied in the live migration ledger 2026-07-26. | None — closed. |
| 12 | MED | PO historical data | Correct PO-2026-0008 status and review overreceipts | Seven open lines are hidden by old aggregate receipt totals. New code uses linewise completion. | Owner-approved reconciliation after reviewing source documents/physical receipts. |
| 13 | MED | Historical test/legacy rows | Decide one E2E invoice, five empty completed deliveries, and PO-2026-0015 receipt gap | Row-level probes identify historical/test data, not a current code path failure. | Present a separate live-data checklist; preserve rows until approved. |
| 14 | MED | Inventory operations | Reconcile 18 negative inventory rows | Negative on-hand is intentional discrepancy evidence. | Use physical counts and `reconcile_negative_inventory`; never zero-clamp. |
| 15 | RESOLVED | Commission recipients | Custom "Other" split recipients create payout-dead-end commissions | Mason chose reject-at-creation (2026-07-22). Live migration `20260722134252_reject_unresolvable_commission_recipients`: validator requires every split recipient to resolve to exactly one active profile, a `BEFORE INSERT OR UPDATE OF recipient_user_id` backstop trigger refuses NULL-recipient rows, and `list_commission_recipients()` feeds the editor dropdown; free-text "Other" removed from the UI. Zero live rows affected; one legacy NULL row (cancelled, $0) grandfathered. See `docs/manual/KNOWN_ISSUES.md`. | None — closed. |
| 16 | LOW | RPC signature hygiene | Three read-only RPCs carry unused `p_idempotency_key` | `generate_batch_statements`, `get_ap_dashboard_summary`, `get_expiring_planned_holds` — verified still true live 2026-07-22; contract noise only. | Fold into the next RPC-hygiene migration; excluded from mutating-idempotency checks. |
| 17 | FIXED LOCALLY | Edge Function resilience | Bound the Google Vision request in `process-document` | Extracted provider client applies one 120-second total deadline across all sequential page batches (45-second default for standalone calls), converts timeout/abort exceptions into a controlled error, and preserves other provider errors; focused tests pass. | Complete governed branch review and deploy only after separate approval. |
| 18 | RESOLVED | Migration documentation | Keep the 2026-07-28 security migrations indexed | PR #263 (`d64c6926`) repaired the missing entries; after the later security migrations through `68b47bf4`, refreshed proof reports 834 migration files and 834 indexed. | None — closed. |
| 19 | RESOLVED | WPS PDF prevention | Add dedicated output coverage for required WPS notice content | Section 11 refresh: `wpsNoticePdf.test.ts` now asserts required language/data, missing-value fallbacks, multiple products, save behavior, and filename. | None — closed. |
| 20 | LOW | Prevention signal | Remove four lint warnings | Section 14 refresh: three React hook dependency warnings and one Edge console warning remain; lint otherwise passes. | Fix as a small warning-cleanup slice and retain zero-warning lint. |
| 21 | LOW | Dependency hygiene | Review router RSC advisory and upgrade normally | `npm audit` flags `react-router`/`react-router-dom` 7.18.1 for an RSC-mode issue; CRX uses client-side `createBrowserRouter`, so exploitability was not established. | Upgrade in a dependency PR with full test/build proof. |
| 22 | LOW | Drift prevention | Make the overload sweep detect mixed extension/application collisions | The current predicate filters extension-owned functions before grouping. Fresh live mixed-ownership probe returned zero rows, so no current drift was found. | Group first, exclude only wholly extension-owned groups, and add a mixed-ownership fixture. |
| 23 | HIGH | Cycle Count inventory | Prevent completion while item quantities are unsaved or reordered | The UI fires an RPC on every keystroke and leaves Complete enabled; parent locking serializes requests but does not preserve latest user intent. | Add local drafts or an ordered per-row queue, gate Complete on all latest writes, and test delayed/out-of-order responses. |
| 24 | MED | Bulk Field Import | Retain one idempotency key per imported field intent | Each attempt inserts with a null field id and a fresh UUID; a lost response or rerun can create a second field. Live duplicate probe returned zero. | Persist per-row keys across retry/reopen and add a lost-response replay test. |
| 25 | MED | Blend Recipes | Key and guard the Duplicate action | Duplicate omits `p_idempotency_key`, passes a null recipe id, and leaves the row action enabled. Live duplicate probe returned zero. | Retain a stable per-row duplicate key, disable the active row, and consider requiring a key on server create. |
| 26 | MED | Inventory audit trail | Preserve reconciliation intent across ambiguous retry | Negative-inventory reconciliation generates a new UUID per attempt, so a lost-response retry can append a second zero-delta audit transaction. | Cache the intent key until success and assert one transaction on replay. |
| 27 | LOW | Receiving notifications | Deduplicate damaged-receipt alerts | Receiving is protected, but notification retries use fresh keys and may alert administrators twice. | Derive a stable receipt/damage-event key or add a notification business-key constraint. |
| 28 | LOW | Idempotency prevention | Cover frontend caller stability and submit gating | Dynamic RPC contract tests validate server definitions; the lint rule cannot catch omitted keys or distinguish per-attempt from per-intent keys. | Add AST caller coverage with narrow exemptions and focused regressions for the four scored caller paths. |
| 29 | LOW | RPC contract governance | Remove seven keyless-mutator exemptions that contradict the hard rule | The seven bodies are naturally replay-safe or trigger-only today, but `AGENTS.md` and the decision log require every mutating RPC to accept and enforce a key; the test allowlist is not a policy exception. | Add enforced keys to externally callable mutators, keep trigger-only helpers private, and delete the exemptions; change policy only through an explicit owner decision. |
| 30 | RESOLVED | Credit reversal lifecycle | Restore overdue status immediately after reversal or unapply | Fixed live 2026-08-12 in the same rollout (PR #388): the shared reversal helper derives `overdue` from the due date, and reversal-only status changes no longer create a second split-billing post snapshot. | None — closed. |
| 31 | RESOLVED | Returns schema documentation | Align documented fields and statuses with the live table | Fixed in repository documentation by PR #388 (merged 2026-08-13) — a docs change, not a live database change: the Returns reference row was regenerated from the live introspected registry, and the docs check now derives its required status set and high-risk columns from that registry. | None — closed. |

## Current Queue Position

Section 9 is current as of its 2026-08-23 verified-remote-main-plus-live-catalog audit. Section 10 is next because it is the oldest section in the completed queue. Sections 10–15 are refreshed manually because the automated runner only encodes Sections 1–9.

## Visibility Notes

- Full ledger and evidence: [2026-07-14-full-gauntlet-codex-only-remediation.md](2026-07-14-full-gauntlet-codex-only-remediation.md).
- No Claude review was used during the Codex-only gauntlet or Codex remediation phase. Direct Claude verification was performed afterward as Mason requested and returned `SHIP-WITH-FOLLOWUPS` with no BLOCKER/HIGH; all code findings and operational preflights are closed. The exact-commit Claude proof remains part of the post-apply pre-push gate.
