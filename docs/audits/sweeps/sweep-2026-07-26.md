# DB invariant sweep — 2026-07-26

Run by the weekly `weekly-db-sweeps-janitor` scheduled task.
Mode: **Claude via Supabase MCP**, read-only (`SELECT` only), project `rhyzpcqhnizqbxphqdkr`.
Runner: `scripts/db-invariant-sweeps/run-sweeps.mjs` (17 predicates discovered).
Baseline compared against: `scripts/db-invariant-sweeps/allowlist.json` (61 entries).
Prior saved run: **none** — `docs/audits/sweeps/` did not exist; this is the first saved run
and therefore the baseline for next week.

## Result: CLEAN — 0 unallowlisted violations across all 17 predicates

| Predicate | Live rows | Allowlisted | New findings |
|---|---|---|---|
| actor-forgery-fin-audit | 0 | — | 0 |
| actor-forgery | 1 | 1 (`cancel_delivery(...)`) | 0 |
| anon-exec-secdef | 44 | 44 of 55 entries matched | 0 |
| auth-bound-role-ungated | 1 | 1 (`process_offline_action(...)`) | 0 |
| commission-admin-active | 0 | — | 0 |
| fin-allocations-bounded | 0 | — | 0 |
| fin-ar-statement-balance | 0 | — | 0 |
| fin-commission-split-sum | 0 | — | 0 |
| fin-invoice-balance-identity | 0 | — | 0 |
| fin-prepay-balance | 0 | — | 0 |
| fin-quote-override-survival | 0 | — | 0 |
| overloads | 0 | — | 0 |
| plpgsql-check | **0 errors** | — | 0 |
| returns-lifecycle-rpc-owned | 0 | — | 0 |
| secdef-searchpath | 0 | — | 0 |
| status-literals | 1 | 1 (`normalize_product_category()`) | 0 |
| ungated-secdef-mutators | 1 | 1 (`log_failed_notification(...)`) | 0 |

## Notable deltas vs the seeded baselines in the README

- **`plpgsql-check` is now 0 errors.** The README records a 2026-06-10 first-scan baseline of
  **30 errors across 11 live functions**. That queue is now empty on the live catalog.
- **`ungated-secdef-mutators` is down to 1 live row.** `save_invoice(p_invoice jsonb, p_items jsonb,
  p_idempotency_key text)` is still allowlisted but no longer flags — its live body now references
  one of the gate helpers. Not a violation; noted so a future body change is not mistaken for new drift.
- **11 stale `anon-exec-secdef` allowlist entries.** These functions no longer hold `anon` EXECUTE at
  all (grants were tightened at some point since the entry was written), so the exemptions are dead
  weight, not risk. Housekeeping only — do **not** treat as findings:
  `prevent_oifa_edit_after_post()`, `_guard_profile_role_lock()`, `adjust_inventory(...)`,
  `allocate_payment(...)`, `calculate_prices_from_margin()`, `get_ar_aging(p_as_of_date date)`,
  `get_ar_reminder_candidates()`, `get_customer_statement(...)`, `is_sales_rep()`,
  `match_quick_receive_items(p_items jsonb, p_vendor text)`, `next_return_number()`.

## Allowlist entries confirmed still live (re-verify on next body edit)

- `anon-exec-secdef` — 44 live rows, all covered. 22 are trigger functions (anon EXECUTE inert),
  13 self-gate on `auth.uid()`/`is_admin()`/`require_admin()`, 9 are pure computed helpers
  (`next_*_number()`, `calculate_billing_splits`, `check_period_open`) with no DML.
- `actor-forgery` — `cancel_delivery(p_delivery_id uuid, p_cancel_reason text, p_performed_by uuid,
  p_idempotency_key text)`.
- `auth-bound-role-ungated` — `process_offline_action(p_client_action_id uuid, p_idempotency_key text)`.
- `ungated-secdef-mutators` — `log_failed_notification(...)`.
- `status-literals` — `normalize_product_category()` (writes `Herbicide` / `Foliar Fertilizer` /
  `Other` — regex-approximation false positives against a different table's `category` CHECK set).

## Method note

No `SUPABASE_DB_URL`/`psql` in this environment, so the runner's Claude mode was used: each
`predicates/*.sql` body was executed one statement per `execute_sql` call via the Supabase MCP and the
returned `violation_key`s were diffed against `allowlist.json`. No writes, no DDL, no DML.
