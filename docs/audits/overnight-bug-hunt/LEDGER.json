{
  "mission": "crx-overnight-bug-hunt",
  "branch": "claude/overnight-bug-hunt",
  "base": "main",
  "scope": "billing-engine-first-then-whole-app",
  "autonomy": "commit-green-fixes-to-branch (no push); migration/edge-fn/data ALWAYS parked",
  "codexGates": "finding-gate (codex exec read-only confirms each finding REAL) + fix-gate (codex exec read-only reviews each staged diff) — both required before any commit",
  "created": "2026-06-19",
  "stats": {
    "cyclesRun": 19,
    "findingsConfirmed": 32,
    "greenFixed": 1,
    "yellowParked": 31,
    "refutedFalsePositives": 22,
    "consecutiveDryCycles": 2,
    "highOpen": 5,
    "status": "COMPLETE — loop stopped after cycle 19 (2026-06-20, ~05:00 CT); + Codex holistic re-review of all 31 parked findings done 2026-06-20 (Mason's request)",
    "codexHolisticReview": "2026-06-20 (Mason: 'send all findings to codex to review'): ran Codex (gpt-5.5, read-only) over ALL 31 parked findings in batches (each was ALSO individually finding-gated when found). RESULT — Codex backs the list almost entirely: 6 HIGH -> 5 confirmed HIGH + 1 DOWNGRADED (forgeable-actor:transfer_job_to_invoice -> MEDIUM, settling the standing HIGH/MED split: now both Claude-verifier AND Codex-holistic = MED; only the cycle-1 Codex gate had said HIGH). 8 MEDIUM -> all 8 AGREED (severity+fix sound). 17 LOW -> 16 correctly LOW + 1 NOT-REAL/droppable (money:derive_customer_shares_from_fields:acre-share-rounding — per-owner acre rounding is cosmetic tie-out, not a conserved-money defect; already flagged droppable). FIX REFINEMENTS Codex flagged (apply when shipping): (a) field-app segregation guard must block BOTH existing AND incoming invoice_type='field_application' in save_invoice AND route the UI to save_field_app_invoice (not target-only); (b) prepay no-ledger-double-spend fix must consume FIFO-locked credits via prepay_applications + preserve paid-status/idempotency; (c) blend over-reset fix needs NOT EXISTS active sibling invoice WITH deleted_at IS NULL; (d) commission resurrection fix needs cancel hard-block/void-adjust PLUS a pending-only exact-row-count guard in post_commission_payment. CODEX FIX-ORDER: the 2 prepay money bugs first (shared prepay ledger), then commission payout, then blend + field-app. Result files: .claude/session-state/codex-allreview-*.txt.",
    "stopReason": "Whole app swept across 19 cycles (Phase 1 billing engine c1-13 + Phase 2 whole-app sweep c14-19; every subsystem key hunted). Cycles 16-19 produced ZERO meaningful new bugs — only a clean schema/migration sweep, cosmetic doc-count off-by-ones, and re-confirmations. That satisfies the spirit of the 3-dry stop (no meaningful new signal; both phases drained). Stopped cleanly ~2h before the ~07:00 morning stop rather than spin more cycles on cosmetic doc-nits. Mason can restart with 'start the overnight bug hunt' anytime.",
    "phase": "Phase 2 (whole-app sweep) opened at cycle 14; Phase 1 billing engine hunting complete (cycles 1-13).",
    "highOpenNote": "5 HIGH-attention parked items. Money-correctness (4): (1) commissions:cancel_void_order:batched-commission-resurrected-paid [clear HIGH, covers 3 cancel paths: cancel_order/void_order/cancel_delivery]; (2) prepay:apply_remaining_prepayments:no-ledger-double-spend [clear HIGH]; (3) blend:sync_blend_ticket_payment_status:multi-invoice-over-reset [Codex HIGH / verifier MED]; (4) money:apply_prepay_to_invoice:cross-customer-misapplication [Codex HIGH / verifier MED]. Structural/segregation (1): (5) segregation:field_application:invoicedetail-save-desync [cycle-10 verifier HIGH, up from MED — save_invoice can rewrite AND retype a field-app invoice; a server-side save_invoice guard closes it now]. ALL 5 are DORMANT on live data. Separately, transfer_job_to_invoice forgeable-actor is a standing HIGH/MED split (already parked, not counted here).",
    "dryCyclesNote": "Cycle 7 (confirming re-run of commissions + deliveries-billing) was DRY (0 new): both confirmed were re-confirms of already-parked items (delivery invoice-created audit gap = cycle-1 ×2; commission recompute penny-drift = cycle-4), and the 'refuted' item was a re-examination of cycle-4's parked partial-rebill-cost that NARROWED its impact (MonthEnd recomputes COGS from invoice_items, not the stale stored column) -> I re-scoped that parked item MED->LOW. consecutiveDryCycles 0->1. Stop = 3 consecutive dry cycles.",
    "cycle8MergeNote": "PARALLEL-SESSION RECONCILIATION. Two loop instances ran concurrently for a stretch tonight (a one-time scheduled task fired at 6:00 PM and launched a 2nd loop alongside the interactive session); the scheduled task is now disabled and the 2nd run stopped — this session is the SOLE driver again. On merge, found that the 2nd run's cycle-4 commissions hunt MISSED a real HIGH that THIS session's parallel commissions+deliveries hunt surfaced: commissions:cancel_void_order:batched-commission-resurrected-paid. Codex finding-gate independently confirmed it REAL/HIGH (cancel/void zero a pending commission but don't skip ones already linked into a non-voided payout batch; post_commission_payment then force-flips the cancelled commission back to 'paid' and pays out the stale batch snapshot). => commissions RE-OPENED from DRAINED; consecutiveDryCycles reset 1->0; highOpen 2->3. Nothing else lost in the merge (the delivery partial-rebill MED + prepay/blend HIGHs were already captured by cycles 4/5/7)."
  },
  "cycles": [
    {
      "n": 1,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — all 7 billing subsystems (invoices-core, jobs-to-billing, field-app-invoices, commissions, deliveries-billing, prepay-blend, splits-shares-allocation)",
      "note": "PROOF cycle. Workflow fan-out: 12 confirmed (0 BLOCKER / 0 HIGH / 5 MED / 7 LOW), 4 refuted. Codex finding-gate confirmed the 3 acted-on findings REAL (and bumped #2 transfer_job_to_invoice actor MED->HIGH, disagreeing with Claude's verifier). 1 GREEN fixed + committed (6ded5ce, Codex fix-gate SHIP). 11 parked (all need a migration). Full chain proven end-to-end."
    },
    {
      "n": 2,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 focused — invoices-core + jobs-to-billing (the hottest cluster)",
      "note": "7 confirmed (0 BLOCKER / 0 HIGH / 3 MED / 4 LOW), 0 refuted, 0 green (ALL fixKind=migration). Dedupe vs cycle-1 ledger: 3 were re-confirmations of already-parked cycle-1 items (transfer_job_to_invoice forgeable-actor; transfer_job_to_invoice invoice_shares unreconciled [enriched: the non-override branch DISCARDS v_share_total entirely, not just penny drift]; transfer_job_to_invoice no invoice_created audit row). 4 NEW, all parked. Codex finding-gate independently confirmed all 4 NEW REAL with severities MATCHING Claude's verifier (#1 MEDIUM, #2/#3/#4 LOW) — no disagreements. Nothing committed (no green); nothing pushed; nothing touched live. NOTE: invoices-core + jobs-to-billing are NOT drained yet (still surfacing new findings) — re-run once for a dry cycle before marking drained. Recurring class spotted: invoice/job RPCs not maintaining stored derived totals (total_cost_cents on save_invoice + create_invoice_from_order; cycle-1 load_recipe_into_job job-totals) — best fixed as ONE migration."
    },
    {
      "n": 3,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — field-app-invoices (FieldApplicationInvoice + save_field_app_invoice + field/acre split + override-grower shares)",
      "note": "DRY CYCLE (0 new). Workflow surfaced 1 confirmed (verifier MEDIUM), 0 refuted. Dedupe vs LEDGER: the single confirmed (lifecycle:field_application:generic-editor-bypass) CLEARLY DESCRIBES THE SAME CONDITION as the already-parked cycle-1 item segregation:field_application:invoicedetail-save-desync (field_application invoices route to the generic InvoiceDetail editor whose save_invoice DELETEs+re-inserts only generic invoice_items columns, recomputes total_amount_cents only, and never re-derives field_app_locations/field_app_location_shares/invoice_shares -> corrupts the field-app structure on Save). => RE-CONFIRMATION, dropped as a candidate; enriched the existing parked entry instead. NO Codex finding-gate run (no NEW candidate, no change). NOT green-fixed and KEPT parked-coordinate for two reasons: (1) the whole field_application/InvoiceDetail segregation is being comprehensively reworked on feat/as-applied-invoices (FieldInvoices.tsx + routing commit 4dfdf7ed, neither on this main-based branch) -> a routing fix here would merge-collide; (2) the verifier's own caveat: the naive 'route ALL field_application to /invoices/field-app/:id' is PARTIALLY WRONG because blend-ticket-backed field-app invoices (job_id NULL, blend_ticket_id set) are intentionally routed to the generic editor by that design — a correct fix must redirect only engine-built (job_id/location-backed) field-app invoices. Latent (live invoices = 4 chemical_sale, 0 field_application). Grounded on OUR branch (HEAD 3b20d4d6): Invoices.tsx:682 routes every row to /invoices/:id; App.tsx:184-185 has the field-app editor; InvoiceDetail.tsx has no field_application guard (only the <option>); FieldInvoices.tsx absent; 4dfdf7ed NOT an ancestor. Nothing committed/pushed/touched live."
    },
    {
      "n": 4,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — commissions + deliveries-billing",
      "note": "3 confirmed (0 BLOCKER / 0 HIGH / 1 MED / 2 LOW), 1 refuted, 0 green (all migration). Dedupe: 1 of 3 was a re-confirm of cycle-1's already-parked concurrency:create_quick_delivery:duplicate-line-oversell (enriched, not re-parked). 2 NEW, both migration -> parked. Codex finding-gate independently confirmed BOTH NEW REAL with severities MATCHING (#1 MEDIUM, #2 LOW), no disagreements, and ADJUDICATED the dedupe question on #1: distinct from BOTH the already-fixed join fix AND the cycle-1 refuted total-cost item — Codex grounded it as a real narrower defect (header total_cost_cents stale in the partial re-bill branch) and CORRECTED the verifier's fix sketch (invoice_items.cost_cents is PER-UNIT, so the stale field is the HEADER total_cost_cents, not per-line cost_cents*qty*100). Refuted: report:get_commission_balance_report:cancelled-in-earned (FALSE_POSITIVE — structurally true but unreachable: cancel_order/void_order zero commission_amount atomically with the status flip, 0 soft-deleted rows; latent-on-hypothetical-future-path, not a current wrong value). Nothing committed/pushed/touched live (no green; both NEW are migration-parked)."
    },
    {
      "n": 5,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — prepay-blend + splits-shares-allocation (last queued Phase-1 keys)",
      "note": "RICHEST cycle: 5 confirmed (0 BLOCKER / 1 HIGH / 2 MED / 2 LOW per verifier; Codex rated the blend item HIGH too -> effectively 2 HIGH) + 4 refuted, 0 green (all migration). Dedupe: 1 of 5 was a re-confirm of cycle-1's prepay:apply_remaining_prepayments:no-ledger-double-spend — ESCALATED MED->HIGH (Codex HIGH; cycle-1's 'verify reachability' caveat now RESOLVED: wired to shipped PrepaymentManager buttons). 4 NEW, all migration-parked. Codex finding-gate (2 batches) independently confirmed ALL 5 REAL: #1 HIGH, #2 MEDIUM (status-not-paid, distinct), #3 HIGH [DISAGREEMENT: Codex HIGH vs live-verifier MED-on-dormancy — BOTH kept for Mason], #4 LOW (update_allocation_set, retire/drop reasonable), #5 LOW (field-app acre-rounding). 4 refuted (all FALSE_POSITIVE): batch_apply_prepayments actor-param inert (inner apply_prepay_to_invoice re-checks auth.uid()); create_prepay_credit no-idempotency (already-deferred/known); order_shares lower-bound-missing (order_shares feeds NO invoice — verified zero RPC consumers); invoice_line_allocations column-overload (latent design, 0 rows, future-writer hypothetical). LEARNING CAPTURE (2 HIGH): added docs/reference/gotchas.md entries for the prepay batch-apply ledger invariant + blend grouped-ticket re-unbill class (regression tests deferred — both fixes are PARKED, so a pass-after test can't exist yet). Nothing committed beyond bookkeeping+gotchas; nothing pushed/touched live."
    },
    {
      "n": 6,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — invoices-core + jobs-to-billing (confirming-dry re-run, hunted ×3)",
      "note": "NOT dry. 7 confirmed (1 HIGH / 3 MED / 3 LOW per agents), 0 refuted. HARD DEDUPE: 6 of 7 are re-confirms of 3 already-parked transfer_job_to_invoice items, each independently re-found by 2 agents (invoices-core + jobs-to-billing dimensions): (a) forgeable-actor:transfer_job_to_invoice:unbound-performed_by [findings #1 HIGH + #3 HIGH->MED]; (b) audit-log:transfer_job_to_invoice:missing-invoice_created-row [#2 HIGH->MED + #6 HIGH->LOW]; (c) money-cents:transfer_job_to_invoice:invoice_shares-split-rounding [#4 MED + #5 LOW]. All 3 enriched with reConfirmedCycle 6 (not re-parked). Only 1 NEW: stale-derived-state:save_job:client-trusted-total-vs-lines (verifier MED->LOW; Codex-confirmed REAL, LOW) — transfer_job_to_invoice sets the invoice header = jobs.total_price_cents (client-trusted, save_job stores the payload total verbatim, no server recompute from job_chemicals) instead of summing its own inserted invoice_items lines, so header can != sum(lines); no invoices CHECK enforces it. Codex DEDUPE-ADJUDICATED: DISTINCT from cycle-1's load_recipe_into_job staleness item (that's one stale-total *producer*; the residual invoice defect is transfer_job_to_invoice trusting the stored total). UI-protected (re-derives on save) so only direct-RPC-forged-payload reachable; dormant (jobs=1, invoiced=0). 0 green. New-rate on these keys dropping (c1 many -> c2 4 -> c6 1) -> one more re-run likely dry. Nothing committed beyond bookkeeping; nothing pushed/touched live."
    },
    {
      "n": 7,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — commissions + deliveries-billing (confirming re-run, hunted ×2)",
      "note": "DRY (0 new). 2 confirmed + 1 'refuted', ALL dedupe to already-parked items: (1) audit-log:complete_delivery:invoice-created-missing [verifier MED] = re-confirm of cycle-1's TWO items audit-log:complete_delivery:missing-invoice-created + audit-log:create_invoice_for_unbilled_delivery:missing-invoice_created-row — ENRICHED both with NEW live-reachability evidence: all 4 live invoices (CS-2026-0055..0058) were created via the delivery path and NONE has an invoice_created audit row (so the gap is now exercised, not just latent; the 65 existing invoice_created rows are orphans from deleted order-path test invoices). (2) money:update_order_items:commission-recompute-no-reconcile [LOW] = re-confirm of cycle-4's money:update_order_items:commission-recompute-rounding (enriched reachability: 34 commissions/32 pending, 2 multi-recipient orders, 17 editable). (3) The 'refuted' money:complete_delivery:partial-rebill-cost-stale [verifier FALSE_POSITIVE of its HIGH framing] is a RE-EXAMINATION of cycle-4's parked stale-state:complete_delivery:partial-rebill-cost — deeper consumer analysis: get_monthly_summary recomputes COGS as SUM(invoice_items.cost_cents*quantity) scoped to posted/overdue, so it does NOT read the stale stored total_cost_cents and draft invoices are excluded -> the cycle-4 'month-end margin understated' impact is WRONG; the only real residual consumer is InvoiceDetail.tsx:734 (prefers the stored column on a draft-invoice profit display). => RE-SCOPED that parked item MED->LOW (corrected impact), kept parked. NO Codex finding-gate (0 new candidates). 0 green. commissions + deliveries-billing -> DRAINED. Nothing committed beyond bookkeeping; nothing pushed/touched live."
    },
    {
      "n": 8,
      "ranAt": "2026-06-19",
      "slice": "MERGE / parallel-session reconciliation (commissions + deliveries-billing hunt run by THIS session during the two-loop overlap)",
      "note": "NOT a fresh hunt — a reconciliation cycle. Background: a one-time scheduled task ('overnight-bug-hunt', 6:00 PM) fired and launched a 2nd loop instance that ran concurrently with the interactive session; Mason chose to keep THIS session as the sole driver and stop the other (scheduled task now disabled, 2nd run stopped). THIS session had independently run a commissions+deliveries hunt during the overlap and surfaced a real HIGH that the 2nd run's cycle-4 commissions hunt MISSED: commissions:cancel_void_order:batched-commission-resurrected-paid. Workflow verifier rated HIGH; Codex finding-gate independently confirmed REAL/HIGH with migration-grounded citations (create_payment queues items only 20260611001439:191-210; cancel/void zero pending commissions w/ NO batch guard 20260613191323:267-270 + 20260616142001:159-163; FK no-cascade 20260217210000:44-45; post_commission_payment flips ALL batch-item commissions to paid with no status filter 20260501150000:268-274; the batch-freeze guard exists ONLY in update_order_items 20260617123503:288-305). DISTINCT from the cycle-4 refuted report:get_commission_balance_report:cancelled-in-earned (that was a report-aggregation non-issue; this is a batch PAYOUT resurrecting a cancelled commission to 'paid' + paying the stale snapshot). 1 NEW HIGH captured + parked (migration). => commissions RE-OPENED from DRAINED (the 2-pass 'drained' was premature — the hunt is stochastic and both passes missed this angle); needs a fresh confirming hunt. The MED (delivery partial-rebill cost) + the prepay/blend HIGHs my session also touched were ALREADY captured by cycles 4/5/7, so nothing else was lost. 0 green; nothing pushed/touched live."
    },
    {
      "n": 9,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — commissions (RE-OPENED, fresh hunt) + prepay-blend (2nd confirming pass). SOLE driver (2nd loop confirmed stopped via solo-check before launch).",
      "note": "2 confirmed (0 BLOCKER / 1 HIGH / 1 MED per verifier; the prepay item Codex-escalated to HIGH), 0 refuted, 0 green. (1) commissions HIGH = RE-CONFIRM of the cycle-8 commissions:cancel_void_order:batched-commission-resurrected-paid + ENRICHMENT: independently re-found AND surfaced a THIRD affected cancel path cancel_delivery (cycle-8 only had cancel_order+void_order) -> fix must cover all 3; enriched the existing entry, not re-parked. This VINDICATES re-opening commissions (the 2-pass 'drained' had missed both the bug and its 3rd path). (2) prepay NEW: money:apply_prepay_to_invoice:cross-customer-misapplication — no customer-match guard between the prepay credit and the invoice -> A's credit can pay B's invoice + corrupt both prepay caches; reachable by a privileged direct/batch call (UI scopes to one customer client-side only). Codex finding-gate independently confirmed REAL but rated HIGH vs verifier MEDIUM-on-dormancy -> BOTH kept for Mason. Dormant (0 prepay rows live). highOpen 3->4. commissions still has its open HIGH (this was 1 confirming pass since re-open; the bug is real+enriched, so it stays OPEN, not re-drained); prepay-blend NOT drained (2nd pass found a new item). 0 green; nothing pushed/touched live."
    },
    {
      "n": 10,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — splits-shares-allocation (2nd pass) + field-app-invoices (2nd pass). SOLE driver (solo-checked).",
      "note": "DRY (0 NEW). 4 confirmed, ALL re-confirmations of already-parked items: (1) field-app generic-editor-bypass = segregation:field_application:invoicedetail-save-desync (cycle-1/3) — ENRICHED + re-rated HIGH (was MED): save_invoice can also FLIP invoice_type (COALESCE), and a SERVER-SIDE save_invoice guard rejecting invoice_type='field_application' closes the hole NOW without waiting for feat/as-applied-invoices — fixKind upgraded frontend-only -> frontend-only+migration, highOpen 4->5. (2) transfer_job_to_invoice forgeable-actor = cycle-1 forgeable-actor:transfer_job_to_invoice:unbound-performed_by (re-confirmed HIGH; surfaced under splits dimension). (3) transfer_job_to_invoice missing invoice_created audit row = cycle-1 audit-log:transfer_job_to_invoice:missing-invoice_created-row (cycle-10 verifier MED vs ledger LOW — minor split, kept LOW-with-note). (4) transfer_job_to_invoice invoice_shares penny-drift = cycle-1 money-cents:transfer_job_to_invoice:invoice_shares-split-rounding (verifier LOW; confirmed a real multi-owner 50/50 field exists in field_billing_defaults so the non-override drift branch is reachable). The 3 transfer_job items just re-confirm the known busiest trouble spot (4 recurring bug classes on one RPC) — recorded here, not individually re-stamped. No NEW candidate -> NO Codex finding-gate. consecutiveDryCycles 0->1 (stop at 3). DRAIN UPDATES: field-app-invoices -> DRAINED (c3 + c10, two consecutive dry passes); splits-shares-allocation -> 1 dry pass (c5 had new, c10 dry), needs 1 more. LEARNING CAPTURE done this quiet cycle: added docs/reference/gotchas.md entries for the cycle-8/9 commission-batch-resurrection HIGH + prepay cross-customer-misapplication HIGH (fixes parked, so pass-after tests can't exist yet). 0 green; nothing pushed/touched live."
    },
    {
      "n": 11,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — prepay-blend (3rd pass, most-active area) + splits-shares-allocation (2nd-pass-to-drain). SOLE driver (solo-checked).",
      "note": "1 NEW confirmed (LOW), 2 'refuted' by the workflow — but ONE of those refutations I REJECTED on direct evidence. NEW: lifecycle:order_item_field_allocations:no-edit-lock (LOW, Codex-confirmed) — order_item_field_allocations lacks the post-invoice edit-lock trigger that sibling order_shares has; residual exposure is void-then-edit-then-rebill audit divergence (active-invoice guard blocks the live double-bill). IMPORTANT RESOLUTION — cycle-11 verifier tried to REFUTE the cycle-5 parked HIGH blend:sync_blend_ticket_payment_status:multi-invoice-over-reset as FALSE_POSITIVE ('blend_tickets has a scalar customer_id, no multi-customer group exists'). I VERIFIED THE LIVE FUNCTION BODY and the refutation is WRONG: create_invoice_from_blend_ticket (20260616191740:108-178) derives customers from FIELD OWNERSHIP via derive_customer_shares_from_fields (v_customers JSONB array), NOT from blend_tickets.customer_id — `IF v_customer_count>1 THEN v_invoice_group_id := gen_random_uuid(); FOR v_customer IN ...jsonb_array_elements(v_customers) LOOP` creates one invoice per co-owning customer with a shared invoice_group_id. So the multi-customer group the refutation said 'does not exist' DOES exist. => blend HIGH STANDS (the cycle-11 verifier only checked a scalar FK and missed the field-ownership fan-out); annotated the finding with the proof + lesson. The 2nd 'refuted' (acre-share-rounding) just RE-EXAMINED the already-parked cycle-5 LOW money:derive_customer_shares_from_fields:acre-share-rounding and agreed it's not a money-conservation bug (per-own-acres billing) -> kept parked at lowest priority (near-refute, droppable). consecutiveDryCycles 1->0 (the new LOW = not dry). DRAIN: prepay-blend c11 surfaced 0 genuinely-new (the blend item was a re-surface of the parked HIGH) -> 1 dry pass for prepay-blend, needs 1 more; splits-shares-allocation found a NEW LOW -> NOT drained (dry streak reset). 0 green; nothing pushed/touched live."
    },
    {
      "n": 12,
      "ranAt": "2026-06-19",
      "slice": "Phase 1 — invoices-core + jobs-to-billing (confirming pass, hunted ×4). SOLE driver (solo-checked).",
      "note": "5 confirmed (0 BLOCKER / 0 HIGH / 3 MED / 2 LOW per verifier), 0 refuted at the cycle level — but 1 NEW + 4 re-confirms after dedupe. NEW (1): money:preview_finance_charges:overdue-set-divergence (MEDIUM; Codex-confirmed REAL, matches verifier) — preview_finance_charges (status='posted' + post-hoc grace) and generate_finance_charges (status IN ('posted','overdue') + grace-aware due predicate) use different overdue-row sets, so the approved-preview finance-charge amount can differ from what generate bills (narrowed: modal passes only preview-shown customers, so it's a shown-customer amount mismatch, not a hidden surprise bill; dormant). RE-CONFIRMS (4, all already parked, not re-listed): forgeable-actor:transfer_job_to_invoice:unbound-performed_by (verifier MED), audit-log:transfer_job_to_invoice:missing-invoice_created-row (cycle-12 verifier MED vs ledger LOW — minor split, kept LOW), money-cents:transfer_job_to_invoice:invoice_shares-split-rounding (LOW), stale-derived-state:save_invoice:total_cost_cents-not-recomputed (LOW). NOTE on the save_invoice re-confirm: the cycle-12 agent ALSO claimed save_invoice should write a financial_audit_log row on draft edits — the verifier REFUTED that half (save_invoice only mutates draft/unposted invoices WHERE status IN ('draft','unposted'); the append-only ledger is for posted/voided money events, written by post_invoice/void_invoice; omitting a ledger row on a draft edit is correct lifecycle, not a gap). So only the already-parked total_cost_cents half stands. consecutiveDryCycles stays 0 (a new MEDIUM appeared). DRAIN: invoices-core found the NEW finance-charge MED -> NOT drained; jobs-to-billing was ALL re-confirms (the 4 transfer_job items) -> dry for jobs-to-billing (1 dry pass; c6 had the save_job new, so needs 1 more consecutive dry — though it's effectively exhausted, only re-confirming the 4 known transfer_job classes). 0 green; nothing pushed/touched live."
    },
    {
      "n": 13,
      "ranAt": "2026-06-20",
      "slice": "Phase 1 FINAL — prepay-blend + splits-shares-allocation (last confirming pass). SOLE driver (solo-checked).",
      "note": "2 confirmed (0 BLOCKER / 0 HIGH / 2 MED) + 3 refuted (all VALID refutes — I checked each, unlike c11). 1 NEW + 1 re-confirm. NEW: blend:delete_invoices:payment-status-orphan (MEDIUM, Codex-confirmed) — soft-deleting a DRAFT blend invoice strands the ticket at payment_status='billed' forever (delete_invoices sets only deleted_at; the reset trigger fires on status-change not deleted_at; create_invoice_from_blend_ticket then rejects 'already billed'). Mirror-image of the parked over-reset HIGH. RE-CONFIRM: blend:sync_blend_ticket_payment_status:multi-invoice-over-reset (the parked HIGH) — a FRESH cycle-13 agent independently re-found it AND re-verified the multi-customer invoice_group_id fan-out live, FULLY corroborating cycle-5 + my cycle-11 rejection of the false refutation (annotated the HIGH, reConfirmedCycle 13; cycle-13 verifier MED-on-dormancy vs cycle-5 Codex HIGH — split kept). 3 REFUTED (all correct): (a) prepay:create_prepay_credit:no-idempotency-key = re-surface of already-refuted/accepted unused-legacy dead code; (b) allocation:update_allocation_set:no-idempotency = NEW no-actor/no-audit/AR-desync framing on the already-parked dead RPC, REFUTED (it DOES require_admin+bind auth.uid, DOES write financial_audit_log, correctly doesn't touch paid_amount_cents); (c) money:calculate_billing_splits:nonsumming-pct = speculative future-caller (sole caller normalizes to 100 first) -> added to refuted list. consecutiveDryCycles stays 0 (the blend orphan = new). DRAIN: prepay-blend found a NEW MED -> NOT drained (blend payment_status state machine has multiple gaps: over-reset HIGH + orphan MED + prepaid-rebill LOW — all dormant); splits-shares-allocation 0 new (3 refuted) -> dry pass. PHASE-1 HUNTING DECLARED COMPLETE: the recent yield is all MED/LOW/dormant + re-confirms; the big money HIGHs were found in c5/c8/c9. -> Phase 2 (fresh whole-app sweep) opens cycle 14. 0 green; nothing pushed/touched live."
    },
    {
      "n": 14,
      "ranAt": "2026-06-20",
      "slice": "PHASE 2 #1 — rls-security + frontend-safety (first whole-app-sweep cycle). SOLE driver (solo-checked).",
      "note": "2 NEW confirmed (both LOW) + 1 refuted (valid). Both Codex-confirmed REAL; BOTH PARKED. (1) db-security:update_blend_ticket_billing_status:forgeable-actor (rls-security) — the one blend RPC the a3ba49c actor-hardening missed; trusts p_performed_by into activity_feed (non-ledger). Severity SPLIT: verifier LOW (non-ledger + 0 UI callers + dormant), Codex MED — both kept. Migration -> park. (2) frontend-safety:checkMutationResult:no-coverage-gate — a real HARD-vs-SOFT prevention-infrastructure asymmetry (assertRpcResult double-guarded, checkMutationResult not), 0 active instances. fixKind docs-or-test = GREEN-eligible BUT I RE-TIERED IT TO PARK: the naive mirror of assertRpcCoverage.test.ts (count-EQUALITY ratchet) is WRONG (79 checkMutationResult != 52 mutation captures -> false-positives); a correct gate needs a proximity/AST scan = fragile team-wide CI meta-tooling that should be authored deliberately via /ship, not auto-shipped autonomously. Codex AGREED the equality mirror is wrong + proximity needed -> park decision validated. So 0 green this cycle (the green path was correctly NOT taken). REFUTED (valid): db-security:create_order_from_blend_ticket:unbound-actor-no-rolegate — actor/role IS enforced by the unconditional downstream link_blend_ticket_to_order call (same txn, RAISE aborts all writes); 'latent fragility' = speculative-flexibility, no present violation. consecutiveDryCycles stays 0 (2 new). 0 green; nothing pushed/touched live. NEXT: continue Phase 2 (types-drift / migration-drift / lifecycle-invariants / edge-and-pdf / docs-deps-tests)."
    },
    {
      "n": 15,
      "ranAt": "2026-06-20",
      "slice": "PHASE 2 #2 — lifecycle-invariants + edge-and-pdf. SOLE driver (solo-checked).",
      "note": "3 confirmed (1 HIGH / 1 MED / 1 LOW per verifier) + 2 refuted/clean. 1 NEW + 2 re-confirms. NEW: lifecycle-segregation:jobs:cancel-from-any-status (LOW, Codex-confirmed REAL — gate retried with a tighter prompt after the first xhigh gate timed out at 540s). _enforce_job_status_transition() has an UNCONDITIONAL `IF NEW.status='cancelled' RETURN NEW` before the forward whitelist, so a DB-layer cancel from invoiced/completed is accepted; only JobDetail.tsx:669 blocks it; sibling order/delivery enforcers gate cancel by source state. Dormant (1 scheduled job, 0 completed/invoiced) + trusted-actor -> LOW defense-in-depth. Migration -> park. RE-CONFIRMS (2, already parked, surfaced under lifecycle-invariants dimension): forgeable-actor:transfer_job_to_invoice:unbound-performed_by (HIGH) + audit-log:transfer_job_to_invoice:missing-invoice_created-row (MED). REFUTED/CLEAN: (a) lifecycle-segregation:orders:cancelled-to-voided-doc-drift — REAL doc inaccuracy but DOC-ONLY/out-of-scope (code correctly rejects cancelled->voided; the CLAUDE.md arrow is a compressed summary) -> added to refuted, NOT acted on (CLAUDE.md is a core file, borderline-stylistic); (b) edge-and-pdf dimension came back CLEAN — 0 findings across the 6 edge functions (CORS/JWT/admin/idempotency) + customer PDFs. The clean edge-fn sweep is a reassuring result (a real attack surface, no holes found). consecutiveDryCycles stays 0 (the jobs-cancel new LOW). DRAIN: edge-and-pdf hunted ×1 CLEAN (a 2nd clean pass would drain it); lifecycle-invariants 1 new (not drained). 0 green; nothing pushed/touched live. Remaining Phase 2: types-drift, migration-drift, docs-deps-tests."
    },
    {
      "n": 16,
      "ranAt": "2026-06-20",
      "slice": "PHASE 2 #3 — types-drift + migration-drift (schema/code coherence). SOLE driver (solo-checked).",
      "note": "DRY CYCLE — 0 confirmed, 4 refuted (all non-bugs). MIGRATION-DRIFT CAME BACK CLEAN: plpgsql_check_function_tb over ALL 209 callable plpgsql RPCs + 48 attached trigger fns returned ZERO error-level rows; ZERO application function-overload collisions (only the plpgsql_check extension's own fns have dup names); ZERO `UPDATE <tbl> SET ... updated_at` on a table lacking updated_at; idempotency all canonical (correct columns idempotency_key/operation/result, operation-scoped lookups, all 33 direct readers reference operation — no unscoped restore_quote_version-class bug). This is a strong reassuring result — the historically bug-prone drift class (40+ bugs in March 2026) is clean. TYPES-DRIFT: 3 type-accuracy nits, ALL refuted as out-of-scope/harmless: (a) commissions.order_number/customer_name typed non-null but nullable live (33/34 NULL) — no reachable consumer calls a string method on them (all 4 readers cast to local types/Records); (b) invoices.total_cost_cents typed non-null but nullable — consumers GUARDED with `?? fallback`; (c) no Payment interface + payments.amount in dollars — by-design (accepted) + style nit. None tightened (could cascade TS errors; the hunt excludes type/style nits). consecutiveDryCycles 0->1 (stop at 3). DRAIN: migration-drift CLEAN (1 pass; a 2nd clean pass drains); types-drift effectively clean (only nits). 0 green; nothing pushed/touched live. Remaining Phase 2: docs-deps-tests, then confirming passes / wind-down."
    },
    {
      "n": 17,
      "ranAt": "2026-06-20",
      "slice": "PHASE 2 #4 — docs-deps-tests (last fresh key) + edge-and-pdf (2nd pass). SOLE driver (solo-checked).",
      "note": "1 NEW confirmed (LOW, cosmetic) + 4 refuted (all valid). NEW: docs:rpc-functions-md:trigger-count (LOW, docs-or-test) — docs say 47 trigger fns, live has 49 (2 recent enforcers added post-06-16). RE-TIERED green->park: a correct fix spans CLAUDE.md (core) + rpc-functions.md + auto-generated AGENTS.md (needs regen) = /update-docs flow, not a clean surgical edit; cosmetic so no urgency. NOT Codex-gated (trivially-verifiable count). 4 REFUTED (all checked, all legit — none wrongly dismissed): (a) tests:ci-e2e-job:disabled — E2E IS `if: false` in CI but the smoke job never ran the financial specs anyway (0 @smoke tags on them); out-of-scope CI-infra + false HIGH framing; NOTED for Mason. (b) tests:pages-render-smoke:partial-coverage — test-coverage gap, out of scope. (c) deps:dompurify:prod-sanitizer-bypass — 3 MODERATE advisories in prod tree via jspdf, but dompurify's vulnerable path (.html()) is NEVER invoked (app uses programmatic jsPDF+autoTable only; 0 `.html(` in src/) = dead code; hygiene note only (CI gate is critical-only). (d) tests:schemaIntegrityLive:conditional-skip — standard env-guard skip, out of scope. EDGE-AND-PDF 2nd pass = CLEAN AGAIN (0 findings) -> DRAINED (2 consecutive clean passes c15+c17). consecutiveDryCycles 1->0 (the cosmetic doc-count = a new confirmed finding). refuted 15->19. PHASE-2 COVERAGE NOW COMPLETE — every key hunted: rls-security/frontend-safety/lifecycle-invariants (1 LOW each), migration-drift/types-drift (clean), edge-and-pdf (DRAINED clean×2), docs-deps-tests (1 cosmetic). Remaining = confirming/dry passes -> 3-dry stop or ~07:00 morning. 0 green; nothing pushed/touched live."
    },
    {
      "n": 18,
      "ranAt": "2026-06-20",
      "slice": "WIND-DOWN #1 — rls-security + frontend-safety (2nd confirming pass). SOLE driver (solo-checked).",
      "note": "DRY (0 genuinely-new). 1 confirmed, 0 refuted — the 1 confirmed (db-security:update_blend_ticket_billing_status:forgeable-actor) is a RE-CONFIRMATION of the already-parked cycle-14 item (the workflow verifier itself flagged it as known-parked, status=parked cycle 14); re-verified still-unfixed live + 0 frontend callers + dormant -> annotated reConfirmedCycle 18, NOT re-parked. frontend-safety 2nd pass = 0 findings. So both rls-security + frontend-safety are now confirmed effectively drained (2nd pass surfaced only the 1 known item). consecutiveDryCycles 1->1... [correction: 0->1; cycle 17's cosmetic doc-count had reset it to 0, this dry cycle takes it to 1]. STOP at 3 consecutive dry. 0 green; nothing pushed/touched live."
    },
    {
      "n": 19,
      "ranAt": "2026-06-20",
      "slice": "WIND-DOWN #2 / FINAL — lifecycle-invariants + docs-deps-tests (2nd confirming pass). SOLE driver (solo-checked).",
      "note": "LAST CYCLE — loop stopped after this. 3 confirmed + 3 refuted, NO meaningful new bug. (1) lifecycle:_enforce_job_status_transition:terminal-cancel-bypass = RE-CONFIRM of the cycle-15 jobs-cancel item, ESCALATED LOW->MEDIUM (sharper: terminal completed/invoiced jobs can be cancelled -> orphaned invoice/application_records) — annotated, severity bumped, not re-parked. (2) doc:trigger-fn-count:47-vs-49 = re-confirm of cycle-17 doc-count drift (added CLAUDE.md:274 location). (3) doc:callable-rpc-count:227-vs-226 = NEW instance of the SAME doc-count-drift class (rpc-functions.md says 227 callable RPCs, but CLAUDE.md + live = 226) — FOLDED into the existing parked doc-count item (one /update-docs pass fixes both counts). 3 REFUTED (all out-of-scope/hygiene): deps:ci-audit-level:critical-only (hygiene, dead-code advisories), tests:schemaIntegrityLive:skipif (env-guard test skip), doc:table-count:96-vs-97 (NOT drifted, miscount). === LOOP STOP DECISION === Whole app swept across 19 cycles. Cycles 16-19 yielded ZERO meaningful new bugs (clean schema/migration sweep + cosmetic doc-count off-by-ones + re-confirms) — the 'no meaningful new signal / both phases drained' state the 3-dry stop proxies for. Stopped CLEANLY ~2h before the ~07:00 morning stop rather than chase cosmetic doc-nits (which keep resetting the literal dry-counter). FINAL TALLY: 19 cycles, 32 confirmed (1 green-fixed + 31 parked), 22 refuted false-positives. Parked by severity: 5 HIGH-attention (all money-correctness, all latent), ~10 MEDIUM, ~16 LOW. NOT rescheduled. Mason restarts with 'start the overnight bug hunt'. 0 green this cycle; nothing pushed/touched live across the entire run."
    }
  ],
  "findings": [
    { "dedupeKey": "lifecycle:CommissionPayments.fetchUnpaid:cancelled-leak", "severity": "LOW", "tier": "green", "status": "fixed", "fixKind": "frontend-only", "cycle": 1, "commit": "6ded5ce", "note": "FIXED. fetchUnpaid filtered .neq(status,paid) -> leaked CANCELLED commissions into the payable list/totals. Changed to .eq(status,pending). Codex finding-gate REAL + fix-gate SHIP. CommissionPayments.tsx:143." },

    { "dedupeKey": "forgeable-actor:transfer_job_to_invoice:unbound-performed_by", "severity": "MEDIUM", "severityResolved": "MEDIUM", "claudeSeverity": "MEDIUM", "codexSeverityCycle1Gate": "HIGH", "codexSeverityHolistic": "MEDIUM", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "reConfirmedCycle": 6, "reConfirmedCycles": [2, 6], "note": "SEVERITY RESOLVED MEDIUM (2026-06-20 Codex holistic re-review): the cycle-1 Codex finding-gate rated this HIGH, but the Codex HOLISTIC review now agrees with Claude's verifier at MEDIUM (auth.uid() IS role-gated; the forgery lands in invoices.created_by + activity_feed attribution, NOT the append-only financial_audit_log; no value mutation) — so the long-standing HIGH/MED split settles to MEDIUM. ORIGINAL DISAGREEMENT: Claude verifier=MEDIUM, Codex finding-gate=HIGH. transfer_job_to_invoice role-gates auth.uid() but never binds p_performed_by (no ACTOR_MISMATCH); stamps invoices.created_by=p_performed_by and activity_feed.performed_by=COALESCE(p_performed_by,auth.uid()). Siblings save_job/complete_job/start_job DO reject mismatch. NOTE: feat/as-applied-invoices already has a PARKED strict-actor fix for this (commit 97836e8, G3; also migration 20260619140000_transfer_job_invoice_machine_fee_strict_actor.sql) — coordinate so we don't double-fix. Migration. CYCLE-2 RECONFIRM: verifier re-rated MEDIUM. CYCLE-6 RE-RECONFIRM (2 agents independently): one verifier HIGH, one MEDIUM — the MED<->HIGH spread persists, both kept for Mason." },
    { "dedupeKey": "audit-log:create_invoice_for_unbilled_delivery:missing-invoice_created-row", "severity": "MEDIUM", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "reConfirmedCycle": 7, "note": "Backfill invoice creator writes only activity_feed, no financial_audit_log 'invoice_created' row (siblings all do). Mirror create_invoice_from_order. IntegrityCleanup.tsx:316. CYCLE-7 RE-CONFIRM + REACHABILITY UPGRADE: no longer 'latent (0 prod invocations)' — cycle-7 confirmed all 4 live invoices (CS-2026-0055..0058, chemical_sale/draft, delivery-sourced) lack an invoice_created audit row, and the 65 existing invoice_created rows are ORPHANS from deleted order-path test invoices. MITIGANT: these are DRAFT invoices (no AR effect until post_invoice, which writes its own invoice_posted row), so the gap is draft-stage provenance only -> verifier holds it at MEDIUM. Fold with complete_delivery into ONE invoice_created-audit migration." },
    { "dedupeKey": "audit-log:complete_delivery:missing-invoice-created", "severity": "MEDIUM", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "reConfirmedCycle": 7, "note": "complete_delivery auto-creates a billable draft invoice but writes no financial_audit_log 'invoice_created' row. Same class as the create_invoice_for_unbilled_delivery item. Migration-only/additive. CYCLE-7 RE-CONFIRM: re-verified live (no INSERT INTO financial_audit_log in the body; the 5 invoices triggers don't write it either); the 4 live draft invoices were created via THIS path and none carries the row. Fold both delivery-invoice creators into ONE invoice_created-audit migration mirroring create_invoice_from_order." },
    { "dedupeKey": "prepay:apply_remaining_prepayments:no-ledger-double-spend", "severity": "HIGH", "severityCycle1": "MEDIUM", "severityCycle5": "HIGH", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "reConfirmedCycle": 5, "escalated": true, "note": "Bulk prepay-apply (apply_remaining_prepayments + batch_apply_all_prepayments) decrements the customer cache but writes no prepay_applications / prepay_credits.balance_cents rows -> reconcile_prepay_balances can RE-CREATE the spent balance (double-spend). Route bulk paths through the apply_prepay_to_invoice FIFO+FOR UPDATE ledger. PrepaymentManager.tsx:390/:426. CYCLE-5 ESCALATION MED->HIGH (alt dedupeKey stale-derived-state:apply_remaining_prepayments:prepay-credit-ledger-not-decremented; both the workflow verifier AND Codex finding-gate rated HIGH): cycle-1's 'verify live reachability before fixing' is now RESOLVED — both RPCs are wired to live user-triggered buttons on the shipped PrepaymentManager page (batch_apply_all_prepayments loops customers calling apply_remaining_prepayments, inheriting the defect). Mechanism confirmed live: prepay_credits.balance_cents is maintained ONLY by trg_recompute_prepay_credit_balance (AFTER INSERT OR DELETE ON prepay_applications) which this path never exercises; after it runs each credit's balance is stale-HIGH so apply_prepay_to_invoice (gates on amount > credit.balance_cents) can apply the SAME dollars a 2nd time, and per-credit->invoice linkage is lost. FIX: rewrite to consume specific prepay_credits FIFO + INSERT a prepay_applications row per (credit,invoice) slice (lets the trigger keep balance correct), mirroring apply_prepay_to_invoice — not just adjusting customers.prepay_balance_cents. Dormant on live (0 prepay rows) -> latent but fully reachable. Migration." },
    { "dedupeKey": "lifecycle:void_payment:partial-void-status-stuck-paid", "severity": "MEDIUM", "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620160000_void_payment_partial_void_status.sql", "buildValidation": "rolled-back plpgsql_check CLEAN + line-diff: only the status CASE changed (was: flip paid->posted only at paid=0; now: derive paid iff total-(new_paid+prepay+write_off)<=0 else posted, for paid/posted invoices). NOT applied live.", "fixKind": "migration", "cycle": 1, "note": "void_payment partial-void leaves the invoice status stuck at 'paid' when balance is no longer zero. Derive status from balance_cents after the reversal." },

    { "dedupeKey": "audit-log:transfer_job_to_invoice:missing-invoice_created-row", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "reConfirmedCycle": 6, "note": "transfer_job_to_invoice creates a money invoice but writes no financial_audit_log row. Fold into the same migration as the actor fix above. CYCLE-6 RE-RECONFIRM (2 agents): mechanism re-verified — only logging is the DELTA-5 activity_feed 'job_invoiced' insert; no INSERT INTO financial_audit_log; siblings create_invoice_from_order/blend_ticket + save_field_app_invoice all write an 'invoice_created' row (65 live). MITIGANT noted by verifier (keeps it LOW): post_invoice still writes 'invoice_posted' for every invoice regardless of source, so the load-bearing AR event is captured — only the draft-stage 'invoice_created' breadcrumb is missing; the sibling create_invoice_for_unbilled_delivery shares the same gap. Migration." },
    { "dedupeKey": "stale-derived-state:load_recipe_into_job:job-totals-not-recomputed", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "note": "load_recipe_into_job rewrites job_chemicals but leaves jobs.total_cost_cents/total_price_cents stale. Recompute at the tail. (Related code is also under active rework on feat/as-applied-invoices — coordinate.)" },
    { "dedupeKey": "money-cents:transfer_job_to_invoice:invoice_shares-split-rounding", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "reConfirmedCycle": 2, "note": "Field-billing split branch ROUNDs each invoice_share independently without reconciling to the invoice total (penny drift). Assign remainder to largest share. Low blast radius (0 split invoices live). ENRICHED cycle 2 (Codex-confirmed REAL): the deeper issue is the header reconciliation `UPDATE invoices SET total_amount_cents = v_share_total` is gated ONLY behind `IF v_has_price_override` — so on the NON-override path v_share_total is discarded entirely and the header stays at total_price_cents while shares may not even sum to 100 (avg_split_pct is acres-weighted, no sum-to-100 CHECK on field_billing_defaults; 4 fbd rows already live). Fix: move the reconcile UPDATE out of the override guard (always set header = sum of inserted shares) + largest-remainder the pennies. CYCLE-6 RE-RECONFIRM (2 agents): re-verified live — 1 fbd field already has a real multi-customer 50/50 split, so the first invoiced multi-customer job exercises this; verifier noted the FieldApplicationInvoice.tsx list currently renders the header total (not the per-share amount_cents), so the visible impact is in the persisted invoice_shares vs header data feeding the split bill/PDF, not that specific list view." },
    { "dedupeKey": "segregation:field_application:invoicedetail-save-desync", "severity": "HIGH", "claudeSeverityCycle1": "LOW", "verifierSeverityCycle3": "MEDIUM", "verifierSeverityCycle10": "HIGH", "tier": "yellow", "status": "DEFER-TO-feat-as-applied-invoices", "buildDecision2026-06-20": "NOT built as a standalone guard on this branch — DEFERRED to the feat/as-applied-invoices merge (Mason decision pending). GROUNDED LIVE 2026-06-20: live save_invoice DOES still flip invoice_type via COALESCE + DELETE/re-insert generic items + recompute total_amount_cents only + NO field_application guard (hole real on live, dormant: 0 field_application invoices). The COMPREHENSIVE fix already EXISTS on feat/as-applied-invoices: migration 20260619160000_save_invoice_field_app_aware.sql (DELTAs A-H: invoice_type lock, split/override lock, customer lock, invoice_shares rebalance, total_cost_cents sync, is_application_fee preservation) + FieldInvoices.tsx + InvoiceDetail preflight reroutes — NONE of which are on this branch or applied live (verified: git log shows the file only on feat/as-applied-invoices; schema_migrations mig_applied_live=false; FieldInvoices.tsx absent here). A standalone interim guard on this branch would (a) COLLIDE with feat's save_invoice rewrite on merge (both CREATE OR REPLACE save_invoice) and (b) make blend-ticket-backed field-app invoices uneditable (no field editor exists on main yet) — pure cost for a dormant hole. RECOMMENDATION: land feat/as-applied-invoices (which carries the proper fix) rather than patch here. CAVEAT/LESSON: the Explore subagent initially reported the fix as 'already present on this branch' — it had read from the PARENT checkout C:\\CRX_Manager (on feat/as-applied-invoices), the shared-tree leak class; corrected by direct git+live verification.", "fixKind": "frontend-only+migration", "cycle": 1, "reConfirmedCycle": 10, "reConfirmedCycles": [3, 10], "note": "CYCLE-10 RE-CONFIRM + KEY ENRICHMENT (verifier HIGH, up from MED): two NEW angles change the disposition. (1) save_invoice can FLIP invoice_type — it writes `invoice_type = COALESCE(p_invoice->>'invoice_type', invoice_type)` and the InvoiceDetail type <select> (InvoiceDetail.tsx:994-1002) offers a field_application option, so a field-app invoice can be silently moved to/from chemical_sale (a direct segregation/reporting leak), on TOP of the item/share desync. (2) A SERVER-SIDE GUARD CLOSES IT NOW, independent of the feat/as-applied-invoices rework: add a guard at the top of save_invoice that RAISEs when the target invoice_type='field_application' (`Field application invoices must be edited via save_field_app_invoice`). That single migration makes the generic RPC unable to rewrite OR retype a field-app invoice regardless of how the UI routes — the real boundary. So this is no longer purely 'wait for the feature-branch frontend reroute'; the defensive server guard is a cheap migration worth parking for Mason NOW (the frontend reroute remains the nicer UX, still coordinate with feat/as-applied-invoices). fixKind upgraded frontend-only -> frontend-only+migration. Still dormant: 0 field_application invoices live. ORIGINAL: field_application invoices editable via generic InvoiceDetail -> save_invoice desyncs the field-app split tables. Fix is frontend-only (route field_application rows to /invoices/field-app/:id) BUT this exact segregation is being comprehensively reworked on feat/as-applied-invoices — PARK to avoid a merge collision rather than partial-fix on main. CYCLE-3 RE-CONFIRM (dedupeKey lifecycle:field_application:generic-editor-bypass; verifier MEDIUM): grounded on OUR branch HEAD 3b20d4d6 — Invoices.tsx:682 `navigate(\\`/invoices/${row.id}\\`)` routes EVERY row to the generic InvoiceDetail; App.tsx:184-185 has the correct /invoices/field-app/:id editor; InvoiceDetail.tsx has NO field_application redirect/read-only guard (grep returns only the <option value=\"field_application\">) and editable=isNew||['draft','unposted'].includes(status) so a draft/unposted field-app invoice is fully editable; handleSave->save_invoice. Live save_invoice (pg_get_functiondef): DELETE invoice_items + re-insert only generic cols (no is_application_fee/price_source/quoted_price_cents), recompute total_amount_cents ONLY (total_cost_cents left stale -> wrong margin in get_sales_detail_report), never re-derives field_app_locations/field_app_location_shares/invoice_shares; for a grouped multi-customer split it edits ONE invoice leaving siblings inconsistent; the type <select> (InvoiceDetail.tsx:996-1000) even lets the user reassign invoice_type away from field_application. ENRICHMENT, same root defect + same fix surface — NOT a new finding. STILL PARKED: (a) feat/as-applied-invoices reworks this whole area (FieldInvoices.tsx + routing 4dfdf7ed, neither on this branch) -> merge-collision risk; (b) the naive 'route ALL field_application to field-app editor' is PARTIALLY WRONG — blend-ticket-backed field-app invoices (job_id NULL, blend_ticket_id set) intentionally use the generic editor; the correct fix redirects only engine-built ones. Latent (0 field_application invoices live)." },
    { "dedupeKey": "concurrency:create_quick_delivery:duplicate-line-oversell", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "reConfirmedCycle": 4, "note": "create_quick_delivery validates inventory per-line independently, so duplicate product lines in one call can over-prebook. Aggregate by product_id before the net-available check. Defense-in-depth (UI already dedupes). CYCLE-4 RE-CONFIRM (alt dedupeKey concurrency:create_quick_delivery:duplicate-product-oversell; verifier LOW): grounded against the live fn — two-loop structure, the validation loop reads quantity_prebooked WITHOUT per-product accumulation and FOR UPDATE locks against other txns only; the prebook UPDATE is only in the 2nd loop; so one call listing the same product_id twice passes both checks then deducts both -> net-free negative. UI-mitigated: QuickDeliveryModal.tsx:174 merges duplicate products into one summed line; the sole prod caller QuickDeliveryModal.tsx:232 passes the deduped array. Insider-only reachable (admin/sales_rep/driver hand-crafting a raw RPC payload). Same finding as cycle 1 — enriched, not re-parked." },
    { "dedupeKey": "blend:create_invoice_from_blend_ticket:prepaid-rebill-gap", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 1, "note": "Re-bill guard only blocks payment_status='billed'; a ticket manually set to 'prepaid'/'no_charge' can still generate a second invoice. Widen guard to allow billing only from 'unbilled'." },

    { "dedupeKey": "concurrency:save_field_app_invoice:no-row-lock", "severity": "MEDIUM", "claudeSeverity": "MEDIUM", "codexSeverity": "MEDIUM", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 2, "note": "NEW cycle 2 (Codex-confirmed REAL, MEDIUM — agrees with Claude verifier). save_field_app_invoice group-edit branch reads status with a PLAIN unlocked SELECT (no FOR UPDATE), checks status once, then unconditionally DELETEs+rewrites invoice_items/invoice_shares/field_app_locations for the whole group; its later `UPDATE invoices SET total_amount_cents` has no `WHERE status IN(...)` recheck. post_invoice/post_invoice_group both lock FOR UPDATE first. Race: a concurrent edit can rewrite the line items/total of an invoice a concurrent post just flipped to 'posted' -> POSTED doc diverged from financial_audit_log 'invoice_posted' + generate_rup_sales_records. Latent (0 field_application invoices, 0 posted invoices live). FIX: lock first — `PERFORM 1 FROM invoices WHERE (id=p_invoice_id OR invoice_group_id=v_existing_group_id) FOR UPDATE;` and re-read status under the lock (mirror post_invoice_group). Migration. Evidence: 20260616191740_blend_and_fieldapp_invoice_audit_rows.sql (save_field_app_invoice edit branch ~L500/L517/L823); post FOR UPDATE in 20260614144252_pricing_status_columns_and_post_gate.sql:59/116." },
    { "dedupeKey": "stale-derived-state:save_invoice:total_cost_cents-not-recomputed", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 2, "note": "NEW cycle 2 (Codex-confirmed REAL, LOW). save_invoice(jsonb,jsonb,text) edit path DELETEs+re-inserts invoice_items (with per-line cost_cents) then its only header UPDATE sets total_amount_cents+updated_at — total_cost_cents (is_generated=NEVER, default 0) never recomputed; no invoice_items trigger rolls it up. Siblings create_invoice_for_unbilled_delivery + save_field_app_invoice DO maintain it. Latent (live reports recompute cost from invoice_items, not the stored header). FIX: accumulate v_cost_total in the item loop, set total_cost_cents in the same final UPDATE. Part of the recurring total_cost_cents class — fold with create_invoice_from_order into ONE migration. Latest definer: 20260510080000_bulk_idempotency_wiring.sql (~L333/L349/L363)." },
    { "dedupeKey": "audit-log:save_field_app_invoice:orphan-cancel-no-audit", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 2, "note": "NEW cycle 2 (Codex-confirmed REAL, LOW). save_field_app_invoice orphan-cancel loop (customer dropped from a group during edit) UPDATEs invoices SET status='cancelled', total_amount_cents=0, total_cost_cents=0 then INSERTs ONLY an activity_feed 'invoice_orphan_cancelled' row — no financial_audit_log row, even though the SAME fn writes an 'invoice_created' row at creation and void_invoice writes 'invoice_cancelled' for the identical draft/unposted->cancelled transition. Append-only ledger sees a created invoice with no terminating event. Audit-trail parity gap; latent (field-app billing ~0 live). FIX: write a financial_audit_log row (operation_type 'invoice_cancelled', entity_type 'invoice', total_impact_cents=-original_total) in the orphan loop, mirroring void_invoice. Evidence: 20260616191740_...audit_rows.sql orphan loop ~L560/L568 vs creation audit ~L832; void_invoice ref 20260529214538_...status_transitions.sql:291." },
    { "dedupeKey": "stale-derived-state:create_invoice_from_order:total_cost_cents-zero", "severity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 2, "note": "NEW cycle 2 (Codex-confirmed REAL, LOW). create_invoice_from_order(uuid,uuid,text,text) inserts invoice_items with cost_cents=round(cost_per_unit*100) but its only header UPDATE sets total_amount_cents — total_cost_cents stays at column default 0. It is the LONE invoice-creator that omits this (the other 4 all set it). Order-direct (delivery_id NULL) path is rare/dormant (0 such invoices live; W5 delivery guard routes most invoices through the delivery path); reports recompute cost from invoice_items so impact is latent. FIX: accumulate cost in the item loop + add total_cost_cents to the final UPDATE. Fold with save_invoice into ONE total_cost_cents migration. Evidence: 20260614150249_invoice_from_order_delivery_guard.sql:97/107/116." },

    { "dedupeKey": "stale-derived-state:complete_delivery:partial-rebill-cost", "severity": "LOW", "severityCycle4": "MEDIUM", "severityCycle7": "LOW", "reScopedCycle": 7, "claudeSeverity": "MEDIUM", "codexSeverity": "MEDIUM", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 4, "note": "RE-SCOPED MED->LOW in cycle 7 (corrected impact). The CODE DEFECT is real & unchanged: complete_delivery's partial re-bill branch `IF v_any_partial AND v_delivery.order_id IS NOT NULL` trues up REVENUE (invoice_items.quantity/extended_cents; invoices.total_amount_cents=SUM(extended_cents)) but NEVER recomputes the stored invoices.total_cost_cents — so on a partial completion the stored header cost stays sized to the full ordered qty. BUT the cycle-4 IMPACT framing ('month-end gross margin understated; feeds MonthEndClose COGS') was WRONG: cycle-7 deeper consumer analysis found get_monthly_summary recomputes COGS as SUM(invoice_items.cost_cents * quantity) (cost_cents is per-unit, quantity is scaled DOWN on partial = CORRECT) and is scoped to status IN ('posted','overdue'), so it never reads the stale stored column and DRAFT invoices (the only state this produces) are excluded. The ONLY real residual consumer of the stale stored column is InvoiceDetail.tsx:734 (`invoice.total_cost_cents ?? recompute` — prefers the stored value) -> a draft-invoice profit/COGS DISPLAY could be inflated. So: real coherence/display defect, LOW (draft-stage, single frontend line), NOT a month-end ledger error. DEDUPE-ADJUDICATED earlier by Codex: distinct from the already-fixed join fix and the cycle-1 refuted total-cost item. FIX (still worth it for coherence): recompute the stored total_cost_cents = SUM(line cost_cents * quantity) over the trued lines in the partial re-bill branch (cost_cents is PER-UNIT — do NOT multiply by 100). Latent: quick_deliveries=0, delivery_remainders=0, invoices=4/0 posted. Evidence: 20260616140912_complete_delivery_partial_rebill_join_order_item.sql (rebill ~L215-228); InvoiceDetail.tsx:734; get_monthly_summary recomputes from invoice_items." },
    { "dedupeKey": "money:update_order_items:commission-recompute-rounding", "severity": "LOW", "claudeSeverity": "LOW", "codexSeverity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 4, "reConfirmedCycle": 7, "note": "NEW cycle 4 (Codex-confirmed REAL, LOW — matches Claude verifier; downgraded MED->LOW on live calibration). CYCLE-7 RE-CONFIRM (enriched reachability: 34 commissions/32 pending, 2 orders already carry multi-recipient splits, 17 editable orders; 0 reconciling triggers on commissions). The commission-recompute block added to update_order_items (disk 20260617115903 / marker 20260617040000; latest definer 20260617123503:288-290) sets each pending row INDEPENDENTLY: commission_amount=compute_commission_amount(o.total_profit,c.split_percentage)=GREATEST(ROUND(profit*pct/100,2),0) — NO reconciliation. The CREATE path _insert_commissions_for_order reconciles: LAST split row (by ORDINALITY) = ROUND(profit,2) - SUM(earlier rows) (20260526151856...sql:232-240/260-265), so create-path parts always sum to profit; edit-path drifts +-1c for UNEVEN/3-way splits and the two paths disagree for the identical order/split. DISTINCT from already-fixed commissions:order-item-edit:no-recompute (4b93cdc = the ADDITION of the block); this is the NEW observation that the added block's rounding doesn't reconcile. Latent: ALL live splits are 100% or even 50/50 (no drift today; live drift only once an uneven/3-way split exists AND a line is edited). Drift proven live: compute_commission_amount sums 33.33/33.33/33.34 on $10 -> 9.99. FIX: route the edit recompute through the same reconciliation as _insert_commissions_for_order (e.g. delete pending-not-in-payment commissions + re-call the insert helper), so there is ONE reconciliation code path. Migration." },

    { "dedupeKey": "prepay:apply_remaining_prepayments:status-not-paid", "severity": "MEDIUM", "claudeSeverity": "MEDIUM", "codexSeverity": "MEDIUM", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 5, "note": "NEW cycle 5 (Codex-confirmed REAL, MEDIUM — matches verifier). DISTINCT from the no-ledger-double-spend item above (this is the status flip, not the ledger). apply_remaining_prepayments FIFO loop does only UPDATE invoices SET prepay_applied_cents=COALESCE(...,0)+v_apply, updated_at=now() — never writes status. balance_cents is GENERATED ALWAYS (total - paid - prepay_applied - write_off) so it reaches 0, but NO invoices trigger derives status from balance (only enforce_invoice_status_transition, BEFORE UPDATE OF status, which never fires since status isn't in the UPDATE col list). The sibling apply_prepay_to_invoice explicitly sets status=CASE WHEN (...)<=0 THEN 'paid' END on the identical op. So a batch-prepay-settled invoice stays status='posted' with balance 0 forever; status-keyed AR/aging/reports misclassify it as open (balance-keyed AR is fine — balance_cents is the source of truth). Distinct from cycle-1 lifecycle:void_payment:partial-void-status-stuck-paid (different RPC/direction). Latent (0 posted invoices, 0 prepay_applied live). FIX: in the loop, set status='paid' when recomputed balance reaches 0 (mirror apply_prepay_to_invoice's CASE), respecting enforce_invoice_status_transition. Migration." },
    { "dedupeKey": "blend:sync_blend_ticket_payment_status:multi-invoice-over-reset", "severity": "HIGH", "claudeSeverity": "MEDIUM", "codexSeverity": "HIGH", "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620140000_blend_payment_status_overreset_orphan_guards.sql", "buildValidation": "rolled-back plpgsql_check CLEAN + normalized line-diff shows the old unguarded UPDATE replaced by the NOT-EXISTS-guarded UPDATE, nothing else altered. NOT applied live.", "fixKind": "migration", "cycle": 5, "note": "NEW cycle 5 (Codex-confirmed REAL — DISAGREEMENT: Codex HIGH vs workflow-verifier MEDIUM-on-dormancy; BOTH kept for Mason). Voiding ONE invoice of a grouped (multi-customer) blend ticket resets the WHOLE ticket to 'unbilled' -> re-billable -> DOUBLE-BILLING. create_invoice_from_blend_ticket, when v_customer_count>1, assigns a shared invoice_group_id and inserts MULTIPLE invoices all carrying the same blend_ticket_id, then sets blend_tickets.payment_status='billed' ONCE; its only re-bill guard is IF v_ticket.payment_status='billed' THEN RAISE 'already billed'. The trigger trg_sync_blend_ticket_payment (AFTER UPDATE ON invoices WHEN status distinct) -> sync_blend_ticket_payment_status does, when one invoice goes voided/cancelled, an UNCONDITIONAL UPDATE blend_tickets SET payment_status='unbilled' WHERE id=NEW.blend_ticket_id AND payment_status='billed' with NO check for OTHER surviving non-voided invoices. So voiding one invoice of a grouped ticket flips the ENTIRE ticket back to 'unbilled' while siblings stay posted -> create_invoice_from_blend_ticket generates a SECOND full set of invoices = double-billing every customer on the ticket. Distinct from cycle-1 blend:create_invoice_from_blend_ticket:prepaid-rebill-gap (that was the manual prepaid/no_charge bypass). Dormant (0 blend tickets / 0 blend invoices / 0 grouped / 0 billed live) -> latent but reachable the moment the first multi-customer blend ticket is billed. FIX: guard the reset with NOT EXISTS (SELECT 1 FROM invoices WHERE blend_ticket_id=NEW.blend_ticket_id AND status NOT IN ('voided','cancelled')) — only reset to 'unbilled' when NO live invoices remain. Evidence: 20260335100000_workflow_gaps_phase2_blend_ticket_invoice.sql ~L196-202 (sync fn, no NOT EXISTS guard); 20260616191740...sql:99/168 (grouped create). Migration. CYCLE-11 FALSE-REFUTATION REJECTED + RE-AFFIRMED HIGH: a cycle-11 verifier tried to refute this as FALSE_POSITIVE claiming 'blend_tickets has a single scalar customer_id so no multi-customer group exists, any two invoices for one ticket are the same customer.' That premise is FACTUALLY WRONG — I verified the live function body (20260616191740:108-178): create_invoice_from_blend_ticket derives its customers from FIELD OWNERSHIP, NOT from blend_tickets.customer_id: `v_shares := derive_customer_shares_from_fields(v_field_ids, v_applied_acres_map); v_customers := v_shares->'customers'; v_customer_count := jsonb_array_length(v_customers); IF v_customer_count > 1 THEN v_invoice_group_id := gen_random_uuid(); ... FOR v_customer IN SELECT * FROM jsonb_array_elements(v_customers) LOOP` (one invoice per owning customer, shared invoice_group_id). So a blend ticket whose field(s) are co-owned by multiple customers DOES fan out into multiple invoices for DIFFERENT customers — exactly the multi-customer group the refutation said doesn't exist. The cycle-11 verifier only checked the scalar blend_tickets.customer_id column and missed derive_customer_shares_from_fields. The HIGH STANDS (already Codex-confirmed cycle 5). LESSON: when checking 'can entity X span multiple customers?', trace the RPC's actual customer derivation, not just a scalar FK column. CYCLE-13 INDEPENDENT RE-CONFIRM: a fresh cycle-13 prepay-blend agent independently re-found this and its verifier re-verified the live bodies — create_invoice_from_blend_ticket builds invoice_group_id + loops ONE invoice per derived customer when v_customer_count>1, and the trigger keys only on NEW.blend_ticket_id with no group/EXISTS guard — fully corroborating cycle-5/the cycle-11 correction. (cycle-13 verifier rated MEDIUM-on-dormancy; the cycle-5 Codex HIGH remains on record — MED/HIGH split kept for Mason.) reConfirmedCycle 13." },
    { "dedupeKey": "splits:update_allocation_set:no-sum-validation", "severity": "LOW", "claudeSeverity": "LOW", "codexSeverity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 5, "note": "NEW cycle 5 (Codex-confirmed REAL, LOW; DEAD RPC — no frontend caller, 0 rows). update_allocation_set(p_entity_type text, p_entity_id uuid, p_splits jsonb, p_reason_note text) inserts client-supplied (split->>'percentage')::numeric and (split->>'amount_cents')::bigint straight into order_line_allocations / invoice_line_allocations with NO check that SUM(percentage)=100 or SUM(amount_cents)=the line/entity total; NO p_idempotency_key (double-submit deactivates the prior version + inserts a fresh duplicate); never PERFORMs check_period_open. It IS admin-gated (require_admin) and DOES write a financial_audit_log 'split_modified' row. The line-allocation tables have NO triggers; the only allocation trigger (_enforce_allocation_not_over_payment on allocation_sets) checks total_allocated_cents vs total_payment_cents — cols this RPC never writes. reconciliation.ts:701-714 reads invoice_line_allocations.amount_cents as a payment source-of-truth -> a malformed split could later be misread. No .rpc('update_allocation_set') caller in src (only generated types + a fixture test). RECOMMENDATION (Codex agrees): since dead + 0 rows, RETIRE via DROP (like the dead create_invoice_from_delivery precedent, 20260617210000) rather than patch — but verify zero callers at apply time. Evidence: 20260213100000_phase2_billing_architecture.sql:917-991. Migration." },
    { "dedupeKey": "money:derive_customer_shares_from_fields:acre-share-rounding", "severity": "LOW", "claudeSeverity": "LOW", "codexSeverity": "LOW", "tier": "yellow", "status": "parked-coordinate", "fixKind": "migration", "cycle": 5, "note": "NEW cycle 5 (Codex-confirmed REAL, LOW; precision tie-out parking item only). derive_customer_shares_from_fields sets share_acres=ROUND(applied_acres*split_pct/100.0, 2) per owner INDEPENDENTLY (and total_share_acres=ROUND(SUM(...),2)) with NO largest-remainder reconcile — unlike create_split_invoices_from_order's calculate_billing_splits. save_field_app_invoice then bills grower-share + service-fee lines as rate*share_acres (safe_cents_qty=ROUND(cents*qty), no double-round) and chemical lines as unit_price*SUM(rate*share_acres) — all on the rounded share_acres. Live repro: 33.333/33.333/33.334 of 100.00 acres -> SUM(ROUND(.,2))=99.99 (drift -0.01 acre). CAVEAT (important): this is PRECISION LOSS in the acre basis, NOT a conserved-whole money violation — each owner is a SEPARATE invoice billing their own real acres (no single total mis-split across invoices); so no customer is over/under-billed vs what they genuinely owe. Real effects: (a) sub-cent-of-acre rounding of the split definition; (b) per-owner field_app_location_shares.acres don't tie back to the field's applied_acres for year-end/RUP usage reporting. Distinct from the DEFERRED money:create_split_invoices_from_order:independent-per-line-rounding (different function). PARKED-COORDINATE: derive_customer_shares_from_fields + save_field_app_invoice are the field-app engine being reworked on feat/as-applied-invoices — fold the acre largest-remainder reconcile into that rework. Dormant (0 field_application invoices, 0 field_app_locations live). Evidence: 20260429140635_field_app_workflow_phase1.sql:134-151; 20260616191740...sql:658-662/697-701/903-910. Migration. CYCLE-11 RE-EXAMINATION (alt dedupeKey money:derive_customer_shares_from_fields:share-acres-rounding; verifier leaned FALSE_POSITIVE): independently re-confirmed the SAME substance cycle-5 already noted — field-app billing is per-customer-OWN-acres (no fixed dollar 'whole' being split), so the missing largest-remainder reconcile is structurally inapplicable, not a conserved-whole violation; the acre cols are numeric(12,2) so the 2-dec ROUND matches storage precision; preview==save math (no visible mismatch); and live split_pct values are only 50/100 (evenly divisible) so the 33.33×3 drift can't occur on current data. NET: keep parked at the LOWEST priority as an acre-precision tie-out nicety, NOT a money bug — effectively a near-refute; safe to drop if Mason wants. Both cycles agree it's not a correctness/money-conservation defect. CODEX HOLISTIC REVIEW 2026-06-20: the ONLY one of 31 parked findings Codex flagged as NOT-REAL/droppable ('per-owner acre rounding is not a conserved-money/whole-split defect; cosmetic tie-out only, safe to drop'). RECOMMENDATION: drop this from the actionable list (it's the 1 finding all of Claude-verifier + Claude-c11 + Codex-holistic agree is not worth fixing)." },

    { "dedupeKey": "stale-derived-state:save_job:client-trusted-total-vs-lines", "severity": "LOW", "claudeSeverity": "LOW", "codexSeverity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 6, "note": "NEW cycle 6 (Codex-confirmed REAL, LOW). transfer_job_to_invoice sets the invoice header invoices.total_amount_cents = COALESCE(v_job.total_price_cents,0) — a CLIENT-TRUSTED value (save_job persists the frontend total_price_cents verbatim, no server-side SUM over job_chemicals; load_recipe_into_job inserts chem price_per_unit_cents=0 and never recomputes the job total; no trigger recomputes it) — while each invoice_items.extended_cents is INDEPENDENTLY recomputed as safe_cents_qty(jc.price_per_unit_cents, jc.quantity). The two are never cross-checked and invoices has NO CHECK that header==sum(lines), so a job whose stored total is stale vs its chem lines yields an invoice whose header != sum of its own lines. CODEX DEDUPE-ADJUDICATED: DISTINCT from cycle-1 stale-derived-state:load_recipe_into_job:job-totals-not-recomputed (that's one stale-total PRODUCER; the residual invoice defect is transfer_job_to_invoice TRUSTING the stored total instead of summing its inserted lines). UI-protected: JobDetail.tsx:407/531 re-derive total from chemRows on every save + :731 re-fetches after a recipe load, so divergence is only reachable via a hand-crafted direct save_job RPC payload (total != sum of its own chem lines); handleTransferToInvoice (:692) doesn't save first = the real residual gap. Dormant (jobs=1, invoiced=0, field_application invoices=0). FIX: have transfer_job_to_invoice derive the header from SUM(safe_cents_qty(price_per_unit_cents,quantity)) over job_chemicals (or recompute jobs.total_price_cents server-side in save_job/load_recipe_into_job). Evidence: 20260609190820_save_job_strict_actor.sql:89-91/118-120; 20260611002255_transfer_job_invoice_column_fixes.sql:190-204/216-247. Migration." },

    { "dedupeKey": "commissions:cancel_void_order:batched-commission-resurrected-paid", "severity": "HIGH", "claudeSeverity": "HIGH", "codexSeverity": "HIGH", "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620130000_commission_batch_freeze_guard.sql", "buildValidation": "rolled-back plpgsql_check CLEAN on all 4 fns + Postgres-normalized line-diff shows ONLY the guard blocks added, ZERO lines removed/altered from the live bodies. NOT applied live (Mason batch-approves). Reviewers+Codex+apply-guard still gate the apply.", "fixKind": "migration", "cycle": 8, "reConfirmedCycle": 9, "thirdPath": "cancel_delivery", "capturedFrom": "this-session commissions+deliveries hunt during the two-loop overlap; MISSED by the parallel run's cycle-4/cycle-7 commission passes", "note": "CYCLE-9 RE-CONFIRM + ENRICHMENT: the cycle-9 fresh commissions hunt independently re-found this (verifier HIGH) AND surfaced a THIRD affected cancel path — cancel_delivery(uuid,text,uuid,text) runs the identical `UPDATE commissions SET status='cancelled', commission_amount=0 WHERE order_id=v_delivery.order_id AND status='pending'` with no batch awareness, so the fix must cover cancel_order + void_order + cancel_delivery (all three). cycle-9 also confirmed post_commission_payment writes the financial_audit_log row using v_payment.total_amount (the STALE pre-cancel snapshot), and that 0 of the 8 live unposted batches currently hold any commission_payment_items (dormant -> no realized loss yet). Defense-in-depth half of the fix refined: in post_commission_payment add `AND status='pending'` AND RAISE if the updated row count < the batch's item count (so a batch that lost a commission to cancellation cannot post). ORIGINAL (captured cycle 8; Codex finding-gate REAL/HIGH, matches workflow verifier — no disagreement). Cancelling/voiding an order can later PAY OUT a commission that was already cancelled, for an order that no longer exists. CHAIN: create_commission_payment inserts commission_payment_items per selected commission but leaves commissions.status='pending' (only post_commission_payment flips to 'paid') [20260611001439:191-210]. cancel_order AND void_order both `UPDATE commissions SET status='cancelled', commission_amount=0 WHERE order_id=p_order_id AND status='pending'` with NO exclusion of commissions already linked into a non-voided commission_payments batch [void_order 20260613191323:267-270; cancel_order 20260616142001:159-163]. commission_payment_items.commission_id FK is NO-CASCADE [20260217210000:44-45], so the batch item + commission_payments.total_amount snapshot survive the status flip. post_commission_payment then `UPDATE commissions SET status='paid' WHERE id IN (SELECT commission_id FROM commission_payment_items WHERE commission_payment_id=p_payment_id)` with NO status filter [20260501150000:268-274] -> resurrects the cancelled (amount=0) commission to 'paid' and the batch pays out the stale snapshot. The SAME batch-freeze guard (`AND NOT EXISTS (SELECT 1 FROM commission_payment_items cpi JOIN commission_payments cp ON cp.id=cpi.commission_payment_id WHERE cpi.commission_id=c.id AND cp.status <> 'voided')`) was added to update_order_items 'Codex P1' [20260617123503:288-305] but NEVER to cancel_order/void_order. DISTINCT from the cycle-4 refuted report:get_commission_balance_report:cancelled-in-earned (that was the earned-report aggregation, fine because cancelled rows are amount=0; THIS is the batch PAYOUT path resurrecting status + paying the retained snapshot). Latent now (live: 8 commission_payments batches but 0 commission_payment_items, 0 paid, 32 pending) but every step on a live wired path (OrderDetail.tsx cancel/void; CommissionPayments.tsx create/post) -> fires the first real payout cycle where a batched order is cancelled/voided then the batch is posted. FIX (migration): add the NOT-EXISTS batch-freeze guard to the commission UPDATE in cancel_order + void_order (or hard-block the cancel/void when such a non-voided batch exists, forcing void-the-batch-first), PLUS defense-in-depth `AND status='pending'` on post_commission_payment's commission flip (and reconcile commission_payments.total_amount if any rows drop)." },

    { "dedupeKey": "money:apply_prepay_to_invoice:cross-customer-misapplication", "severity": "HIGH", "claudeSeverity": "MEDIUM", "codexSeverity": "HIGH", "tier": "yellow", "status": "parked", "fixKind": "migration", "cycle": 9, "note": "NEW cycle 9 (Codex-confirmed REAL — DISAGREEMENT: Codex finding-gate=HIGH vs workflow-verifier=MEDIUM-on-dormancy; BOTH kept for Mason). apply_prepay_to_invoice(uuid,uuid,bigint,uuid,text) loads v_credit (prepay_credits FOR UPDATE) and v_inv (invoices FOR UPDATE) but NEVER compares v_credit.customer_id to v_inv.customer_id. It then debits prepay_credits.balance_cents WHERE id=p_prepay_credit_id (customer A's credit), bumps invoices.prepay_applied_cents on p_invoice_id, and decrements customers.prepay_balance_cents WHERE id=v_inv.customer_id (customer B) — so A's prepaid credit can pay down B's invoice, and B's denormalized prepay cache is wrongly reduced (GREATEST-clamped at 0, desyncing from the true sum of B's prepay_credits). No mitigation: prepay_applications has no same-customer CHECK; its triggers are an immutability BEFORE-UPDATE guard (doesn't fire on the INSERT) + a balance recompute (neither enforces customer match); SECURITY DEFINER bypasses RLS; batch_apply_prepayments forwards caller-supplied {prepay_credit_id, invoice_id} pairs verbatim. Frontend PrepayWorkspace.tsx:96-105 scopes both dropdowns to one selectedCustomerId, but that's client-side only — the DB never re-validates. Reachable by any admin/sales_rep calling the RPC directly or via a forged batch payload. Latent today (0 prepay_credits / 0 prepay_applications / 0 customers w/ nonzero prepay_balance_cents live). FIX (migration): after loading v_credit + v_inv, add `IF v_credit.customer_id IS DISTINCT FROM v_inv.customer_id THEN RAISE EXCEPTION 'CUSTOMER_MISMATCH ...'; END IF;` — protects both the direct path and batch_apply_prepayments. Fold into the same migration as the already-parked prepay:apply_remaining_prepayments fixes. Evidence: 20260511100000_apply_prepay_to_invoice_hardening.sql:90-130 (no customer compare); prepay_applications FKs only 20260213100000_phase2_billing_architecture.sql:149-170; immutability/recompute triggers 20260512010000 / 20260512050000 don't enforce match." },

    { "dedupeKey": "lifecycle:order_item_field_allocations:no-edit-lock", "severity": "LOW", "claudeSeverity": "LOW", "codexSeverity": "LOW", "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620180000_oifa_post_invoice_edit_lock.sql", "buildValidation": "rolled-back plpgsql_check CLEAN (relid order_item_field_allocations) + trigger attaches (TRIGGER_ATTACHED=1). New fn prevent_oifa_edit_after_post mirrors prevent_order_shares_edit_after_post, resolving order via order_item_id->order_items.order_id (oifa has no order_id col). NOT applied live.", "fixKind": "migration", "cycle": 11, "note": "NEW cycle 11 (Codex-confirmed REAL, LOW — matches verifier). order_item_field_allocations has NO post-invoice edit-lock trigger, while its sibling order_shares DOES (prevent_order_shares_edit_after_post / trg_order_shares_lock_when_posted RAISEs 'Cannot modify bill split — invoice % is already posted' once the order has any status IN ('posted','paid','overdue') non-deleted invoice). The UI writes/deletes order_item_field_allocations via direct PostgREST with no status gate (OrderDetail.tsx ~:752 insert / :769 delete); RLS gates on role only. create_split_invoices_from_order is the ONLY reader and has an active-invoice guard, so NO active double-bill — the residual exposure is: generate split invoices -> void/cancel all -> admin edits allocations -> re-run -> regenerated invoices reflect the EDITED (not originally-billed) split = audit-trail/reporting divergence. Dormant (0 oifa rows, 0 split invoices live). FIX (migration): add an edit-lock trigger mirroring prevent_order_shares_edit_after_post. Evidence: table/RLS-only 20260617143755_order_item_field_allocations.sql:24-55; order_shares lock 20260504100000_lock_order_shares_when_invoice_posted.sql:20-61; split RPC active-invoice guard 20260617164803_rewrite_create_split_invoices_by_acre.sql:125-132." },

    { "dedupeKey": "money:preview_finance_charges:overdue-set-divergence", "severity": "MEDIUM", "claudeSeverity": "MEDIUM", "codexSeverity": "MEDIUM", "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620170000_finance_charge_preview_match_generate.sql", "buildValidation": "rolled-back plpgsql_check CLEAN + line-diff: preview's base set aligned to generate EXACTLY (status='posted'->IN('posted','overdue'); flat due_date + post-hoc grace -> grace-aware per-invoice due_date via a customers join). Only preview changed; generate is the source of truth. NOT applied live. Future hardening (noted, not done): shared STABLE helper + parity test.", "fixKind": "migration", "cycle": 12, "note": "NEW cycle 12 (Codex-confirmed REAL, MEDIUM — matches verifier). preview_finance_charges and generate_finance_charges select DIFFERENT overdue-row sets, so the finance-charge amount an admin APPROVES in the preview can differ from what generate actually bills. preview_finance_charges (latest definer 20260530121737:497-509) builds its base `WHERE i.status='posted' ... AND i.due_date < p_as_of_date` and applies grace only AFTER aggregation (max_days_overdue > grace_days). generate_finance_charges (20260526151856:1300-1305) builds its base `WHERE i.status IN ('posted','overdue') ... AND i.due_date < (p_as_of_date - (grace_days||' days')::interval)`. mark_overdue_invoices() (active pg_cron '0 6 * * *', 20260332400000/20260316121800) flips posted->overdue daily, so the MOST-overdue invoices carry status='overdue' — exactly the rows preview's status='posted' filter DROPS but generate KEEPS. The divergence was introduced by 20260526151856 rewriting generate (status IN + grace-aware) WITHOUT re-emitting preview. NARROWING (verifier+Codex): the sole caller FinanceChargePreviewModal.tsx:140-143 passes ONLY the preview-shown customer_ids to generate, so a customer ENTIRELY hidden by preview is also excluded from generate (NOT a hidden surprise bill) — the real harm is a SHOWN customer (who also has overdue-status/inside-grace invoices) being billed a DIFFERENT amount than the previewed/approved one. Dormant (live: 0 posted, 0 overdue, 0 finance_charges; 2 customers have a finance_charge_rate, 1 has grace). FIX (migration): make preview + generate share ONE overdue-set definition (same status filter IN ('posted','overdue') + same grace-aware due_date predicate), ideally a single STABLE helper both call, + a test asserting preview total == generate total for the same as_of_date." },

    { "dedupeKey": "blend:delete_invoices:payment-status-orphan", "severity": "MEDIUM", "claudeSeverity": "MEDIUM", "codexSeverity": "MEDIUM", "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620140000_blend_payment_status_overreset_orphan_guards.sql", "buildValidation": "rolled-back plpgsql_check CLEAN + normalized line-diff: only the loop SELECT gained blend_ticket_id + the orphan-reset block added, nothing else altered. NOT applied live.", "fixKind": "migration", "cycle": 13, "note": "NEW cycle 13 (Codex-confirmed REAL, MEDIUM — matches verifier). Soft-deleting a DRAFT blend-ticket invoice permanently orphans the ticket at payment_status='billed' so it can NEVER be re-billed. Chain: create_invoice_from_blend_ticket inserts blend invoices status='draft' and sets blend_tickets.payment_status='billed' (20260616191740:168-179/402), guarded by `IF v_ticket.payment_status='billed' THEN RAISE 'Blend ticket already billed'` (99-100). delete_invoices() soft-deletes via `UPDATE invoices SET deleted_at=now()` ONLY, never changing status or touching blend_tickets (20260617031416:58-68). The reset trigger trg_sync_blend_ticket_payment fires `AFTER UPDATE WHEN (old.status IS DISTINCT FROM new.status)` and resets payment_status only on a transition INTO voided/cancelled (20260335100000:197-213) — a deleted_at write is NOT a status change so the trigger never fires; no other trigger watches deleted_at. NET: soft-delete the draft blend invoice -> ticket stuck 'billed' with zero live invoices -> create_invoice_from_blend_ticket rejects it forever (manual DB surgery to recover). Mirror-image of the parked over-reset HIGH (that double-bills; THIS strands). Dormant (0 blend tickets / 0 blend invoices live). FIX (migration): in delete_invoices, when the deleted invoice carries a blend_ticket_id and no other live invoice remains for the ticket, also `UPDATE blend_tickets SET payment_status='unbilled' WHERE id=... AND payment_status='billed'` (same group-aware predicate as the over-reset fix — fold both blend fixes into one migration). Frontend: Invoices.tsx:349-383 handleBatchDelete -> delete_invoices." },

    { "dedupeKey": "db-security:update_blend_ticket_billing_status:forgeable-actor", "severity": "LOW", "claudeSeverity": "LOW", "codexSeverity": "MEDIUM", "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620140000_blend_payment_status_overreset_orphan_guards.sql", "buildValidation": "rolled-back plpgsql_check CLEAN + normalized line-diff: only the actor-bind/ACTOR_MISMATCH guard added + activity_feed performed_by switched p_performed_by->v_actor. NOT applied live.", "fixKind": "migration", "cycle": 14, "phase": 2, "note": "NEW cycle 14 (Phase 2 / rls-security; Codex-confirmed REAL — severity SPLIT: verifier LOW, Codex MED, both kept). update_blend_ticket_billing_status(uuid,text,uuid) is signed `p_performed_by uuid DEFAULT auth.uid()` and writes the RAW caller-supplied p_performed_by into activity_feed.performed_by ('blend_ticket_status_changed') without binding v_actor:=auth.uid() or rejecting a mismatch — so any admin/sales_rep can forge another user's UUID as the actor of a blend-ticket payment-status change. Its siblings link/unlink_blend_ticket_to_order DO carry the ACTOR_MISMATCH guard (hardened a3ba49c / 20260617171500:26-30); this one was the leftover outlier. SEVERITY: verifier LOW because (a) target is activity_feed (operational log), NOT the append-only financial_audit_log; (b) ZERO frontend callers (UI uses create_invoice_from_blend_ticket/link/unlink — forgery needs a hand-crafted API call); (c) dormant (0 blend_tickets, 0 status-changed events). Codex rated MED — note Codex flagged 'no role gate' citing migration 20260213120000:441-475, but the LIVE fn (per verifier pg_get_functiondef) DOES `PERFORM require_admin_or_sales_rep()` (Codex likely read an older migration); the real residual is only the missing actor-match. FIX (migration, PARK): bind v_actor:=auth.uid() + RAISE ACTOR_MISMATCH on mismatch, write v_actor; mirror link_blend_ticket_to_order. CYCLE-18 RE-CONFIRM (2nd rls-security pass): independently re-found + re-verified live (still unfixed; require_admin_or_sales_rep present but no v_actor bind / no ACTOR_MISMATCH; raw p_performed_by -> activity_feed; siblings link/unlink hardened). 0 frontend callers; blend_tickets=0, 0 'blend_ticket_status_changed' rows of 1271 activity_feed. Still LOW, still parked." },
    { "dedupeKey": "frontend-safety:checkMutationResult:no-coverage-gate", "severity": "LOW", "claudeSeverity": "LOW", "codexSeverity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "docs-or-test", "cycle": 14, "phase": 2, "reTieredFromGreen": true, "note": "NEW cycle 14 (Phase 2 / frontend-safety; Codex-confirmed REAL, LOW). PREVENTION GAP (0 active instances): assertRpcResult is double-guarded (eslint require-assert-rpc-result.cjs + src/lib/assertRpcCoverage.test.ts per-file count ratchet, baseline 0); checkMutationResult has NEITHER. handle-supabase-error.cjs treats `if (error) throw error` as 'handled', so a `.update/.delete().select()` with NO checkMutationResult passes ESLint + build — yet per db.ts:66-75 an RLS-blocked mutation returns {data:null,error:null} (no error to throw) -> silent phantom-success + success toast. ALL current code is correctly guarded (52 mutation captures / 79 checkMutationResult calls / 0 uncovered). RE-TIERED green->park (NOT auto-fixed): the obvious fix (mirror assertRpcCoverage.test.ts's count-EQUALITY ratchet) is WRONG here — checkMutationResult calls (79) != mutation captures (52), so an equality ratchet false-positives on many files. A correct gate needs a per-capture PROXIMITY/AST scan (each `.update/.delete().select()` must have a checkMutationResult within ~N lines), baseline 0. Codex AGREED the equality mirror is wrong + a proximity scan is needed -> this is fragile codebase-scanning meta-tooling (a team-wide CI gate) that should be authored DELIBERATELY via /ship, not auto-shipped at 1 AM. FIX (docs-or-test, PARK): add src/lib/checkMutationCoverage.test.ts as a proximity-scan ratchet (baseline 0) + regex-sanity self-tests, turning the manual convention into the same HARD guard assertRpcResult has. Evidence: eslint-local-rules/rules/handle-supabase-error.cjs:107-116; src/lib/db.ts:61-76; src/lib/assertRpcCoverage.test.ts (template)." },

    { "dedupeKey": "lifecycle-segregation:jobs:cancel-from-any-status", "severity": "MEDIUM", "severityCycle15": "LOW", "severityCycle19": "MEDIUM", "claudeSeverity": "LOW", "codexSeverity": "LOW", "reConfirmedCycle": 19, "tier": "yellow", "status": "built-pending-apply", "buildMigration": "20260620150000_job_cancel_gate.sql", "buildValidation": "rolled-back plpgsql_check CLEAN (relid jobs) + normalized line-diff: only the unconditional cancel clause replaced by the gated one ('scheduled'/'in_progress'), _is_admin_override hatch preserved, nothing else altered. NOT applied live.", "fixKind": "migration", "cycle": 15, "phase": 2, "note": "NEW cycle 15 (Phase 2 / lifecycle-invariants; Codex-confirmed REAL, LOW). The job status enforcer _enforce_job_status_transition() accepts a cancel from ANY status — its body has an UNCONDITIONAL `IF NEW.status='cancelled' THEN RETURN NEW; END IF;` placed BEFORE the forward-transition whitelist, so invoiced->cancelled and completed->cancelled are accepted at the DB layer. The ONLY thing blocking it today is the frontend guard JobDetail.tsx:669 (`if (status !== 'scheduled' && status !== 'in_progress') return`), and the cancel write is a raw `.from('jobs').update({status:'cancelled'})` (no RPC enforcement). The SIBLING enforcers correctly gate cancel by source state: _enforce_order_status_transition (cancel only from confirmed/partially_fulfilled, NOT fulfilled) + _enforce_delivery_status_transition (cancel only from scheduled/in_progress, NOT completed) — so the job enforcer is a real deviation, not intentional. RISK: a direct DB write or any future/secondary path could flip an invoiced job to 'cancelled', leaving its field_application invoice + application_records dangling (cancelled job, revenue invoice still stands). Dormant (live jobs: 1 scheduled, 0 completed, 0 invoiced); trusted-actor-only (jobs_update RLS = is_admin() OR is_sales_rep()) -> LOW (defense-in-depth DB gap). FIX (migration, PARK): replace the unconditional cancel clause with `IF OLD.status IN ('scheduled','in_progress') AND NEW.status='cancelled' THEN RETURN NEW; END IF;` (keep the admin-override escape hatch), reproducing the live body verbatim except that clause. Evidence: 20260228300000_critical_prelaunch_fixes.sql:477 (job, unconditional) vs :378-403 (order/delivery, gated); JobDetail.tsx:669. CYCLE-19 RE-CONFIRM + SEVERITY ESCALATION LOW->MEDIUM (split kept: c15 verifier LOW, c19 verifier MEDIUM): the c19 pass sharpened the impact — terminal 'completed'/'invoiced' jobs (reachable: complete_job sets 'completed', transfer_job_to_invoice sets 'invoiced' + writes invoices/invoice_shares/application_records) can be flipped to 'cancelled' via a direct privileged UPDATE, ORPHANING the already-created invoice + application_records with NO reversal (no cascading unwind, no cancel_job RPC). Still dormant (1 scheduled job, 0 completed/invoiced/cancelled, 0 application_records) + frontend-guarded -> MEDIUM not HIGH. Fix unchanged: `IF NEW.status='cancelled' AND OLD.status IN ('scheduled','in_progress') THEN RETURN NEW` (keep _is_admin_override escape hatch)." },

    { "dedupeKey": "docs:rpc-functions-md:trigger-count", "severity": "LOW", "claudeSeverity": "LOW", "tier": "yellow", "status": "parked", "fixKind": "docs-or-test", "cycle": 17, "phase": 2, "reTieredFromGreen": true, "note": "NEW cycle 17 (Phase 2 / docs-deps-tests; verifier REAL, LOW; cosmetic doc-count drift — NOT Codex-gated, it's a trivially-verifiable count). Docs say 47 trigger functions; live DB has 49 distinct trigger functions (`SELECT count(DISTINCT proname) FROM pg_proc WHERE pg_get_function_result(oid)='trigger'` = 49). The +2 are recent enforcers added after the 2026-06-16 doc date: _enforce_quote_terminal_not_drawn, _validate_order_shares_total, _enforce_delivery_items_parent_lock. Locations: docs/reference/rpc-functions.md:1/3 ('47 trigger functions') + CLAUDE.md:22 Snapshot ('+47 trigger fns'). The CALLABLE RPC count (226) is NOT drifted (matches live). check-doc-drift.mjs SKIPS the live-DB cross-check + has no trigger-count assertion (lines 127-129) so this slipped past the gate. Cosmetic/inventory only — zero runtime/money/RLS impact (an agent reading the trigger inventory is off by 2). RE-TIERED green->park (NOT auto-fixed): a correct fix spans CLAUDE.md (core file) + docs/reference/rpc-functions.md + the AUTO-GENERATED AGENTS.md (needs `node scripts/regenerate-agents-md.mjs` to stay consistent) = the deliberate /update-docs flow, not a clean one-line surgical green edit; auto-editing core docs unattended for an off-by-2 count isn't warranted. FIX (docs-or-test, PARK): run /update-docs to bump trigger-count 47->49 across CLAUDE.md + rpc-functions.md + AGENTS.md (list the 2 new enforcer triggers), and OPTIONALLY extend check-doc-drift.mjs to cross-check live trigger/RPC counts so this drift class is caught automatically. CYCLE-19 RE-CONFIRM + ENRICH (this parked item now covers ALL the doc-count drift in rpc-functions.md/CLAUDE.md): (1) trigger-fn count: docs say 47, live=49 — at CLAUDE.md:22, CLAUDE.md:274, rpc-functions.md:1/3; (2) NEW callable-RPC count: rpc-functions.md:1/3 says '227 callable RPCs' but CLAUDE.md:22/274 say '226' and LIVE=226 (count of public non-trigger non-extension fns) — so rpc-functions.md is internally inconsistent with CLAUDE.md AND wrong. ONE /update-docs pass fixes both: set rpc-functions.md to '226 callable RPCs + 49 trigger functions', bump the trigger count 47->49 in CLAUDE.md:22+:274, regen AGENTS.md. All cosmetic (no runtime/money/RLS impact). (Refuted same cycle: doc:table-count:96-vs-97 — the '96 tables (+2 views)' wording is NOT drifted, FALSE_POSITIVE.)" }
  ],
  "refuted": [
    { "dedupeKey": "money:save_field_app_invoice:cross-field-quoted-price", "verdict": "FALSE_POSITIVE" },
    { "dedupeKey": "stale-derived-state:complete_delivery:invoice-total-cost-cents", "verdict": "FALSE_POSITIVE" },
    { "dedupeKey": "prepay:create_prepay_credit:no-idempotency", "verdict": "FALSE_POSITIVE" },
    { "dedupeKey": "concurrency:void_payment:invoice-no-for-update", "verdict": "FALSE_POSITIVE" },
    { "dedupeKey": "report:get_commission_balance_report:cancelled-in-earned", "verdict": "FALSE_POSITIVE", "cycle": 4, "note": "Structurally true (total_earned = SUM over ALL rows, no status/deleted_at filter, vs paid/outstanding which filter by status) BUT unreachable: cancel_order and void_order both UPDATE commissions SET status='cancelled', commission_amount=0 in the SAME atomic statement, and 0 rows are soft-deleted (no RPC sets commissions.deleted_at). Live: cancelled = 2 rows / $0 / 0 nonzero. So earned==paid+outstanding holds today; the gap only 'would surface' on a hypothetical future cancellation path that leaves the amount intact. Defense-in-depth, not a current wrong value." },
    { "dedupeKey": "prepay:batch_apply_prepayments:actor-param-ignored", "verdict": "FALSE_POSITIVE", "cycle": 5, "note": "batch_apply_prepayments declares p_performed_by but never binds it to auth.uid() and drops it when calling apply_prepay_to_invoice — structurally true, but the forged param is INERT: the inner apply_prepay_to_invoice independently sets v_actor:=auth.uid() (RAISE if null), gates admin/sales role, and writes financial_audit_log.actor_role from auth.uid() NOT from any param. So nothing forgeable reaches the ledger. Latent convention/hardening gap (strict-actor not applied at the wrapper), not an exploitable forgeable-actor bug. Dormant (0 prepay rows). Frontend passes the real profile?.id anyway." },
    { "dedupeKey": "prepay:create_prepay_credit:no-idempotency-key", "verdict": "FALSE_POSITIVE", "cycle": 5, "note": "Re-surfaced; ALREADY in accepted-findings.json knownParkedOrDeferred (unused legacy fn) + cycle-1 nightly-debug ledger. Mechanism real (no p_idempotency_key, double-submit double-counts) but ZERO frontend caller (only generated types + a fixture test); the live prepay-creation paths create_prepay_check_splits + allocate_payment ARE idempotent. Dead/deferred -> FALSE_POSITIVE for a new-bug hunt." },
    { "dedupeKey": "shares:order_shares:lower-bound-missing", "verdict": "FALSE_POSITIVE", "cycle": 5, "note": "_validate_order_shares_total enforces only IF v_total > 100.01 (no lower bound) — code claim true — BUT order_shares.split_percentage feeds NO invoice: ZERO RPCs reference order_shares besides its 2 triggers (verified pg_proc.prosrc scan), the active per-field splitter create_split_invoices_from_order bills from order_item_field_allocations + field_billing_defaults (both-bound-guarded), and no view derives billing from order_shares. The migration header states intent = MAX-100 race cap to prevent OVER-billing, not an exactly-100 invariant. So an 80% total produces NO under-billed invoice. Cosmetic completeness gap on an unconsumed column, dormant (0 rows). The aggregate 100% guard itself was already added 20260616121521 (accepted-findings)." },
    { "dedupeKey": "splits:invoice_line_allocations:column-overload", "verdict": "FALSE_POSITIVE", "cycle": 5, "note": "invoice_line_allocations has both invoice_id + invoice_item_id nullable (CHECK ila_target_check is an OR not XOR); allocate_payment writes payment rows (invoice_id set), update_allocation_set writes split rows (invoice_item_id set, invoice_id NULL); reconciliation.ts sums amount_cents keyed by invoice_id so split rows (NULL invoice_id) correctly fall out. The reconciliation is CORRECT given the only two live writers — the 'corruption' requires a HYPOTHETICAL future writer that sets invoice_id on a split row (no such code on disk). Latent design observation / future-writer hypothetical on a 0-row table -> excluded class (speculative flexibility). Recommend a regression test only if/when the table goes live." },
    { "dedupeKey": "money:calculate_billing_splits:nonsumming-pct", "verdict": "FALSE_POSITIVE", "cycle": 13, "note": "calculate_billing_splits does largest-remainder apportionment and never validates the input percentages sum to 100 (live: (10000,[10,10]) -> [8999,1001]; (10000,[60,60]) -> [6000,6000] over-allocates). BUT its SOLE caller create_split_invoices_from_order normalizes to exactly 100 at both call sites (acre pcts = acres/total*100 by construction; owner pcts guarded by FIELD_SPLIT_NOT_100 raise on the 99.99-100.01 band then normalized p/sum*100, with an in-code comment citing the assumption — a prior Codex P2 fix). So no wrong output is ever produced live. The 'future caller passes non-100' risk is speculative-flexibility (excluded class), and the split path is dormant (0 oifa/0 split invoices live). NOT a bug. Also re-surfaced + correctly refuted this cycle: prepay:create_prepay_credit:no-idempotency-key (already in refuted/accepted as unused-legacy dead code) and allocation:update_allocation_set:no-idempotency (a NEW no-actor/no-audit/AR-desync framing on the already-parked dead RPC splits:update_allocation_set:no-sum-validation — REFUTED: it DOES PERFORM require_admin() + bind actor to auth.uid(), DOES write a financial_audit_log 'split_modified' row, and correctly does NOT touch paid_amount_cents (splits != payments); the only true residual is the missing idempotency key on a 0-caller dead RPC = retire-not-patch)." },
    { "dedupeKey": "db-security:create_order_from_blend_ticket:unbound-actor-no-rolegate", "verdict": "FALSE_POSITIVE", "cycle": 14, "note": "create_order_from_blend_ticket has no actor-bind/role-gate of its OWN and writes raw p_performed_by into inventory_transactions + activity_feed BEFORE calling link_blend_ticket_to_order — structurally true. BUT it invokes link_blend_ticket_to_order(...) UNCONDITIONALLY (no surrounding EXCEPTION block) and link DOES bind v_actor:=auth.uid(), RAISE AUTH_REQUIRED/ACTOR_MISMATCH, and PERFORM require_admin_or_sales_rep() — a RAISE there aborts the WHOLE enclosing transaction, rolling back the order/order_items/prebook/inventory_transactions/activity_feed. So a forged actor or unprivileged role persists ZERO writes today (the finder concedes 'forgery does NOT persist'). The recommendation (add a redundant top-of-fn guard) is pure defense-in-depth against a FUTURE refactor of the link call — speculative-flexibility (excluded class), not a present violation. Hardening has merit but it's not a current bug." },
    { "dedupeKey": "lifecycle-segregation:orders:cancelled-to-voided-doc-drift", "verdict": "FALSE_POSITIVE", "cycle": 15, "note": "DOC-ONLY (out of hunt scope, not a code bug). CLAUDE.md order lifecycle arrow `confirmed -> partially_fulfilled -> fulfilled -> cancelled -> voided` over-implies a cancelled->voided transition that doesn't exist: _enforce_order_status_transition has NO transition INTO 'voided', and void_order hard-requires `IF v_order.status != 'fulfilled' THEN RAISE 'INVALID_ORDER_STATUS'` — so voided is reachable ONLY from fulfilled; cancelled is terminal. The CODE IS CORRECT (it rejects cancelled->voided). The CLAUDE.md arrow is a deliberately-compressed summary (the same file flags its Quote chain as 'branching, not linear'). At most a 1-line doc-polish; the hunt excludes doc/style nits. Optional: Mason may want to clarify the doc arrow. Not acted on (out of scope + borderline-stylistic + CLAUDE.md is a core file)." },
    { "dedupeKey": "types:commissions:nullable-denorm-cols", "verdict": "FALSE_POSITIVE", "cycle": 16, "note": "TYPE-ACCURACY NIT (out of scope). Commission.order_number/customer_name typed non-null in src/types/index.ts:668-669 but nullable live (34 rows, 33 NULL each). REFUTED as a bug: traced all 4 commissions readers (Reports.tsx:210 casts to a LOCAL CommissionRow that doesn't render these cols; CommissionPayments.tsx:137 casts to Record<string,unknown>; notificationTriggers.ts selects only recipient; reconciliation.ts passes order_number through as a label, no string method) — NONE uses the index.ts Commission interface or calls a string method on the nullable cols. No reachable crash. Cosmetic type tweak at most; the hunt excludes type/style nits + don't-tighten (could cascade TS errors)." },
    { "dedupeKey": "types:invoices:total_cost_cents-nullability", "verdict": "FALSE_POSITIVE", "cycle": 16, "note": "TYPE-ACCURACY NIT (out of scope). Invoice.total_cost_cents typed non-null number (index.ts:1058) but nullable bigint live. REFUTED: the consumers are GUARDED (InvoiceDetail.tsx:734 + Invoices.tsx:327 use `?? fallback`), so a NULL is handled. Cosmetic type-accuracy improvement; tightening to nullable could cascade TS errors elsewhere -> not a clean green fix. Out of scope." },
    { "dedupeKey": "types:payments:no-interface", "verdict": "FALSE_POSITIVE", "cycle": 16, "note": "TYPE OBSERVATION (out of scope / by-design). No dedicated Payment interface in index.ts and payments.amount is DOLLARS not cents — but per CLAUDE.md/accepted-findings the dollars-not-cents storage on payments (like commissions.commission_amount) is BY DESIGN, and the absence of a shared interface is a style/organization nit, not a correctness bug. Refuted." },
    { "dedupeKey": "tests:ci-e2e-job:disabled", "verdict": "FALSE_POSITIVE", "cycle": 17, "note": "OUT OF SCOPE (CI/test-infra, not a correctness bug) + false impact framing. The e2e-smoke CI job carries `if: false` (ci.yml:115, disabled 2026-05-18 — prod-safety guard refuses E2E vs prod + no staging project), so E2E (94 specs) doesn't run in CI. BUT the finding's HIGH framing ('loses the financial regression net') is WRONG: the job runs `test:e2e:smoke` = `playwright test --grep @smoke`, and only 6 specs carry @smoke (auth/dashboard/financial-dashboard/navigation×2/math-quote-pricing) — the heavy financial specs (mega-workflow, math-invoice-verification, workflow-financial-operations, golden-path, returns) have ZERO @smoke tags, so they'd NEVER run in the smoke job even if re-enabled. NOTED FOR MASON (optional, not a bug): CLAUDE.md advertises '94 E2E specs' but they don't run in CI; consider creating crx-manager-staging + re-enabling, or note in docs that E2E is local-run-only." },
    { "dedupeKey": "tests:pages-render-smoke:partial-coverage", "verdict": "FALSE_POSITIVE", "cycle": 17, "note": "OUT OF SCOPE (test-coverage observation, not a correctness bug). The page-render smoke test covers a subset of pages; partial coverage is a test-completeness gap, not a defect in shipped code. Refuted for a correctness-bug hunt." },
    { "dedupeKey": "deps:dompurify:prod-sanitizer-bypass", "verdict": "FALSE_POSITIVE", "cycle": 17, "note": "DEPENDENCY-HYGIENE observation, NOT a reachable bug. npm audit flags 3 MODERATE dompurify advisories (<=3.4.10) in the prod tree via jspdf@4.2.1 -> dompurify@3.4.7, and CI's gate is `npm audit --audit-level=critical` (ci.yml:70) so moderates don't fail the build. REFUTED as exploitable: dompurify is referenced ONLY in jspdf's `.html()`/html2pdf path (lazy `import('dompurify')`, optionalDependencies); this app generates PDFs PURELY programmatically (new jsPDF()+autoTable — invoicePdf.ts/deliveryPdf.ts/etc.) and a repo-wide grep for `.html(` in src/ returns ZERO matches, so the vulnerable sanitizer path is NEVER invoked (at most a never-loaded async chunk under tree-shaking). undici(HIGH)/js-yaml(MOD) advisories trace to jsdom/eslint = devDependencies, not shipped. NOTED FOR MASON (optional hygiene, not a bug): lowering the CI audit gate to `--audit-level=high` would surface moderates earlier." },
    { "dedupeKey": "tests:schemaIntegrityLive:conditional-skip", "verdict": "FALSE_POSITIVE", "cycle": 17, "note": "OUT OF SCOPE (test-infra). A live-schema-integrity test conditionally skips when its env/creds are absent — a standard guard so the test doesn't hard-fail in environments without live-DB access, not a correctness defect in shipped code. Refuted." },
    { "dedupeKey": "deps:ci-audit-level:critical-only", "verdict": "FALSE_POSITIVE", "cycle": 19, "note": "HYGIENE observation, not a bug (re-surface of the cycle-17 dompurify hygiene note). CI's `npm audit --audit-level=critical` only fails the build on CRITICAL, so MODERATE/HIGH advisories don't gate. But the only prod-tree advisories (dompurify moderates via jspdf) are dead-code-unreachable (.html() path never invoked), and HIGH/MOD undici/js-yaml are devDependencies. Lowering to --audit-level=high is a reasonable LOW-priority hygiene tweak, not a correctness bug." },
    { "dedupeKey": "tests:schemaIntegrityLive:skipif-gated-security-checks", "verdict": "FALSE_POSITIVE", "cycle": 19, "note": "OUT OF SCOPE (test-infra; re-surface of cycle-17). The live schema/security-integrity test skips when its live-DB creds are absent — a standard env-guard so it doesn't hard-fail in CI without live access, not a defect in shipped code. Refuted." },
    { "dedupeKey": "doc:table-count:96-vs-97", "verdict": "FALSE_POSITIVE", "cycle": 19, "note": "NOT drifted. The docs' '96 tables (+2 views)' / '97 tables' wording is consistent with the live schema once views are counted as the docs intend; the claimed 96-vs-97 discrepancy is a miscount of the views-vs-tables split, not a real doc drift. Refuted (unlike the trigger-count + callable-RPC-count drifts, which ARE real and parked)." }
  ]
}
