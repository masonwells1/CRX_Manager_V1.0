# Claude → Fable handoff: PR #231 remediation

> **STATUS — resolved. Verified 2026-07-29.** PR #231 was **closed** on 2026-07-26T16:11:35Z, which is
> the §8.1 recommendation below. This document is kept as a **historical post-mortem, not an open
> action list** — do not work §8 as a to-do. Its durable lesson is §9: a green pre-push run against a
> stale base is not evidence of correctness against `main`.

**From:** Claude (Opus 5), session rooted at `C:\` (not `C:\CRX_Manager`)
**Date:** 2026-07-26
**Subject:** PR #231 is largely duplicate and partly regressive. Recommend closing it, not rebasing.
**Companion document:** a Codex session is writing its own handoff on the same subject. Where we disagree, treat both as claims to re-verify, not as settled fact. Divergences are called out in §7.

---

## 1. Verdict

**PR #231 should be closed.** Highlights from its 13 files — this list is not an exhaustive tally, the full per-file disposition of all 13 is in §4: two are byte-identical to `origin/main`, three are superseded by newer versions already live, one breaks the test suite on merge, and one would roll the gauntlet ledger backwards by four sections. Three documents are genuinely new and worth lifting onto a clean branch.

I opened this PR. The core mistake was mine: I committed and PR'd work rescued off a stale detached HEAD **without first checking whether it had already landed in `origin/main`.** It had.

---

## 2. What Fable is being asked to decide

1. Close PR #231, or rebase and salvage it?
2. Which of the three unique documents are worth keeping, given §5 casts doubt on one of them.
3. Whether the HIGH inventory-location finding still needs separate work, or whether Codex Stage B2 already fixes it (§6, unresolved).

---

## 3. Object identifiers

| Thing | Value |
|---|---|
| PR | `#231` — **closed unmerged 2026-07-26T16:11:35Z**; draft and `mergeable: CONFLICTING` when inspected below |
| Branch | `claude/gauntlet-s3-s4-and-phase3-artifacts-2026-07-26` |
| Head commit | `29486b5e` |
| Branch base | `bf2a60ef` — **28 commits behind `origin/main`** when cut |
| `origin/main` at time of writing | `31d8e4d3` |
| Codex Stage B2 | `origin/codex/supplier-pricing-phase3-stage-b2`, 6 commits ahead of `main`, active (commit 13 min before writing) |

**Nothing in PR #231 was applied to any database.**

---

## 4. File-by-file disposition (verified against `origin/main` this session — with one stated exception)

The one exception is `live-foundation-gauntlet-index.md`: it was confirmed *different* from `main` but
never compared line by line, and its row says so. Do not read that row as confirmed evidence.

| File | Finding | Evidence |
|---|---|---|
| `supabase/migrations/20260722222743_product_families_return_policy_foundation.sql` | **Superseded.** `main` has `20260723193312_..._foundation.sql`, same name, later timestamp, **1,271 lines vs 818**, 479 lines divergent. Main's is **live**. Mine carries the *earlier* timestamp so it would order *before* the live one. | `git ls-tree`, line counts, `diff` |
| `src/lib/rpcContracts.test.ts` | **Breaks on merge.** Adds `set_product_phase3_metadata` to `MIGRATION_ONLY_RPCS_WITH_IDEMPOTENCY`. On `main` that bucket is **empty** and the RPC is in `MUTATING_RPCS_WITH_IDEMPOTENCY` (~line 1470), because it is already in `src/types/supabase.ts`. The migration-only test asserts the RPC is **not** in generated types — it now is → test fails. | Read `origin/main` source directly |
| `scripts/smoke/smoke-supplier-pricing-phase3-return-policy.sql` | **Byte-identical** to `main`. 476 lines both sides. Zero value. | `git diff --quiet` |
| `docs/audits/gauntlet/2026-07-22-section-03-...-refresh.md` | **Byte-identical** to `main`. 84 lines both sides. Zero value. | `git diff --quiet` |
| `scripts/smoke/prove-...-return-policy-concurrency.mjs` | **Superseded.** `main` 329 lines vs mine 318. | line counts + `diff` |
| `docs/audits/gauntlet/live-foundation-gauntlet-summary.md` | ⚠️ **REGRESSIVE — see §5** | line-by-line read of both |
| `docs/audits/gauntlet/live-foundation-gauntlet-index.md` | Differs from `main`. **Not individually verified** — presumed regressive by association, but confirm before acting. | `git diff --quiet` only |
| `docs/audits/gauntlet/2026-07-26-section-04-...-refresh.md` | Unique, but **reliability disputed — see §5** | content read |
| `docs/audits/2026-07-22-codex-to-claude-supplier-pricing-phase3-handoff.md` | Unique. Worth keeping. | `git cat-file -e` |
| `docs/audits/2026-07-25-codex-to-claude-phase3-stage-b-handoff.md` | Unique. Worth keeping. | `git cat-file -e` |
| `docs/audits/sweeps/sweep-2026-07-26.md` | Unique. Worth keeping. | `git cat-file -e` |
| `docs/CHANGELOG.md` | New entry describing the preservation commit. Superseded if the PR closes. | — |
| `docs/app-workflow-map.html` | Regenerated by the pre-commit hook. Incidental. | — |

---

## 5. The two findings that matter most

### 5a. The ledger edit rolls `main` backwards

`live-foundation-gauntlet-summary.md`:

- **`main`** — Sections **1–8** refreshed. **Section 9 queued next.** Records the 2026-07-22 Sections 5–8 T3 close-out: five mutable-`search_path` functions hardened live via `20260722111620`, the 07-08 idempotency HIGH/MED verified fixed, Section 2 statement HIGH/MED confirmed applied live via `20260720185135`, one Section 8 MED fixed live (`unapply_credit_memo` replay binding), one Section 7 HIGH parked for Mason (custom "Other" commission recipients can never enter a payout batch; zero live rows affected).
- **PR #231** — Sections **1–4** only. **Section 5 queued next.** Dated *later* (07-26 vs 07-22) but contains strictly *less*.

Merging PR #231's ledger would erase the Sections 5–8 record, including the parked Section 7 HIGH awaiting an owner decision.

### 5b. The two ledgers contradict each other on Section 4

- **`main`:** *"The July 22 Section 4 refresh found one HIGH bulk-quote side door and fixed it in branch"* — `BulkQuoteImport` inserted directly into `quotes`/`quote_sections`/`quote_items`; fix routes imports through `save_quote`, enforces draft-only creation, preserves dosage/price override payloads, rejects accepted/terminal imported statuses.
- **PR #231:** *"The July 26 Section 4 refresh found no confirmed quote-to-payment lifecycle wiring defects, with only a stale-checkout scope warning."*

My Section 4 audit ran from the 28-commits-behind checkout and **its own text records a stale-checkout scope warning.** It reported "no defects" in an area where a more thorough audit four days earlier found and fixed a HIGH.

**Recommendation: treat PR #231's Section 4 report as unreliable, not as an independent second opinion.** If Fable wants Section 4 coverage, re-run it from current `origin/main`.

---

## 6. Open *at the time of writing* (2026-07-26) — could not resolve then

These are preserved as the uncertainty this session actually had. They are **not** current remediation
guidance: three days of work landed after this was written. Re-derive each against current `main`
before acting on it.

1. **Does Stage B2 fix the HIGH inventory-location bypass?** B2 touches `src/pages/InventoryPage.tsx`, the exact file behind the finding *"inline edit writes `inventory.location` directly, bypassing the transfer ledger; live triggers neither record nor block it."* I did **not** read B2's diff to confirm. Check before queueing that as separate work — it currently sits at position 6 in `main`'s fix queue.
2. **Does my 818-line migration contain anything `main`'s 1,271-line version dropped?** I compared filenames, line counts, and diff size — **not** semantics. Near-certainly a superset, but if anyone wants to be sure that nothing was lost rather than superseded, that diff has to be read. Low priority; the live version is authoritative.
3. **Is the index ledger regressive the same way as the summary?** Confirmed different, not individually compared.

---

## 7. Where this handoff may disagree with Codex's

Codex owns Stage B2 and the live `20260723193312` migration; it has direct knowledge of both that I lack. Weight its account above mine on:

- what Stage B2 does and whether it addresses the inventory-location HIGH;
- whether the 1,271-line migration deliberately dropped anything from the 818-line draft;
- the true state of the Section 4 finding.

Weight this document above Codex's on:

- the contents and disposition of PR #231 — I created it and compared every file;
- the `rpcContracts.test.ts` failure mechanism, read directly from `origin/main`;
- the ledger regression in §5a.

If the two handoffs conflict on a **fact**, re-derive it from `origin/main` rather than picking a side.

---

## 8. Recommended remediation

1. **Close PR #231** with a comment pointing here. Do not rebase — the only salvage is three documents, and rebasing risks the §5a ledger regression surviving into `main`.
2. **Lift onto a fresh branch cut from current `origin/main`:** the two Codex→Claude phase 3 handoffs and `sweep-2026-07-26.md`. Nothing else.
3. **Drop entirely:** the migration, the `rpcContracts.test.ts` change, both smoke scripts, the Section 03 report, both ledger files, and the CHANGELOG entry.
4. **Decide separately** whether to keep the Section 04 report at all, given §5b. My recommendation is to drop it and re-run Section 4 from current `main`.
5. **Before closing,** confirm §6.1 so the inventory-location HIGH is neither double-worked nor dropped.

---

## 9. Verification standard applied

Every claim above was derived from `git` against `origin/main` in this session — file existence via `git cat-file -e`, content equality via `git diff --quiet`, line counts via `wc -l`, and the test-failure mechanism by reading `origin/main:src/lib/rpcContracts.test.ts` directly. Nothing is asserted from memory or from the PR description.

**Not verified:** the three items in §6, and no code in PR #231 was executed against a live database by me. The pre-commit and pre-push hooks did run in full on `29486b5e` (lint, build, all 3,954 tests, workflow map, typecheck) and passed **against the stale base** — which is precisely why they did not catch any of this. Green checks on a stale base are not evidence of correctness against `main`.
