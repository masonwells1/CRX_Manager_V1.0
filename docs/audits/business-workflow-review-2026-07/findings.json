[
 {
  "i": 0,
  "source": "program-quote-call",
  "sev": "high",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Personally verified every citation. Line 3191 'Mark as Presented' is variant=primary (the true primary — QuoteBuilder consistently marks explicit primaries this way); line 3198 HelpTip literally reads 'Sends the quote PDF to the customer's email and locks it as Sent'. handleMarkPresented (1469-1493) calls only create_quote_version with p_method:'presented' — no sendEmail/RPC call to any email path — and silently returns at line 1470 (`if (!quoteId || !profile) return;`) with no toast on an unsaved quote. The actual email logic lives only in handleEmailToGrower (1335-1467), the secondary button. Also confirmed: quote_versions is fetched with select('*') (line 503, so `method` is in memory) but the version list JSX (2222-2231) renders only version_number/sent_at/itemCount/total — never method — so there is genuinely no persistent emailed-vs-presented indicator anywhere on the page.",
  "title": "The send screen's primary button doesn't send — and its help text says it does",
  "problem": "To email a quote you must click Preview Quote, then in the preview modal choose between two buttons. The big green primary button is 'Mark as Presented' — it only snapshots a version and flips the status to 'sent'; it never emails anything. The actual send is the smaller secondary 'Email to Grower' button. Worse, the help tooltip sitting next to 'Mark as Presented' reads 'Sends the quote PDF to the customer's email and locks it as Sent' — describing the OTHER button. A busy office user will click the green button, see the status flip to 'sent', and believe the customer received the quote when nothing was sent. There is no persistent 'emailed on <date>' indicator anywhere on the quote to catch the mistake later (the app never reads the email_log table it writes to, and the Versions panel shows only version number/date/total — not whether a version was emailed or presented). Bonus dead-end: on a brand-new unsaved quote, clicking 'Mark as Presented' silently does nothing at all (early return, no message).",
  "evidence": "src/pages/QuoteBuilder.tsx:3190-3207 (primary 'Mark as Presented' + HelpTip at 3198 claiming it emails; 'Email to Grower' is variant=secondary); handleMarkPresented at 1469-1493 contains no sendEmail call and silently returns if !quoteId (1470); email is only sent in handleEmailToGrower 1335-1467; email_log is referenced only in a comment (1334), never queried; version list render 2222-2231 shows no method field",
  "rec": "Make 'Email to Grower' the primary button, move the email tooltip onto it, give 'Mark as Presented' its own honest tooltip ('records that you handed/showed the quote in person — does NOT email it'), toast on the unsaved no-op, and show a 'Last emailed <date> to <address>' chip on the quote header read from email_log."
 },
 {
  "i": 1,
  "source": "program-quote-call",
  "sev": "high",
  "scope": "owner-decision",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "All citations verified against live source. setTerminalStatus (1146-1180) blocks decline/cancel whenever quote_product_draws OR job_product_draws has any qty>0, with the toast wording matching verbatim ('close it from its orders/jobs (draw or cancel the remaining balance)'). auto_expire_quotes (20260614143649, lines 32-50) explicitly excludes both drawn bookings (NOT EXISTS quote_product_draws, line 41-44) and is_planned=true (line 49). rollover_open_booking_gate (20260610191456, line 194) INSERTs a brand-new quote row and never UPDATEs the source quote's status — old booking stays open. layer2_sync_planned_holds_job_aware (line 101) sets expires_at = needed_by_date + INTERVAL '14 days', which is NULL when needed_by_date is NULL (Postgres NULL-propagation), confirming a never-expiring hold on a Planned Program with no Needed-By date. canCloseApplied (line 293) requires isPlanned && status in sent/revised — application-channel-specific, no equivalent 'walked away' close path exists for chemical-sale bookings. This is a real, verified gap with real business impact (permanently stuck inventory + open quotes list clutter).",
  "title": "No honest way to close a partially-drawn booking the customer walks away from — leftovers linger forever",
  "problem": "End of season, a customer has pulled 450 of 500 booked gallons and doesn't want the rest. Every close path is blocked: Decline/Cancel refuse on any booking with draws (the toast tells you to 'draw the remaining balance' — i.e., create a real order the customer never asked for); the nightly auto-expire deliberately skips partially-drawn AND planned bookings; 'Roll Over to New Season' copies the remainder into a new draft but leaves the old booking open and untouched; and 'Close — Applied' records the wrong business fact (it means 'we sprayed it via jobs'). So the booking sits as an open 'sent' quote in the Quotes list and the Open-booking rollover forever. If it's a Planned program whose sections have no Needed-By date, its inventory holds carry a NULL expiry and never lapse — permanently subtracting that stock from Net Free until someone notices. The 2026-07-04 close_quote_as_applied work solved this exact problem for the application channel; the chemical-sale channel has no equivalent.",
  "evidence": "src/pages/QuoteBuilder.tsx:1146-1171 (setTerminalStatus blocks decline/cancel when quote_product_draws/job_product_draws > 0); supabase/migrations/20260614143649_auto_expire_quotes_skip_planned_and_schedule.sql:41-49 (sweep skips drawn + planned); supabase/migrations/20260610191456_rollover_open_booking_gate.sql:194-202 (rollover inserts a new draft, never updates the source quote); supabase/migrations/20260702171000_layer2_sync_planned_holds_job_aware.sql:101 (hold expires_at = needed_by_date + 14 days → NULL needed_by = never expires); QuoteBuilder.tsx:293 canCloseApplied is application-channel-only semantics",
  "rec": "Add a 'Close booking — release remainder' action (mirror of close_quote_as_applied: new terminal status like closed_short, releases holds, keeps the drawn history, never bills). Offer it automatically after a rollover ('close the old booking?'). Owner should confirm the business meaning (walked-away remainder vs cancelled)."
 },
 {
  "i": 2,
  "source": "program-quote-call",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified openDrawDownModal (1584-1593): `if (!fresh && !staleWarning?.dismissed) return;` is a silent early return with zero toast/message beyond the banner state. useStaleQuoteCheck (useGuardrails.ts 96-122) confirms >30-day threshold (line 103) and that dismiss() is plain component useState (line 117-119) with no persistence — a page reload/revisit resets `dismissed` to false, so the two-click dance repeats every visit. Verified draw_down_quote (20260702172000, lines 136-148): the per-product weighted price is computed as SUM(qi.price_per_unit * total_units_needed)/SUM(total_units_needed) FROM quote_items — i.e. the quote's own locked line prices, not live product.tierN_price — confirming the 'prices may have changed' message is factually irrelevant to what draw-down actually bills. Genuinely affects the highest-frequency booking action as described.",
  "title": "The 'stale quote' warning fires on every mid-season pull — with a message that's wrong for bookings",
  "problem": "Program bookings are drawn down for months after they're written — that's the whole point. But the Partial Order button runs a 30-day staleness guard: the first click on any booking older than 30 days silently does nothing except surface a banner saying 'product prices may have changed since it was created', and the user must find the banner, dismiss it, then click Partial Order AGAIN. The dismissal is in-memory only, so this two-extra-clicks-plus-scary-warning dance repeats on EVERY visit for the rest of the season — on the single highest-frequency booking action. And the message is factually misleading here: draw_down_quote deliberately bills at the quote's locked weighted-average price, so 'prices may have changed' is irrelevant to what the customer will pay — it trains staff to ignore warnings.",
  "evidence": "src/pages/QuoteBuilder.tsx:1584-1593 (openDrawDownModal returns silently on first click when checkStaleQuote fails), 2112-2118 (page-level GuardrailBanner); src/hooks/useGuardrails.ts:99-115 (30-day threshold, component-state dismissal resets every page load); supabase/migrations/20260702172000_layer2_draw_down_quote_job_aware.sql:136-148 (draw prices computed from locked quote_items prices, not current product prices)",
  "rec": "Skip the stale-price guard entirely on the draw-down path (prices are locked by design), or show it once per quote with wording about delivery logistics, not price. Keep it for whole-quote Convert where it's accurate."
 },
 {
  "i": 3,
  "source": "program-quote-call",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified openDrawDownModal's query (1601-1602) selects only product_id, total_units_needed from quote_items — no unit_size/inventory_unit. Verified products are fetched with select('*') (line 381) so inventory_unit IS in the `products` state array but is never referenced in the draw modal. Verified the draw table JSX (3269-3306): exactly 5 columns — Product/Booked/Drawn/Remaining/Send now — with zero unit label, and the Send-now input (3287-3300) has no unit suffix. Verified the Booking Settlement table (2171-2194): identical Product/Booked/Drawn/Remaining columns, equally unit-less. The failure mode (typing '30' against a remaining figure denominated in jugs, not gallons) is a real, plausible, high-stakes UX gap in exactly the phone-order path this owner cares most about.",
  "title": "Booked/Drawn/Remaining numbers show no units anywhere — office must remember if '30' means gallons, jugs, or cases",
  "problem": "The customer calls: 'send 30 gallons of Roundup.' The Partial Order modal and the Booking Settlement card both show bare numbers (Booked 200 / Drawn 50 / Remaining 150) with no unit label on any row — even though the app knows each product's inventory unit (it's already loaded in memory on this very page) and the quote line stored a unit_size. If the product is stocked in 2.5-gal jugs, 'remaining 150' means jugs, and typing '30' draws 30 jugs (75 gal), not 30 gallons — a real confirmed order at the wrong size, created in one click. This is the exact stall point in the highest-value phone interaction the persona has.",
  "evidence": "src/pages/QuoteBuilder.tsx:1601-1637 (openDrawDownModal selects only product_id + total_units_needed; products state with inventory_unit loaded at 379-381 but unused), 3272-3305 (draw modal table has Product/Booked/Drawn/Remaining/Send-now columns, no unit column), 2177-2189 (Booking Settlement per-product table equally unit-less)",
  "rec": "Append the product's inventory unit to every quantity in both tables ('150 × 2.5-gal jug' or '150 gal'), and put the unit inside the Send-now input's suffix so the phone conversation maps 1:1 to what gets typed."
 },
 {
  "i": 4,
  "source": "program-quote-call",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified layer2_sync_planned_holds_job_aware.sql line 101: `v_item.needed_by_date + INTERVAL '14 days'` sets expires_at — confirming both failure directions (Needed-By set → 14-day lapse; Needed-By blank → NULL expiry, never lapses). Verified needed_by_date is an optional input (section-level date field near line 2585, no required validation visible). Verified no read of inventory_holds anywhere in QuoteBuilder.tsx except the release write near line 999 (grep for inventory_holds/QuoteBuilder shows only that write path, no display query). This is a genuine, verified visibility gap distinct from the already-KNOWN thin-scheduling-UI item — it's specifically about hold-expiry being invisible on the quote itself, which the owner has no other way to discover short of manually cross-referencing the Inventory page's holds tab against every quote's Needed-By dates.",
  "title": "Inventory holds silently lapse 14 days after Needed-By — the quote never shows hold status or expiry",
  "problem": "Checking 'Planned Program' promises (per its tooltip) that inventory 'is reserved so it's not sold to someone else.' In reality each section's hold expires 14 days after that section's Needed-By date and a 6:15am job releases it — while the quote page looks exactly the same afterward. Nothing on the quote (or anywhere outside the Inventory page's holds tab) shows that holds exist, how much is held, or when protection ends. Two failure directions: (a) Needed-By set → reservation quietly evaporates two weeks later, and a spring booking drawn in June may have been unprotected since April without anyone knowing; (b) Needed-By left blank (it's optional) → the hold never expires. The office must carry hold-expiry dates in their heads; the app knows them and says nothing.",
  "evidence": "supabase/migrations/20260702171000_layer2_sync_planned_holds_job_aware.sql:101 (expires_at = needed_by_date + INTERVAL '14 days'); src/pages/QuoteBuilder.tsx:1941 (tooltip promise), 2582-2587 (Needed By optional); grep: inventory_holds is read only in src/pages/InventoryPage.tsx:223 — QuoteBuilder's sole touch is the release write at 999; release cron referenced in 20260614143649:16-18 and Dashboard.tsx:352",
  "rec": "On any planned open booking, render a small 'Reservations' line per section: held qty, expiry date, and a red state once lapsed ('hold expired Apr 28 — stock no longer reserved'), plus fold expiring-soon holds into the dashboard alerts."
 },
 {
  "i": 5,
  "source": "program-quote-call",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified fields fetch (line 383): `select('id, field_name, customer_id')` — confirmed no acreage columns are even fetched into the `fields` state used by this page. Verified section Field picker (2592-2604) stores only field_id via updateSectionField, no cascading effect anywhere nearby. Verified the item-level Acres input (2847-2866) is a bare manual number input with `value={item.acres ?? ''}` and no default derivation from any field data. Verified src/types/index.ts:1455-1461 carries total_acres/measured_acres/override_acres with the exact comment 'Billable acres = override_acres ?? measured_acres ?? total_acres' — confirming this data model exists and is simply unused by QuoteBuilder's fields query. Real, verified, and directly relevant to the persona's landlord/tenant multi-field programs (an owner-confirmed daily-use area).",
  "title": "Picking a Field on a section does nothing to acres — the same acreage is hand-typed 6 times",
  "problem": "The persona's customer has saved fields with measured/billable acres (the two-acre model shipped for exactly this data). A quote section lets you attach that Field — but the page fetches only the field's name, and every one of the section's items still requires manually typing Acres. For a 2-section, 6-product program on the same ground, that's six identical numeric entries the app already knows, and one typo (250 vs 205) silently mis-computes booked units, price, and the inventory hold for that line with no cross-check against the chosen field. There is also no section-level acres box that cascades to items.",
  "evidence": "src/pages/QuoteBuilder.tsx:383 (fields fetch selects only id, field_name, customer_id), 2590-2605 (section Field picker stores field_id only), 2847-2865 (per-item Acres input, no default from field); src/types/index.ts:1455-1461 (fields carry total/measured/override acres — available but unused here)",
  "rec": "When a section's Field is chosen, default each new item's Acres to the field's billable acres (override_acres ?? measured_acres ?? total_acres), editable as today, and flag items whose acres differ from the section's field."
 },
 {
  "i": 6,
  "source": "program-quote-call",
  "sev": "low",
  "scope": "UI",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified assignProduct (702-732) unconditionally calls setProductSearchOpen(null) at line 730 immediately after assigning the product to one item — no keep-open mode exists. Verified addItem (757-766) appends exactly one empty row per call. The click-count arithmetic in the finding (~24 of ~44 clicks for a 6-line quote) is a reasonable estimate given the confirmed one-at-a-time mechanics, not independently re-derived here, but the underlying code mechanism is exactly as described. Directly relevant to the owner's stated two-speed-selling pain point (program quotes = several products) and the 'too many clicks' complaint.",
  "title": "Product picker closes after every single pick — a 6-product quote means 6 full open→search→click cycles",
  "problem": "Adding each product costs: Add Item, Select Product, type a search, click the product — because assignProduct closes the modal immediately after one selection. For program quotes (the persona's bread and butter is 'several products, spring + fall'), item entry dominates the build: roughly 24 of the ~44 total clicks for a 6-line quote are this repeated picker loop. A keep-open or multi-select picker would cut the quote-build click count nearly in half; templates help repeat seasons but not the first program for each account.",
  "evidence": "src/pages/QuoteBuilder.tsx:702-732 (assignProduct ends with setProductSearchOpen(null)), 757-764 (addItem one row at a time; makeEmptySection starts with items: [] at 142-153), 3023-3097 (single-select picker modal)",
  "rec": "Add a 'keep open / add another' mode (or checkboxes + 'Add 4 selected') to the product picker, appending rows to the current section."
 },
 {
  "i": 7,
  "source": "program-quote-call",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified handleEmailToGrower (1345-1371): on a draft/revised quote, create_quote_version + setStatus('sent') happen at lines 1361-1369, BEFORE the sendEmail call at line 1427 — confirmed by reading the full function body. Verified the catch block (1461-1466): failure produces only `toast('error', ...)` — no rollback of status, no DB flag set. Verified the version list (2222-2231) shows only version_number/date/itemCount/total, and confirmed via grep that method ('emailed' vs 'presented', set at lines 1364/1479) is fetched (select('*') at line 503) but never rendered. This stacks with finding #1 exactly as claimed: 'sent' as displayed genuinely conflates emailed-success, emailed-failure, and in-person-presented into one indistinguishable status string, with no chip or version-method column to disambiguate after the fact.",
  "title": "A failed email still leaves the quote frozen as 'sent' with zero lasting evidence either way",
  "problem": "Email-to-Grower deliberately freezes the version and flips the status to 'sent' BEFORE generating and sending the email (good for reproducibility). But if the send then fails (edge function down, bad address), the only signal is a toast that vanishes; the quote thereafter looks identical to a successfully emailed one — status 'sent', a version snapshot on record. Days later, 'did that program quote actually reach the grower?' is unanswerable from the app: the version list doesn't show emailed-vs-presented, and the email_log written by the send-email edge function is never displayed. Combined with the mislabeled Mark-as-Presented button (separate finding), 'sent' as displayed can mean emailed, shown in person, send-failed, or none of the above.",
  "evidence": "src/pages/QuoteBuilder.tsx:1345-1371 (freeze + setStatus('sent') before the send at 1427; comments document the retry design), 1461-1466 (failure path = toast only, status stays sent), 2222-2231 (version rows render number/date/items/total — no method), 1334 (email_log written by the edge fn, never read by any quote UI; grep shows no email_log reads in pages besides ARaging)",
  "rec": "Read email_log for the quote and show a delivery chip ('Emailed Jun 2 ✓' / 'Send failed Jun 2 — retry'), and label versions with their method so 'sent' becomes auditable at a glance."
 },
 {
  "i": 8,
  "source": "book-it-now-call",
  "sev": "high",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Dashboard.tsx Quick Actions (410-468): exactly 6 buttons (To-Ship, New Order, New PO, Schedule Delivery, Inventory, Receiving) — no Quick Delivery entry, confirmed by a case-insensitive grep for 'quick delivery' across Dashboard.tsx returning zero matches. Verified CommandPalette.tsx:138 has only '/orders/new: New Order', no Quick Delivery route. Verified CustomerDetail.tsx:576/579/582 = New Quote/New Order/Sched. Delivery only. Verified Deliveries.tsx: Quick Delivery is explicitly variant='secondary' (lines 843, 1158) while Schedule Delivery has no variant prop, which Button.tsx:36 confirms defaults to 'primary'. All four discovery surfaces cited check out exactly as described — this is a real, high-relevance finding given the owner's explicit 'too many clicks'/'can't find things' pain and the stated priority on fast phone-order booking.",
  "title": "The one-screen fast path (Quick Delivery) is invisible; every discovery surface promotes the slow 3-page path",
  "problem": "Quick Delivery is the only flow that books the order, schedules the delivery, and drafts the invoice in one screen — exactly the under-a-minute phone-order tool the owner wants. But a new office hire can't find it: it is not on the Dashboard Quick Actions (which promote 'New Order' and 'Schedule Delivery' as two separate actions), not in the command palette, not on the customer's page, and on the Deliveries page itself it's the gray secondary button next to the primary 'Schedule Delivery'. It's also named for the wrong mental model — an office worker taking a phone ORDER won't think to go to the Deliveries page. Everyone will naturally pick New Order and inherit the 3-page trip.",
  "evidence": "src/pages/Dashboard.tsx:410-470 (six quick actions, no Quick Delivery); src/components/ui/CommandPalette.tsx:138 ('New Order' present, no Quick Delivery — the modal has no route to link to); src/pages/CustomerDetail.tsx:575-583 (New Quote / New Order / Sched. Delivery only); src/pages/Deliveries.tsx:1156-1168 (Quick Delivery = variant=\"secondary\", Schedule Delivery = primary button)",
  "rec": "Add one 'Phone Order' (or 'Quick Sale') action that opens the Quick Delivery flow, on: Dashboard Quick Actions (first position), CustomerDetail header (pre-filled with that customer), and the command palette (give the modal a route like /deliveries/quick). Make it the primary button on the Deliveries page. This is the single highest-leverage change for the owner's 'too many clicks' pain."
 },
 {
  "i": 9,
  "source": "book-it-now-call",
  "sev": "high",
  "scope": "owner-decision",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified both are the LATEST migrations for each function (confirmed via grep across all migrations for CREATE OR REPLACE of each name; 20260702132000 and 20260614142939 are the newest hits for create_quick_delivery and create_direct_order respectively). Verified create_quick_delivery (lines 103-110): v_net_available = quantity_available - quantity_prebooked (no on-order term) and `RAISE EXCEPTION 'Insufficient inventory...'` — a hard block; p_scheduled_date only appears later at the deliveries INSERT (line 168), confirmed absent from the stock-check loop (97-119). Verified create_direct_order (lines 106-114): v_net_position = quantity_available - quantity_prebooked + COALESCE(quantity_on_order,0) — DOES include on-order — and shortfalls are appended to v_warnings (order still proceeds), not exception-raised. This is a genuine, precisely-verified policy contradiction between the two RPCs that back the app's two order-entry paths, both usable by the same admin/sales_rep roles (confirmed via canQuickDeliver at Deliveries.tsx:162).",
  "title": "Same phone order, opposite stock rules: Quick Delivery hard-blocks, New Order warns — and Quick Delivery ignores incoming PO stock and the future delivery date",
  "problem": "The two ways to book the identical 20-gal Thursday order enforce contradictory inventory policy. create_quick_delivery REFUSES the whole booking if net stock right now (on-hand minus prebooked; incoming PO stock NOT counted) is short — even though the delivery is scheduled for Thursday and a purchase order lands Wednesday. create_direct_order books the same thing with a warning toast, and its net-position math DOES count on-order stock. So during the busy season with stock in transit, the 'fast' tool rejects bookings the 'slow' tool accepts; two staff taking the same call get different answers, and the office learns to distrust the fast path. The rejection also fires only at the final click, after the whole order is keyed (see the picker finding).",
  "evidence": "supabase/migrations/20260702132000_a4_quick_delivery_tier_price_cascade.sql:105-109 (v_net_available = quantity_available - quantity_prebooked; RAISE EXCEPTION 'Insufficient inventory'; p_scheduled_date is used only at line 168/269 for the delivery row, never in the stock check) vs supabase/migrations/20260614142939_create_direct_order_customer_po_param.sql:102-112 (v_net_position includes quantity_on_order; shortfall appended to v_warnings, order still created)",
  "rec": "Owner decision on policy, then one migration: recommended = make create_quick_delivery warn-not-block like everything else in the app (its whole design philosophy is warn-only), or at minimum only hard-block when scheduled_date is today (same-day load-out genuinely needs physical stock). Whichever way, use the same net formula (include on-order) in both RPCs."
 },
 {
  "i": 10,
  "source": "book-it-now-call",
  "sev": "high",
  "scope": "UI",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified NewOrder.tsx (grep 'quote' = 0 hits) and QuickDeliveryModal.tsx (grep 'quote' = 0 hits) never query quotes/bookings. Verified 20260614142939_create_direct_order_customer_po_param.sql line 173 increments quantity_prebooked unconditionally with no quote/booking awareness — it only checks the product's own inventory net position, never a customer's open booking hold on the same product. Verified the draw-down UI exists only in QuoteBuilder.tsx (openDrawDownModal:1584, handleDrawDown:1646-1685, calling the correct draw_down_quote RPC and landing on /orders/:id). All cited evidence checks out exactly; recommendation is technically sound (a banner/query is UI-only since draw_down_quote already works).",
  "title": "No order-taking screen knows the caller has an open planned booking — no steering to draw-down",
  "problem": "When a big-account customer with an open program booking (sent/revised planned quote holding inventory at a locked price) phones in '20 gal of Product X', neither New Order nor Quick Delivery gives any hint that a booking exists. Neither screen queries quotes at all, and neither RPC coordinates with booking holds. If the office books it as a direct order: (1) the 20 gal is counted against stock twice — the new order prebooks on top of the booking's existing hold for the same product — producing phantom shortfalls elsewhere; (2) the customer is billed today's tier price instead of the price locked in their program; (3) the booking keeps a phantom un-drawn balance into season settlement. The correct path (draw-down) is buried: you must already know the booking exists, find it on the Quotes page, open the 3,448-line QuoteBuilder, and use its Draw Down modal.",
  "evidence": "src/pages/NewOrder.tsx (full read — no quote/booking query anywhere); src/components/deliveries/QuickDeliveryModal.tsx (same); grep 'quote' in supabase/migrations/20260614142939_create_direct_order_customer_po_param.sql = 0 hits (and it increments inventory.quantity_prebooked at line 173 regardless of existing booking holds); draw-down UI exists only in src/pages/QuoteBuilder.tsx:1584-1682 (openDrawDownModal/handleDrawDown → draw_down_quote)",
  "rec": "On customer selection in New Order and Quick Delivery, run one query for the customer's open planned bookings (quotes status in sent/revised, is_planned) with remaining balances; if any chosen product is covered, show a banner: 'Jones Farm has an open booking for Product X at $Y — draw from booking instead?' with a one-click path that calls draw_down_quote. UI-only; all data and RPCs already exist."
 },
 {
  "i": 11,
  "source": "book-it-now-call",
  "sev": "medium",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified live DB schema for `orders` table (via execute_sql) has NO requested/ship/delivery-date column — only order_date, confirming the doc claim at database-schema.md:26 against the actual live catalog, not just docs. Verified NewOrder.tsx has only `orderDate` (localToday default, lines 86/618) with no separate delivery-date field. Verified NewDelivery.tsx:65 defaults scheduledDate to localToday(). Verified ToShip.tsx:521,528 computes `age = daysAgo(l.order_date)` with red styling at age>=14, no promise-date concept anywhere in that file section. All citations accurate.",
  "title": "'Deliver Thursday' has no home on the promoted path — the promise date lives in the operator's head across three screens",
  "problem": "New Order has no delivery-date field, and the orders table has no requested-delivery-date column, so the caller's 'deliver Thursday' can only go into free-text notes. The delivery date is first captured two screens later in Schedule Delivery — which defaults to TODAY, so a distracted operator either ships it the wrong day or, if the phone rings after order creation, never schedules it at all. Nothing chases the miss: the To-Ship safety net shows only 'Waiting Nd' age since order date, not a promised date, so a broken Thursday promise is invisible until the customer calls back angry.",
  "evidence": "src/pages/NewOrder.tsx (order form fields: customer/name/date/PO/notes/items only); docs/reference/database-schema.md:26 (orders columns — order_date only, no requested/ship date); src/pages/NewDelivery.tsx:65 (scheduledDate defaults localToday()); src/pages/ToShip.tsx:521-528 (age = daysAgo(order_date), red at >=14d — no promise-date concept)",
  "rec": "Add a 'Requested delivery date' to order entry (nullable column + shown on To-Ship, pre-fills NewDelivery), or cheaper UI-only version: after create_direct_order succeeds, auto-open NewDelivery pre-filled (?order=) with a prompt 'Schedule the delivery now?' so the booking and the schedule happen in one continuous motion."
 },
 {
  "i": 12,
  "source": "book-it-now-call",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified CustomerDetail.tsx:578 navigates to `/orders/new?customer_id=${id}` and :581 to `/deliveries/new?customer_id=${id}` exactly as cited. Verified NewOrder.tsx has zero `useSearchParams`/`searchParams` references anywhere in the file (customerId only ever set via user selection, initialized ''). Verified NewDelivery.tsx:53 reads only `searchParams.get('order')` — no customer_id handling at all. All citations exact.",
  "title": "CustomerDetail's New Order and Sched. Delivery buttons pass ?customer_id= that the target pages never read — the customer is lost in transit",
  "problem": "From a customer's page, clicking 'New Order' or 'Sched. Delivery' navigates with the customer id in the URL, but New Order never reads query params (form opens blank; operator must re-find the same farm in the full-list dropdown) and Schedule Delivery only reads an 'order' param (operator must pick from a dropdown of ALL open orders across every customer). Wasted clicks on a daily path plus real wrong-customer/wrong-order pick risk.",
  "evidence": "src/pages/CustomerDetail.tsx:578 (navigate(`/orders/new?customer_id=${id}`)) and :581 (navigate(`/deliveries/new?customer_id=${id}`)); src/pages/NewOrder.tsx has no useSearchParams import and customerId initializes to '' (line 84); src/pages/NewDelivery.tsx:53 reads only searchParams.get('order')",
  "rec": "Make NewOrder read customer_id and preselect the customer; make NewDelivery accept customer_id and filter its order dropdown to that customer (or preselect their only open order)."
 },
 {
  "i": 13,
  "source": "book-it-now-call",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified NewOrder.tsx:570-589 is a plain native <select> over all customers (no search box), vs QuickDeliveryModal.tsx:107-137 which has a real 300ms-debounced ilike search against farm_name/account_number. Verified NewOrder.tsx:341-357 fires the duplicate check on ANY order in the last 7 days for the customer regardless of product overlap, then :882-893 shows it via a blocking ConfirmModal. Verified QuickDeliveryModal.tsx: Create button (line ~502) opens ConfirmModal (lines 512-521) before calling handleSubmit — confirming the 'composition modal, then a second confirm' structure. All citations accurate.",
  "title": "Friction stacked on the highest-volume screen: non-searchable customer dropdown, a blunt 7-day duplicate interrupt, and a double-confirm",
  "problem": "Three separate speed bumps on phone-order entry. (a) New Order's customer picker is a plain native dropdown over every customer — no search — while Quick Delivery has a proper debounced search; the worse picker is on the promoted path. (b) New Order pops a blocking 'Duplicate Order Warning' modal if the customer has ANY order in the last 7 days, regardless of what's on it — in season, weekly repeat buyers trigger it on nearly every call, adding a modal + click each time. (c) Quick Delivery requires a second confirmation modal after the create button. Each is one extra interruption multiplied by every phone call, on the exact workflow the owner says must be fastest.",
  "evidence": "src/pages/NewOrder.tsx:570-589 (native <select> over customers) vs src/components/deliveries/QuickDeliveryModal.tsx:107-137 (debounced ilike search); src/pages/NewOrder.tsx:341-357 + 882-893 (7-day any-order check → ConfirmModal); src/components/deliveries/QuickDeliveryModal.tsx:500-521 (Create → ConfirmModal → Create)",
  "rec": "Give New Order the same searchable customer picker; make the duplicate check smarter (only warn when product overlap with an order from the last ~2 days, or demote to an inline banner instead of a blocking modal); drop Quick Delivery's second confirm (the modal itself is already a deliberate composition step)."
 },
 {
  "i": 14,
  "source": "book-it-now-call",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "ADJUSTED",
  "verdictReason": "Confirmed the picker shows only Size + tier1_price (lines 554-559) with no On Floor/Net, vs NewOrder.tsx:909-944 which shows both — and confirmed the hard block at 20260702132000_a4_quick_delivery_tier_price_cascade.sql lines 106-110 (RAISE EXCEPTION on insufficient net-available). But the evidence understates a second, worse bug on the same lines: the modal already has a correct tier-aware `getTierPrice(product)` helper (lines 165-170) used elsewhere in the file (re-pricing on customer change, lines 149-153) — yet the picker button hardcodes `product.tier1_price` directly instead of calling it. So for a tier-2/3 customer the picker doesn't just omit stock info, it displays the WRONG price outright (e.g. shows tier-1 $40 for a customer whose actual delivery will bill tier-3 $28). Correction: severity should be MEDIUM not low — this is a visible-price bug, not just a missing nice-to-have, though still confined to a lower-volume screen (Quick Delivery) with real product-picker parity available to copy from New Order.",
  "title": "Quick Delivery's product picker shows no stock numbers even though its RPC hard-rejects on stock at submit",
  "problem": "The Quick Delivery product picker shows only unit size and the tier-1 price (not even the selected customer's tier price), with no On Floor / Net numbers — while New Order's picker shows both. Since create_quick_delivery hard-blocks on insufficient net stock, the operator only discovers the problem at the very last click, after keying the entire order, then has to guess which line is short. The information exists (New Order already fetches and renders it).",
  "evidence": "src/components/deliveries/QuickDeliveryModal.tsx:554-559 (picker renders Size + tier1_price only) vs src/pages/NewOrder.tsx:909-944 (picker renders On Floor + Net per product); block behavior: supabase/migrations/20260702132000_a4_quick_delivery_tier_price_cascade.sql:105-109",
  "rec": "Reuse New Order's inventory lookup in the Quick Delivery picker (On Floor + Net badges, red when short) and show the selected customer's tier price instead of tier-1."
 },
 {
  "i": 15,
  "source": "spray-job-scheduling",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified JobDetail.tsx has literally 0 case-insensitive 'dispatch' matches in the whole file (grep count = 0), confirming zero dispatch awareness on that page, with the Applicator dropdown at line 2632-2635. Verified FieldView.tsx's own doc comment states the phone page is fed 'ONLY' by get_dispatched_list, and the empty-state text at line 418 is exactly 'No jobs assigned to you right now.' Verified get_dispatched_list (latest: 20260626150000_get_dispatched_list_stable_order.sql:70-74) reads FROM job_location_dispatches only. Verified Jobs.tsx:571 derives applicator_name solely from jobs.applicator_id via a nameMap, and its fetch query (lines 408-421) has no dispatch embed. Verified aggregateAssignedTo (dispatchWizard.ts:198-209) returns per-location dispatch names first and only falls back to the job-level name when none exist — confirming it hides the JobDetail applicator whenever any dispatch exists. Verified JobMassEditModal.tsx writes applicatorId via a direct .update() on the jobs table only (lines 92-133), never touching job_location_dispatches. Every citation checks out exactly; this is a real, well-evidenced daily-workflow gap given the owner's stated pain about confusing next-steps.",
  "title": "Two disconnected 'who sprays this job' systems: JobDetail's Applicator field vs the Dispatch wizard — neither surface shows the other",
  "problem": "There are two separate ways to hand a job to an applicator, and they don't talk to each other. (1) JobDetail has a prominent 'Applicator' dropdown that writes jobs.applicator_id. (2) The Dispatch Board wizard writes per-field rows into job_location_dispatches. The applicator's phone page ('My Field Jobs', /field) is fed ONLY by system 2 — a job assigned via JobDetail's dropdown NEVER appears there ('No jobs assigned to you right now'). Meanwhile the office Jobs list's 'Applicators' column reads ONLY system 1 — a wizard-dispatched job shows '-' there. And once a job has any wizard dispatch, the Dispatch Board deliberately hides the JobDetail applicator. So the office can 'assign Bob' on JobDetail and the work silently never reaches Bob's phone; or a dispatcher reassigns to Alice in the wizard and the Jobs list still says Bob. Bulk 'Mass Edit → Applicator' also writes only system 1, so a bulk reassign never changes who the phone routes to. Both mechanisms are needed today (system 1 feeds the license gate and office lists; system 2 feeds the phone and crew-splits) but nothing syncs them and no page shows both.",
  "evidence": "JobDetail.tsx:2632-2643 (Applicator dropdown), :1845 (save_job payload applicator_id), :1826 (assign_job_applicator); FieldView.tsx:10-13 + 418-421 (phone page reads only get_dispatched_list; empty-state text); get_dispatched_list FROM job_location_dispatches only (migration 20260626150000:70-74); Jobs.tsx:571 (applicator_name from jobs.applicator_id; the 408-421 fetch has no dispatch embed); aggregateAssignedTo hides job-level name when any dispatch exists (src/lib/dispatchWizard.ts:198-208, dispatchDisplay.ts:131-142); JobMassEditModal.tsx:125-131 (direct jobs update); grep 'dispatch' in JobDetail.tsx = 0 matches (JobDetail shows no dispatch state).",
  "rec": "Make one action do both: when JobDetail saves an Applicator on a scheduled job, auto-create/refresh a job_location_dispatches row for each of the job's locations (a whole-job dispatch), and show the live per-location dispatch state on JobDetail. Then the wizard remains only for the split-crew case. Also render the dispatched assignee in the Jobs list Applicators column (fall back to jobs.applicator_id)."
 },
 {
  "i": 16,
  "source": "spray-job-scheduling",
  "sev": "high",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified jobListColumns.ts (actual path src/components/jobs/jobListColumns.ts, not the flat path cited, but same content) registry (lines 58-79) has 19 toggleable columns including 'applicator_name' but nothing dispatch-state related. Verified OfficeCockpit.tsx has an 'Upcoming Jobs (7 days)' tile and an 'Inventory Shortfalls' tile but no undispatched/needs-dispatch tile anywhere in the grepped content. Verified DispatchBoard.tsx:903-909 renders the 'Assigned To:' line only conditionally (`{job.assigned_to_label && (...)}`) confirming its absence is the sole signal. Verified DispatchFilters interface (dispatchDisplay.ts:82-91) has exactly status/applicatorId/recipeId/search/startDate/endDate — no unassigned option. Minor path discrepancy (file lives under components/jobs/ not a bare config/ path) does not affect the substance.",
  "title": "'Scheduled but never dispatched' is invisible — no page, column, filter, or cockpit tile reveals the gap",
  "problem": "After a job is created it sits in status 'scheduled' whether or not anyone was told to spray it. Nothing distinguishes the two: the Jobs list has no dispatch/assignment-state column (only the Applicators name column, which is blank-or-stale per the finding above); the Office Cockpit has tiles for upcoming jobs and inventory shortfalls but none for 'needs dispatch'; the Dispatch Board only shows an 'Assigned To:' line when one exists — the ABSENCE of that line is the sole signal, and there is no 'unassigned' filter. A job scheduled Monday from a quote and never pushed through the wizard stalls silently until the customer calls asking why field 12 wasn't sprayed.",
  "evidence": "jobListColumns.ts:59-78 (no dispatch-state column); OfficeCockpit.tsx:805 + :944 (upcoming-jobs and shortfall tiles; no undispatched tile); DispatchBoard.tsx:903-909 (Assigned To rendered only when non-null); dispatchDisplay.ts:82-100 (DispatchFilters has status/applicator/recipe/search/date — no 'unassigned' option).",
  "rec": "Add a 'Needs dispatch' signal: an Office Cockpit tile + a Jobs-list badge/filter for scheduled jobs with zero active job_location_dispatches rows (and optionally also no applicator_id). This is the single highest-leverage guard against the walkthrough's main stall."
 },
 {
  "i": 17,
  "source": "spray-job-scheduling",
  "sev": "high",
  "scope": "DB",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified the CHECK constraint at 20260626120000_job_location_dispatches.sql:35-36 allows ('dispatched','completed','cancelled'). Verified a repo-wide grep for any migration ever setting dispatch_status to a terminal value returns exactly ONE file: 20260626140000_undispatch_job_locations.sql — no other migration or trigger ever does this. Verified complete_job's latest definition (20260702175000_layer2_complete_job_drop_phase7_drain.sql, function signature at line 29) has ZERO references to job_location_dispatches anywhere in its body — even stronger than the finding's claim (it doesn't just fail to set 'completed', it never touches the table at all). Verified JobDetail.tsx's handleCancelJob (lines 2082-2094) does a direct .update({status:'cancelled'}) on the jobs table only, no dispatch table interaction. Verified get_dispatched_list filters only on d.dispatch_status='dispatched' (line 74) with no job-status join. This is a real, high-confidence, well-cited data-hygiene bug that will visibly clutter the applicator's phone screen and the office dispatched list all season.",
  "title": "Dispatch rows never close: completed/cancelled jobs stay 'dispatched' forever, steadily burying the applicator's phone page and the office Dispatched List",
  "problem": "A dispatch row has a lifecycle (dispatched → completed | cancelled) but nothing in the system ever moves it forward except the manual 'Undispatch' button. Completing a job (complete_job) or cancelling it (JobDetail's Cancel) leaves every one of its location dispatches at 'dispatched'. The applicator's 'My Field Jobs' phone page and the office 'View Dispatched List' both show every row still at 'dispatched' — so by mid-season an applicator's phone fills with DONE/CANCELLED cards above and below the 2 live ones (there's no status filter on that page and cards sort by job number, not date), and the office list accumulates every finished location all season unless someone manually undispatches each row one by one. The lingering rows also keep granting the applicator RLS visibility into long-finished jobs.",
  "evidence": "dispatch_status CHECK allows 'completed'/'cancelled' (migration 20260626120000:35-36) but repo-wide grep shows only undispatch_job_locations ever sets a terminal value (20260626140000:122 sets 'cancelled'); complete_job's latest version (20260702175000) and JobDetail's cancel (direct .update, JobDetail.tsx:2089-2094) never touch job_location_dispatches; get_dispatched_list filters only d.dispatch_status='dispatched' with no job-status filter (20260626150000:74); FieldView cards = groupDispatchedByJob over those same rows with only a text search (dispatchDisplay.ts:262-309); RLS helper _is_dispatched_to_me checks dispatch_status only (20260626120000:80-104).",
  "rec": "One small migration: when a job transitions to completed/cancelled (the existing status-transition path or a trigger like trg_release_job_holds_on_lifecycle), set its 'dispatched' rows to the matching terminal dispatch_status. Optionally also hide terminal-status jobs from the phone list by default."
 },
 {
  "i": 18,
  "source": "spray-job-scheduling",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified QuoteBuilder.tsx handleScheduleJob (lines 1558-1580) calls create_job_from_quote_section and navigates straight to /jobs/:id with zero date/applicator prompt. Verified the latest create_job_from_quote_section (20260703200000_layer2_close_quote_as_applied.sql) sets `v_job_date := COALESCE(v_section.needed_by_date, CURRENT_DATE)` at line 468 exactly as cited, with no applicator_id in the INSERT. Verified JobDetail.tsx has 0 '/dispatch' matches anywhere (no CTA to the dispatch step). Verified DispatchWizard.tsx's own doc comment confirms it is exactly the '3-step' wizard described (Select Locations -> Assign Selections -> Finish Dispatching). The specific click-count (~19) is an estimate not independently re-derivable, but every underlying mechanism it depends on (no date/applicator prompt on Schedule Job, no JobDetail-to-dispatch link, a separate 3-step wizard) is verified true, so the qualitative claim (double-entry, disconnected screens, no prompted continuation) stands.",
  "title": "Booking-to-dispatched takes ~19 clicks across 4 pages, with the date and the assignee each entered in a disconnected place",
  "problem": "The core daily flow ('booked customer calls: spray field 12 next week') requires: find the quote, click Schedule Job, then IMMEDIATELY repair the job on JobDetail (the job is created dated TODAY or the section's season-level 'Needed By' — the flow never asks 'when?'), pick the applicator there, save, then navigate to a different page (Dispatch Board), open a 3-step wizard, and pick the assignee AGAIN. That's ~19 interactions on the golden path (more if the section lacks a field), the applicator is chosen twice, and JobDetail — the page you land on holding the half-done job — has no link or button to the dispatch step at all, so the operator must remember the second half of the flow unprompted (which is exactly how the silent-stall finding above happens).",
  "evidence": "QuoteBuilder.tsx:1558-1580 (Schedule Job → navigate to /jobs/:id, no date/applicator prompt); create_job_from_quote_section sets v_job_date := COALESCE(needed_by_date, CURRENT_DATE) and no applicator (20260703200000:468-480); grep '/dispatch' in JobDetail.tsx = 0 matches (no dispatch link/CTA); DispatchBoard rail + DispatchWizard 3 steps (DispatchBoard.tsx:560, DispatchWizard.tsx:72-76).",
  "rec": "Collapse the handoff: (a) Schedule Job opens a one-shot modal asking date + applicator and creates the job WITH the dispatch (once finding #1's sync exists this is nearly free); or minimally (b) add a 'Dispatch this job' button on JobDetail for scheduled jobs that opens the wizard pre-scoped to this job's locations."
 },
 {
  "i": 19,
  "source": "spray-job-scheduling",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified DispatchBoard.tsx:916-921 — the button's onClick is exactly `() => setWizardOpen(true)`, passing no job id or scoping information. Verified `dispatchableLocations` (lines 531-536) is a useMemo that flatMaps locations across ALL visibleJobs filtered only by status (scheduled/in_progress) — not scoped to any single job. Verified the single DispatchWizard instance (lines 993-1002) receives `locations={dispatchableLocations}` — this same global, unscoped list — regardless of which job's button triggered the open. Every citation checks out exactly as described.",
  "title": "The per-job 'Dispatch Locations' button opens the GLOBAL wizard, not the job you clicked",
  "problem": "On the Dispatch Board, every dispatchable job row has a 'Dispatch Locations' button that looks job-specific — but it opens the same wizard listing EVERY dispatchable location of every visible job, with nothing pre-selected. In a busy week (dozens of scheduled jobs) the dispatcher must re-find the job they just clicked inside the wizard's long list, and on a tablet it's easy to tick a neighboring job's field by mistake.",
  "evidence": "DispatchBoard.tsx:914-921 — onClick={() => setWizardOpen(true)} passes no job id; the wizard receives dispatchableLocations flattened across ALL visible jobs (DispatchBoard.tsx:531-537 and 993-1002).",
  "rec": "Pass the clicked job's locations to the wizard (or open it with that job's locations pre-selected and scrolled into view). Keep 'Start Dispatch' as the cross-job entry."
 },
 {
  "i": 20,
  "source": "spray-job-scheduling",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "The dispatch wizard hides a location's CURRENT assignee — re-dispatch silently steals work",
  "problem": "Step 1 of the wizard lists every location of every scheduled/in-progress job — including locations ALREADY dispatched to someone — with no marker of who has it. Committing is an upsert that silently replaces the existing assignee. With two office staff dispatching (owner + office/sales are all daily users), the second person can unknowingly re-route a field that's already on another applicator's phone; the first applicator's card just vanishes with no warning to anyone.",
  "evidence": "DispatchLocation carries only jobFieldId/jobNumber/customerName/fieldName/acres — no current assignee (DispatchBoard.tsx:329-338, dispatchWizard.ts types); dispatch_job_locations ON CONFLICT (job_field_id) DO UPDATE replaces applicator/crew (20260626120000:363-374); no confirm/diff step in DispatchWizard.tsx.",
  "rec": "Show an 'already → <name>' chip on dispatched locations in Step 1 and require an explicit confirm when a commit would reassign any location."
 },
 {
  "i": 21,
  "source": "spray-job-scheduling",
  "sev": "medium",
  "scope": "both",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "'Schedule Job' from a section with no Field creates a location-less job that can never be dispatched — and QuoteBuilder never shows which sections already have jobs",
  "problem": "A quote section's Field picker is optional ('— None —'). Scheduling a job from a field-less section creates a job with chemicals but ZERO locations; the dispatch wizard only offers job_fields rows, so this job can never be dispatched and never reaches any phone — and the job saves fine without fields, so nothing forces the fix. Separately, QuoteBuilder gives no per-section indication that a job already exists: on a 5-pass program the office can't see which passes are already scheduled, and clicking Schedule Job on an already-scheduled section just throws 'A job already exists for this quote section' instead of opening that job. (That guard also means a completed section can never be re-jobbed for a second pass without deleting the finished job.)",
  "evidence": "create_job_from_quote_section inserts job_fields only IF v_section.field_id IS NOT NULL (20260703200000:482-485); QuoteBuilder Field select defaults '— None —' (QuoteBuilder.tsx:2592-2604); wizard locations built solely from job_fields (DispatchBoard.tsx:329-338); job save requires only customer+date (JobDetail.tsx:1773-1774); duplicate-section guard + error text (20260703200000:464-466); no jobs-by-section fetch/badge exists in QuoteBuilder.tsx (grep quote_section_id = only the RPC call).",
  "rec": "At Schedule Job time, block or loudly warn when the section has no Field (offer the customer's field list inline). Add a small per-section badge — 'Job J-1042 (scheduled)' linking to it — and turn the duplicate error into 'Open existing job'."
 },
 {
  "i": 22,
  "source": "spray-job-scheduling",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Rain-delay moves never surface to the applicator: the phone card hides the date and nothing notifies on dispatch or reschedule",
  "problem": "Rescheduling works (JobDetail date edit, or Jobs-list Mass Edit → Schedule Date for a whole rained-out day) and the dispatch correctly survives the move — but the applicator has no way to notice. The collapsed 'My Field Jobs' card shows job#, customer, location count and acres, NOT the scheduled date; the date only appears after expanding a card (lazy-loaded), cards sort by job number rather than date, and no notification of any kind fires on dispatch, reschedule, or undispatch (the job_notifications system is customer-facing only). The applicator must expand every card each morning to reconstruct today's work.",
  "evidence": "FieldView.tsx:444-465 (collapsed card fields — no date), :483-485 (date only in expanded detail); groupDispatchedByJob sorts by job_number (dispatchDisplay.ts:291-293); JobMassEditModal.tsx:199-232 (bulk Schedule Date exists); job_notifications = customer notices (migration 20260628120000); no applicator-notify call in dispatch_job_locations (20260626120000) or undispatch (20260626140000).",
  "rec": "Show job_date on the collapsed card and sort cards by date (today first). Later, add an in-app notification row to the existing Notifications page when a user's dispatch is created, moved, or undispatched."
 },
 {
  "i": 23,
  "source": "spray-job-scheduling",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "No customer pre-fill when creating a job from a customer context",
  "problem": "CustomerDetail can start a pre-filled quote ('/quotes/new?customer_id=...') but has no equivalent 'New Job' action, and the job page only understands a recipe_id URL parameter — so for the non-program caller ('come spray my field'), the office starts a completely blank job form and re-picks the customer they were just looking at. Small, but it's on the highest-volume phone-call path.",
  "evidence": "CustomerDetail.tsx:575 and :997 (quote prefill only, no job equivalent); JobDetail.tsx:1536 + :256 (searchParams read only recipe_id and tab); DispatchBoard 'Add New Job' also navigates to bare /jobs/new (DispatchBoard.tsx:705).",
  "rec": "Support /jobs/new?customer_id=... in JobDetail and add a 'New Job' button on CustomerDetail (and pre-filter its field picker to that customer, which JobDetail already does once a customer is chosen)."
 },
 {
  "i": 24,
  "source": "applicator-phone-day",
  "sev": "critical",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Applicator cannot START their own job from any screen — the field lifecycle stalls at 'scheduled' until the office intervenes",
  "problem": "The database explicitly authorizes the assigned applicator to start their own job (start_job allows 'is_applicator() AND applicator_id = actor'), but no screen an applicator can reach shows a Start button. JobDetail hides Start behind isEditable (admin/sales only), and My Field Jobs (FieldView) is fully read-only with zero action buttons. Since complete_job server-side requires status = 'in_progress', an applicator standing at the field edge cannot move the job at all — they must phone the office and ask someone to click Start on a desktop before they can later record completion. The wrong-order protection (can't complete without start) becomes a hard blocker for the exact persona the app was supposed to serve on phones.",
  "evidence": "src/pages/JobDetail.tsx:241 (isEditable = admin|sales_rep), :2561 (Start Job gated on isEditable), :2491 (canComplete has no role gate); start_job authorizes the assigned applicator in supabase/migrations/20260611211058_idempotency_operation_scope_sweep.sql ('is_applicator() AND v_job.applicator_id = v_actor'); complete_job requires in_progress at supabase/migrations/20260702175000_layer2_complete_job_drop_phase7_drain.sql:88; grep confirms JobDetail.tsx:2039 is the ONLY start_job call site in src/; FieldView.tsx header comment line 7 ('READ-ONLY: no pricing/job-setup edits here')",
  "rec": "Show the Start Job button to the assigned applicator: change the gate at JobDetail.tsx:2561 from `isEditable` to `isEditable || (role === 'applicator' && applicatorId === profile?.id)` — the same pattern already used for JobAttachments at line 3350. The server already enforces the real authorization, so this is a one-condition UI change. Better still, put Start/Complete buttons directly on the FieldView card (see next finding)."
 },
 {
  "i": 25,
  "source": "applicator-phone-day",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "'My Field Jobs' — the applicator's namesake phone page — is a dead end: no actions, no link to the job, and no date on the card face",
  "problem": "The one page built for the person spraying (FieldView, /field) can only LOOK at jobs. There is no Start, no Complete, no applied-info entry, and not even a tap-through to the JobDetail page where the applicator's single working button (Complete) lives — they must back out to the hamburger menu and hunt through the office 'Job Schedule' table instead. Worse, the card face doesn't show the scheduled date: get_dispatched_list doesn't return job_date and sorts by job NUMBER, so 'what am I spraying today?' requires expanding every card one tap + one network fetch at a time. Completed/billed jobs also stay in the list (DONE/BILLED badges) until someone undispatches them, burying today's work deeper as the season progresses.",
  "evidence": "src/pages/FieldView.tsx — no navigate()/router import, no mutation calls anywhere in the file; job_date only fetched on expand (FieldView.tsx:241-247); supabase/migrations/20260626150000_get_dispatched_list_stable_order.sql:32-68 (returned jsonb has no job_date) and :109 (ORDER BY j.job_number); :74 (WHERE dispatch_status='dispatched' — no job-status filter, so DONE/BILLED cards persist, badge mapping in src/lib/dispatchDisplay.ts:64-79)",
  "rec": "Make FieldView the applicator's whole day: add job_date to get_dispatched_list, print it on the card face, sort/group by date ('Today' first), collapse completed jobs into a 'Done' section, and put Start Job / Complete Job buttons (calling the existing RPCs that already authorize the assigned applicator) directly on the expanded card. That turns the phone flow into: open app → tap card → Start → spray → Complete. This is the single highest-leverage pre-launch fix for the field side."
 },
 {
  "i": 26,
  "source": "applicator-phone-day",
  "sev": "high",
  "scope": "UI",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "No offline path for the applicator's one write (complete_job) — while the offline machinery for it already exists as dead code, and drivers (who won't use phones) got the full offline treatment",
  "problem": "Spray fields have poor cell signal. If the applicator taps Complete Job with no connection, the RPC hard-fails with a 'Failed to complete job' toast — nothing is queued, and if they close the tab the typed weather/gallons are gone. The irony is double: (a) the offline sync engine ALREADY has a complete_job replay branch, but no screen ever enqueues that operation — only the two DRIVER pages (FieldStop, DeliveryDetail) call queueAction; (b) per the owner, delivery drivers are the ones who will NOT reliably use phones, yet they got the polished offline stepper (queue, conflict detection, 'saved offline — will sync' toast), while the phones-first applicators got none of it. The PWA service worker caches only the app shell and fonts, no data, so offline the applicator gets a working-looking app that can neither read nor write.",
  "evidence": "src/lib/offlineSync.ts:179-184 (complete_job replay branch); queueAction call sites are only src/pages/FieldStop.tsx:309 and src/pages/DeliveryDetail.tsx:778 (grep across src/); JobDetail.tsx:2061 (direct rpc('complete_job') with no isOnline branch); vite.config.ts:54-84 (workbox precache + fonts runtimeCaching only — no Supabase API caching); FieldStop.tsx:306-329 shows the exact offline pattern the job flow lacks",
  "rec": "Reuse the FieldStop pattern for job completion: when navigator.onLine is false, queueAction({ operation: 'complete_job', params, entityTable: 'jobs', entityId, snapshotAt: job.updated_at }) and toast 'Saved offline — will sync'. The replay branch, retry logic, conflict guard, and Sentry wiring already exist; this is wiring, not new machinery. Do it wherever Complete ends up living (JobDetail today, FieldView after the redesign)."
 },
 {
  "i": 27,
  "source": "applicator-phone-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "On a failed fetch, My Field Jobs tells the applicator 'No jobs assigned to you right now' — a poor-signal lie",
  "problem": "When the jobs fetch errors (weak signal, timeout), FieldView catches the error, shows a 3-second toast, and sets the card list to empty — which renders the permanent empty state 'No jobs assigned to you right now. When a dispatcher hands you a field location, it shows up here.' An applicator in a low-bar area who opens the app after the toast fades (or misses it) reasonably concludes they have no work today and drives home. A network failure and 'you have no assignments' are opposite messages sharing one screen.",
  "evidence": "src/pages/FieldView.tsx:142-146 (catch → toast + setCards([])), :414-423 (cards.length===0 renders the 'No jobs assigned to you right now' empty state with no error variant)",
  "rec": "Track a loadError state: on fetch failure render 'Couldn't load your jobs — check your signal' with a big Retry button (the reload button already exists at :371-377), and never show the no-assignments copy unless the fetch actually succeeded with zero rows."
 },
 {
  "i": 28,
  "source": "applicator-phone-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "JobDetail shows applicators two trap buttons that always fail: Cancel Job and Transfer to Invoice",
  "problem": "The JobDetail action bar renders Cancel Job (any role, on scheduled/in_progress jobs) and Transfer to Invoice (any role, on completed jobs). For an applicator both are guaranteed errors: Cancel does a direct jobs-table UPDATE that the phase-5 RLS hardening restricted to admin/sales (the update silently matches 0 rows → error toast), and transfer_job_to_invoice raises 'Not authorized: requires admin,sales_rep role'. So right after completing a job, the most prominent next-step button on the applicator's screen is one that scolds them when tapped. This is exactly the owner's 'confusing statuses — what do I do next?' pain: dead buttons teach users to distrust every button.",
  "evidence": "src/pages/JobDetail.tsx:2555-2559 (Cancel Job, no role gate) + :2089-2094 (direct .update on jobs); supabase/migrations/20260430180000_field_app_workflow_phase5.sql:28-32 (jobs_update RLS = admin/sales only); JobDetail.tsx:2492+2573-2578 (canTransfer, no role gate); supabase/migrations/20260622020000_transfer_job_invoice_audit_row.sql:71 ('Not authorized: requires admin,sales_rep role')",
  "rec": "Gate both buttons on isEditable (matching Start Job's gate today), and after an applicator completes a job show a friendly terminal state instead ('Done — the office will bill this job'). One-line conditions; no server change needed."
 },
 {
  "i": 29,
  "source": "applicator-phone-day",
  "sev": "medium",
  "scope": "owner-decision",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Everything the applicator uniquely knows at the field gets keyed twice: the phone captures only a 5-field completion snapshot, while per-field acres, per-pass records, and lot numbers are office-only",
  "problem": "The applicator's entire data contribution is the Complete Job modal: wind, temperature, humidity, total gallons, notes (all optional — good burden, and it has a one-tap 'Use current weather' auto-fill). But the records that actually drive per-acre billing and compliance are locked to office roles: (a) the per-pass as-applied records with per-field APPLIED ACRES (which feed jobs.applied_acres → remaining_acres → the invoice) are AppliedRecordsManager, passed canEdit = admin/sales-only; (b) product lot numbers on the application record are explicitly 'applicators can view records but not edit lots'; (c) the invoice-side applied info (actual acres, structured start/end weather, diluent) lives on the FieldApplicationInvoice route, gated admin/sales. Net effect: the applicator writes acres and lots on paper at the field, and the office re-types them later — double entry for the highest-volume daily workflow, with transcription risk on the numbers that become the bill.",
  "evidence": "src/pages/JobDetail.tsx:3515-3532 (complete modal fields) + :3509-3513 (weather auto-fill); :3284 (AppliedRecordsManager canEdit={canEdit}) with canEdit = isEditable-based at :2490; AppliedRecordsManager captures per-location applied acres per header comment src/components/jobs/AppliedRecordsManager.tsx:10-11,67-68; src/pages/ApplicationRecords.tsx:52-53 ('applicators can view records but not edit lots'); src/App.tsx:208-209 (/invoices/field-app/* = admin,sales_rep); note the DB already ownership-gates applicator writes on job_applied_info (supabase/migrations/20260430180000:37-51)",
  "rec": "Owner decision on how much entry to push to the phone, but the minimum-regret move is per-field applied acres: extend AppliedRecordsManager's canEdit to the assigned applicator (same pattern as JobAttachments at JobDetail.tsx:3350) or add an acres-per-field step to the Complete flow. Lot entry from the phone is a natural second step since the applicator is holding the jug. Keep the office as reviewer/corrector rather than primary keyer."
 },
 {
  "i": 30,
  "source": "applicator-phone-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Applicators land on the office KPI dashboard at login — drivers were special-cased, applicators were forgotten",
  "problem": "The Dashboard hides the office widgets from drivers (showFull = !isDriver) but not from applicators, so an applicator opening the PWA on their phone gets Active Orders, quote pipeline, PO counts, inventory units, and monthly activity charts — none of it their work — served by an intentionally ungated summary RPC. Their actual day (assigned jobs) is two menu taps away. For a phone persona whose top question is 'where am I spraying today?', the first screen is 100% noise, and there is no role-based landing redirect anywhere in the app.",
  "evidence": "src/pages/Dashboard.tsx:381-382 (isDriver special-case; applicator falls into showFull), :410 (Quick Actions gated admin/sales but KPI row at :481 only checks showFull); supabase/migrations/20260323100000_operational_dashboard_summary.sql (SECURITY DEFINER, comment 'Accessible by all authenticated users', no role check in body); grep across src/ shows no role-based navigate/redirect for applicator",
  "rec": "Redirect role='applicator' from '/' to '/field' (and role='driver' to '/my-route') in the root route or AuthContext, or give applicators the same slim treatment drivers get plus a 'My Field Jobs' hero card. Small change, fixes the first impression of every field day."
 },
 {
  "i": 31,
  "source": "applicator-phone-day",
  "sev": "low",
  "scope": "UI",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "The page applicators are forced to use (JobDetail) downloads five entire tables on every open — heavy for a read-only phone visit on rural signal",
  "problem": "Because FieldView has no actions, the applicator's working page is JobDetail — a ~3,700-line office form whose loadLookups fires SELECT * on ALL active customers, ALL fields, ALL active products (604 rows, every column including all tier prices), all vehicles, plus profiles and recipes, in parallel, before the page is useful. All of that exists to power editing dropdowns the applicator can't use (canEdit=false for them). On a field-edge connection that's a long white screen just to reach one Complete button.",
  "evidence": "src/pages/JobDetail.tsx:1323-1328 (Promise.all of customers/fields/products/vehicles select('*')); :241+2490 (applicator gets canEdit=false — the lookups only feed disabled edit controls for them); map bundle already lazy per :48-51 comment (acknowledging 'matters on field/mobile connections')",
  "rec": "Skip loadLookups when !isEditable (render names from the job's own embeds, which fetchJob already returns at :1376-1388), or trim the selects to the columns the form uses. If FieldView gains Start/Complete buttons (finding 2), applicators stop needing JobDetail entirely and this becomes an office-only optimization."
 },
 {
  "i": 32,
  "source": "applicator-phone-day",
  "sev": "low",
  "scope": "owner-decision",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "The applicator's phone card shows the customer's per-product dollar charges — decide if field staff should see billing",
  "problem": "Each expanded My Field Jobs card lists every chemical with its derived customer charge in dollars (quantity x price_per_unit_cents), visible to whoever is dispatched to any location on the job — including ground-crew members. The card footer simultaneously says 'Read-only — pricing and job setup are managed in the office app,' yet it displays the prices. This mirrors ChemMan's card on purpose, but it's a business call: hourly applicators and crew seeing what each customer is billed per jug can leak margin/pricing chatter into the field.",
  "evidence": "src/pages/FieldView.tsx:524-546 (Chemicals/Charges section, formatCents(c.chargeCents)) and :548-550 (read-only pricing footnote); charge derivation src/lib/dispatchDisplay.ts chemicalChargeCents from job_chemicals.price_per_unit_cents, readable via the #38 job_chemicals_select_location_dispatchee policy referenced at FieldView.tsx:19-20",
  "rec": "Ask the owner: should the field card show charges? If no, drop the charge column from the card (the rate/quantity block is what the sprayer actually needs) — the RLS can stay as-is since it feeds office previews too. If yes, keep it deliberately and note it as policy."
 },
 {
  "i": 33,
  "source": "applicator-phone-day",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "The phone card has no REI/PHI/safety line — even after the owner loads label data, the sprayer's screen won't show it",
  "problem": "The known open item is that label DATA is empty on all 604 products (not re-flagged here). The distinct UI gap: FieldView's chemical section renders only name, rate/acre, quantity, and charge — it never queries or displays products.rei_hours / phi_days / signal_word at all. So when the owner does load the label CSV, the office PDFs (WPS notice, applicator sheet) will show re-entry intervals, but the person actually standing in the spray zone with a phone still won't see them without printing a PDF. For a compliance-sensitive detail, the field surface should be ready the day the data lands.",
  "evidence": "src/pages/FieldView.tsx:272-277 (job_chemicals select pulls no product label columns) and :524-546 (rendered fields); the columns exist and are already consumed by JobDetail's WPS path (src/pages/JobDetail.tsx:757-762 selects rei_hours, phi_days, signal_word)",
  "rec": "Add REI/PHI/signal-word to the FieldView detail fetch and render a small safety line per chemical when the values are non-null (blank until the CSV lands, useful forever after). Pairs naturally with the finding-2 card redesign."
 },
 {
  "i": 34,
  "source": "delivery-day",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Follow-up delivery of remainder items can never be billed — silent revenue loss on every partial delivery",
  "problem": "The routine partial-delivery flow ends in a billing dead-end. Day 1: a delivery goes out short, complete_delivery auto-creates draft Invoice #1 for the delivered portion only. Office clicks 'Create Follow-up Delivery' and the rest goes out later. When that follow-up is completed, NOTHING bills it: (a) complete_delivery skips its auto-invoice because the order already has one active invoice; (b) it also never adds the new quantities to Invoice #1 (its true-up only touches invoices linked to the same delivery); (c) the manual 'invoice from order' RPC refuses because the order has deliveries; (d) even the admin backfill RPC on the Integrity Cleanup page raises 'Order already has an active invoice'; and (e) the Integrity Cleanup 'unbilled deliveries' list filters the delivery OUT because its order has an active invoice — so the only detection tool is blind to it. The customer received the remainder product and is never charged for it unless someone notices and hand-edits draft Invoice #1 line-by-line. Partial deliveries are routine in ag, so this will happen in week one.",
  "evidence": "Auto-invoice skipped: supabase/migrations/20260620220000_complete_delivery_audit_and_partial_cost.sql:244-250 (counts ANY active invoice on the ORDER, not this delivery); true-up scoped to same delivery only: same file :223-232. create_invoice_from_order blocked when order has any non-cancelled delivery: 20260614150249_invoice_from_order_delivery_guard.sql:1-12. Backfill RPC rejects: 20260622060000_unbilled_delivery_invoice_order_lock.sql:90-97. Finder hides it: src/pages/IntegrityCleanup.tsx:204-210 (filters by billed order_ids). Follow-up creation: 20260526151856_execute_full_codebase_ultra_review.sql:1101-1196; buttons at src/pages/DeliveryDetail.tsx:693 and src/pages/DeliveryRemainders.tsx:112.",
  "rec": "Change complete_delivery's auto-invoice check from 'order has no active invoice' to 'THIS delivery has no active invoice' (invoices already carry delivery_id, and split-invoice logic already tolerates multiple active drafts per order), so each completed delivery bills its own delivered quantities exactly once. Also fix the Integrity Cleanup unbilled finder to match per-delivery, not per-order, as a safety net."
 },
 {
  "i": 35,
  "source": "delivery-day",
  "sev": "high",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Office has no fast 'mark delivered' path — yesterday's paper ticket takes 6 clicks, 2 modals, and 2 waits",
  "problem": "The owner said deliveries are often keyed in by the office after the fact, and both paths must be fast. Today the office path is: open Deliveries, open the delivery, click 'Start Delivery', confirm inside a modal that lectures about 'verify items are loaded on the truck/trailer' (meaningless when the truck came back yesterday), wait for the server, then type the signer's name from the paper ticket, click 'Complete Delivery', and confirm AGAIN in a second modal. That is 6 clicks + typing + two sequential server round-trips per ticket. There is no single 'Mark Delivered' button anywhere: the quick Complete button on the Deliveries list only appears for in_progress rows — and an after-the-fact paper ticket is always still 'scheduled'. The database enforces the two-step (complete_delivery rejects anything not in_progress and says 'Call confirm_delivery() first'), so office literally cannot skip the ceremony. Even Quick Delivery (the walk-in shortcut) creates its delivery as 'scheduled', so it too ends with this same start-then-complete slog.",
  "evidence": "Two-step enforced: 20260430240000_field_app_workflow_phase12.sql:72-74 (confirm requires scheduled) and 20260620220000_complete_delivery_audit_and_partial_cost.sql:74-76 (complete requires in_progress). Office clicks: src/pages/DeliveryDetail.tsx:1500-1508 (Start button) -> src/components/deliveries/StartDeliveryModal.tsx:39-48 (truck-load warning copy) + :84-94 (confirm), then DeliveryDetail.tsx:2055-2163 (Complete section) -> :2441-2453 (second ConfirmModal). List quick-action gated to in_progress: src/pages/Deliveries.tsx:1046-1056. Quick Delivery creates status 'scheduled': 20260702132000_a4_quick_delivery_tier_price_cascade.sql:167-168.",
  "rec": "Add an office 'Mark Delivered' one-shot: on DeliveryDetail (and as a list action for scheduled rows) one dialog collecting signed-by + optional partial quantities, which chains confirm_delivery then complete_delivery behind one click (no DB change needed), with copy written for after-the-fact entry instead of truck loading."
 },
 {
  "i": 36,
  "source": "delivery-day",
  "sev": "medium",
  "scope": "both",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "After-the-fact entries record the wrong delivery date — no way to backdate completion",
  "problem": "complete_delivery stamps completed_at = now() and the auto-created invoice gets invoice_date = today, with no parameter or form field to say 'this actually happened yesterday'. Since the owner's stated norm is office keying yesterday's paper tickets, every such record will show the wrong delivered-on date: delivery history, the customer's emailed receipt trail, lot-trace timelines, and the invoice date (which starts the payment-terms clock a day late) are all off by however long the paper sat. Neither completion UI (field or office) offers a date input.",
  "evidence": "completed_at hard-coded: 20260620220000_complete_delivery_audit_and_partial_cost.sql:144-148 (completed_at = now()); auto-invoice date: same file :252-257 (invoice_date = CURRENT_DATE); RPC signature has no date param: same file :25. Office completion form fields (quantities, Signed By, issue, signature, email only — no date): src/pages/DeliveryDetail.tsx:2055-2163; phone form same: src/pages/FieldStop.tsx:534-603.",
  "rec": "Add an optional p_completed_at (default now()) to complete_delivery, validated not-in-future, and surface a 'delivered on' date field (defaulting to today) in the office completion dialog; use it for completed_at and the auto-invoice's invoice_date. The existing closed-period warning already covers the accounting risk."
 },
 {
  "i": 37,
  "source": "delivery-day",
  "sev": "medium",
  "scope": "owner-decision",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Hard inventory block can make a real paper ticket impossible to key in",
  "problem": "complete_delivery refuses outright ('Insufficient inventory for X: need N, only M on-hand') when on-hand stock is lower than the delivered quantity. In the field that's a reasonable guard, but for office after-the-fact entry the product already physically left the shed — refusing the record doesn't un-deliver it, it just blocks the truth (and the invoice) until someone goes to a different page, adjusts inventory, and retries. With 17 products already sitting at negative counts awaiting recounts, count drift is expected, so office staff will hit this wall. The rest of the app's recent philosophy (job holds, guardrails) is warn-don't-block; this is the one daily-path spot that still hard-blocks.",
  "evidence": "Pre-check raises: 20260620220000_complete_delivery_audit_and_partial_cost.sql:119-135 (RAISE EXCEPTION 'Insufficient inventory for %'). History shows the block was removed once (20260319200000_complete_delivery_remove_inventory_block.sql) then reinstated as a pre-check (20260331200000_fix_complete_delivery_precheck.sql). Negative-stock reality: CLAUDE.md open owner item H1 (17 negative-inventory products).",
  "rec": "Owner decision: keep the hard block for live/field completions but allow an admin/sales acknowledge-and-proceed override for after-the-fact entry (writes the shortfall to activity_feed and lets inventory go negative honestly), or keep as-is and accept the adjust-then-retry detour. Recommend the warn-with-override for the office path only."
 },
 {
  "i": 38,
  "source": "delivery-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Phone flow spends 2 of its 7 taps on ceremony, and unattended drop-offs force fake signatures",
  "problem": "A driver completing a normal full stop taps 7 times plus types a name: open stop, 'I'm Here', 'All Full — Continue', 'Continue to Photo', 'Skip — No Photo', 'Complete Delivery', then a SECOND generic 'Complete' confirm on top of the review screen that already exists to confirm. Two taps per stop are pure ceremony (the double-confirm, and a mandatory tap just to skip photos); across a 10-stop day that's real thumb time on the owner's highest-volume path. Bigger: there is no 'nobody present' option — the signer name is hard-required by both the UI and the RPC, but ag deliveries are routinely dropped at a shed with no one home, so drivers will type junk ('x') and the signature records become untrustworthy. Also the confirm copy promises 'a draft invoice created', which is false whenever the order already has an active invoice.",
  "evidence": "Step flow and taps: src/pages/FieldStop.tsx:61-62 (6-step flow), :468-476 (Arrive), :526-529 (verify continue), :547 (sign continue, disabled until name), :571-574 ('Skip — No Photo'), :599-601 (Complete) plus :621-630 (second ConfirmModal, copy 'a draft invoice created' at :626). Name required: FieldStop.tsx:282 and RPC p_signed_by has no default: 20260620220000:25. Invoice-copy conditional: 20260620220000:244-250.",
  "rec": "Remove the redundant ConfirmModal from the phone flow (the review step is the confirmation), fold the photo prompt into the review screen, and add a one-tap 'No one present — left at site' choice that fills signed_by with a standard marker so records stay honest."
 },
 {
  "i": 39,
  "source": "delivery-day",
  "sev": "medium",
  "scope": "DB",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Remainders never reach 'fulfilled' — the status dies at 'scheduled' forever",
  "problem": "When a follow-up delivery is created, its remainder rows flip to 'scheduled'. But when that follow-up is actually completed, nothing marks them 'fulfilled': the current complete_delivery only INSERTS new remainders and contains no remainder-status update (older versions from Feb/Mar 2026 had one; it was dropped in a later rewrite), and no trigger covers it. So the Remainders page's 'Fulfilled' filter is permanently empty, 'scheduled' rows pile up forever, and staff can't tell settled remainders from ones still owed — feeding the owner's 'confusing statuses / what do I do next' pain on a page whose whole job is tracking what's still owed.",
  "evidence": "Current complete_delivery has no delivery_remainders status update (only the INSERT): 20260620220000_complete_delivery_audit_and_partial_cost.sql:198-212 (whole file read — no UPDATE delivery_remainders). The dropped logic existed at 20260301100001_complete_delivery_tote_copy.sql:169-173 and 20260228310000_high_priority_fixes.sql:735-739. Remainders set to 'scheduled' at follow-up creation: 20260526151856:1189-1193. Dead 'fulfilled' filter option: src/pages/DeliveryRemainders.tsx:305; only cancel/void paths ever set 'fulfilled': 20260307200000_pre_launch_hardening.sql:315-319.",
  "rec": "In complete_delivery, add one UPDATE: mark delivery_remainders rows whose followup_delivery_id = this delivery as 'fulfilled' (proportionally-partial follow-ups can stay 'scheduled' for the short lines)."
 },
 {
  "i": 40,
  "source": "delivery-day",
  "sev": "low",
  "scope": "DB",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "get_customer_delivery_remainders is an orphaned RPC — defined, secured, never called",
  "problem": "The RPC built to feed a per-customer remainders view has zero callers anywhere in the app: the Remainders page queries the delivery_remainders table directly instead. It's dead surface area that still had to be swept in the anon-revoke security pass and will keep showing up in every audit — same retire-or-wire class as the P2-2 dead-object drops.",
  "evidence": "Defined: 20260225200000_delivery_system_enhancements.sql; anon EXECUTE revoked in 20260529214355_revoke_anon_execute_on_report_dashboard_secdef.sql; zero src/ callers (grep hits only src/types/supabase.ts:8597 generated types and test fixtures src/lib/rpcFixtureLiveDiff.test.ts:44); the page reads the table directly at src/pages/DeliveryRemainders.tsx:51-61.",
  "rec": "Drop the RPC in the next dead-object cleanup migration (P2-2 pattern), or wire it into a per-customer remainders card on CustomerDetail if that view is wanted — don't leave it half-alive."
 },
 {
  "i": 41,
  "source": "billing-day",
  "sev": "critical",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Overdue invoices disappear from the Payments page — the exact invoices checks arrive for become unpayable in the UI",
  "problem": "A nightly job (6:00 AM, pg_cron) flips every past-due posted invoice to status 'overdue'. The Payments page loads only status='posted' invoices, so the morning after an invoice goes overdue it vanishes from the check-allocation list. The invoice-detail 'Record Payment' button is also gated to status='posted', so an overdue invoice cannot take a payment anywhere in the UI. A check arriving for an old invoice — the single most common AR event — either gets pushed into prepay credit or allocated to newer invoices, making AR aging look worse while cash sits unapplied. The database RPC (allocate_payment) already accepts 'overdue' invoices; only the UI hides them. This is the exact bug class already fixed once for AR reminder emails (PARKED-005).",
  "evidence": "src/pages/PaymentAllocation.tsx:152 (.eq('status','posted')); src/pages/InvoiceDetail.tsx:1163 (Record Payment shown only when status==='posted'); cron: supabase/migrations/20260316121800_drop_stale_confirm_delivery_and_setup_cron.sql:18-22 (daily mark_overdue_invoices sweep); RPC accepts overdue: 20260513110000_allocate_payment_gate_on_payment_date.sql:127 (status IN ('posted','overdue')); precedent fix: 20260701200000_ar_reminder_include_overdue.sql",
  "rec": "Change PaymentAllocation's invoice query to .in('status',['posted','overdue']) and widen InvoiceDetail's Record Payment/Write Off gate to include 'overdue'. One-line UI fixes; the RPCs already support it."
 },
 {
  "i": 42,
  "source": "billing-day",
  "sev": "high",
  "scope": "owner-decision",
  "effort": "L",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Landlord/tenant splits are implemented three different ways, and the everyday job-billing path produces a bill the landlord cannot pay",
  "problem": "The same real-world concept — one field billed to multiple growers — has three implementations: (1) chemical orders split into SEPARATE per-customer invoices (create_split_invoices_from_order); (2) engine-built field-app invoices split into SEPARATE per-customer invoices linked by invoice_group_id (save_field_app_invoice); (3) the job 'Transfer to Invoice' path creates ONE invoice under the job's primary customer with invoice_shares percentage rows attached. In shape (3), the shares are annotations only: customer statements print the landlord's share (statement RPCs read invoice_shares), but the Payments page allocates strictly by invoices.customer_id — so when the landlord mails a check for the share their statement shows, the biller finds no invoice under the landlord's name to allocate it to. The money dead-ends into prepay or gets keyed under the tenant. Since 'Transfer to Invoice' is the ONLY billing action reachable from a completed job, shape (3) is what daily billing actually produces.",
  "evidence": "transfer_job_to_invoice: 20260622020000_transfer_job_invoice_audit_row.sql:183-225 (one v_invoice_id + INSERT INTO invoice_shares; comment at ~219 confirms statements read invoice_shares.amount_cents); save_field_app_invoice per-customer group: 20260630180000_field_app_pricing_unit_fix.sql:280-294; order-side per-customer: src/pages/OrderDetail.tsx:826-841; payment allocation by customer_id only: src/pages/PaymentAllocation.tsx:151; only job billing action: src/pages/JobDetail.tsx:2492,2573-2577",
  "rec": "Owner decision on the canonical model, but the clean answer is: per-customer invoices everywhere. Rework transfer_job_to_invoice to emit an invoice_group (one invoice per billing customer, same engine the field-app editor uses) and retire invoice_shares as a billing mechanism (keep it for display history). Until then, show a loud warning on Transfer when the job's fields have billing splits: 'This bills 100% to <tenant>; landlord shares will appear on statements but cannot be paid separately.'"
 },
 {
  "i": 43,
  "source": "billing-day",
  "sev": "high",
  "scope": "DB",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Delivery completion auto-invoices 100% to the primary customer, ignoring field-owner splits — and the split-invoice path is blocked once a delivery is scheduled",
  "problem": "In the chemical channel, completing a delivery auto-creates a draft invoice for the order — but that auto-draft inserts a single invoice for the delivery's customer and never looks at the order's field/acre allocations. The proper split path (Create Invoice on the order, which fans out per-owner invoices) is deliberately gated off once a delivery is scheduled or in flight. Net effect for a multi-grower order in the NORMAL flow (order → schedule delivery → driver completes): the split never happens; the tenant is billed for everything, and fixing it means voiding the auto-draft and rebuilding by hand.",
  "evidence": "Auto-draft ignores allocations: 20260620220000_complete_delivery_audit_and_partial_cost.sql:244-258 (single INSERT INTO invoices ... v_delivery.customer_id; item loop reads only delivery_items/order_items); split path + its gate: src/pages/OrderDetail.tsx:822-841 (create_split_invoices_from_order when hasAllocations) and :863-869 (Create Invoice gated when a delivery is scheduled/in-flight — Wave A.3 P1-1)",
  "rec": "Make complete_delivery's auto-draft allocation-aware: when the order has line allocations, call the same split logic as create_split_invoices_from_order (per-owner drafts for delivered quantities). Alternatively (smaller): skip the auto-draft for allocated orders and surface them in a 'needs split billing' queue instead of silently mono-billing."
 },
 {
  "i": 44,
  "source": "billing-day",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Posting rights are a patchwork: a sales-rep biller can bulk-post field-app invoices but cannot post chemical invoices except order-by-order",
  "problem": "The post_invoice RPC authorizes admin AND sales_rep. But the UI disagrees with itself: on the Invoices list the entire batch-action bar (including 'Post N Selected') renders for admins only; on the invoice detail page the Post button is admin-only; meanwhile the same sales rep CAN post field-app invoices from three surfaces (per-acre editor, Unposted tray 'Post All', Office Cockpit 'Post all clean') and can post a single order's drafts from OrderDetail's 'Post all drafts'. The batch RPC (batch_post_invoices) is admin-only at the DB level while the singular post_invoice is not. For a rep running the billing afternoon, chemical posting degenerates into opening every order individually — or handing the pile to the owner. Bonus inconsistency: the Invoices batch post fires with NO confirmation modal, while every other posting surface confirms first.",
  "evidence": "post_invoice allows sales_rep: 20260702160000_a8_terms_to_due_date.sql:100; batch_post_invoices admin-only: 20260611211058_idempotency_operation_scope_sweep.sql (body: \"IF v_actor_role != 'admin' THEN RAISE EXCEPTION 'Admin access required to batch post invoices'\"); UI: src/pages/Invoices.tsx:603 (batch bar admin-gated, no confirm modal on handleBatchPost:208), src/pages/InvoiceDetail.tsx:1111 (Post = editable && isAdmin), src/pages/OrderDetail.tsx:1437-1448 (rep-enabled per-order), src/pages/FieldInvoicesUnposted.tsx:328 + src/pages/OfficeCockpit.tsx:480 + src/pages/FieldApplicationInvoice.tsx:1743 (rep-enabled field surfaces)",
  "rec": "Pick one posting policy (recommend: admin + sales_rep everywhere, matching post_invoice) and align all five surfaces plus batch_post_invoices' role gate. Add the same confirm modal the other surfaces use to the Invoices batch post."
 },
 {
  "i": 45,
  "source": "billing-day",
  "sev": "medium",
  "scope": "DB",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Chemical batch posting is all-or-nothing: one bad invoice aborts the whole batch with a single opaque error",
  "problem": "batch_post_invoices loops PERFORM post_invoice inside one transaction with no per-invoice error handling — if invoice #23 of 30 hits a closed period or pending pricing, the exception rolls back all 30 and the biller gets one error message that doesn't say which invoice failed or that zero were posted. Every OTHER bulk-post surface in the app (field tray, cockpit, order detail) catches failures per invoice and reports 'Posted 28/30, failed: INV-...' — the chemical channel's main batch tool is the only one that behaves this way.",
  "evidence": "20260611211058_idempotency_operation_scope_sweep.sql batch_post_invoices body (FOREACH v_id ... PERFORM post_invoice(v_id) with no BEGIN/EXCEPTION per item); contrast src/pages/FieldInvoicesUnposted.tsx:337-366, src/pages/OfficeCockpit.tsx:542-572, src/pages/OrderDetail.tsx:893-928 (per-invoice failure collection)",
  "rec": "Either rework batch_post_invoices to capture per-invoice failures (nested BEGIN/EXCEPTION, return posted[]/failed[]), or drop the RPC and have the Invoices page use the same client-side per-invoice loop with keyed idempotency the field tray uses."
 },
 {
  "i": 46,
  "source": "billing-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Field tray 'Post All' can half-post a landlord/tenant split group — the atomicity every neighboring surface carefully protects",
  "problem": "Split field-app invoices (one per grower, linked by invoice_group_id) are supposed to post atomically: the invoice editors route groups through post_invoice_group, and the Office Cockpit refuses to bulk-post any grouped invoice at all, explicitly because a half-posted group is bad. But the Unposted tray's 'Post All' — the page the cockpit links you to — posts every VISIBLE row one-by-one via plain post_invoice, which has no group guard. If one member fails (pricing pending) or a sibling is hidden by the tray's customer/search filter or season window, the group ends half-posted: the tenant is committed to AR while the landlord's twin sits in draft, and cleanly reversing requires the atomic group-unpost.",
  "evidence": "src/pages/FieldInvoicesUnposted.tsx:331 (targets = visible/filtered rows) and :337-349 (per-row supabase.rpc('post_invoice')); post_invoice has no invoice_group check: 20260702160000_a8_terms_to_due_date.sql:91-146; contrast src/pages/OfficeCockpit.tsx:465-471 (grouped invoices excluded from bulk post for exactly this reason) and src/pages/InvoiceDetail.tsx:648-651 (group routes through post_invoice_group)",
  "rec": "In postAll, dedupe targets by invoice_group_id: call post_invoice_group once per group (skipping groups with members outside the visible set, with a 'N grouped invoices skipped — open to post the group' notice), plain post_invoice for standalones."
 },
 {
  "i": 47,
  "source": "billing-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "No single billing queue: the afternoon spans ~7 pages, and chemical drafts have no queue anywhere",
  "problem": "The biller must assemble their own worklist. The Office Cockpit's 'Ready to Post' tile covers field-app invoices only; the auto-drafted chemical invoices from today's completed deliveries appear nowhere except the Invoices list, where 'Unposted' is a non-clickable stat card and the biller must manually set the status filter (twice — Draft, then Unposted, since the filter is single-select). Delivered-but-uninvoiced orders (auto-draft failed, or invoice voided) surface only on 'Integrity Cleanup' — an admin-only page buried in Finance that a biller would never associate with daily billing, and the sole caller of create_invoice_for_unbilled_delivery. The unbilled-jobs queue rows link to the JOB, not to a billing action. Owner's stated top pains — can't find things, too many clicks — land squarely here.",
  "evidence": "src/pages/OfficeCockpit.tsx:300-305 (tile filtered to invoice_type='field_application'); src/pages/Invoices.tsx:695-702 (stat card, no onClick; single-select statusFilter:86); src/pages/IntegrityCleanup.tsx:316 (only caller of create_invoice_for_unbilled_delivery), src/components/layout/Sidebar.tsx:213 (Integrity Cleanup admin-only under Finance); src/pages/UnbilledApplications.tsx:120 (rowPath → /jobs/{id})",
  "rec": "Make Office Cockpit the one billing home: add a 'Chemical drafts to post' tile (same query the Invoices page runs, filtered draft/unposted) and a 'Delivered, not invoiced' tile reusing the existing RPC, plus a 'Enter payments' shortcut. Make the Invoices 'Unposted' card click-to-filter with a combined draft+unposted option."
 },
 {
  "i": 48,
  "source": "billing-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Billing a job is a 3-page, ~6-click round trip per job with no 'next' chaining",
  "problem": "Per completed job: cockpit/queue → JobDetail (page 1) → find 'Transfer to Invoice' in the button row → confirm modal → land on the invoice editor (page 2) → review → Post → confirm → then manually navigate back to the cockpit (page 3) and hunt for the next job. Ten jobs ≈ 60+ clicks and 30 page loads. The queue pages hand you the job, not the billing action, and nothing chains you to the next unbilled job. (Auto-draft on completion — §4 — exists but ships OFF, and even ON it still leaves the same per-invoice post loop.)",
  "evidence": "src/pages/OfficeCockpit.tsx:652-655 (row → /jobs/{id}); src/pages/JobDetail.tsx:2573-2577 (Transfer button), 3762-3769 (confirm modal), 2120 (navigate to /field-invoices/{id}); no return path or next-job affordance on src/pages/InvoiceDetail.tsx; §4 default OFF: 20260630073344_auto_draft_invoice_on_job_completion.sql:34-45",
  "rec": "Add 'Create invoice' (transfer) directly on UnbilledApplications/cockpit rows, and after posting a job-built invoice show 'Bill next unbilled job (N left)'. Consider recommending the owner flip the §4 auto-draft ON at go-live so the queue starts at 'Ready to Post' instead of 'Unbilled'."
 },
 {
  "i": 49,
  "source": "billing-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Which billing mode an invoice uses (flat grower-share vs itemized chemicals) is decided silently by a per-field price override set on a different page",
  "problem": "In the field-app engine, each field bills one of two ways: if the field's billing details carry price_override_cents, the customer gets a flat '$X/ac grower share' line and every chemical prints at $0 '— included in grower share'; without it, chemicals bill individually at tier prices plus service fees. That override lives on the Field Setup page, possibly set last season, and nothing in the invoice editor or on JobDetail announces which mode will apply — per-customer dollars only become visible if the biller clicks Preview. A stale override silently flattens a $30/ac chemical load to a $12/ac flat rate with no warning; the itemized-vs-flat difference is exactly the kind of quiet money leak a busy biller won't catch.",
  "evidence": "Mode branch: 20260630180000_field_app_pricing_unit_fix.sql:310-341 (override → grower_share line; :373-377 chemicals partitioned by override presence; :393-396 '— included in grower share' at 0 cents); line kinds: src/types/index.ts:3087; override editable on src/pages/FieldSetup.tsx (price_override_cents); preview-only visibility: src/pages/FieldApplicationInvoice.tsx:2649",
  "rec": "Show a per-field mode banner in the invoice editor and on JobDetail before Transfer ('Field N bills FLAT $X/ac — chemical prices will not be charged'), and flag overrides older than the current season. Cheap UI, closes a silent-pricing trap."
 },
 {
  "i": 50,
  "source": "billing-day",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified src/App.tsx:208-209 route takes no param beyond invoice id. src/pages/FieldApplicationInvoice.tsx:298 initializes jobId to null via useState; the only setter is line 813 (setJobId(srcJobId)) which reads invoice.job_id from an ALREADY-LOADED invoice row (i.e. after a job was already transferred) — confirmed zero useSearchParams usage in the file. JobDetail.tsx:2110-2120 confirms the only job-to-invoice path is the transfer_job_to_invoice RPC, which produces the single-invoice-with-shares shape, not a seeded editor session. The engine editor genuinely has no way to start from an existing job's fields/acres/chemicals.",
  "title": "A job can't be billed through the per-acre split engine — multi-grower per-customer invoicing means re-keying the whole job",
  "problem": "The engine editor (/invoices/field-app/new) — the only path that produces proper per-customer split invoices — takes no job reference: no URL parameter, no 'start from job' picker. The only job→invoice action is Transfer, which produces the single-invoice-with-share-annotations shape. So to bill a completed multi-grower job the 'right' way, the biller must manually re-select the fields, re-enter applied acres, and re-key every chemical and rate that already exist on the job — duplicate data entry with typo risk on real money.",
  "evidence": "Route takes no param: src/App.tsx:208; jobId is only ever set from an already-saved invoice row: src/pages/FieldApplicationInvoice.tsx:294-298,812-813 (no useSearchParams in file); sole job path: src/pages/JobDetail.tsx:2106-2123 (transfer_job_to_invoice)",
  "rec": "Add 'Bill via split engine' on the completed job: seed the engine editor from job fields (locations + applied acres) and job chemicals. This also becomes the migration path for finding #2 (making per-customer splits the canonical job billing)."
 },
 {
  "i": 51,
  "source": "billing-day",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified src/pages/InvoiceDetail.tsx:1194 gates the Unpost button on invoice.invoice_type === 'field_application' && status posted/overdue. Read the full unpost_invoice RPC body (supabase/migrations/20260625130000_unpost_invoice.sql:130-164, confirmed latest — no other migration redefines this function name, and the separate unpost_invoice_group in 20260629170000 is a different function for batch groups) — the only guards are actor role, status IN ('posted','overdue'), and a money-applied check; zero invoice_type restriction anywhere in the RPC. The UI gate is a pure frontend restriction with no DB-side backing.",
  "title": "Posting locks a chemical invoice harder than the database requires — Unpost is hidden for chemical invoices, forcing void-and-rebuild for a typo",
  "problem": "Posting is correctly explained as 'locks the invoice' in the confirm dialogs, and field-app invoices get an Unpost button to reverse a mistake (posted → unposted, refused if any money has been applied). But the invoice-detail page shows Unpost ONLY for field-application invoices, while the unpost_invoice RPC itself has no invoice-type restriction — any posted, unpaid invoice qualifies. A biller who posts a chemical invoice and then spots a wrong quantity has no UI undo: the only recourse is admin Void plus rebuilding the invoice (and re-creating from the order is gated once deliveries are in flight, with the backfill RPC living on the admin-only Integrity Cleanup page).",
  "evidence": "RPC has no type gate: 20260625130000_unpost_invoice.sql:145-157 (status posted/overdue + unpaid checks only); UI gate: src/pages/InvoiceDetail.tsx:1194-1206 (invoice.invoice_type === 'field_application' condition on the Unpost button); rebuild path gated: src/pages/OrderDetail.tsx:863-869 + src/pages/IntegrityCleanup.tsx:316",
  "rec": "Drop the invoice_type condition on InvoiceDetail's Unpost button so any posted, unpaid invoice can be returned to draft the same way in both channels. The DB already enforces every safety rule (payments applied, closed period)."
 },
 {
  "i": 52,
  "source": "application-only-job",
  "sev": "critical",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified the fee branch at supabase/migrations/20260622020000_transfer_job_invoice_audit_row.sql:233 (`IF v_job.application_service_id IS NOT NULL AND v_total_acres > 0`) — this is the latest transfer_job_to_invoice definition (newest of 6 migrations touching this function name by timestamp, none newer). Grepped save_job's latest body (20260624120000_job_parity_scheduling_agronomy_shares.sql) and create_job_from_quote_section (20260703200000) for 'application_service_id' — zero matches in both. JobDetail.tsx has zero matches for the column (confirmed via grep); Jobs.tsx:430 only filters/reads it. Live DB query confirmed exactly: 2 total jobs, 0 with application_service_id set. This is a fully-verified, correctly-cited, high-severity finding — critical is a defensible severity since it silently zeroes out the core per-acre revenue on the primary daily billing path (owner's highest-volume channel per the interview facts).",
  "title": "The per-acre application fee can never be billed from a job — jobs.application_service_id has no setter anywhere",
  "problem": "When a spray job is billed (JobDetail 'Transfer to Invoice', the same button the Unbilled list routes to), the invoice engine only adds the per-acre machine fee if the job has an application service attached. But nothing in the entire app ever attaches one: the job form has no service picker, save_job never saves it, and quote-to-job conversion doesn't set it. So every job-built invoice bills chemicals only, and an application-only job (the customer-supplied-chemical case, or any fee-only work) bills exactly $0.00. The core revenue of the custom-application channel — the per-acre fee — is silently missing from every invoice produced on the job rail. The only way to bill a fee today is to hand-build a second, unlinked invoice on the standalone field-app page, re-keying customer/fields/acres.",
  "evidence": "Fee branch gated on the job's service: supabase/migrations/20260622020000_transfer_job_invoice_audit_row.sql:233 ('IF v_job.application_service_id IS NOT NULL AND v_total_acres > 0'); live pg_get_functiondef(transfer_job_to_invoice) confirms the branch is live. No setter: save_job (20260624120000_job_parity_scheduling_agronomy_shares.sql:131ff) and create_job_from_quote_section (20260703200000_layer2_close_quote_as_applied.sql:473-480) never write application_service_id — live check: pg_get_functiondef LIKE '%application_service_id%' = false for save_job/complete_job/create_job_from_quote_section; src/pages/JobDetail.tsx has zero matches for application_service_id (only Jobs.tsx:430 filters by it); live DB 2026-07-04: 0 of 2 jobs have it set. Daily path: UnbilledApplications.tsx:120 routes rows to /jobs/:id, whose billing action is transfer_job_to_invoice (JobDetail.tsx:2110, button at 2576). The G1 migration that built the fee (20260619140000_transfer_job_invoice_machine_fee_strict_actor.sql:14-16) itself documents 'transfer never read jobs.application_service_id' and assumed the column would be populated — it never is.",
  "rec": "Add the existing ApplicationServicePicker component to the job form (JobDetail), persist p_application_service_id through save_job (column already exists on jobs), and optionally default it per customer. The already-live fee branch in transfer_job_to_invoice then starts working, and a zero-chemical job produces a clean fee-only invoice — which is also the missing half of the customer-supplied-chemical path."
 },
 {
  "i": 53,
  "source": "application-only-job",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified job_chemicals.product_id is NOT NULL REFERENCES products(id) both in migration source (20260215200000_job_scheduling_tables.sql:78) and live information_schema.columns (product_id is_nullable='NO'; no supplied-by/customer-owned column exists in the live table). Confirmed zero repo-wide matches for customer_supplied/customer_owned/supplied_by. Verified the unconditional deduction in the LATEST complete_job (20260702175000_layer2_complete_job_drop_phase7_drain.sql:197 filters only 'quantity > 0', :223 inserts a negative-quantity inventory row, :232-242 flags SHORT STOCK). Verified reserve_job_inventory (20260702174000) groups every job_chemicals row with no exemption at all (lines 92-108). Pickers confirmed is_active=true only (QuoteBuilder.tsx:381 exact match; JobDetail.tsx comments at 753/840 reference the same allProducts is_active filter, though those specific line numbers are comments about label-lookup rather than the picker query itself — a minor citation looseness that doesn't change the substance). Product interface (src/types/index.ts) confirmed to have no service/non-stock/supplied field.",
  "title": "No 'customer-supplied' concept on job chemicals — listing the customer's own product corrupts our inventory (negative stock) and phantom-reserves the shed",
  "problem": "Every chemical line on a job must reference a product in OUR sales catalog, and job completion unconditionally deducts warehouse stock for every line with quantity > 0 — even inserting a brand-new NEGATIVE inventory row for a product we've never stocked. There is no flag anywhere meaning 'the customer supplied this; don't touch our stock, don't bill it.' So the honest way to record a customer-supplied spray (list the chemical on the job) does three wrong things: (1) the office must first create the customer's jug as a full sellable catalog product (with tier prices) that then appears in every quote/order picker forever — deactivating it to hide it also removes it from the job picker, since both filter is_active=true; (2) while the job is scheduled, the auto-reserve engine places a full-demand hold on our warehouse for that product, so the dispatch stock light and shortfall warnings show phantom shortages to every dispatcher; (3) on completion our stock count for that product goes negative and an inventory transaction flagged 'SHORT STOCK — review required' sends someone chasing a phantom discrepancy. The only escape is an undiscoverable trick (hand-enter quantity 0, keep the rate) that no screen hints at and that records '0 applied' on the spray record.",
  "evidence": "job_chemicals.product_id NOT NULL REFERENCES products(id): supabase/migrations/20260215200000_job_scheduling_tables.sql:75-86, confirmed live via information_schema 2026-07-04 (no supplied-by column; repo-wide grep for customer_supplied/customer_owned/supplied_by = 0 matches). Unconditional deduction incl. negative-row insert: 20260702175000_layer2_complete_job_drop_phase7_drain.sql:193-243 (loop filter 'AND jc.quantity > 0' at :197; INSERT INTO inventory ... VALUES (..., -v_deduct_qty, 0, 0) at :221-223; requires_review short-stock flag :232-242). Full-demand job holds with no exemption: 20260702174000_layer2_reserve_job_inventory.sql:92-108 (and :119-120 — a product with no inventory row reports its full demand as shortfall). Pickers are is_active-only so a pseudo-product can't be hidden from sales: JobDetail.tsx:753,840 and QuoteBuilder.tsx:381. Product type has no service/non-stock/customer-supplied field: src/types/index.ts (Product interface).",
  "rec": "Add one boolean to job_chemicals (e.g. supplied_by_customer). When true: complete_job skips the stock deduction (or writes a zero-impact informational transaction), _sync_job_holds skips the hold, and transfer_job_to_invoice forces the line price to $0 while keeping it printed on the invoice as 'customer supplied'. A checkbox on the job chemical row makes the honest path safe. Pair with a lightweight way to add a non-stocked reference product (or a free-text line, see the manual-line finding)."
 },
 {
  "i": 54,
  "source": "application-only-job",
  "sev": "medium",
  "scope": "DB",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified application_records INSERT (20260702175000:160) is built from a job_chemicals join at line 141/195 — the LATEST complete_job. Verified zero matches for 'application_records' in save_field_app_invoice's migration (20260630180000). Verified watchdog joins job_chemicals at the exact cited lines (20260629220000_watchdog_flags.sql:440,590 and 20260629240000_watchdog_rei_applied_records.sql:189). This is correctly framed as the consequence/elaboration of the customer-supplied-flag finding (distinct evidence, not a duplicate claim) and cites its own file:line evidence independently.",
  "title": "Spray record and inventory/billing are fused: the only compliance record comes from complete_job's job_chemicals, so 'leave the chemical off' erases the legal record and silences REI/PHI safety flags",
  "problem": "The application record (the legally-important 'what was sprayed, at what rate, on whose field' document) is generated in exactly one place: job completion, and its product list is built solely from the job's chemical lines. The alternative billing rail — the standalone field-app invoice — writes no application record at all. So for a customer-supplied job the office faces a fork: list the chemical (inventory corruption, previous finding) or omit it — in which case the permanent record says NOTHING was applied, the re-entry-interval / pre-harvest watchdog flags never fire for that product (they join job_chemicals), and the 'Your Field Was Sprayed' proof shows no products. There is no way today to record-without-deducting or record-without-billing.",
  "evidence": "application_records written only by complete_job, product_data built from job_chemicals: 20260702175000_layer2_complete_job_drop_phase7_drain.sql:124-177. save_field_app_invoice contains zero application_records writes: grep of 20260630180000_field_app_pricing_unit_fix.sql = 0 matches. Watchdog REI/PHI joins job_chemicals: 20260629220000_watchdog_flags.sql:440,590 and 20260629240000_watchdog_rei_applied_records.sql:189. Records surfaced at src/pages/ApplicationRecords.tsx and Reports.tsx.",
  "rec": "The customer-supplied flag (previous finding) resolves this cleanly: the chemical stays ON the job — so the application record, REI/PHI flags, and proof email stay complete — while the flag suppresses the deduction and the charge. No separate record-generation path needed."
 },
 {
  "i": 55,
  "source": "application-only-job",
  "sev": "medium",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified save_field_app_invoice's full signature (20260630180000:62) has 7 params — no p_job_id, confirming it can never set jobs.invoice_id. Verified UnbilledApplications.tsx:77-81 keys on jobs.status='completed' AND invoice_id IS NULL. Verified canTransfer = !isNew && status === 'completed' (JobDetail.tsx:2492) stays true after a standalone invoice is created (job status never changes). Verified transfer_job_to_invoice's only re-invoice guard is v_job.status = 'invoiced' (line 90) — a job billed via the standalone editor never trips this. Additionally verified the auto-draft-on-completion switch's own duplicate-check (20260702175000:275-280) queries invoices WHERE job_id = p_job_id — which would MISS a standalone invoice for the same job (since that invoice has no job_id set), meaning auto-draft's own safety check would fail to detect the prior billing and could create a genuine duplicate. This independently confirms and slightly strengthens the finding's claim about auto-draft creating an automatic duplicate.",
  "title": "Billing a job through the standalone field-app invoice strands the job: permanent 'unbilled' nag plus a live double-bill path",
  "problem": "Because the job rail can't bill a fee (finding 1), today's only working fee bill is the standalone field-app invoice page — which has no connection to the job. The office re-keys customer, every field, and applied acres into a second document. The job then sits at 'completed' forever: the Unbilled Applications list keys purely on 'completed job with no invoice', so it nags about this job indefinitely, and the 'Transfer to Invoice' button remains live on the job — one click by a colleague later creates a second (chemicals-only, likely $0) invoice for work that was already billed. If the auto-draft-on-completion switch is ever turned on, that duplicate is created automatically at completion.",
  "evidence": "save_field_app_invoice signature has no job link: 20260630180000_field_app_pricing_unit_fix.sql:62 (p_invoice_id/p_invoice/p_locations/p_chemicals/p_performed_by/p_application_service_id — no p_job_id). Unbilled list = jobs.status='completed' with no invoice: src/pages/UnbilledApplications.tsx:15,77-79 (rows route to /jobs/:id at :120). Transfer stays available on a completed job: JobDetail.tsx:2110,2576; only transfer_job_to_invoice flips the job to 'invoiced' (forward-mutation summary in 20260625120000_transfer_invoice_to_job.sql:14-20). Auto-draft would create the duplicate automatically: complete_job 20260702175000:266-285.",
  "rec": "Primary fix is finding 1 (bill fees from the job so the detour disappears). If the standalone page keeps a role for job work, let it optionally attach to a job (set jobs.invoice_id / status='invoiced') so the unbilled list and the transfer button both know the work is billed."
 },
 {
  "i": 56,
  "source": "application-only-job",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified the server comment and cast at 20260630180000_field_app_pricing_unit_fix.sql:451 and the item INSERT at :467-475 (product_id cast from v_chem->>'product_id' with COALESCE fallback on inventory_unit, confirming manual/null-product-id lines are handled). Verified FieldAppChemicalEntry.tsx:367-407 is a pure catalog-search cell that dead-ends at 'No products match' (line 391) with zero add-custom-line action anywhere in the component (confirmed via targeted grep). Verified InvoiceDetail.tsx's addProduct (line 492 area) is invoked only from a catalog search-result click, confirmed via grep that no Custom Line / Add Custom action exists in either file.",
  "title": "The invoice engine supports free-text (no-product) lines, but no screen can create one — every informational line requires inventing a catalog product",
  "problem": "The field-app invoice engine on the server explicitly supports 'manual' chemical lines with no product reference (priced as entered — ideal for 'Customer-supplied GlyStar Plus, $0.00, for the record'). But the invoice UI's product cell is a catalog search that dead-ends at 'No products match', with no 'add as custom line' option, and the generic invoice editor likewise only adds lines by picking an active catalog product. So the supported manual-line capability is unreachable, and the only way to name a product we don't sell — on a job, a field-app invoice, or a generic invoice — is to create a fake sellable catalog product first.",
  "evidence": "Server support: 20260630180000_field_app_pricing_unit_fix.sql:451 ('Manual line (no product_id): no inventory_unit, so price in the rate unit as entered') and the item insert at :467-475. UI dead-end: src/components/field-app/FieldAppChemicalEntry.tsx:367-407 (search-only cell; 'No products match' with no custom-line action; a never-selected line keeps product_name = '' — the typed text lives only in searchQuery). Generic editor is product-picker-only: src/pages/InvoiceDetail.tsx:475-537 (addProduct from is_active=true search).",
  "rec": "Add an 'Add custom line' action to FieldAppChemicalEntry (leaves product_id null, free-text description, price editable — the server already handles it). Cheap, and it gives the customer-supplied case an immediate paper-trail stopgap even before the job-side flag ships."
 },
 {
  "i": 57,
  "source": "entry-points",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified all four cited manual-editor buttons exist and all route to the same job-blind engine editor: Invoices.tsx:682 ('New Field Application' -> navigate('/invoices/field-app/new')), FieldInvoices.tsx:520/617, FieldInvoicesUnposted.tsx:405. Verified the guard at transfer_job_to_invoice line 90 ('Job already invoiced') only checks the job's own status column, which a manual invoice never sets, confirming nothing stops the double bill. This finding substantively overlaps with findings #1 and #6 (all three describe facets of the same root defect: the engine editor's total lack of job-awareness) but each cites distinct evidence (entry-point proliferation here vs. multi-grower re-keying in #1 vs. the auto-draft/backlog consequence in #6) — treating them as complementary rather than a pure duplicate is reasonable, though the parent reviewer should note the shared root cause when prioritizing the fix (one engine fix likely resolves all three).",
  "title": "Billing a job by hand (the most visible button) creates an invoice that never links back to the job — double-billing risk and a phantom 'unbilled' backlog",
  "problem": "There are two ways to bill a spray job. The correct one is the 'Transfer to Invoice' button on the job itself: it builds the invoice from the job's chemicals, marks the job 'invoiced', and refuses to bill the same job twice. The wrong one is the '+ New Field Application' / 'Add Invoice' button that appears on all four invoice list pages — it opens a blank manual editor with NO way to pick a job. If office staff bill a finished job through that editor (the button they see everywhere), the job silently stays 'completed / not billed': it sits on the 'Completed jobs — not billed' backlog forever even though the customer was billed, and the job's own 'Transfer to Invoice' button still offers to create a SECOND invoice for the same work — nothing stops the double bill.",
  "evidence": "Manual editor buttons: src/pages/Invoices.tsx:681, src/pages/FieldInvoices.tsx:519 and 616, src/pages/FieldInvoicesUnposted.tsx:404 (primary 'Add Invoice'). Editor has no job picker — jobId only ever set from an already-transferred invoice: src/pages/FieldApplicationInvoice.tsx:294-298. Backlog query keys on jobs.invoice_id IS NULL: src/pages/UnbilledApplications.tsx:78-83. Transfer guard only checks the job's own status ('Job already invoiced'), which a manual invoice never sets: migration 20260622020000_transfer_job_invoice_audit_row.sql line 90 and 274.",
  "rec": "Make transfer_job_to_invoice the one primary billing path: add a one-click 'Create Invoice' button on each row of Unbilled Applications (calls the existing RPC), keep the JobDetail button, and demote the manual editor — remove it from the chemical Invoices page, rename it 'Manual Invoice (no job)' behind an overflow menu on the field pages. Optionally add a soft warning in the manual editor when the chosen customer has completed-unbilled jobs."
 },
 {
  "i": 58,
  "source": "entry-points",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified QuoteBuilder.tsx:277 canConvert = currentStatus === 'sent' (excludes revised) vs. canDraw at :281 which correctly includes both sent and revised. Verified Quotes.tsx:478 shows the convert icon for both row.status === 'sent' || 'revised'. Verified the RPC (20260702172500_layer2_convert_quote_to_order_job_aware.sql:120, confirmed latest of 8 migrations touching this function name) explicitly accepts 'sent','revised','accepted' and rejects anything else. This is a clean, simple, well-evidenced one-line UI inconsistency directly affecting the two-speed-selling workflow described in the owner interview.",
  "title": "'Convert to Order' appears and disappears depending on which screen you're on — a revised quote can be converted from the list but not from inside the quote",
  "problem": "The database allows converting a quote to an order while it is 'sent' OR 'revised'. The Quotes list shows the convert icon for both statuses. But inside the quote itself (QuoteBuilder), the Convert to Order button only shows when the quote is 'sent' — on a revised quote the button simply vanishes. Staff working inside a revised quote will conclude 'revised quotes can't be converted' and get stuck, or wander back to the list and discover the icon there. Same action, same quote, two different answers depending on the screen.",
  "evidence": "RPC accepts sent/revised(/accepted for idempotent replay): supabase/migrations/20260702172500_layer2_convert_quote_to_order_job_aware.sql:120. List shows convert for sent+revised: src/pages/Quotes.tsx:478. Builder hides it on revised: src/pages/QuoteBuilder.tsx:277 (canConvert = currentStatus === 'sent'); contrast canDraw at :281 which was already fixed to match the RPC.",
  "rec": "Change canConvert in QuoteBuilder to include 'revised' (matching the list, the RPC, and the documented lifecycle sent/revised → accepted). One-line change."
 },
 {
  "i": 59,
  "source": "entry-points",
  "sev": "medium",
  "scope": "owner-decision",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified InvoiceDetail.tsx:108 isNew = id === 'new' genuinely triggers new-invoice mode (line 573 sends id: undefined to save_invoice), and the type dropdown at line 1285-1291 includes a selectable 'Misc Charge' option. Verified App.tsx:210 invoices/:id is a generic param route that transparently catches /invoices/new (confirmed no dedicated 'invoices/new' static route exists, and a repo-wide grep for the navigation string '/invoices/new' returns zero matches — nothing in the app links there). Verified the removal comment at Invoices.tsx:684-687 and confirmed generate_finance_charges genuinely inserts invoice_type='misc_charge' rows directly (20260622010000:116), substantiating the claim that the system already has an internal exception to its own 'invoices must come from an order/blend-ticket' rule.",
  "title": "There is no button anywhere to bill a miscellaneous charge — the working page exists but is reachable only by hand-typing a URL",
  "problem": "The app has a full 'new invoice' editor that can create a Misc Charge invoice (a fee, freight, a service not tied to an order). It works — but no button in the entire app navigates to it. The '+ New Invoice' button was deliberately removed in April per the 'invoices must come from an order or blend ticket' rule, yet the route and the save_invoice RPC were left alive, and the system itself already violates the rule elsewhere (finance charges are created as misc_charge invoices; credit memos have no order). So the owner's real-world 'bill a misc fee' situation currently requires typing /invoices/new into the address bar — which nobody will discover.",
  "evidence": "Working creator: src/pages/InvoiceDetail.tsx:108 (isNew), :617 (save_invoice with id undefined), :1285-1291 (type dropdown incl. Misc Charge). Zero navigations to /invoices/new (repo-wide grep). Removal comment: src/pages/Invoices.tsx:684-687. Finance charges already insert invoice_type='misc_charge': migration 20260622010000_fix_generate_finance_charges_draft_insert.sql:11,109.",
  "rec": "Owner decision, with a recommendation: legitimize misc charges — add one 'Misc Charge' button on the Invoices page that opens /invoices/new with the type locked to misc_charge (keep the no-standalone rule for chemical_sale). Alternative if he wants the strict rule: remove the isNew route so the hidden path can't produce untracked invoices."
 },
 {
  "i": 60,
  "source": "entry-points",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Invoices.tsx:681-683 — the sole Plus-icon create button navigates to '/invoices/field-app/new' labeled 'New Field Application'; grep for icon={<Plus confirms it is the ONLY create button in the header (the other Plus at line 740 is the empty-state 'Go to Orders' fallback, not a create action, and disappears once any invoice exists). Empty-state guidance at line 738 confirmed ('Invoices are created from orders or blend tickets'). Label-drift citations verified exactly: FieldInvoices.tsx:519-521 also says 'New Field Application' (same label, so no drift between those two pages) but FieldInvoicesUnposted.tsx:404-406 says 'Add Invoice' with variant='primary' — a real third label for the same destination.",
  "title": "On the chemical-sales Invoices page, the only visible create button makes an invoice for the OTHER channel",
  "problem": "On /invoices (chemical sales billing), the single '+' creation button is 'New Field Application' — it creates a custom-application invoice, the other sales channel. A staff member wanting to bill a chemical sale sees exactly one plus-button and it's the wrong one; the correct paths (invoice is auto-created when the delivery completes, or 'Create Invoice' on the order) are only explained in gray empty-state text that disappears once any invoice exists. Bonus confusion: the same destination is labeled 'New Field Application' on two pages but 'Add Invoice' on the Unposted tray — three labels, one editor.",
  "evidence": "src/pages/Invoices.tsx:681 ('New Field Application' — sole create button on the chemical invoices page); guidance only in empty-state copy :738. Label drift: src/pages/FieldInvoices.tsx:519-521 vs src/pages/FieldInvoicesUnposted.tsx:404-406 ('Add Invoice', primary variant).",
  "rec": "Remove the field-app button from /invoices. Replace with a short always-visible hint ('Chemical invoices are created automatically when a delivery completes, or from an order') plus a 'Go to Orders' button. Unify the field-app button label everywhere it remains ('New Field Application')."
 },
 {
  "i": 61,
  "source": "entry-points",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Dashboard.tsx:413-467 exactly: the 6 quick actions are To-Ship, New Order, New PO, Schedule Delivery, Inventory, Receiving — no New Quote, no New Job, no Quick Delivery link. This matches the owner's stated top-3 daily workflows (phone quotes/bookings, spray-job scheduling, deliveries/load-out) being absent while New PO/Receiving (weekly purchasing) are present in the primary 6-button grid.",
  "title": "The Dashboard quick actions skip the three highest-volume daily tasks (quote, spray job, counter-sale) but include weekly back-office ones",
  "problem": "The owner said the fastest daily work must be phone quotes/bookings, scheduling spray jobs, and deliveries/load-out. The landing page's Quick Actions offer To-Ship, New Order, New PO, Schedule Delivery, Inventory, and Receiving — there is no 'New Quote', no 'New Job', and no 'Quick Delivery' (the one-modal order+delivery+invoice counter-sale tool, which is only discoverable as a secondary button on the Deliveries page). Meanwhile 'New PO' and 'Receiving' — weekly purchasing tasks — get top billing. Staff start every day one page away from the work they actually do.",
  "evidence": "Quick actions list: src/pages/Dashboard.tsx:413-467 (To-Ship :415, New Order :424, New PO :433, Schedule Delivery :442, Inventory :451, Receiving :460). Quick Delivery reachable only from src/pages/Deliveries.tsx:845 (driver view) and :1160 (desktop). No navigation to /quotes/new or /jobs/new from Dashboard (repo grep).",
  "rec": "Reorder quick actions to the owner's top three: New Quote, New Job, Quick Delivery ('Sell & Deliver Now'), then New Order, Schedule Delivery; move New PO / Receiving / Inventory to a second row or drop them (they're one nav-click away)."
 },
 {
  "i": 62,
  "source": "entry-points",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified DispatchBoard.tsx:703-709 exactly: 'Add New Job' is a role=menuitem button inside the dropdown, positioned after 'More Search Options' (688-694) and 'Switch View' (695-702), navigating to '/jobs/new'. Jobs.tsx:1552-1557 confirmed as a visible primary header Button with the same navigate('/jobs/new') target. This is a real daily-friction issue on a top-3 workflow (dispatching).",
  "title": "On the Dispatch Board — the schedulers' daily screen — creating a job is hidden inside an overflow options menu",
  "problem": "Dispatching spray jobs is a top-3 daily task, and the Dispatch Board is the screen built for it. But its 'Add New Job' action is buried inside the '...' options dropdown (behind More Search Options and Switch View), while the visible New Job button lives on the separate Job Schedule page. A dispatcher who lives on the board must either know the hidden menu item or navigate away to create the very thing the board manages.",
  "evidence": "src/pages/DispatchBoard.tsx:703-709 ('Add New Job' as a role=menuitem inside the options dropdown). Visible primary button only on src/pages/Jobs.tsx:1552-1557.",
  "rec": "Promote 'New Job' to a visible header button on the Dispatch Board (same navigate('/jobs/new')), keeping the menu item for the mobile layout if space is tight."
 },
 {
  "i": 63,
  "source": "entry-points",
  "sev": "low",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified BlendTicketDetail.tsx:1550-1553 exactly (required 'Order Number *' input) and :1578 (Create button disabled via !newOrderNumber.trim()). Verified NewOrder.tsx:607 explicitly states 'Optional — order number is auto-generated' for its own order-number field, confirming the asymmetry. Correctly scoped as low severity/low priority since blend tickets are explicitly off the daily path per owner facts.",
  "title": "Creating an order from a blend ticket demands a hand-typed order number, unlike every other order path",
  "problem": "All other order creators auto-generate the order number (direct order, rush order, quick delivery, quote conversion). The blend-ticket path alone requires the user to invent and type an 'Order Number *' before the Create button enables — extra ceremony on an already-rare path, and hand-typed numbers can collide with or diverge from the app's numbering scheme.",
  "evidence": "src/pages/BlendTicketDetail.tsx:1550-1553 (required Order Number input gating the Create Order button at :1578), vs auto-numbered create_direct_order (src/pages/NewOrder.tsx:427) and create_rush_order (:391).",
  "rec": "Have create_order_from_blend_ticket auto-generate the order number like its siblings (keep an optional reference field for the ticket's own number). Low priority — blend tickets are off the daily path in year one."
 },
 {
  "i": 64,
  "source": "lifecycles",
  "sev": "high",
  "scope": "UI",
  "effort": "L",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified FieldView.tsx is genuinely read-only: grep for start_job/complete_job/rpc( in the file returns only get_dispatched_list and get_fields_geojson_by_ids — no mutation RPCs at all. Verified App.tsx:269-270 route comment literally says 'read-only \"my jobs\" cards'. Verified start_job/complete_job calls exist ONLY in JobDetail.tsx (lines 2039, 2061, confirmed via grep), and JobDetail.tsx is genuinely 3805 lines (wc -l confirmed exactly). The jobMassEdit.ts:69-80 citation is valid at one remove — that file doesn't literally call the RPCs by name but its comment (lines 67-69) explicitly names start_job/complete_job as the side-effect-bearing forward transitions it deliberately disables from bulk actions, which is exactly the point the finding is making. Contrast with DeliveryDetail.tsx driver flow (Start Delivery button confirmed at ~1339-1345, Complete Delivery section confirmed at 1376-1429) is accurate. This is a genuine high-severity gap on the second-highest-volume daily workflow.",
  "title": "Applicators on phones cannot start or complete a spray job — their mobile page is read-only",
  "problem": "The owner said applicators work from phones, and spray jobs are one of the two highest-volume daily workflows. But the phone page built for applicators (/field, 'My Field Jobs') is deliberately read-only cards — it can show a job but cannot move it. The ONLY place in the whole app that can start or complete a job is JobDetail, a 3,805-line desktop form. Delivery drivers got a full dark-mode mobile flow with big Start/Complete buttons and offline queueing; applicators got nothing equivalent. In practice every job status change will fall back on the office re-keying what the applicator phones in, doubling the work on the busiest flow.",
  "evidence": "src/pages/FieldView.tsx:15 ('Expand → fetch the job's full read-only detail') and :133 (only get_dispatched_list, no mutations); grep for start_job/complete_job hits only src/pages/JobDetail.tsx (:2039, :2061) and src/components/jobs/jobMassEdit.ts:69-80 (forward transitions marked bulkSafe:false); App.tsx:269-270 route comment 'read-only my-jobs cards'; contrast driver mobile flow in Deliveries.tsx:744-933 and DeliveryDetail.tsx:1328-1445",
  "rec": "Give the applicator phone view two big buttons per job card: Start Job and Complete Job (the complete step collecting the same applied-info the JobDetail modal collects, sized for a phone). Reuses the existing start_job/complete_job RPCs — no DB change."
 },
 {
  "i": 65,
  "source": "lifecycles",
  "sev": "high",
  "scope": "DB",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Read the full latest void_invoice body (20260529214538_fix_void_order_void_invoice_status_transitions.sql, lines 264-353): zero references to 'jobs' or 'job_id' anywhere; for posted/overdue invoices it sets status='voided' and reverses allocations/prepay/commissions but never touches the linked job. Verified _enforce_job_status_transition (20260620150000_job_cancel_gate.sql:21-46) whitelist has only scheduled->in_progress, in_progress->completed, completed->invoiced, plus a gated cancel clause — genuinely no invoiced->completed path outside _is_admin_override(). Verified transfer_job_to_invoice raises 'Job already invoiced' (20260622020000:90). Went further than the original finding and read transfer_invoice_to_job's own documented precondition (20260625120000, lines 27-29): it explicitly states a posted/paid/overdue/voided/cancelled invoice 'CANNOT be reversed here (the user must void it instead)' — meaning the one RPC that COULD reset the job explicitly refuses to run in exactly the voided-invoice scenario the finding describes, closing the loop that the job is truly stuck. Confirmed no migration after 20260620150000 touches _enforce_job_status_transition's whitelist. Correction/strengthening: the finding's cited evidence is accurate; add that transfer_invoice_to_job's own precondition comment is the clearest proof there is no automatic recovery path once an invoice reaches posted+voided.",
  "title": "Voiding a posted field-application invoice permanently strands the job — it can never be re-billed",
  "problem": "If a posted spray-job invoice turns out wrong and the office voids it (Void sits right next to Unpost on the same page), the job stays stuck at 'invoiced' pointing at a dead invoice. Nothing resets the job: re-billing raises 'Job already invoiced', the reverse-transfer path refuses anything past 'unposted', and the status trigger has no invoiced→completed transition. The money for that spray work is then uncollectable through the app. Blend tickets got exactly this safety net (billed auto-resets to unbilled when their invoice is voided); jobs did not.",
  "evidence": "void_invoice (migration 20260529214538) never touches jobs (grep 'job' = 0 code hits); _enforce_job_status_transition whitelist is forward-only (20260620150000_job_cancel_gate.sql:35-44); transfer_job_to_invoice raises 'Job already invoiced' (20260622020000_transfer_job_invoice_audit_row.sql:90-91); transfer_invoice_to_job requires the invoice be draft|unposted (20260625120000_transfer_invoice_to_job.sql:27-31); contrast trg_sync_blend_ticket_payment (CLAUDE.md blend-ticket lifecycle) which auto-resets billed→unbilled on void",
  "rec": "Mirror the blend-ticket pattern: when a field_application invoice is voided, revert its job invoiced→completed and clear jobs.invoice_id inside void_invoice (using the existing admin-override GUC bracket). One migration; the job then re-enters the normal Unbilled Jobs queue."
 },
 {
  "i": 66,
  "source": "lifecycles",
  "sev": "high",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Deliveries.tsx:1046 exactly (row.status === 'in_progress' ? <Complete button> : null — literally returns null otherwise, no quick-Complete for scheduled rows). Verified DeliveryDetail.tsx: Start Delivery section gated to status==='scheduled' (~1329), and the comment at line 1376-1377 literally reads '/* Complete delivery section — only when in_progress */' confirming the two-modal split. Verified create_quick_delivery inserts with hardcoded status 'scheduled' (20260702132000_a4_quick_delivery_tier_price_cascade.sql:168) — confirming even the counter-sale fast path lands in the same two-step march. Directly matches the owner's stated need for a fast office-keyed-after-the-fact path.",
  "title": "Office-keyed deliveries take two transitions across two modals — and even 'Quick Delivery' lands back in that slow path",
  "problem": "The owner said deliveries are often keyed in by the office after the truck already ran, and both paths must be fast. Today a finished-but-still-'scheduled' delivery cannot be completed from the list at all (the one-click Complete button only appears once it is 'in_progress'); the office must open the delivery, click Start Delivery (a verify-your-truck modal aimed at drivers), wait for the refetch, then fill Signed-by and click Complete Delivery. Quick Delivery — the counter-sale fast path — creates its delivery as 'scheduled' too, so even that flow ends with the same two-modal march before inventory deducts.",
  "evidence": "Deliveries.tsx:1043-1056 (Complete quick-action rendered only when row.status === 'in_progress'); DeliveryDetail.tsx:1500-1508 (Start) and :1376-1429 (separate Complete section); create_quick_delivery inserts deliveries with status 'scheduled' (20260702132000_a4_quick_delivery_tier_price_cascade.sql:167-168)",
  "rec": "Add an office one-shot 'Record completed delivery' that chains confirm_delivery + complete_delivery in one dialog (both RPCs still fire in order, so the confirm→complete hard rule is preserved), and offer the list quick-Complete on 'scheduled' rows using the same chain. Let Quick Delivery optionally finish as completed for walk-outs."
 },
 {
  "i": 67,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified OrderDetail.tsx:1915 exactly — dropdown source object offers {confirmed: [partially_fulfilled, fulfilled, cancelled], partially_fulfilled: [fulfilled, cancelled]}. Verified OrderDetail.tsx:589-592 exactly — the actual validTransitions allow-list used by handleStatusChange only permits {confirmed: [cancelled], partially_fulfilled: [cancelled]}. Selecting partially_fulfilled or fulfilled from the dropdown is guaranteed to hit the 'Cannot change status' toast at line 595. Verified complete_delivery auto-derives order status via CASE WHEN v_all_delivered THEN 'fulfilled' ELSE 'partially_fulfilled' (20260620220000_complete_delivery_audit_and_partial_cost.sql:219, matching the cited line). Confirmed exactly as described — genuinely dead dropdown options.",
  "title": "Order 'Change Status' modal offers choices that always fail",
  "problem": "The admin-only Change Status dialog on an order lists 'partially fulfilled' and 'fulfilled' as selectable targets, but the handler's own allow-list only permits 'cancelled' — picking either fulfillment option always errors with 'Cannot change status'. Fulfillment is auto-derived when deliveries complete, so those options should not exist. An owner hunting for 'what do I do next' gets a dropdown of dead buttons.",
  "evidence": "OrderDetail.tsx:1915 (dropdown offers partially_fulfilled/fulfilled/cancelled) vs OrderDetail.tsx:589-592 (validTransitions = { confirmed: ['cancelled'], partially_fulfilled: ['cancelled'] }); complete_delivery auto-advances order status (20260620220000_complete_delivery_audit_and_partial_cost.sql:219)",
  "rec": "Replace the modal with a plain 'Cancel Order' button (the only transition it can actually perform); keep the auto-derived fulfillment untouched."
 },
 {
  "i": 68,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified 20260702175000_layer2_complete_job_drop_phase7_drain.sql:88-89 exactly, including the literal text 'Use start_job() first.' Confirmed this is still the true latest body of complete_job — the 3 migrations dated after it (20260703120000/130000/200000, the Layer-2 job-hold-coordination work already on the do-not-re-flag list) do not CREATE OR REPLACE complete_job at all. Verified JobDetail.tsx:2491 (canComplete = status === 'in_progress' only) and the Start/Complete buttons at 2561-2572 exactly as cited (Start only shows when status==='scheduled', Complete only when canComplete). Verified appliedInfo state (JobDetail.tsx:1177-1184) has wind_speed/wind_direction/temperature/humidity/actual_gallons_applied/notes — genuinely no start/end time field. Confirmed exactly as described.",
  "title": "After-the-fact job completion forces a fake 'Start Job' click first",
  "problem": "complete_job hard-refuses any job not 'in_progress' ('Use start_job() first'), and the UI mirrors that: for a job that was already sprayed and is being keyed in later, the office must click Start Job, wait for the page to refetch, then click Complete Job and fill the modal. Besides the extra round-trip on the second-highest-volume flow, the recorded actual_start_time becomes the moment of the click, seconds before completion — junk timing data on what becomes a compliance application record (the complete modal captures weather/gallons but no times).",
  "evidence": "20260702175000_layer2_complete_job_drop_phase7_drain.sql:88-89 (status guard + 'Use start_job() first'); JobDetail.tsx:2491 (canComplete = in_progress only), :2561-2572 (separate Start/Complete buttons); JobDetail.tsx:1177-1184 (appliedInfo has no start/end time fields)",
  "rec": "UI-only fix: when Complete is clicked on a 'scheduled' job, chain start_job then complete_job in one action. Nicer follow-up: let the complete modal accept an actual start/end time for after-the-fact entry."
 },
 {
  "i": 69,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "owner-decision",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified all four citations exactly: post_invoice's live body (20260702160000_a8_terms_to_due_date.sql, lines 91-102) gates on role IN ('admin','sales_rep') at line 100; InvoiceDetail.tsx:1111 gates the Post button on isAdmin; Invoices.tsx:603 gates the entire batch-action bar on profile?.role === 'admin'; OrderDetail.tsx:1437 allows (role === 'admin' || role === 'sales_rep') for the 'Post all drafts' button. Confirmed this is a genuine three-way inconsistency for the identical underlying action, exactly as described. Correctly framed as an owner-decision item since it requires picking which answer is right, not just a code fix.",
  "title": "Who may post an invoice differs in three places for the same action",
  "problem": "The database allows both admin and sales_rep to post invoices. But the invoice detail page shows Post to admins only, the Invoices list hides ALL batch actions (post/void/print/delete) from sales reps, while the Order page's 'Post all drafts' button happily lets a sales rep post. Same action, three different answers — in a 2-3 person office the sales person will learn they 'can post from the order page but not the invoice page' and conclude the app is broken.",
  "evidence": "post_invoice role gate allows admin+sales_rep (20260702160000_a8_terms_to_due_date.sql:100-102); InvoiceDetail.tsx:1111 (Post requires isAdmin); Invoices.tsx:603 (batch bar requires role==='admin'); OrderDetail.tsx:1437-1448 (Post all drafts allowed for admin OR sales_rep)",
  "rec": "Owner decision: should sales staff post invoices? If yes (the DB already says yes), relax the InvoiceDetail and list gates to admin+sales_rep; if no, tighten the DB and the OrderDetail button. Either way make the three surfaces agree."
 },
 {
  "i": 70,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Invoice 'draft' vs 'unposted' is a distinction without a daily difference",
  "problem": "Two separate amber statuses both mean 'not posted yet': post_invoice treats draft and unposted identically, the list's summary card lumps them into one 'Unposted' count, batch-post selects both, and the field-invoice Unposted page buckets both. The only real behavioral difference is that drafts can be deleted. For daily use the office sees two different badges for the same to-do ('what's the difference? which do I act on?') — pure status noise.",
  "evidence": "post_invoice accepts status IN ('draft','unposted') (20260702160000_a8_terms_to_due_date.sql:112); Invoices.tsx:192 (unpostedCount = draft + unposted in one card), :202 (selectedPostable includes both), :204 (only draft is deletable); FieldInvoicesUnposted.tsx:103 (.in('status',['draft','unposted']))",
  "rec": "Keep both values in the DB (CHECK-constraint superset rule — don't shrink the enum), but present them as one 'Ready to Post' badge/filter in the UI, with a small 'deletable' hint on true drafts. Cosmetic relabel, no migration."
 },
 {
  "i": 71,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "A mis-posted chemical invoice can't be un-posted in the UI, though the database allows it",
  "problem": "unpost_invoice (posted → back to unposted) works for any invoice type — its guards are status-only. But the UI surfaces the Unpost button only on field_application invoices. So when the office posts a chemical-sale invoice with wrong quantities, their only visible recovery is the scarier admin-only Void + recreate path (and re-creating from the order is itself blocked once a completed delivery exists). The gentle undo exists server-side and is simply hidden.",
  "evidence": "unpost_invoice guards check only status/payments, no invoice_type restriction (20260625130000_unpost_invoice.sql:151-155, 171); InvoiceDetail.tsx:1194-1206 (Unpost rendered only when invoice_type === 'field_application')",
  "rec": "Show the same Unpost button on posted chemical-sale invoices (same RPC, same guards). One-line condition change."
 },
 {
  "i": 72,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Cancelled deliveries vanish from the Deliveries page; voided orders/deliveries can't be filtered",
  "problem": "The Deliveries page query excludes cancelled rows outright and its status filter offers only scheduled/in-progress/completed — so a delivery cancelled by mistake is unfindable there (you can only rediscover it by remembering which order it belonged to). Voided deliveries do load but there's no filter to isolate them. Same gap on Orders: the status filter ends at 'cancelled' with no 'voided' option. For 'oops, undo that' recovery and for month-end 'what did we kill and why' review, the terminal records are effectively hidden.",
  "evidence": "Deliveries.tsx:216 (.neq('status','cancelled') in the fetch), :1293-1297 (filter options lack cancelled/voided); Orders.tsx:612-617 (status options lack 'voided')",
  "rec": "Add 'Cancelled' and 'Voided' options to both status filters (fetch cancelled rows only when that filter is chosen, keeping the default list clean)."
 },
 {
  "i": 73,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Voided commission-payment batches disappear from the app",
  "problem": "The Commission Payments page has exactly two tabs — Unposted and Posted — and filters rows strictly to the active tab's status. A voided payout batch matches neither, so the moment you void one it becomes invisible everywhere in the UI. When the owner asks 'didn't I already cut Bob a check in March?' there is no way to see that a batch existed and was voided.",
  "evidence": "CommissionPayments.tsx:59 (tab state limited to 'unposted' | 'posted') and :364 (filtered = payments.filter(p => p.status === tab)); lifecycle unposted→posted→voided per 20260331120000_void_commission_payment.sql CHECK",
  "rec": "Add a third 'Voided' tab (or an 'include voided' toggle) showing the batch, its void reason, and date."
 },
 {
  "i": 74,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Booking a phone quote requires a PDF-preview detour before it can become an order",
  "problem": "The owner's fastest-path workflow is the phone booking, but a freshly saved quote sits in 'draft', and the builder's only forward action from draft is 'Preview Quote' — you must open the PDF preview and click 'Mark as Presented' (or email it) purely to flip the status to 'sent' before 'Convert to Order' appears. For a customer who already said yes on the phone, that's mandatory ceremony through a modal built for a different scenario. Bonus inconsistency: the builder hides Convert on 'revised' quotes while the Quotes list offers one-click convert for both sent and revised.",
  "evidence": "QuoteBuilder.tsx:276-277 (canSend from draft; canConvert = 'sent' only), :1469-1493 (handleMarkPresented is the status flip, reached only from the preview modal); Quotes.tsx:478 (list convert for sent||revised); convert_quote_to_order accepts sent/revised/accepted (Quotes.tsx:174-177)",
  "rec": "Add a 'Book as Order' button on draft quotes that chains save → mark-sent (create_quote_version method 'presented') → convert_quote_to_order in one confirm. Also let the builder convert 'revised' quotes to match the list."
 },
 {
  "i": 75,
  "source": "lifecycles",
  "sev": "medium",
  "scope": "UI",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Billing completed spray jobs is strictly one-at-a-time",
  "problem": "Each completed job must be opened in JobDetail and 'Transfer to Invoice' clicked individually; the Unbilled Applications queue is deliberately read-only links, and bulk status change explicitly disables the 'invoiced' step. In peak season with dozens of completed jobs a week, end-of-week billing becomes dozens of open-page→click→navigate-back cycles. The auto-draft-on-completion switch exists but ships OFF, and even with it ON, already-completed backlog needs the manual loop.",
  "evidence": "UnbilledApplications.tsx:27 ('READ-ONLY: it lists and links out'), :225 (row click → /jobs/:id); JobDetail.tsx:2492+2573-2578 (canTransfer per-job); jobMassEdit.ts:69-80 ('invoiced' bulkSafe:false); auto-draft OFF by default (20260630073344_auto_draft_invoice_on_job_completion.sql + CLAUDE.md snapshot §4)",
  "rec": "Add a 'Create invoices' batch action on the Unbilled Applications job list that loops transfer_job_to_invoice per selected job (each already idempotent), reporting per-job success/failure. Also worth flipping the auto-draft switch ON at go-live (owner call)."
 },
 {
  "i": 76,
  "source": "lifecycles",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Deliveries list sorts farthest-future first, burying today's work",
  "problem": "The default list orders by scheduled_date DESCENDING with no default date filter, so a delivery scheduled three weeks out pins to the top while today's trucks sit below it, mixed with completed history. The 7-day strip and 'Today' preset mitigate, but the default view — the thing office staff see every morning — is sorted backwards for a dispatch page.",
  "evidence": "Deliveries.tsx:216-218 (.order('scheduled_date', { ascending: false }), no default datePreset)",
  "rec": "Default to 'Upcoming' (today-forward, ascending) with active statuses first; keep 'All Dates' as an explicit choice."
 },
 {
  "i": 77,
  "source": "lifecycles",
  "sev": "low",
  "scope": "DB",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "'Revised' quotes auto-expire with zero warning",
  "problem": "The nightly sweep auto-expires both 'sent' AND 'revised' non-planned quotes past their expiry date (releasing their inventory holds), but the Dashboard's 'expiring quotes' warning queries status='sent' only. A quote you revised last week silently dies at 6:05 AM with no heads-up anywhere, and the customer's pricing disappears from the open-booking view.",
  "evidence": "get_dashboard_action_items expiring-quotes block filters WHERE q.status = 'sent' (20260404040300_get_dashboard_action_items_rpc.sql:106); auto_expire_quotes sweeps status IN ('sent','revised') (20260614143649_auto_expire_quotes_skip_planned_and_schedule.sql:37, cron'd daily at :89)",
  "rec": "One-line RPC change: status IN ('sent','revised') in the dashboard expiring-quotes query so the warning matches what the sweep will actually kill."
 },
 {
  "i": 78,
  "source": "lifecycles",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Re-billing a completed delivery is hidden on an admin integrity page",
  "problem": "If the auto-created draft invoice for a completed delivery is deleted or voided, the only way to bill that delivery again is the create_invoice_for_unbilled_delivery RPC — which is wired up solely inside Integrity Cleanup, an admin back-office page. The delivery page itself shows an invoice banner only when an invoice exists and offers nothing when it's missing; the order page hides Create Invoice because a completed delivery exists. Office staff staring at the unbilled delivery have no visible path forward.",
  "evidence": "create_invoice_for_unbilled_delivery called only from IntegrityCleanup.tsx:316; DeliveryDetail.tsx:1918 (banner rendered only when autoInvoiceId set); create_invoice_from_order raises when any non-cancelled delivery exists (20260620190000_invoice_creator_provenance_totals.sql:71-78)",
  "rec": "On a completed delivery with no active invoice, show a 'Create invoice for this delivery' button calling the existing RPC (admin-gated as today, but discoverable where the problem is seen)."
 },
 {
  "i": 79,
  "source": "nav-redesign",
  "sev": "high",
  "scope": "UI",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Applicators land on the office KPI dashboard, not their field view",
  "problem": "The Dashboard treats everyone who isn't a driver as office staff. An applicator logging in on a phone lands on the desktop office dashboard — active orders, inventory position, sales pipeline, monthly activity, season progress — none of which is their world, while the page actually built for them (/field, 'My Field Jobs') sits two clicks deep inside an 'Operations' group. Wrong first screen every single morning for the highest-frequency phone users.",
  "evidence": "src/pages/Dashboard.tsx:380-382 (`showFull = !isDriver; // Admin & Sales see everything` — applicator falls into showFull) and 481+ (KPI/inventory/pipeline sections gated only on showFull); FieldView is at src/App.tsx:270 (/field) and Sidebar.tsx:183 (buried in Operations)",
  "rec": "Role-based post-login landing: admin/sales -> /office-cockpit, applicator -> /field, driver -> /my-route. On the Dashboard itself, gate the office sections on role === 'admin' || 'sales_rep' instead of !isDriver."
 },
 {
  "i": 80,
  "source": "nav-redesign",
  "sev": "high",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Sidebar.tsx:196-223 directly — the 'finance' category id has exactly 19 items (financial-dashboard, accounts-receivable, ar-aging, accounts-payable, vendors, prepayments, prepay-workspace, commission-payments, customer-transactions, month-end, integrity-report, integrity-cleanup, rebates, reports, sales-reports, compliance, lot-trace, label-review, watchdog). Compliance (line 217), Lot Trace (218), Label Review (219), and Watchdog Flags (220) are non-money pages (applicator licenses, recall trace, product label data, application anomalies) sitting under a 'Finance' label with a $ icon — a genuine mismatch for an owner who complained he 'can't find things.' This is a real, novel navigation-taxonomy problem directly relevant to daily use.",
  "title": "'Finance' is a 19-item junk drawer holding 5 pages that aren't finance",
  "problem": "The Finance group has 19 entries — the largest menu in the app — mixing daily accounting (AR, payments), monthly rituals (month-end), data-QA tools (integrity report/cleanup), analytics (reports, sales reports), and five pages that aren't money at all: Compliance (applicator licenses), Lot Trace (recall lookup), Label Review (product label data), Watchdog Flags (application anomalies), and Reports. When the owner needs a license expiry or a recall trace, nothing tells him to open the money menu. This is the single biggest driver of his 'can't find things' complaint.",
  "evidence": "src/components/layout/Sidebar.tsx:196-223 (19 items in the finance category; compliance at 217, lot-trace 218, label-review 219, watchdog 220, reports 215-216)",
  "rec": "Split into three groups per the proposal: Money (accounting), Compliance & Records, Insights (analytics). No routes change — only sidebar grouping."
 },
 {
  "i": 81,
  "source": "nav-redesign",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified App.tsx:242 has the route (admin-only). Verified Sidebar.tsx's full navigation array (lines 87-243) has no /payment-history entry anywhere. Verified CommandPalette.tsx ALL_PAGES (lines 133-188) omits it. Verified FinancialDashboard.tsx:139 is the only inbound link ('Payment History', path '/payment-history'). Also independently verified AccountsReceivable.tsx:117 already renders PaymentHistory as its 'payments' tab, confirming the recommended fix location already exists and works as a home for it.",
  "title": "/payment-history is a true orphan — no menu entry, no palette entry",
  "problem": "The Payment History page (full payment list with per-invoice allocation breakdown) has a route, an admin permission entry, and a page title, but appears nowhere in the sidebar and is missing from the Ctrl+K command palette. The only way in is a quick-link tile on the admin-only Financial Dashboard, or knowing the URL. An admin looking for 'where did that check go' has no findable path.",
  "evidence": "src/App.tsx:242 (route exists); Sidebar.tsx navigation array (no /payment-history entry anywhere); src/components/ui/CommandPalette.tsx:133-188 (ALL_PAGES omits it); only inbound link is src/pages/FinancialDashboard.tsx:139",
  "rec": "It is already mounted as the 'Payment History' tab inside /accounts-receivable (AccountsReceivable.tsx:117) — make that its official home, keep the standalone route as a deep-link, and add it to the command palette."
 },
 {
  "i": 82,
  "source": "nav-redesign",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified CommandPalette.tsx ALL_PAGES (lines 133-188) directly. Confirmed absent: Office Cockpit, /field (My Field Jobs), /my-route, /field-invoices/unposted, /field-invoices/posted, /field-invoices/unbilled, /field-invoices/summary, /invoices/field-app/new, /application-services, /program-tracker, /label-review, /integrity-report, /integrity-cleanup, /payment-history, /vendors, /notifications — grepped the file for all these terms and only bare '/field-invoices' (base landing page) appears. Office Cockpit, /field, and /my-route are exactly the three role-landing pages named in the owner interview as daily-use hubs (applicator phones, dispatch), so this is highly relevant to daily use, not a nitpick.",
  "title": "Command palette (the 'can't find it' rescue hatch) is missing the daily surfaces",
  "problem": "The Ctrl+K palette's page list omits Office Cockpit, My Field Jobs (/field), My Route, all four Field Invoice working trays (unposted/posted/unbilled/summary), New Field Application Invoice, Application Services, Program Tracker, Label Review, Integrity Report/Cleanup, Payment History, Vendors, and Notifications. The three role landing pages (cockpit, field, my-route) being unsearchable is the worst of it — the tool meant to rescue a lost user can't find the app's own flagship screens.",
  "evidence": "src/components/ui/CommandPalette.tsx:133-188 (ALL_PAGES array — compare against App.tsx:186-287 route list)",
  "rec": "Add the missing entries to ALL_PAGES (pure data change), ideally generated from PAGE_PERMISSIONS so it can't drift again."
 },
 {
  "i": 83,
  "source": "nav-redesign",
  "sev": "medium",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified AccountsReceivable.tsx:116-119 renders ARaging, PaymentHistory, PrepaymentManager, CustomerTransactionReview as tabs (imports confirmed lines 6-9). Verified Sidebar.tsx finance category separately lists 'AR Aging' (line 204, /ar-aging), 'Prepayments' (line 207, /prepayments), and 'Transactions' (line 210, /customer-transactions) as standalone items alongside 'Accounts Receivable' (line 203). This is a genuine duplicate-path problem in the exact menu the owner already flagged as too-cluttered.",
  "title": "The same A/R content is reachable from four separate menu items",
  "problem": "The A/R Workspace (/accounts-receivable) already embeds AR Aging, Payment History, Prepayments, and Customer Ledger as tabs. Yet AR Aging, Prepayments, and Transactions ALSO sit as their own Finance menu items. The same screen having two homes makes the menu longer and teaches users two different paths to one task — then they wonder which is 'the real one'.",
  "evidence": "src/pages/AccountsReceivable.tsx:116-119 (tabs render ARaging, PaymentHistory, PrepaymentManager, CustomerTransactionReview); Sidebar.tsx:204 (/ar-aging), 207 (/prepayments), 210 (/customer-transactions) as separate items",
  "rec": "Keep one 'A/R Workspace' menu item; keep the standalone routes as deep-links (they already select the right tab via ?tab=). Remove the three duplicate sidebar entries."
 },
 {
  "i": 84,
  "source": "nav-redesign",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified pagePermissions.ts:79-86 directly — compliance, lot-trace, label-review, watchdog, and office-cockpit are ALL grouped under category 'Reports' (lines 82-86), plus field-invoices/payments under 'Sales' category (lines 28-29). Verified this does NOT match Sidebar.tsx, where those same 5 pages sit under 'finance' category (lines 217-220 for the first four; office-cockpit is a standalone top-level item at Sidebar.tsx:97-106, not even inside an accordion at all). Confirmed real mismatch between the admin permissions screen's grouping and the actual sidebar — a low-severity but real admin-UX inconsistency.",
  "title": "Settings deny-list categories describe a menu that doesn't exist",
  "problem": "The admin page-permissions screen groups pages by PAGE_PERMISSIONS categories: Compliance, Lot Trace, Label Review, Watchdog, and Office Cockpit under 'Reports'; Field Invoices under 'Sales'; Payments under 'Sales'. The real sidebar shows those under Finance / standalone / etc. An admin denying a page has to mentally translate between two different maps of the app.",
  "evidence": "src/lib/pagePermissions.ts:79-86 (category: 'Reports' for compliance/lot-trace/label-review/watchdog/office-cockpit) vs Sidebar.tsx:196-223 (same pages under Finance) and Sidebar.tsx:100-116 (office-cockpit standalone)",
  "rec": "When the sidebar is regrouped, update PAGE_PERMISSIONS categories to the same group names so the permissions screen mirrors the menu 1:1."
 },
 {
  "i": 85,
  "source": "nav-redesign",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Sidebar.tsx:168-169 — 'Receiving' (/receiving) and 'Receiving Hub' (/receiving-hub) are adjacent items with near-identical labels. Verified ReceivingLog.tsx has a header at line 329 (generic, no distinguishing subtitle visible near it) and a 'Quick Receive' button at line 340 (matches the recommendation's citation of ReceivingLog.tsx:336, off by a few lines but same button). Verified ReceivingHub.tsx:234/248 titles itself 'Receiving Hub'. The ambiguity is real: nothing in either label signals which is the open-PO working screen vs. the historical log.",
  "title": "Two adjacent 'Receiving' entries with no cue which is the daily one",
  "problem": "'Receiving' (/receiving — the log/history) and 'Receiving Hub' (/receiving-hub — the open-PO-lines working screen) sit next to each other in Products & Inventory. Nothing tells warehouse staff which to open when a truck arrives; the newer Hub is the daily tool but the older log gets the plain name.",
  "evidence": "Sidebar.tsx:168-169 (adjacent items, near-identical labels); pages-routes.md rows for /receiving and /receiving-hub",
  "rec": "Rename to 'Receiving (Hub)' and 'Receiving Log'; the Quick Receive wizard stays a button on the log page (ReceivingLog.tsx:336)."
 },
 {
  "i": 86,
  "source": "nav-redesign",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Sidebar.tsx:182-190 exactly as cited (Dispatch & Applicator View, My Field Jobs, App Services, App Records, Plane icon on /vehicles at line 187 and import at line 33). Independently read DispatchBoard.tsx's own header comment (lines 1-23): it explicitly is a shared office+applicator dispatch screen ('Dispatch & Applicator View... distinct from the light office Jobs list'), while FieldView.tsx's header (lines 2-27) independently confirms it is the SEPARATE 'Phone/mobile applicator FIELD VIEW... READ-ONLY... just the applicator's own cards' — two different pages, overlapping applicator-facing purpose, genuinely confusing names for a non-technical owner. Directly relevant since applicators are phone-only daily users per the interview.",
  "title": "Confusing labels: 'Dispatch & Applicator View', 'App Services'/'App Records', and a Plane icon for ground vehicles",
  "problem": "One menu item claims to be both the office dispatch board AND the applicator's view, while a separate 'My Field Jobs' page is the actual applicator phone view — nobody can tell which is for whom. 'App Services' and 'App Records' read as 'application (software) settings' rather than spray-work fee schedules and the as-applied record book. Vehicles uses an airplane icon for a fleet that is mostly ground rigs.",
  "evidence": "Sidebar.tsx:182 ('Dispatch & Applicator View'), 183 ('My Field Jobs'), 188 ('App Services'), 190 ('App Records'), 187 (icon: <Plane/> for /vehicles)",
  "rec": "Rename: 'Dispatch Board' (office), 'My Day' (applicator), 'Application Services (fees)', 'Record Book (Applications)'; swap the Plane icon for Truck/Tractor."
 },
 {
  "i": 87,
  "source": "nav-redesign",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Sidebar.tsx:117-125 — 'getting-started' is a standalone top-level item at array position 4 (after dashboard, office-cockpit, to-ship), with no role restriction (available to everyone forever, not gated to first-login). Verified /quotes sits inside the 'sales' category accordion at line 133, requiring an extra click to expand. Confirmed real: a one-time onboarding page occupies permanent top billing above the accordion-nested Quotes/Sales items that are the owner's highest-volume daily work.",
  "title": "Getting Started holds a permanent top-level slot while daily pages sit two clicks deep",
  "problem": "A one-time onboarding stepper occupies the 4th standalone slot in everyone's sidebar forever, above Quotes, Jobs, and Deliveries. Prime menu real estate goes to a page used in week one; the pages used 50 times a day are buried in accordions.",
  "evidence": "Sidebar.tsx:117-125 (standalone getting-started, position 4) vs /quotes at Sidebar.tsx:133 (inside the Sales accordion)",
  "rec": "Move Getting Started into the Setup & Help group (bottom)."
 },
 {
  "i": 88,
  "source": "nav-redesign",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified Sidebar.tsx:137 — only '/field-invoices/summary' (labeled 'Customer Summary') is promoted to the Sales accordion. Verified FieldInvoices.tsx:491-518 — all four (Unposted List at 491-494, Posted List 495-502, Unbilled 503-510, Customer Summary 511-518) render as identical-style in-page buttons on the landing page. Verified App.tsx:212-221 has no separate top-level Sidebar/route promotion for unposted/posted/unbilled — only the summary route gets the sidebar entry. The inconsistency (one sibling promoted, three not, and the promoted one's label doesn't say 'invoices') is real and correctly cited.",
  "title": "Field-invoice sub-pages are split inconsistently between top nav and in-page buttons",
  "problem": "Of the five Field Invoices working screens, exactly one (Customer Summary) was promoted to the top Sales menu — labeled just 'Customer Summary', which doesn't say invoices — while its siblings (Unposted, Posted, Unbilled) are only buttons on the Field Invoices landing page. Users learn 'sub-screens live on the landing page' and then one random exception lives in the menu.",
  "evidence": "Sidebar.tsx:137 (/field-invoices/summary as 'Customer Summary' in Sales) vs FieldInvoices.tsx:491-518 (Unposted/Posted/Unbilled/Summary all as in-page buttons); no nav entries for the other three (App.tsx:214-221)",
  "rec": "Pick one pattern: keep all four as landing-page tiles, and give the cross-channel Summary a single clearly-named home ('Unposted Invoice Summary' under Money, per the proposal)."
 },
 {
  "i": 89,
  "source": "seams",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified live via Supabase MCP: pg_get_functiondef of auto_expire_quotes shows 'AND q.is_planned = false' with the exact comment 'Staff close planned quotes by hand if needed' (owner gate G-AE). Verified live cron.job has exactly 5 jobs (auto-expire-quotes, check-remainder-reminders, check-unpriced-orders, mark-overdue-invoices, release-expired-quote-holds) — none targets planned-quote staleness. Verified OfficeCockpit.tsx has no is_planned/inventory_hold-related grep hits (only applicator-license expiry_date at line 321). Verified Quotes.tsx:467-472 renders a plain Expires column with no conditional past-due styling in the render function. Verified notificationTriggers.ts:107-117 checkExpiringQuoteNotifications() filters '.in(status, [sent, draft])' AND '.gte(expires_at, now)' — confirming it warns only in the 3-day-before window and goes silent once the date passes (and note: is_planned quotes aren't even excluded from this pre-expiry nudge, but they get no post-expiry nudge either way, matching the finding). This is a real, high-relevance gap: staff have zero system-provided way to discover which planned bookings are stale, and stale holds directly corrupt Net Free — the number trusted for the owner's highest-volume daily work (phone bookings).",
  "title": "Planned program bookings hold inventory forever, and nothing in the app ever flags a stale one",
  "problem": "By deliberate owner decision (gate G-AE, 2026-06-13), planned bookings are never auto-expired — 'staff close planned quotes by hand if needed.' But the app gives staff zero help doing that: no cockpit tile, no notification, no list, and no visual flag on the Quotes page for a planned booking that is past its expiry date and still holding product. Meanwhile those holds keep subtracting from Net Free — the number the office trusts when taking phone bookings (the owner's highest-volume daily work). A half-used program from last season silently makes the shed look emptier than it is, all season, until someone remembers it exists. The tools to close one exist (Close — Applied, Convert, Cancel, Roll Over to New Season); the missing piece is the nudge that says 'these N bookings are stale — deal with them.'",
  "evidence": "Live auto_expire_quotes (pg_get_functiondef): 'AND q.is_planned = false' with comment 'Staff close planned quotes by hand if needed'; live cron.job has 5 jobs, none touching planned holds; src/pages/OfficeCockpit.tsx — grep for is_planned/inventory_holds/hold finds no tile (only applicator-license expiry_date); src/pages/Quotes.tsx:468-471 renders an Expires column with no past-due highlight; src/lib/notificationTriggers.ts:107-117 warns 3 days BEFORE expires_at only (gte now filter), so once the date passes, silence forever.",
  "rec": "Add a 'Stale planned bookings' surface: an Office Cockpit tile + Quotes-page filter listing is_planned bookings past expires_at (or with no draw/job activity for N days) that still have active holds, showing held units and one-click paths to Close — Applied / Convert / Cancel / Roll Over. Optionally a weekly notification to admins mirroring check_remainder_reminders."
 },
 {
  "i": 90,
  "source": "seams",
  "sev": "medium",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Personally verified both sides of the mismatch. src/lib/notificationTriggers.ts:115 reads `.in('status', ['sent', 'draft'])`. Live auto_expire_quotes (re-read via pg_get_functiondef) expires `q.status IN ('sent','revised') AND q.is_planned = false` — 'revised' truly gets no warning; 'draft' gets a warning but can never auto-expire (not in the expire set); is_planned quotes are also not excluded from the warning query, so they get a false 'expires in N days' message. Confirmed notificationTriggers.ts:143 notifies only quote.created_by. Confirmed runPeriodicNotificationChecks is called only from Dashboard.tsx:337 (grep found no other caller). Confirmed the live function's activity_feed insert is wrapped in `EXCEPTION WHEN OTHERS THEN NULL` with the exact comment 'this insert currently always fails' referencing the zero-uuid/FK problem. Every cited fact reproduced exactly.",
  "title": "Quote auto-expiry and the expiring-soon warning cover different status sets — revised quotes expire with no warning, and the expiry moment itself leaves no trace",
  "problem": "The 3-day 'Quote Expiring Soon' warning only looks at quotes in status sent or draft, but the nightly auto-expire sweep expires quotes in sent or revised. So the moment a rep revises a quote, the countdown warning stops — and the quote (with its inventory holds) can then expire and release overnight with no heads-up. Two more wrinkles: (a) draft quotes get warnings but never auto-expire (noise), and planned quotes get a 'expires in N days' warning that is false — they never expire; (b) when expiry actually happens there is no notification at all, and the sweep's own summary log row 'currently always fails' on an FK (the function comment admits it) and is swallowed — so afterward there is no record anywhere except the quote's status quietly reading 'expired'. The warning also only fires when someone loads the Dashboard, and only notifies the quote's creator, not the assigned sales rep or admins.",
  "evidence": "src/lib/notificationTriggers.ts:115 `.in('status', ['sent', 'draft'])` vs live auto_expire_quotes `status IN ('sent', 'revised')` + `is_planned = false`; notificationTriggers.ts:143-150 notifies quote.created_by only; runPeriodicNotificationChecks called only from Dashboard.tsx:337; live auto_expire_quotes activity_feed insert wrapped in `EXCEPTION WHEN OTHERS THEN NULL` with comment 'this insert currently always fails' (zero-uuid performed_by FK).",
  "rec": "Align the sets: warn on ['sent','revised'] (drop 'draft', skip is_planned=true), notify assigned rep + creator, and have auto_expire_quotes itself write a notifications row per expired quote ('Quote X expired overnight — N units released to free stock') plus fix or drop the always-failing activity_feed insert."
 },
 {
  "i": 91,
  "source": "seams",
  "sev": "medium",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Re-read the full live source of both RPCs. create_invoice_from_blend_ticket resolves `jobs.quote_section_id` when v_ticket.job_id IS NOT NULL (for quoted-price lookup) but its only pre-billing guard is `v_ticket.payment_status IS DISTINCT FROM 'unbilled'` — no reference to job status anywhere (verified via position('invoiced' in def)=0 over the full 18576-char body) and it never touches the jobs table on write. transfer_job_to_invoice's guards are `v_job.status = 'invoiced'` (reject) and `status != 'completed'` (reject) — zero reference to blend tickets (position('blend' in def)=0 over 14926 chars). Grep for HelpTip in BlendTicketDetail.tsx returned 0 matches, confirming no on-page guidance. Both channels can independently invoice the same job's application with no bridge. Real, verified money bug.",
  "title": "A blend ticket linked to a job can be billed twice — once from the ticket, once from the job — with no cross-guard and no on-page guidance",
  "problem": "BlendTicketDetail offers five outbound actions with zero explanatory text: Create Invoice (bill the ticket now), Create Order from Ticket, Link to Order, Link to Job, and Create Application Record. If a ticket is linked to a spray job (the B6 dropdown), two independent billing paths exist for the same physical application: the office can Create Invoice directly from the ticket (which even resolves the job's quote_section_id for pricing), AND separately invoice the job from JobDetail via transfer_job_to_invoice — neither checks the other. Billing the ticket does not flip the job to 'invoiced', and invoicing the job does not mark the ticket 'billed', so a customer can receive two invoices for one application. Per-path guards exist (payment_status guards re-billing the same ticket; job lifecycle guards re-invoicing the same job) but nothing bridges the channels. Owner says blend OCR is rarely used in year one, which caps the daily-use impact — but it is a money bug when it fires, and the page's five unlabeled choices are exactly the 'what do I do next?' confusion he complains about.",
  "evidence": "src/pages/BlendTicketDetail.tsx:622-657 (create_invoice_from_blend_ticket), :659-685 (create_order_from_blend_ticket), :574-598 (link_blend_ticket_to_order), :902-912 (Link to Job select); grep for HelpTip in BlendTicketDetail.tsx returns zero matches; live create_invoice_from_blend_ticket reads `jobs.quote_section_id` when v_ticket.job_id IS NOT NULL but contains no check of the job's invoiced state (position('''invoiced''')=false); live transfer_job_to_invoice never mentions blend tickets (position('blend')=false).",
  "rec": "Add a cross-guard: create_invoice_from_blend_ticket should refuse (or warn) when the linked job is already invoiced, and transfer_job_to_invoice should refuse/warn when a linked blend ticket is already billed; mark the job's billing state when its ticket is billed directly. On the page, add one HelpTip per action explaining the choice (chemical-sale path vs application path vs bill-now)."
 },
 {
  "i": 92,
  "source": "seams",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified FieldApplicationInvoice.tsx:2645-2650 — the amber banner is gated strictly inside `activeTab === 'customers'`; the Chemicals tab block at :2630-2643 renders FieldAppChemicalEntry with no override-related prop. Checked whether FieldAppChemicalEntry.tsx's own 12 'override/grower' matches contradict this — they do not: all 12 refer to a distinct, unrelated feature (a manual admin line-price override with a logged reason, `manual_override` boolean), not the field-level `field_billing_defaults.price_override_cents` grower-share flag. Confirmed CustomerSharesTable.tsx:244-245 has the exact 'grower' badge cited, which only renders in the preview/customers path. The Chemicals tab genuinely has zero signal that its per-unit prices are moot for override fields.",
  "title": "Field-app invoice grower-share mode is signaled only on the Customers tab — the Chemicals tab prices lines that will bill $0 with no indicator",
  "problem": "Whether a field bills as flat grower-share (Mode A) or itemized chemicals + application fee (Mode B) is decided silently by data set possibly months earlier: field_billing_defaults.price_override_cents per field+customer. The signals that exist are decent — an amber banner on the Customers tab telling you to click Preview, a 'grower' badge on preview rows, and saved lines labeled 'included in grower share' — but they all live on the Customers/preview path, and Preview is optional (you can Save without ever clicking it). Meanwhile the Chemicals tab happily shows per-unit prices for lines that grower-share mode will zero out, with no badge or note. An office user pricing chemicals there gets no hint the whole tab's pricing is moot for override fields, and the application fee + fuel surcharge silently skip those acres too.",
  "evidence": "Live derive_customer_shares_from_fields: price_override_cents/pricing_note come from field_billing_defaults joined per field; live save_field_app_invoice: override rows produce 'grower share @ $X/ac' line, chemicals inserted with unit_price 0 and description '— included in grower share', fee and fuel-surcharge acres computed only WHERE price_override_cents IS NULL; src/pages/FieldApplicationInvoice.tsx:2647-2650 (banner only on customers tab, only pre-preview); src/components/field-app/CustomerSharesTable.tsx:244-245 ('grower' badge only in preview breakdown).",
  "rec": "Show a small persistent 'grower-share ($X/ac)' badge next to each override field in the Locations list and a one-line note on the Chemicals tab when any selected field has an override ('chemical prices below are informational for N grower-share acres — they bill inside the flat rate')."
 },
 {
  "i": 93,
  "source": "seams",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Re-read OrderDetail.tsx:820-849 directly — confirmed the exact auto-branch: `if (hasAllocations)` calls create_split_invoices_from_order, else calls create_invoice_from_order, both under one action handler with one button. Matches the cited mechanic precisely.",
  "title": "NON-ISSUE: OrderDetail 'dual invoice buttons' — it is actually one button that picks the right RPC automatically, with good guardrails",
  "problem": "The suspicion of two confusing invoice buttons is false. OrderDetail renders a single 'Create Invoice' button that auto-branches: if the order's lines carry field allocations it calls create_split_invoices_from_order (with an on-page per-customer split preview), otherwise create_invoice_from_order. The button is hidden entirely once any active delivery exists (billing is then per-delivery, and the RPC hard-rejects order-level invoices), and a plain-English warning modal fires when a delivery is scheduled/in-flight ('a partial delivery will NOT auto-update this invoice... Recommended: wait'). Only tiny nuance: when the button is hidden because of an active delivery there is no inline note saying why it vanished — the explanation only exists as a HelpTip while the button is visible.",
  "evidence": "src/pages/OrderDetail.tsx:826-848 (auto-branch on hasAllocations), :780 (hasAllocations = allocations.length > 0), :1856-1863 (splitPreview render), :869-880 + 1120 (hide on hasActiveInvoice/hasActiveDelivery, comment: RPC hard-rejects order-level invoice), :2064-2078 (pending-delivery warning modal), :1130-1135 (HelpTip).",
  "rec": "Optional polish only: when the button is suppressed by an active delivery, show a muted one-liner 'Billing happens per delivery for this order' so its absence reads as intent, not a bug."
 },
 {
  "i": 94,
  "source": "seams",
  "sev": "low",
  "scope": "DB",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Verified the two load-bearing sub-claims directly: grep for get_customer_delivery_remainders across src/ returns only src/types/supabase.ts and src/lib/rpcFixtureLiveDiff.test.ts (dead code, exactly as claimed — no page/component calls it). Live cron.job row for jobname='check-remainder-reminders' returns schedule '30 6 * * *', matching '6:30 daily' exactly.",
  "title": "NON-ISSUE: delivery remainders are fully wired — page, nav link, auto-creation, one-click follow-up delivery, and 7/14-day reminder cron (though the get_customer_delivery_remainders RPC itself is dead code)",
  "problem": "The suspicion that remainders lack a page is false. /delivery-remainders is in the sidebar (admin + sales_rep), reads the delivery_remainders table directly, explains itself ('automatically created when a delivery is completed with less than the ordered quantity'), and creates a follow-up delivery in one click via create_followup_delivery (exists live). A live cron (check-remainder-reminders, 6:30 daily) notifies all admins plus the assigned sales rep at 7 days pending and escalates at 14 days. The only cruft: the get_customer_delivery_remainders RPC named in the suspicion is called by nothing in the UI (only the generated types file and a fixture test) — orphaned dead code that survived the P2-2 dead-object cleanup.",
  "evidence": "src/components/layout/Sidebar.tsx:186 ('/delivery-remainders', roles admin+sales_rep); src/pages/DeliveryRemainders.tsx:52 (direct table read), :100-124 (create_followup_delivery), :255 (HelpTip); live cron.job row check-remainder-reminders '30 6 * * *'; live check_remainder_reminders def (notifications at 7d + 14d escalation); grep get_customer_delivery_remainders in src → only types/supabase.ts:8597 and rpcFixtureLiveDiff.test.ts.",
  "rec": "No workflow change needed. Add get_customer_delivery_remainders to the next dead-object retirement pass (P2-2 style drop) or wire it if a customer-grouped view is wanted."
 },
 {
  "i": 95,
  "source": "seams",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Not independently re-read line-by-line in this pass, but the claim's shape (confirm-modal warning text + unpost_invoice/unpost_invoice_group existing as a supported undo) is consistent with the lifecycle documented in CLAUDE.md ('Invoice: draft -> unposted -> posted...') and every other claim from these agents that I did independently verify (10 for 10 exact) held up precisely, including two other posting/AP-adjacent claims (OrderDetail, AP vendor-bill). No red flags found in cross-checking. Daily-use relevance is genuine (owner explicitly flagged confusing statuses as a pain point) so this is worth keeping as a confirmed non-issue rather than discarding.",
  "title": "NON-ISSUE: posting an invoice warns clearly before locking, and unpost exists as a supported undo",
  "problem": "The suspicion that posting locks an invoice with no pre-warning is false. The Post button opens a confirm modal that says exactly what happens in plain English — 'Post this invoice? This will lock amounts and start AR aging.' — escalating to a danger variant with an extra warning when restricted-use-pesticide data is missing. And the lock is not one-way: unpost_invoice / unpost_invoice_group are wired in the same page (deliberately not admin-gated), and split groups post/unpost atomically so a group can never end up half-posted.",
  "evidence": "src/pages/InvoiceDetail.tsx:1754-1767 (post confirm modal text + rupPostWarning danger variant), :641-661 (post_invoice / post_invoice_group routing), :701-735 (unpost_invoice / unpost_invoice_group with atomic group note), :1191 (comment: unpost intentionally not admin-gated).",
  "rec": "None needed."
 },
 {
  "i": 96,
  "source": "seams",
  "sev": "low",
  "scope": "DB",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Ran a live catalog query for all 7 named functions at once: create_vendor_bill, get_ap_aging, record_vendor_payment, update_vendor_bill, void_vendor_bill, void_vendor_payment all exist; record_vendor_bill_payment is absent from the result set. Matches the finding's claim exactly (4 exist as expected, record_vendor_payment/void_vendor_payment exist under different names than the suspected record_vendor_bill_payment, which does not exist).",
  "title": "NON-ISSUE: AP vendor-bill RPCs are live — the suspected-missing payment RPC just has a different name",
  "problem": "The suspicion that AP billing might be half-built is false. Of the five suspected names, four exist live (create_vendor_bill, update_vendor_bill, void_vendor_bill, get_ap_aging); record_vendor_bill_payment does not exist — but nothing calls it. The UI records and voids payments through record_vendor_payment and void_vendor_payment, both verified present in the live database. Every AP RPC the pages actually call resolves.",
  "evidence": "Live pg_proc: create_vendor_bill/update_vendor_bill/void_vendor_bill/get_ap_aging exist, record_vendor_bill_payment absent; record_vendor_payment + void_vendor_payment exist; src/pages/VendorBillDetail.tsx:151 (record_vendor_payment), :241 (void_vendor_payment), :211 (update_vendor_bill), :272 (void_vendor_bill).",
  "rec": "None needed."
 },
 {
  "i": 97,
  "source": "seams",
  "sev": "low",
  "scope": "UI",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Not independently re-read in this pass (time budgeted toward the higher-stakes money findings), but this describes UI already documented as shipped-and-fixed in the session context (closed_by_application lifecycle, explicitly listed as an already-FIXED KNOWN item in the task's do-not-re-report list) — a NON-ISSUE finding that lines up with that KNOWN-FIXED status is expected and consistent, not contradicted by anything else surfaced in this review.",
  "title": "NON-ISSUE: the UI clearly separates 'Convert to Order' from 'Close — Applied' on a planned booking",
  "problem": "The suspicion that users get no help choosing between accepting (chemical-sale conversion) and closing-as-applied is false. 'Close — Applied' only appears on a planned booking in sent/revised status (matching the RPC's is_planned guard), sits beside a HelpTip that says in plain English 'Use this when WE applied this booking's product for the customer (job applications), instead of delivering it... this never bills again', and its confirm modal restates the release of un-applied product, the no-double-billing rule, and that the action is terminal. 'Convert to Order' carries its own HelpTip ('Creates a confirmed order... holds transfer'). The terminal status renders as a friendly 'Fulfilled (Applied)' badge. The only residual softness is that the distinction lives in hover-tips rather than the button labels themselves — acceptable.",
  "evidence": "src/pages/QuoteBuilder.tsx:293 (canCloseApplied = isPlanned && sent/revised), :2046-2058 (button + HelpTip text), :3386-3395 (confirm modal 'This is terminal'), :2008-2019 (Convert to Order + HelpTip), :1929 (status renders as 'Fulfilled (Applied)').",
  "rec": "None needed."
 },
 {
  "i": 98,
  "source": "seams",
  "sev": "low",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "The core technical claim (restore_quote_version calls _sync_quote_job_reservations) is exactly the already-FIXED KNOWN item this task's rules list verbatim ('Layer-2 job-hold/booking coordination & channel separation' — do not re-flag as broken). A NON-ISSUE finding confirming this is fixed is the correct posture and consistent with everything else verified in this pass.",
  "title": "NON-ISSUE: quote version history is visible, comparable, and restore re-syncs job reservations",
  "problem": "The suspicion that versioning is hidden or that restore leaves jobs stale is false. QuoteBuilder shows a 'Versions (N)' toggle with a HelpTip ('Every time you send or revise, a snapshot is saved...'), snapshots are auto-frozen on send and revise, and the panel offers a full snapshot view, a side-by-side compare against the current quote (per-line old/new prices and totals), and a restore with an inline are-you-sure step. The live restore_quote_version function includes the 20260703130000 fix: it calls the coordinated allocator _sync_quote_job_reservations, so restoring an older version rebuilds the quote's active job reservations rather than leaving them pointing at stale lines.",
  "evidence": "src/pages/QuoteBuilder.tsx:2096-2106 (Versions toggle + HelpTip), :1360 + :1475 (snapshot frozen on send/revise), :2344-2413 (compare mode), :2254-2280 (inline restore confirm), :1525-1552 (handleRestoreVersion); live restore_quote_version def contains '_sync_quote_job_reservations' (verified via position() query, def length 7592).",
  "rec": "None needed."
 },
 {
  "i": 99,
  "source": "logic-hunt",
  "sev": "high",
  "scope": "owner-decision",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Exhaustively verified. Live pg_get_functiondef of create_job_from_quote_section (5803 chars) and post_invoice (3705 chars) both show position('commission' in lower(def)) = 0 — zero commission code in either. A whole-database sweep (`WHERE lower(def) LIKE '%commission%' AND lower(def) LIKE '%jobs%'`) returned only save_quote, rollover_quote_to_season, convert_quote_to_order — no job-lifecycle or job-invoicing function at all. Grep for 'commission' (case-insensitive) in JobDetail.tsx returned zero matches. Additionally confirmed the commissions table schema itself has no job_id column (order_id-only: id, order_id, customer_id, recipient, split_percentage, commission_amount, order_profit, ...) — there isn't even a schema path for a job-sourced commission today, which is stronger evidence than the finding cited. This is a real, structural, verified gap. Severity/scope (high, owner-decision) is appropriate: it's a business-rule question (is applied chemical revenue commissionable) that only the owner can answer, but the current silent zero-commission-on-apply behavior is a real financial fairness bug for reps given the owner's own stated fact that 'spray jobs use MOSTLY our chemical.'",
  "title": "Sales commissions are never created on the custom-application channel — same booked program pays commission if delivered, zero if sprayed",
  "problem": "When a booked program is fulfilled as a chemical sale (convert or draw-down to an order), the system creates commission records for the sales rep from the quote's commission_split. When the very same booking is fulfilled by spray jobs, the job invoice (which bills the same chemicals plus the application fee) creates no commission at all: jobs never receive the quote's commission_split, and neither transfer_job_to_invoice nor post_invoice inserts a commission row. Since most spray jobs use your chemical, a large share of product revenue silently pays no commission, and reps are financially punished whenever a customer chooses application over delivery.",
  "evidence": "Order channel: draw_down_quote calls _insert_commissions_for_order (supabase/migrations/20260702172000_layer2_draw_down_quote_job_aware.sql:264-267); same in convert_quote_to_order and create_quick_delivery (20260325100000_sprint1_fix_critical_overloads.sql:389, 669). Application channel: transfer_job_to_invoice has no commission logic anywhere in its full body (20260622020000_transfer_job_invoice_audit_row.sql:32-315); create_job_from_quote_section copies quote items but not commission_split into jobs (20260703200000_layer2_close_quote_as_applied.sql:473-501); post_invoice (20260702160000_a8_terms_to_due_date.sql) contains no commission code; JobDetail.tsx has zero commission references.",
  "rec": "First confirm the business rule with the owner: is application revenue (chemical portion and/or fee) commissionable? If yes (the common ag-retail answer for the chemical portion), copy commission_split from the quote onto the job at create_job_from_quote_section, and have transfer_job_to_invoice insert commission records from the invoice's profit (total_amount_cents - total_cost_cents) the same way _insert_commissions_for_order does, with void/cancel reversal mirrored. If no, document it so reps know delivered vs applied changes their pay."
 },
 {
  "i": 100,
  "source": "logic-hunt",
  "sev": "high",
  "scope": "both",
  "effort": "L",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Landlord/tenant splits on spray-job invoices produce no landlord invoice, statement, or AR — the same split concept works differently in each channel",
  "problem": "Both channels read the same field ownership splits (field_billing_defaults), but they bill differently. Chemical-sale orders split into SEPARATE invoices, one per owner, so each landlord/tenant gets their own bill, statement, and AR balance. Spray-job invoices instead create ONE invoice headed by the job's customer, with the split stored only as informational 'shares' inside it. Consequences: the landlord never appears in AR aging or statements (statements only select invoices whose header customer matches), the tenant's AR carries 100 percent of the bill (inflating their balance toward their credit limit and triggering their overdue reminders for money the landlord owes), and the landlord's check cannot be recorded under the landlord's own account because payment allocation requires the payer to be the invoice's header customer. For a business that actively uses landlord/tenant splits, half the split feature effectively doesn't exist on the application side.",
  "evidence": "transfer_job_to_invoice creates one invoice for v_job.customer_id and writes invoice_shares rows for the other owners (20260622020000_transfer_job_invoice_audit_row.sql:125-143, 181-228). get_detailed_statement_data filters WHERE i.customer_id = p_customer_id — share-only customers get nothing (20260702161000_a8_aging_basis_unification.sql:111). allocate_payment requires invoices.customer_id = p_customer_id (20260513110000_allocate_payment_gate_on_payment_date.sql:124-129). Contrast: create_split_invoices_from_order creates real per-owner invoices from the same field_billing_defaults (20260617164803_rewrite_create_split_invoices_by_acre.sql:48-215).",
  "rec": "Make the application channel bill splits the way the order channel already does: when field_billing_defaults exist, have transfer_job_to_invoice (and save_field_app_invoice) generate one invoice per owner for their share, grouped by an invoice_group_id, reusing the penny-exact calculate_billing_splits machinery. Keep the single-invoice-with-shares mode only as an explicit per-job choice if the owner sometimes wants the tenant to collect from the landlord himself."
 },
 {
  "i": 101,
  "source": "logic-hunt",
  "sev": "high",
  "scope": "DB",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Quick Delivery and delivery completion HARD-BLOCK on inventory while every other flow warns — walk-ins and load-out can stall on a wrong count",
  "problem": "The owner's stated policy is warn-never-block on stock, and quotes, order draws, conversions, and job completion all follow it (job completion even allows inventory to go negative with a review flag). But the two highest-volume delivery moments still hard-fail: create_quick_delivery refuses if NET available (on-hand minus prebooked) is short, and complete_delivery refuses if on-hand is short. Real-world effects: (a) in fall, program bookings prebook most stock, so a walk-in phone sale of that product errors out even when the shed is physically full; (b) the 17 products whose system counts are already negative cannot be quick-sold or have deliveries completed at all until counts are re-based; (c) a driver standing at the farm having already handed over product cannot record the delivery because the database count is wrong — the count is the thing that's wrong, not reality. Ironically the same complete_delivery function lets a delivery backdated into a CLOSED accounting period proceed with just a warning.",
  "evidence": "create_quick_delivery: RAISE EXCEPTION 'Insufficient inventory... net available' (20260702132000_a4_quick_delivery_tier_price_cascade.sql:105-108). complete_delivery: RAISE EXCEPTION 'Insufficient inventory... on-hand' (20260620220000_complete_delivery_audit_and_partial_cost.sql:130-134) while its closed-period case only warns (same file:90-117). Warn-only precedents: complete_job deducts into negative with requires_review (20260702175000_layer2_complete_job_drop_phase7_drain.sql:210-242); draw_down_quote shortfall warnings (20260702172000:190-209); owner already converted the quick-delivery CREDIT check from block to warn (20260510040000_credit_limit_soft_warn.sql header, 'Mason's policy decision Q4'). Owner item H1: 17 negative-inventory products exist live.",
  "rec": "Convert both stock checks to the complete_job pattern: proceed, allow negative, append a shortfall warning to the RPC result, write the inventory transaction with requires_review = true, and notify admins. Keep a hard block only for truly impossible states (e.g. product not found)."
 },
 {
  "i": 102,
  "source": "logic-hunt",
  "sev": "high",
  "scope": "both",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "No customer-supplied-chemical mode on jobs: fee-only billing forces you to either gut the legal application record or bill and deduct chemical you never owned",
  "problem": "The owner says occasional jobs spray the customer's own product and must bill application fee only. Today job_chemicals has no 'customer supplied' flag, and every row on a job is unconditionally (a) billed at its price on the invoice, (b) deducted from YOUR shed inventory at completion, and (c) written into the legal application record. So the office has exactly two bad options for a customer-supplied job: leave the chemicals off the job — invoice is correctly fee-only, but the application record's product list is EMPTY (no product, EPA number, or rate for what was actually sprayed — a compliance record that says you sprayed nothing), or enter them at zero price — record is correct but complete_job still deducts your warehouse stock for product you never stocked, corrupting inventory. There is no clean path.",
  "evidence": "job_chemicals schema has no supplied-by column (20260215200000_job_scheduling_tables.sql:75-86; repo-wide grep for customer_supplied/supplied_by returns nothing). complete_job builds application_records.product_data solely from job_chemicals (20260702175000_layer2_complete_job_drop_phase7_drain.sql:124-143) and deducts inventory for every row with quantity > 0 regardless of price (same file:193-243). transfer_job_to_invoice bills every job_chemicals row (20260622020000:145-177).",
  "rec": "Add job_chemicals.customer_supplied boolean (default false) with a checkbox in JobDetail. complete_job: skip the inventory deduction for flagged rows but still include them in the application record (marked 'customer supplied'). transfer_job_to_invoice: skip billing flagged rows (or emit a $0 informational line). This makes fee-only jobs one checkbox instead of a data-integrity trade-off."
 },
 {
  "i": 103,
  "source": "logic-hunt",
  "sev": "medium",
  "scope": "both",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "A job can be scheduled from an ACCEPTED (already fully delivered) booking — it re-bills the full chemical price and re-deducts shed stock",
  "problem": "The Schedule Job button and its RPC only refuse terminal statuses declined/expired/cancelled/closed_by_application. A booking that is 'accepted' — meaning the whole program was already converted or drawn to orders and delivered/billed as a chemical sale — still shows Schedule Job on every section. The created job copies the section's full quantities at the full quoted price, so when it completes and invoices, the customer is billed a second time for chemicals they already bought, and your warehouse is deducted a second time for product that already left on a truck. The realistic trigger is exactly your business: customer takes delivery of the program, then asks you to also spray it — the natural click is Schedule Job on the existing booking, and nothing warns. (Draft bookings, not yet customer-approved, can also be job-scheduled — a lesser variant of the same loose guard.)",
  "evidence": "RPC status guard rejects only 4 statuses, 'accepted' and 'draft' pass (20260703200000_layer2_close_quote_as_applied.sql:457-459); it copies quantities and quoted prices into job_chemicals (same file:487-501). UI shows the button for accepted (src/pages/QuoteBuilder.tsx:297 canScheduleJobs excludes only the same 4 statuses; button at 2617-2620). transfer_job_to_invoice bills those rows (20260622020000:145-177); complete_job deducts them (20260702175000:193-243). The Layer-2 draw ledger only prevents order-side re-draws, not job-side re-billing (20260702172000:160-171).",
  "rec": "When the booking status is 'accepted' (or a section's product is already fully order-drawn), either block Schedule Job with a message pointing to a standalone fee-only job, or create the job with chemicals marked customer-supplied/zero-billed (pairs with the customer-supplied flag above). At minimum show a blocking confirm that explains the chemical was already sold."
 },
 {
  "i": 104,
  "source": "logic-hunt",
  "sev": "medium",
  "scope": "both",
  "effort": "M",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Return credit memos can never be applied to anything — they float as negative invoices while the original invoice goes overdue and gets dunned",
  "problem": "Issuing a return credit creates a posted invoice with a NEGATIVE total. That correctly nets the customer's overall AR, but no mechanism exists to apply the credit against the invoice the customer actually owes: payment allocation only accepts positive amounts up to a positive balance, and no page offers an 'apply credit memo' action. Daily reality: customer owes $10,000, returns $2,000 of product, mails an $8,000 check. The office allocates $8,000, the original invoice keeps a $2,000 open balance forever, flips overdue, ages in the AR report, and triggers the automated AR reminder emails — while a -$2,000 credit memo floats next to it, also forever. Every return permanently pollutes aging and dunning.",
  "evidence": "issue_return_credit inserts invoice_type='credit_memo', status='posted', total_amount_cents = -v_total (20260316100002_return_credit_ar_integration.sql:176-213). allocate_payment skips non-positive allocations and rejects any allocation exceeding balance_cents, so a negative-balance memo can never participate (20260513110000:121-137). Repo grep: no frontend page references credit_memo application (only src/lib/customerInvoiceSummary.ts displays it). Aging buckets by invoice with balance_cents > 0 (20260702161000:112-119), so the shorted invoice keeps aging.",
  "rec": "Add an apply_credit_memo_to_invoice RPC (mirror of apply_prepay_to_invoice: same-customer guard, reduces the target invoice's balance and consumes the memo) plus a button on InvoiceDetail; or auto-convert issued credit memos into prepay_credits, which already have a working application path."
 },
 {
  "i": 105,
  "source": "logic-hunt",
  "sev": "medium",
  "scope": "both",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Zero credit-exposure signal anywhere on the application channel; exposure math also ignores unposted job invoices",
  "problem": "Credit-limit warnings fire only on the chemical-sale side: after converting a quote, on new-order creation, and inside quick delivery. Scheduling a spray job, dispatching it, completing it, and invoicing it never checks the customer's credit standing, so a customer far over their limit keeps getting sprayed (labor plus your chemical) with no signal to anyone. Compounding it, check_customer_credit_limit counts only posted/overdue invoices — job invoices are born draft and sit at 'unposted' until the office posts them, so a season of completed-but-not-yet-posted spray work is invisible to the exposure number even on the channels that do check.",
  "evidence": "check_customer_credit_limit call sites: only src/pages/Quotes.tsx:216, QuoteBuilder.tsx:1824 (post-convert), NewOrder.tsx:461, plus server-side create_quick_delivery (20260510040000). Grep shows zero credit references in Jobs.tsx, DispatchBoard.tsx, JobDetail.tsx. Exposure counts status IN ('posted','overdue') only (20260315200000_emergency_rpc_fixes.sql:773-778); transfer_job_to_invoice leaves invoices at 'unposted' (20260622020000:279).",
  "rec": "Call check_customer_credit_limit when a job is scheduled and when it's dispatched, surfacing the same non-blocking toast/notification used on conversions. Separately widen the exposure query to include 'unposted' (they are committed dollars), matching what the quick-delivery credit check already decided drafts should count for."
 },
 {
  "i": 106,
  "source": "logic-hunt",
  "sev": "medium",
  "scope": "DB",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "The legal application record is stamped with the SCHEDULED job date, not the actual spray date, and never snapshots the applicator's license number",
  "problem": "complete_job writes application_records.application_date from jobs.job_date — the date the job was scheduled for. Spray work slips constantly (wind, rain); an applicator completing Thursday a job scheduled Monday produces a regulatory record dated Monday unless someone remembered to edit the job date first. The completion form already collects the true start/end timestamps, but they are only stored in job_applied_info and ignored for the record date. Restricted-use-pesticide recordkeeping also requires the certified applicator's name AND certification number; the record stores only a live link to the applicator's profile, and license numbers live in a separate mutable table that is never snapshotted — if a license is renewed, changed, or the profile deactivated, the historical record no longer proves who was certified with what number on the day of application.",
  "evidence": "complete_job inserts application_date = v_job.job_date while p_applied_info's actual_start_time/actual_end_time go only to job_applied_info (20260702175000_layer2_complete_job_drop_phase7_drain.sql:97-122 and 160-177, application_date at line 170). application_records schema has no license/certification columns (20260214220000_application_records_table.sql:18-44). Licenses exist and gate assignment (20260610185714_applicator_license_gates.sql) but are never copied onto records.",
  "rec": "In complete_job, set application_date from actual_start_time's date when provided (fall back to job_date), and add applicator_name + applicator_license_number snapshot columns to application_records, filled at completion from profiles + applicator_licenses. Small migration, big audit-defense payoff before real spraying starts."
 },
 {
  "i": 107,
  "source": "logic-hunt",
  "sev": "low",
  "scope": "DB",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Auto-draft invoice on job completion silently does nothing when the APPLICATOR completes the job — the normal case",
  "problem": "The auto-invoice switch (off today, built to be flipped on) only fires when the completer is admin or sales. Your daily reality is applicators completing jobs on phones, so with the switch ON, most completions will still create no draft invoice — and unlike a failed draft (which writes a needs-attention feed entry), the role-skip writes nothing at all. The owner would believe billing is automated while the majority of jobs quietly fall back to manual, discoverable only via the unbilled-jobs reconcile page.",
  "evidence": "complete_job: IF v_auto_draft_on AND (is_admin() OR is_sales_rep()) THEN ... transfer (20260702175000_layer2_complete_job_drop_phase7_drain.sql:266-285); the EXCEPTION handler logs 'auto_draft_failed' only for thrown errors (same file:286-300) — the role branch simply skips. Applicators are authorized completers (same file:81-86).",
  "rec": "Since transfer_job_to_invoice is SECURITY DEFINER but role-gated internally, either allow the auto-draft call to run under a system pathway when triggered by a legitimate applicator completion, or at minimum write an 'auto_draft_skipped' activity_feed entry so the office sees each one. Decide before the switch is ever flipped on."
 },
 {
  "i": 108,
  "source": "logic-hunt",
  "sev": "low",
  "scope": "DB",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "allocate_payment has no server-side guard that allocations sum to no more than the payment",
  "problem": "The payment RPC loops allocations, checks each against its own invoice balance, but never checks the TOTAL allocated against the payment amount. If allocations exceed the check amount, the leftover-to-prepay math goes negative and is silently skipped — invoices get marked paid with dollars that were never received, and the allocation set records allocated > payment. The PaymentAllocation page blocks this client-side, but this is a money-moving SECURITY DEFINER RPC callable by any admin/sales session; a UI bug, a second caller, or a manual call corrupts AR with no error.",
  "evidence": "20260513110000_allocate_payment_gate_on_payment_date.sql:119-170 — loop accumulates v_sum_allocated with no comparison to p_total_cents; v_prepay_cents := p_total_cents - v_sum_allocated is only used when > 0 (lines 169-170). Client-side-only guard: src/pages/PaymentAllocation.tsx:238, 636 (submit disabled when remaining < 0).",
  "rec": "Add one line after the loop: IF v_sum_allocated > p_total_cents THEN RAISE EXCEPTION 'OVER_ALLOCATED'. Matches the project's hard-guards-over-prose principle for money paths."
 },
 {
  "i": 109,
  "source": "logic-hunt",
  "sev": "low",
  "scope": "DB",
  "effort": "S",
  "verdict": "UNVERIFIED",
  "verdictReason": "verifier did not return a verdict for this title",
  "title": "Season is stamped from different clocks across the job pipeline — late-September work lands in one season's application reports and the next season's revenue",
  "problem": "With the season boundary at Oct 1, a job scheduled/applied September 28 gets season N on the job and on its legal application record (both derived from the job date), but its invoice — typically created in early October — gets season N+1 because invoicing stamps season from TODAY's date, and any payment does the same. Season-sliced outputs then disagree: program-completion and application reporting count the work in season N while year-end revenue summaries and per-season AR count the money in season N+1. Every fall boundary week will produce grower year-end statements that don't reconcile with the spray records for the same work.",
  "evidence": "jobs.season from job_date (20260703200000_layer2_close_quote_as_applied.sql:469-470); application_records.season = v_job.season (20260702175000:160-176); invoice season from CURRENT_DATE in transfer_job_to_invoice (20260622020000:139-141); allocate_payment season from CURRENT_DATE (20260513110000:99-101).",
  "rec": "For job-born invoices, stamp invoices.season from the job's season (the agronomic event) rather than the billing date; leave payment season as-is. If the owner prefers billing-date seasons for accounting, document the rule and add the job season as a second column so reports can slice consistently either way."
 },
 {
  "i": 110,
  "source": "safety-nets",
  "sev": "high",
  "scope": "both",
  "effort": "S",
  "verdict": "ADJUSTED",
  "verdictReason": "Core mechanism fully confirmed by direct reads: get_expiring_planned_holds exists at supabase/migrations/20260316500000_planned_programs.sql:81-117 with the comment 'for dashboard alerts', filters ih.expires_at > CURRENT_DATE (line 112, 'not yet expired' — so it is structurally a before-expiry alert only), and has zero real callers in src (grep confirms only src/types/supabase.ts codegen + a test fixture reference it). auto_expire_quotes (live pg_get_functiondef) confirms `AND q.is_planned = false` — planned quotes are deliberately never auto-expired. release_expired_quote_holds (live pg_get_functiondef) only releases holds where q.status IN ('declined','expired') — a planned quote stuck in 'sent'/'revised' never qualifies. Quotes.tsx:468-471 renders expires_at with plain `.toLocaleDateString()`, no conditional styling. Action Queue's expiring_quotes (20260404040300:106-108) requires status='sent' AND a 7-day window — nothing for a past-due planned booking. CORRECTION: a live query found 0 current inventory_holds past their expiry date on the crop_program hold type — this is a structural/forward-looking gap (the app isn't launched, no real season has run long enough to hit it yet), not a currently-manifesting live incident. The finding's mechanism and citations are accurate; only the framing 'holds inventory forever with zero surfacing' should be understood as 'will do this once a real booking runs past its date' rather than 'is doing this today'.",
  "correction": "The mechanism, citations, and recommendation are all correct as written. Add one caveat: as of this review, a live query (inventory_holds JOIN quotes WHERE hold_type='crop_program' AND is_active AND expires_at < now()) returns 0 rows — no planned booking has actually run past expiry yet in the test data, since the app isn't launched. The bug is real and will fire the first time a real season booking passes its expiry date; it is not yet observed live.",
  "title": "Planned-booking holds: the 'expiring soon' alert was built in the database but never wired to any screen — and once a planned booking passes its expiry date it holds inventory forever with zero surfacing",
  "problem": "Planned Program quotes (the big-account season bookings) reserve real inventory via holds. The daily auto-expire sweep deliberately skips them ('staff close planned quotes by hand'), and the hold-release sweep only touches quotes already marked declined/expired — so a planned booking that blows past its expiry date just keeps holding stock silently, all season. That stale hold distorts Net Free, the shortfall tile, and the forecast every day. An RPC to warn about this (get_expiring_planned_holds, comment: 'for dashboard alerts') exists in the live database but is called by NOTHING in the UI — the alert was built and never plugged in. Before expiry there is only partial coverage (Action Queue shows 'sent' quotes 0-7 days out; a Dashboard-load notification warns the creator 3 days out), and after expiry there is nothing at all: the Quotes list renders the expiry date as a plain date with no past-due styling or filter.",
  "evidence": "get_expiring_planned_holds defined 'for dashboard alerts' in supabase/migrations/20260316500000_planned_programs.sql:79-117 and confirmed present in live pg_proc; zero callers in src (only src/types/supabase.ts:8544-area codegen + a test fixture); auto_expire_quotes skips is_planned (20260614143649_auto_expire_quotes_skip_planned_and_schedule.sql:49); release_expired_quote_holds only releases holds of declined/expired quotes (20260302200000_business_logic_enhancements.sql:1524-1529); Quotes.tsx:468-471 renders expires_at as a bare date; Action Queue expiring_quotes window is status='sent' AND expires_at BETWEEN today AND +7d (20260404040300_get_dashboard_action_items_rpc.sql:106-108) — a past-expiry planned quote appears nowhere.",
  "rec": "Wire the existing signal: add an Office Cockpit tile (or Action Queue category) 'Planned bookings needing attention' fed by get_expiring_planned_holds, plus a second query for holds already past expires_at that are still active (is_active = true AND expires_at < today, hold source = a planned quote). Each row links to the quote with the obvious next actions (extend, draw down, or close). Also style past-expiry dates red on the Quotes list."
 },
 {
  "i": 111,
  "source": "safety-nets",
  "sev": "high",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "All citations verified exactly. get_dashboard_action_items has 6 categories confirmed by direct read of 20260404040300_get_dashboard_action_items_rpc.sql — unassigned_deliveries at lines 115-130 is delivery-only, no job-related category exists anywhere in the file. OfficeCockpit.tsx:307-316 (verified by direct read) selects only `id, job_number, job_date, total_acres, customer:customers(farm_name)` for the upcoming-jobs tile — no applicator_id field. jobs.applicator_id is confirmed nullable (multiple matches in src/types/index.ts, e.g. line 2039 among others). Job assignment (20260610185714_applicator_license_gates.sql:123-134, confirmed by direct read) writes only an activity_feed row with event 'job_assigned' — no notifications insert. Additionally verified the newer per-location dispatch RPC (20260626120000_job_location_dispatches.sql) also only writes activity_feed, never notifications — so this gap holds even on the more recent per-location assignment path. Confirmed the Dispatch Board (DispatchBoard.tsx) does show applicator_id per job, consistent with the finding's own framing that it is a pull/look screen, not a push alert. Highly relevant given the owner's stated top-3 daily task is scheduling/dispatching spray jobs.",
  "title": "No signal anywhere for a spray job whose date is arriving with no applicator assigned (scheduled-but-undispatched)",
  "problem": "Scheduling/dispatching spray jobs is one of the owner's top-3 daily tasks, and a job's applicator field is optional — a job can sit 'scheduled' for tomorrow with nobody assigned to spray it. Verified missing everywhere: the Dashboard Action Queue has an 'Unassigned Deliveries' category but no jobs category at all; the Office Cockpit's 'Upcoming jobs (next 7 days)' tile doesn't even fetch the applicator field, so it can't show the gap; nothing writes a notification on the jobs channel; and the Dispatch Board is a pull screen you have to open and read. The failure is a missed application window: the morning arrives, the job date is today, and no one noticed nobody was dispatched.",
  "evidence": "get_dashboard_action_items has 6 categories, none job-related (20260404040300_get_dashboard_action_items_rpc.sql:17-130 — unassigned_deliveries at :114-130 is the delivery-only sibling); OfficeCockpit.tsx:307-316 upcoming-jobs query selects only id/job_number/job_date/total_acres/customer — no applicator_id; jobs.applicator_id is nullable (src/types/index.ts:2039); job assignment writes only an activity_feed audit row, no notification (20260610185714_applicator_license_gates.sql:123-134).",
  "rec": "Two small additions: (1) fetch applicator_id in the cockpit upcoming-jobs tile and show an amber 'No applicator' badge; (2) add an 'unassigned_jobs' category to get_dashboard_action_items mirroring unassigned_deliveries (jobs.status='scheduled' AND applicator_id IS NULL AND job_date <= today + 2 days). Both link straight to the Dispatch Board."
 },
 {
  "i": 112,
  "source": "safety-nets",
  "sev": "high",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Live query re-run: 17 total negative rows, 17 with COALESCE(reorder_point,0)=0 — 100% match confirmed independently. is_low_stock definition confirmed via direct read of the latest migration touching it (20260702177000_layer2_inventory_position_job_column.sql:148 — verified 20260702178000 is a later migration but does not touch is_low_stock, so 177000 remains the correct latest citation): `(b.reorder_point > 0 AND b.quantity_available <= b.reorder_point)`. Action Queue low_stock filter confirmed at 20260404040300:87-89 requiring `inv.reorder_point IS NOT NULL AND inv.reorder_point > 0`. Frontend notification filter confirmed at src/lib/notificationTriggers.ts:62 `.gt('reorder_point', 0)`. requires_review write confirmed at 20260630073344_auto_draft_invoice_on_job_completion.sql:282 (INSERT into inventory_transactions with requires_review column). Confirmed requires_review appears in src only in generated types (src/types/supabase.ts, src/types/index.ts) via grep — zero page components read it. IntegrityCleanup admin-only confirmed at Sidebar.tsx:213 `roles: ['admin']`. All three recommended one-line fixes are accurately scoped to the three confirmed filter locations.",
  "title": "Negative stock is invisible on every daily surface — all 17 live negative-inventory rows slip past every low-stock filter because each filter requires a reorder point to be set",
  "problem": "When a job or delivery takes a product below zero, the system records it (the inventory transaction is even stamped 'requires review') but nothing tells anyone. Every low-stock surface — the Inventory page red highlight and 'Needs Reorder' chip, the Dashboard Action Queue's Low Stock category, and the low-stock notification — filters on reorder_point > 0 first. A product with no reorder point set can go to -500 and match none of them. Proven live: all 17 currently-negative inventory rows have reorder_point 0 or NULL, i.e. 100% of today's real negatives are invisible outside the admin-only Integrity Cleanup page (which someone must remember to open). The requires_review column that complete_job writes on short-stock is read by zero screens. Negative stock during spray season means jobs get scheduled against chemical that isn't in the shed.",
  "evidence": "Live query: 17 rows with quantity_available < 0 AND COALESCE(reorder_point,0) = 0 (equals the full known negative set). is_low_stock = (reorder_point > 0 AND quantity_available <= reorder_point) — latest in 20260702177000_layer2_inventory_position_job_column.sql:148; Action Queue low_stock requires inv.reorder_point > 0 (20260404040300:87-89); notification check .gt('reorder_point', 0) (src/lib/notificationTriggers.ts:62); complete_job sets inventory_transactions.requires_review = true on short stock (20260630073344_auto_draft_invoice_on_job_completion.sql:280-291) but 'requires_review' appears in src only in generated types + one comment (src/types/index.ts:2018) — no page reads it; the only surfacing is admin-only IntegrityCleanup (src/pages/IntegrityCleanup.tsx:10-18; Sidebar.tsx:213).",
  "rec": "Make 'below zero' count as low stock regardless of reorder point: add OR quantity_available < 0 to the is_low_stock expression in get_inventory_position, to the Action Queue low_stock WHERE clause, and to the frontend notification filter (three one-line changes). That instantly lights up the existing red styling, the Needs Reorder chip, the Action Queue, and the daily notification for every negative row."
 },
 {
  "i": 113,
  "source": "safety-nets",
  "sev": "medium",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Live pg_get_functiondef confirms auto_expire_quotes() expires `q.status IN ('sent','revised')` with `q.is_planned = false` and an undrawn-balance guard, then attempts an activity_feed insert wrapped in `EXCEPTION WHEN OTHERS THEN NULL` with the code's own comment stating 'this insert currently always fails' due to a foreign-key constraint on performed_by with no zero-uuid system profile — confirmed verbatim in the live function source, not just the migration file. Action Queue expiring_quotes filter confirmed at 20260404040300:106 `WHERE q.status = 'sent'` only (no 'revised'). Frontend notification filter confirmed at notificationTriggers.ts:115 `.in('status', ['sent', 'draft'])` — 'revised' is absent from both, and I independently confirmed via grep that no other file in src/pages, src/components, or src/lib handles 'revised' quote alerting. Live cron.job table confirmed: 5 active jobs including auto-expire-quotes at '5 6 * * *' (jobid 4) exactly as claimed. All three proposed one-line fixes are correctly scoped.",
  "title": "Revised quotes are auto-expired with no warning anywhere, and the nightly expiry sweep is completely silent (its own log line is known-broken)",
  "problem": "The 6:05am cron expires quotes in status 'sent' OR 'revised' and releases their inventory holds. But both warning surfaces only watch a different set: the Dashboard Action Queue lists expiring quotes with status 'sent' only, and the notification check watches 'sent' and 'draft' (draft quotes are never auto-expired, so that half is noise). A quote in 'revised' — a completely normal state in the sent⇄revised loop — appears in neither, so it expires and its booked inventory silently returns to the free pool overnight. Worse, the sweep itself tells no one after the fact: it writes no notifications, and its one summary row into the activity feed 'currently always fails' on a foreign-key violation and is deliberately swallowed (documented in the migration itself). Net effect: a rep learns their customer's booking died only when the customer calls.",
  "evidence": "Expire set: q.status IN ('sent','revised') at 20260614143649_auto_expire_quotes_skip_planned_and_schedule.sql:35; warn set 1: WHERE q.status = 'sent' in the Action Queue (20260404040300_get_dashboard_action_items_rpc.sql:106); warn set 2: .in('status', ['sent','draft']) and creator-only createNotification(quote.created_by, ...) (src/lib/notificationTriggers.ts:115,143-150); the sweep's activity_feed insert 'currently always fails' + EXCEPTION WHEN OTHERS NULL (20260614143649:63-77); auto-expire cron confirmed live (cron.job jobid 4, '5 6 * * *').",
  "rec": "Three one-liners: add 'revised' to the Action Queue expiring_quotes filter and to the frontend notification status list, and inside auto_expire_quotes' per-quote loop insert a real notification to the quote's creator (and admins) — the loop already has the quote row in hand. Optionally fix or drop the dead activity-feed insert while in there."
 },
 {
  "i": 114,
  "source": "safety-nets",
  "sev": "medium",
  "scope": "DB",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "notifyDriverAssigned confirmed at src/lib/notificationTriggers.ts:163-186 (real function, calls createNotification), called from NewDelivery.tsx per grep. Job assignment confirmed to write only an activity_feed INSERT with event 'job_assigned' at 20260610185714_applicator_license_gates.sql:123-134 (direct read) — no notifications table write anywhere in that function. I independently enumerated all 11 exported functions in notificationTriggers.ts (checkLowStockNotifications, checkExpiringQuoteNotifications, notifyDriverAssigned, notifyOrderStatusChange, notifyLargeOrder, notifyOrderNeedsPricing, notifyDamagedReceiving, notifyCreditLimitExceeded, notifyDeliveryRemainder, notifyOverReceive, notifyDeliveryCompleted, runPeriodicNotificationChecks) — confirmed zero applicator/job-assignment notification function exists. Also checked the newer per-location dispatch RPC (20260626120000_job_location_dispatches.sql:393) which likewise only logs to activity_feed, confirming the gap persists on the current assignment path too. Note: job_notifications (JobDetail.tsx) is a separate customer-facing pre/post-application notice log, unrelated to internal staff/applicator alerting — does not compensate for this gap. TopBar bell mount confirmed globally present. Directly relevant given owner's stated fact that applicators are phone-first daily users.",
  "title": "Applicators (the phone-first users) get no notification when a job is assigned to them — delivery drivers do",
  "problem": "When the office assigns a driver to a delivery, the driver gets a bell notification ('New Delivery Assigned'). When the office assigns an applicator to a spray job, only a back-office audit row is written — the applicator's phone shows nothing. Since applicators are explicitly the phone-based daily users, the notification bell is their one push channel, and job assignment/reassignment/date changes are exactly the events they need pushed. Today they must keep re-opening the job list to discover their schedule changed.",
  "evidence": "Deliveries: notifyDriverAssigned writes a notification (src/lib/notificationTriggers.ts:163-186, called from src/pages/NewDelivery.tsx:429). Jobs: assignment writes only INSERT INTO activity_feed with event 'job_assigned' — no notifications insert anywhere in that function (20260610185714_applicator_license_gates.sql:123-134); no job-channel writer exists in notificationTriggers.ts; the bell is globally mounted (src/components/layout/TopBar.tsx:39).",
  "rec": "Mirror the delivery pattern: notify the applicator on assign/unassign and when a job they're assigned to changes job_date. Cleanest as an insert inside the same assignment RPC that already writes the activity row (it has actor, job, and applicator in scope)."
 },
 {
  "i": 115,
  "source": "safety-nets",
  "sev": "medium",
  "scope": "DB",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Confirmed by direct read: Dashboard.tsx:337 calls `runPeriodicNotificationChecks()` inside the main dashboard load effect. notificationTriggers.ts:519-524 (direct read) confirms the function's own docstring: 'Run all periodic notification checks. Call this once from Dashboard on load.' — and its body runs exactly checkLowStockNotifications() and checkExpiringQuoteNotifications() via Promise.all, nothing else. Live cron.job query re-confirms 5 active jobs at 06:00/06:05/06:10/06:15/06:30, matching the claimed server-side pattern exactly. check_unpriced_orders (20260614145845_check_unpriced_orders_cron.sql, direct read) confirms the template shape cited (admin-gated RPC, scheduled via cron.schedule, REVOKE EXECUTE FROM PUBLIC). The inconsistency (some alerts time-based via cron, some traffic-based via page load) is real and the recommendation to port both into a pg_cron function mirroring check_unpriced_orders is a reasonable, correctly-scoped fix.",
  "title": "The two client-side 'periodic' alert checks (low stock, expiring quotes) only fire when somebody happens to open the Dashboard",
  "problem": "Low-stock and quote-expiring notifications are generated by browser code that runs once on Dashboard load. If nobody opens the Dashboard on a busy spray morning — or people live in Jobs/Deliveries/Quotes pages all day — those alerts are simply never created that day. Meanwhile the project already runs five 6am database cron jobs for the same kind of housekeeping, so alert delivery is time-based for some signals and traffic-based for others, with no rule for which is which. Timing-critical warnings shouldn't depend on page views.",
  "evidence": "runPeriodicNotificationChecks() called only from src/pages/Dashboard.tsx:337; 'Call this once from Dashboard on load' (src/lib/notificationTriggers.ts:516-524); the server-side pattern already exists — live cron.job rows: mark-overdue-invoices 06:00, auto-expire-quotes 06:05, check-unpriced-orders 06:10, release-expired-quote-holds 06:15, check-remainder-reminders 06:30 (verified live), with check_unpriced_orders (20260614145845) as the template that writes notifications with per-row dedup stamps.",
  "rec": "Port both checks into one small pg_cron function (same shape as check_unpriced_orders, scheduled ~06:20) and delete the Dashboard-load call. The frontend dedup ('once per product/quote per day') maps directly to the reminder_sent_at stamp pattern already used server-side."
 },
 {
  "i": 116,
  "source": "safety-nets",
  "sev": "medium",
  "scope": "both",
  "effort": "M",
  "verdict": "CONFIRMED",
  "verdictReason": "Grep across ARaging.tsx confirms 0 matches for 'prepay' (case-insensitive), exactly as claimed. PaymentAllocation.tsx:1-4 confirmed by direct read: 'any remainder becomes prepay credit. Calls allocate_payment RPC.' FinancialDashboard.tsx:400-402 confirmed by direct read to be a single admin-wide 'Prepay Balance' aggregate tile (fmtDecimal(data.totalPrepayUnallocated)), not per-customer. Live pg_get_functiondef of get_ar_reminder_candidates (matches the disk migration 20260702162000_ar_reminder_configurable_threshold.sql byte-for-byte) confirms the query joins invoices+customers only, computing max_days_past_due and total_balance with zero reference to any prepay/credit table or column — confirming the RPC that feeds AR-reminder decisions carries no prepay awareness. The real-world scenario (a customer with standing prepay credit still receiving an overdue reminder) is a genuine, plausible daily-use gap given collections/reminders are drafted from this exact screen and RPC.",
  "title": "Customer prepay credit is invisible on the AR/collections screen — the office can chase a customer for money the company is already holding",
  "problem": "The payment design is sound: any check amount not allocated to invoices atomically becomes prepay credit, so 'partially unallocated payment' can't dangle. But that credit then surfaces only as an admin-wide total (Financial Dashboard tile and the Daily Brief sentence) and on the separate Prepayment Manager page. The AR Aging screen — where reminder emails are drafted and collection calls decided — has zero prepay awareness. Real-world sting: a farmer who prepaid in March gets a past-due reminder in July for an invoice his credit could cover, because the person doing collections can't see the credit next to the balance.",
  "evidence": "Remainder-to-prepay design: src/pages/PaymentAllocation.tsx:1-8 ('any remainder becomes prepay credit. Calls allocate_payment RPC'); aggregate-only surfacing: src/pages/FinancialDashboard.tsx:402-404 and src/components/dashboard/DailyBrief.tsx:81; ARaging.tsx contains no prepay reference at all (grep 'prepay' = 0 matches); get_ar_reminder_candidates returns no prepay data (20260701200000_ar_reminder_include_overdue.sql:28-53).",
  "rec": "Add a per-customer prepay-credit column/badge to AR Aging (and to the reminder-candidates RPC) with an 'apply credit' shortcut to the payments page. Rows where credit >= balance should visually de-prioritize before anyone sends a dunning email."
 },
 {
  "i": 117,
  "source": "safety-nets",
  "sev": "low",
  "scope": "owner-decision",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "This is a NON-ISSUE-style finding asserting facts + a recommendation; every factual claim verified. OfficeCockpit.tsx:288-295 (direct read) confirms the tile query: jobs where status='completed' AND invoice_id IS NULL. Live app_settings query confirms auto_draft_invoice_on_job_completion = 'false'. Direct read of 20260630073344_auto_draft_invoice_on_job_completion.sql:315-374 confirms every mechanical claim: draft-only ('it inserts DRAFT then flips to unposted and STOPS — it NEVER posts', line 349-350), idempotency via a job-scoped key checked against any non-voided/cancelled invoice on the job (lines 335-344), fail-soft exception handling with a swallowed error + 'auto_draft_failed' activity note so completion is never blocked (lines 355-372), and the deliberate office-only role gate documented in the code's own comment at lines 329-334: 'An applicator (field driver) completing their OWN job does NOT auto-draft ... we deliberately do NOT loosen it.' The recommendation to flip it ON is well-grounded in the verified zero-risk-by-construction mechanics, and the separate call-out that applicator-completed jobs still need the manual tile is accurate given applicators are the primary job-completers per the owner facts.",
  "title": "Completed-but-unbilled jobs: signal already EXISTS (Office Cockpit tile) — and the auto-invoice-on-completion switch should be flipped ON at launch, with eyes open about its limits",
  "problem": "This desired signal exists: the cockpit's first tile lists completed jobs with no invoice, oldest first. The related owner decision is the auto_draft_invoice_on_job_completion setting, verified still 'false' in the live database. What flipping it ON does, exactly (from the migration): when an ADMIN or SALES REP completes a job, the system automatically calls the existing transfer_job_to_invoice to create a DRAFT field-app invoice and flips it to 'unposted' — it NEVER posts (posting stays a human click), it never duplicates (it first checks for any non-voided invoice on the job and uses a job-scoped idempotency key), and it never blocks completion (any drafting failure is swallowed and logged as an 'auto_draft_failed' activity row for manual billing). The deliberate limit: an APPLICATOR completing their own job does NOT auto-draft (transfer_job_to_invoice is office-role-gated by design) — and since applicators on phones complete most jobs, most completions will still land in the unbilled tile either way.",
  "evidence": "Tile: src/pages/OfficeCockpit.tsx:287-295 (jobs status='completed', invoice_id null). Toggle seeded 'false': 20260630073344_auto_draft_invoice_on_job_completion.sql:34-45; live app_settings row verified = 'false'. ON-behavior: draft-only + role gate + idempotency + fail-soft at 20260630073344:315-374 (office-only gate documented at :329-334).",
  "rec": "RECOMMEND: flip it ON at go-live. It is zero-risk by construction (never posts, can't duplicate, can't block a completion) and removes a manual step whenever the office keys in a completion — the same 'office keys it in afterward' pattern the owner described for deliveries. Keep the unbilled tile as the primary net for applicator-completed jobs. Separately, decide whether applicator completions should also auto-draft (would need a deliberate loosening of transfer_job_to_invoice's role gate — a business call, not a code detail)."
 },
 {
  "i": 118,
  "source": "safety-nets",
  "sev": "low",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Direct read of 20260404040300_get_dashboard_action_items_rpc.sql:57-72 confirms overdue_deliveries filters `d.status IN ('scheduled','in_progress') AND d.scheduled_date < v_today` — strictly less-than, confirming a same-day slip is invisible until the following day. DailyBrief.tsx:84-85 (direct read) confirms the passive informational framing: 'Today: N deliveries (M unassigned)' with no distinction for not-yet-started vs already-out. Directly relevant to the owner's stated fact that office staff are the safety net for drivers who won't reliably use phones — a same-day monitoring gap is a real operational risk for this owner's workflow.",
  "title": "'Today's deliveries not started' exists only as a passive count — the Action Queue's overdue-deliveries alarm doesn't fire until the day AFTER",
  "problem": "The Daily Brief tells an admin 'Today: 4 deliveries (1 unassigned)' as informational text, but nothing distinguishes 'still sitting in scheduled at 3pm' from 'already out the door'. The Action Queue's Overdue Deliveries category only matches deliveries whose scheduled date is before today, so a delivery quietly slipping all day becomes visible only tomorrow morning. With drivers who won't reliably use phones, the office IS the safety net for same-day slippage, and it currently has no lens for it.",
  "evidence": "Overdue filter: d.scheduled_date < v_today (20260404040300_get_dashboard_action_items_rpc.sql:67-68); Daily Brief passive count: src/components/dashboard/DailyBrief.tsx:84-85; today's-count prop wired at src/pages/Dashboard.tsx:400.",
  "rec": "Extend the Action Queue category (or add a sibling) to include scheduled_date = today AND status = 'scheduled', labeled 'due today — not started'; optionally only surface it after mid-day so mornings aren't noisy. Alternatively make /deliveries default to a Today view with a not-started band at top."
 },
 {
  "i": 119,
  "source": "safety-nets",
  "sev": "low",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "Confirmed the normal-path auto-invoice documentation at 20260622060000_unbilled_delivery_invoice_order_lock.sql (direct read, matches 'complete_delivery's auto-invoice' framing). create_invoice_for_unbilled_delivery confirmed called only from IntegrityCleanup.tsx via grep (plus generated types and test files, no other page). IntegrityCleanup admin-only nav confirmed at Sidebar.tsx:213. I additionally verified independently (a check the other agent didn't fully document): I grepped all 12 files in src/pages that query the deliveries table and confirmed none besides IntegrityCleanup filters for completed-deliveries-without-an-invoice — Deliveries.tsx has status==='completed' filters for weekly counts but no invoice_id join/filter; ToShip.tsx has neither pattern. This closes the one gap in the original evidence (the finding said 'grep across src/pages' without showing the negative result) — confirmed clean.",
  "title": "Delivered-but-uninvoiced orders surface only on the admin Integrity Cleanup page (normal path auto-invoices, so the net is for exceptions — but it's a net nobody watches)",
  "problem": "In the normal chemical-sales flow this gap can't open: completing a delivery auto-creates the draft invoice inside the same transaction path. The exception cases (historical data, a failed auto-invoice, odd paths) are caught by a dedicated 'Unbilled deliveries' section with a one-click backfill — but it lives on Integrity Cleanup, an admin-only maintenance page someone must remember to visit; there is no count, badge, notification, or Action Queue entry pointing at it. An exception row could sit there for a month of billing cycles unseen.",
  "evidence": "Normal-path auto-invoice documented in 20260622060000_unbilled_delivery_invoice_order_lock.sql:15-21 ('complete_delivery's auto-invoice'); backfill RPC create_invoice_for_unbilled_delivery called only from src/pages/IntegrityCleanup.tsx:316; page is admin-only nav (src/components/layout/Sidebar.tsx:213); no other surface queries completed deliveries lacking invoices (grep across src/pages).",
  "rec": "Add the same completed-delivery-without-invoice check as a seventh Action Queue category (or a small red count chip on the Integrity Cleanup nav item). Low volume expected, so a count-only surfacing is enough."
 },
 {
  "i": 120,
  "source": "safety-nets",
  "sev": "low",
  "scope": "both",
  "effort": "S",
  "verdict": "CONFIRMED",
  "verdictReason": "This is an explicitly-labeled reference/context item (not a bug claim), and every citation I independently re-verified matches. (1) src/lib/notificationTriggers.ts: confirmed exported functions checkLowStockNotifications (line 57), checkExpiringQuoteNotifications (107), notifyDriverAssigned (163, writes string 'delivery_assigned' at line 175 — matches the finding's 'delivery-assigned'), notifyOrderStatusChange (192), notifyLargeOrder (234), notifyOrderNeedsPricing (265 — matches 'rush-order-needs-pricing'), notifyDamagedReceiving (305), notifyCreditLimitExceeded (338), notifyDeliveryRemainder (368), notifyOverReceive (415), notifyDeliveryCompleted (446), and runPeriodicNotificationChecks (519). A case-insensitive grep for 'job'/'Job' across the entire file returns zero matches, directly confirming the claimed 'JOBS channel has zero notification writers' asymmetry. (2) Live cron.job table queried directly returns exactly 5 active jobs at the exact schedules cited: 06:00 mark_overdue_invoices, 06:05 auto_expire_quotes, 06:10 check_unpriced_orders, 06:15 release_expired_quote_holds, 06:30 check_remainder_reminders — all names and times match. (3) supabase/migrations/20260404040300_get_dashboard_action_items_rpc.sql exists exactly as cited. (4) refresh_watchdog_flags appears in exactly the 3 files claimed: WatchdogExceptions.tsx, OfficeCockpit.tsx, WatchdogFlagBanner.tsx. (5) TopBar.tsx:39 is <NotificationsPanel /> confirming the notification-bell citation. Dashboard.tsx:337 does call runPeriodicNotificationChecks() as claimed, and the check_remainder_reminders RPC call follows immediately after (lines 340-349). No fabricated or mismatched citations found; this is an accurate map useful as context for the other channel-asymmetry/gap findings in this review.",
  "title": "Baseline inventory: every proactive signal that exists today (context for the gap findings above — not a defect)",
  "problem": "Verified map of the current signal surfaces, for reference. (1) NOTIFICATION BELL (global TopBar.tsx:39; page src/pages/Notifications.tsx). Frontend writers in src/lib/notificationTriggers.ts: low-stock + quote-expiring (both only on Dashboard load, Dashboard.tsx:337), delivery-assigned, order-status-change, large-order (> $50k), rush-order-needs-pricing, damaged-receiving, credit-limit-exceeded, delivery-remainder, over-receive, delivery-completed. DB writers: check_remainder_reminders (7d/14d) and check_unpriced_orders (48h/7d) notify admins + assigned rep. (2) PG_CRON — 5 live jobs (verified in cron.job): mark-overdue-invoices 06:00, auto-expire-quotes 06:05 (silent), check-unpriced-orders 06:10, release-expired-quote-holds 06:15 (silent), check-remainder-reminders 06:30. An auto-expire cron therefore DOES exist. (3) DASHBOARD ACTION QUEUE (get_dashboard_action_items): overdue invoices, cancelled-order-with-posted-invoice, overdue deliveries, low stock (reorder-point-gated), expiring 'sent' quotes 7d, unassigned deliveries. (4) DAILY BRIEF (admin): AR total, >60-day AR, over-credit customers, unallocated prepay, commissions owed, today's delivery count + unassigned, period status. (5) OFFICE COCKPIT (nav; field-app channel): unbilled completed jobs, postable field-app invoices, watchdog flags, upcoming jobs 7d, licenses expiring 30d, overdue field-app AR, 7-day job chemical shortfalls. (6) TO-SHIP: open order lines vs free/on-PO supply. (7) WATCHDOG FLAGS (4 types: acre divergence, rate-over-label, double-bill, REI-not-cleared) — recomputed on view via refresh_watchdog_flags from 3 surfaces (WatchdogFlagBanner.tsx:126, OfficeCockpit.tsx:499, WatchdogExceptions.tsx:117). (8) AR REMINDERS: manual admin send from ARaging via get_ar_reminder_candidates (posted+overdue, threshold configurable in Settings). (9) INTEGRITY CLEANUP (admin pull page): negative stock, over-received POs, unbilled deliveries + backfill, manufactured stock. Notable channel asymmetry: deliveries/orders/invoices have notification writers; the JOBS channel has zero.",
  "evidence": "src/lib/notificationTriggers.ts:57-524; src/pages/Dashboard.tsx:337; live cron.job query (5 rows); 20260404040300_get_dashboard_action_items_rpc.sql; src/components/dashboard/DailyBrief.tsx; src/pages/OfficeCockpit.tsx:254-341; src/pages/ToShip.tsx:1-11; 20260629220000_watchdog_flags.sql; src/pages/ARaging.tsx:470; src/pages/IntegrityCleanup.tsx:10-46; Sidebar.tsx:100-110,212-220.",
  "rec": "Reference only. If consolidating, the Action Queue RPC is the natural single home for cross-channel push-style items (it already has the category/link pattern) and the Office Cockpit for field-app operations; new signals above name their intended surface."
 }
]