## 2026-09-04 - Match the consolidate stale-order guard in executable code only

- Changed the F1 guard test that pins `handleConsolidateDrafts`' stale-order check so its regex runs over executable code instead of raw source text. Comments and string literals inside the searched region previously counted as matches, so replacing the real guard with a comment that merely QUOTED it left the test green — the check could be satisfied by prose describing the very thing it was supposed to prove.
- Raised by CodeRabbit (round 3) on PR #584 and verified before fixing rather than assumed: with the guard replaced by a commented-out copy of itself, the previous test passed 14/14. After the fix that same mutation fails, and restoring the guard turns it green again.
- Added `stripCommentsAndStrings`, which blanks line comments, block comments and string/template literals, substituting spaces so byte offsets into the original text stay valid. The two region offsets are still located in the RAW source so the RPC name inside its string literal remains findable; a comment forging an earlier call site only shrinks the searched region, which fails closed.
- Updated the docblock's CLAIM LIMITED note to match what the check now proves. It remains a lexical check: it does not execute the handler, does not prove React state timing, and cannot see a guard that is present but unreachable.
- No production code changed. `src/pages/OrderDetail.tsx` is byte-identical to the previous commit; CodeRabbit's other finding on this head asked for the `order.id !== id` guard that is already present at line 1084, ahead of the RPC call.
