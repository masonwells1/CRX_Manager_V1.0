## 2026-09-13 - Reject late keyless standalone holds during the inventory cutover

The full independent Sol review returned DB-AUTHZ-CUTOVER-001 HIGH on the parked hold-binding migration. Read-only production evidence confirmed its pinned old body and four current hold-writing functions. A new case in the existing network-disabled PostgreSQL 17 prover paused a real authenticated sales-user call at the stock lock, installed the candidate, then observed the old NULL-key/NULL-force call commit a 500-unit hold against 100 available after cutover. The prior documented acceptance of that residual is superseded by this demonstrated failure and focused correction.

The unapplied migration now installs a separate invoker BEFORE INSERT barrier on inventory_holds before renaming the old body. It owns standalone manual/crop_program rows with NULL source_id; the three current automatic sync writers supply source_id and retain their path. The barrier requires the existing wrapper's actor-bound key/fingerprint context and active profile. It uses the existing concurrent-replay uncertainty token so a keyed old invocation keeps its original browser request if a new-wrapper retry already succeeded. Hold writers are locked before receipt writers, source_id's expected shape is checked, the new barrier is private and pinned on replay, and postflight validates its installed trigger, owner, invoker mode, search path and grants. No new storage protocol, traffic revocation or live multi-transaction rollout is introduced.

The isolated full-chain prover passed on the updated draft: 93 predecessor migrations replayed, legacy receipt refused with old body preserved, late keyless call rejected with zero holds, rolled-back hold smoke passed, both keyed callers returned the same actual hold ID with one bound receipt, source-backed job/program insert shapes passed without standalone context, rerun passed, and a deliberately changed barrier was refused and preserved. The stale-context smoke case now reuses a real earlier receipt fingerprint to exercise the independent receipt-key check. This boundary test does not claim to execute each automatic sync RPC end to end.

All six migration contract tests passed, including independently recomputed wrapper and insert-barrier hashes. Broader recovery verification passed 209 tests across 16 suites; typecheck, lint and build passed. Fresh full exact-head Sol review and protected remote delivery gates remain required before publication and merge. Migration 20260908130000 remains NOT APPLIED; all data mutations and the changed-function fixture occurred only in the disposable network-disabled container, which the existing prover removed.
