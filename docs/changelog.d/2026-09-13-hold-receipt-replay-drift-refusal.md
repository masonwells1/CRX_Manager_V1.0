## 2026-09-13 - Refuse changed hold receipt bindings before migration replay

GitHub Codex review of PR #685 confirmed that the unapplied hold migration could overwrite a later receipt-binding hotfix during a manual rerun. The preflight now checks the normalized receipt-binding function body and its existing trigger registration before replacing either, refusing an unexpected overload, changed body, changed registration, or occupied trigger name. The first application still permits the new function and trigger to be absent.

The existing disposable PostgreSQL prover now deliberately changes the receipt-binding body and disables its trigger separately, requires the named preflight refusals, and verifies each changed object is preserved. It then restores its own disposable fixture and retains the existing hold-barrier drift test. The full PostgreSQL 17 proof passed with 93 predecessor migrations, zero late keyless holds, the rolled-back receipt chain, one hold and one bound receipt with identical replay IDs, unchanged rerun, and all three changed-object refusals. Source-backed insert tests do not execute every sync RPC end to end.

An independent contract test recomputes the emitted body hash. All 214 tests across 16 suites, typecheck, lint, build and documentation drift checks passed. The first prover draft captured only the last line of a multi-line fixture definition and failed during restoration; the corrected full-definition capture passed the full rerun. Scoped SQL and exact-head independent reviews plus protected delivery remain required. No live migration apply, business mutation, grant expansion, RPC payload or money math changes are authorized by this correction.
