## 2026-08-31 - Serialize final gauntlet write boundaries

Receiving reversal now preserves the established PO-item → PO lock order before taking the shared accounting-month lock and rechecking period and active-bill safeguards. Vendor-bill creation preserves vendor → PO ordering before computing the cumulative PO threshold. Cycle Count item mutations now lock and revalidate the parent count in a `BEFORE` trigger, so a late insert either advances the revision before completion or fails after completion. Stale intent-bound receiving keys are retired after a definitive database refusal, so the operator is not trapped retrying an unusable key. The migrations are local only and have not been applied to production; a deterministic network-isolated PostgreSQL 17 proof exercises both winning orders for billing/reversal, close/reversal, supplier-cost/reversal, supplier-cost/billing, and insert/completion.
