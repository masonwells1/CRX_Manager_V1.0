## 2026-09-13 - Name lifecycle checks without implying review completion

The independent exact-candidate review found that non-opened events could return an ignored or reset success under the final-review display name. The single trusted job now reports candidate snapshot for opened events and candidate lifecycle for every other event. No green lifecycle result attests completed review; the actual authenticated exact-head formal review and findings disposition remain the delivery evidence. The job key, triggers, permissions and actual dispatch/reconciliation checks remain intact.

Completed trusted lifecycle checks use the same retry handling and completion/app/workflow provenance requirements as snapshots and legacy trusted runs. Focused regressions reject a final-review display name for either event branch and verify trusted retries plus pending/foreign check rejection for both current names. No application, database, account permission or billing changes are included.
