## 2026-09-13 - Preserve saved inventory and payment retry identities through redraws

CodeRabbit's final review of PR #677 identified a concrete retry availability defect: inline hook options reran acknowledgment persistence after the same attempt was already saved, and a later redundant sessionStorage write failure could replace that safe attempt with a reconciliation-only lock. The previous optional performance-only disposition is superseded by this demonstrated failure path.

The hook now remembers successful acknowledgment persistence by the full acknowledgment key, surface, scope, and request version. Both preparation and restoration record success only after the actual write succeeds. An ordinary redraw skips the redundant write. Changing the active identity invalidates that memory, and a new request version still requires actual persistence. Genuine required-write failures retain the existing fail-closed behavior; transaction preparation and durable coordinator writes remain mandatory.

A rendered real-hook regression injects a later acknowledgment write failure during repeated inline-options redraws, preserves the original payload/key, and verifies an exact retry after the fault is removed. It failed against the previous implementation and passes with this correction. A second regression verifies returning to a different route identity still requires persistence and blocks on a genuine write failure. Existing recovery tests remain applicable.

The same review's minor and trivial items are corrected together: separate the migration label from its stamp in the earlier changelog, recompute the normalized emitted wrapper SHA-256 in the existing migration test while retaining drift assertions, and correct the payment test comment to describe the form retaining its confirmed amount. SQL executable, RPC payloads, money arithmetic, grants, and authentication are unchanged. Migration 20260908130000 remains NOT APPLIED; no live business transaction or storage fault was injected.

Focused verification passed all 56 tests across the shared hook, hold binding, and rendered vendor payment suites. Type check, lint, and build passed. Full coverage verification, fresh exact-candidate adversarial proof, remote checks, and final CodeRabbit clearance remain required before protected delivery. Existing dirty central and occupied Claude worktrees and PR #677 are preserved.
