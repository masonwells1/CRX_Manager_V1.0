## 2026-09-04 - Make lifecycle audit evidence explicit

- Required all three lifecycle audit workflows to compare every covered entity against live database constraints instead of treating workflow prose as authoritative.
- Completed the quote status reference, corrected the E2E path, and added the omitted `epa-lookup` edge function to the overnight audit.
- Added deterministic guidance checks for these pointers and aligned the documented ledger surface with the executable guard.
