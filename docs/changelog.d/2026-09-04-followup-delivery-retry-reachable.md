## 2026-09-04 - Make the retained follow-up-delivery key actually reachable

- Fixed `handleCreateFollowup` in `src/pages/DeliveryDetail.tsx`, which had no `try/finally`. F1 moved `followupIdem.resetKey()` to after `assertRpcResult`, which is correct — an ambiguous reply must keep the key so the server can replay it. But `assertRpcResult` throws on exactly that reply, and the throw skipped `setCreatingFollowup(false)`, leaving the button disabled. The retained key could not be spent: the only escape was navigating away, which unmounts the page and drops the key, so the retry travelled under a FRESH key the server cannot replay — the duplicate F1 exists to prevent.
- This was introduced by this PR. Before the F1 reorder the key was already retired by the time the assert ran, so retention was not being relied on; after it, the retention became load-bearing and the unreachable retry nullified it on this one path.
- Raised as a non-blocking MEDIUM by the `gpt-5.6-sol` push proof on head `f4f88e24d`. The handler now matches `handleStartDelivery` directly below it: `try` around the RPC, `catch` toasting a sanitized error, `finally` clearing the loading flag.
- Swept the other eight sites where this PR moved a reset after its assert, rather than fixing only the reported one. All were already safe: `OrderDetail`'s cancel/split/create-invoice and every `Returns` handler run inside `runCriticalAction`, which clears its flag in a `finally`; `FieldApplicationInvoice`'s transfer and `OrderDetail`'s consolidate have explicit `finally` blocks; `InvoiceDetail`'s transfer, `MonthEndClose`'s statements and both `PrepaymentManagerPanel` handlers clear the flag after the `try/catch`; `FieldApplicationInvoice`'s delete sets no loading flag at all.
- VERIFICATION LIMIT: typecheck and the two idempotency guard suites (24 tests) pass. The corrected handler has NOT been exercised in a browser against a live ambiguous reply, so "the button re-enables and the retry replays" is reasoned from the control flow and the matching sibling handler, not observed.
