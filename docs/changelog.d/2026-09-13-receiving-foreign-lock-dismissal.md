## 2026-09-13 - Let staff leave a receipt or payment owned by another screen

Receiving Hub, Vendor Bill Detail and Purchase Order Detail now allow Cancel, X, Escape and backdrop dismissal when another screen owns the saved request. Dismissal leaves the durable request and original key intact; this screen still cannot submit or change the saved request. Close controls remain disabled while this screen owns its pending receipt or payment.

Purchase Order Detail now keeps a confirmed-cleanup warning visible, resets that notice only on a fresh unlocked request or successful cleanup, and uses the same guarded exception-reporting tags as the other receiving screens. Reloaded receiving locks use outcome-neutral reconciliation wording. Its existing frozen-request retry, data refresh and post-commit side-effect handling remain intact.

Rendered regressions exercise Cancel and X for all three foreign-lock screens, confirm saved keys remain unchanged and no second RPC is submitted, and assert confirmed Hold/Adjustment/Payment/Purchase Order receiving banners. Receiving Hub and Vendor Payment use the real durable-intent hook with fake IndexedDB and mocked RPC responses; Purchase Order Detail uses its existing explicit locked-state hook fixture. No live business mutation or browser storage inspection was performed. No hook/storage protocol, RPC payload, authoritative money math, executable SQL, permissions or authentication changes. Inventory migration 20260908130000 remains NOT APPLIED.
