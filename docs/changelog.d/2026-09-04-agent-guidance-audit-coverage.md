## 2026-09-04 - Agent guidance audit coverage

- Restored `epa-lookup` to every whole-repository Edge Function audit and removed the deleted `seed-admin` target.
- Documented the active-job condition that prevents unsafe `closed_short` quote transitions.
- Routed testing work to the QA reference and added deterministic coverage for all three corrections.
