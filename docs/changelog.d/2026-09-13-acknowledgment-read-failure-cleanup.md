## 2026-09-13 - Treat unreadable saved acknowledgments as cleanup failures

The actual GitHub Codex review of PR #678 identified that acknowledgment reads returning the synthetic reconciliation-only record were mistaken for a valid different request version during cleanup. A confirmed handler could therefore report cleanup success and close, while the stale acknowledgment reappeared as a pending lock after reload.

Acknowledgment cleanup now rejects the existing synthetic blocked record before comparing request versions. Missing acknowledgments remain an ordinary no-op, and a verified valid record belonging to a different request version remains untouched. No storage format, retry protocol, business mutation, SQL executable, grants, authentication, or money calculation changed.

Both injected sessionStorage read failure and malformed saved acknowledgment regressions failed against the previous implementation because cleanup resolved successfully. They now require cleanup failure, preserve the original in-memory payload, and reconcile the same original key after the saved acknowledgment becomes readable. A separate test verifies a valid newer acknowledgment is preserved. The rendered vendor payment regression injects the read failure after the RPC returns its confirmed result and observes the recorded-once notice, disabled original amount and close control, no green completion, then exact-payload/key retry and close after recovery.

All 60 focused hook, hold-binding and rendered payment tests passed. Broader recovery verification passed 208 tests across 16 suites, including every affected receiving, inventory and payment consumer. Typecheck, lint and build passed. Fresh exact-head adversarial proof, remote checks and final CodeRabbit clearance remain required before protected delivery. PR #678 and its branch are preserved and no CodeRabbit review was spent on its known defect. Migration 20260908130000 remains NOT APPLIED; no live business data or browser storage was mutated.
