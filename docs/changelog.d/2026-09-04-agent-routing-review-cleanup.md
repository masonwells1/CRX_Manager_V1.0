## 2026-09-04 - Agent routing review cleanup

- Replaced the last active reviewer pointers to removed `CLAUDE.md` rule sections with `AGENTS.md` task routing and the relevant workflow documents.
- Standardized active audit language on `CRX Hard Rules`, verified Graphify routing for both Claude and Codex, and repinned the intentionally changed gauntlet prompts.
- Kept the protected live-data guard unchanged after its source-fingerprint test correctly rejected a cosmetic edit.
