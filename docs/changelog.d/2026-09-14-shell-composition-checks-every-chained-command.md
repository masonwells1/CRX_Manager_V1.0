## 2026-09-14 - Backtick and caret check now looks at every command in a chain

- **What changed:** `ghHiddenByShellComposition` in `.claude/hooks/codex-push-lib.mjs` compares the gh operations of EVERY command in a chained line, before and after PowerShell's backtick and cmd.exe's caret are removed. It used to compare only the first merge the parser found in the whole line.
- **Why:** `gh pr merge 692 --squash; gh pr me`rge 2 --admin --squash` looked the same both ways, because the first merge is unchanged. PowerShell then ran an administrator merge second. Both merge guards call this check once, before reading segments. Found by the local gpt-5.6-sol review of PR #630 on 2026-09-14. On main, even a single backtick-spliced `--admin` merge is allowed, so this closes a gap in protection #630 adds rather than a regression.
- **Proof:** the real Claude and Codex merge guards ran as hook subprocesses on that exact command, with the green PR #692 as the first merge. Both now deny it with the backtick message. New shared-library and Codex end-to-end cases cover a hidden second merge and a hidden later `gh api` DELETE. A control proves a backtick that changes no gh operation is not refused.
